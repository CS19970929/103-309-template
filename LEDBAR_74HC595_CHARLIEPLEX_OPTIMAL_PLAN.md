# LedBar 74HC595 + 5Pin Charlieplexing 最优显示方案

## 1. 硬件边界

当前硬件是单颗 `74HC595` 的 `QA~QE` 直接驱动数码管 `D0~D4`，`OE` 固定使能，MCU 只能输出 5bit 高低电平图样，不能把任意 3 根非目标线切成高阻。

这意味着它不是理想 Charlieplexing。软件能做的是在 31 个非零 5bit 图样中选择一组扫描序列，让目标段尽量全覆盖、非目标串亮尽量少、视觉尽量稳定；不能从物理上保证所有 `0~100% + 充电图标` 都完全无串亮。

## 2. “完美显示”的判定

本方案按离线穷举判定每个业务状态是否完美：

- 目标段集合：当前 SOC 数字、百分号、可选充电图标需要点亮的段。
- 可用图样集合：`74HC595` 输出 `1~30` 时，按实际 `D0~D4` 路由会被点亮的段集合。
- 完美条件：选出的每个图样都不点亮非目标段，并且这些图样的并集正好覆盖全部目标段。

在当前实测路由下，以下值可做到逻辑无串亮：

- `%` 常亮、未充电：`64, 68, 69, 85, 86, 88, 89`
- `%` 常亮、充电图标亮：`0, 8, 60, 64, 68, 69, 80, 85, 86, 88, 89, 100`

不在上述列表的状态至少会有少量非目标段被带亮。现象通常是某个数字段或图标段出现较暗的虚亮；串亮程度由查表中的 `extra` 分数表示。

## 3. 当前实现策略

代码位置：`103 + 309/Project/Source/LedBar.c`

实现分四层：

- 物理层：`LedBar_595WriteByte()` 用普通 GPIO 模拟 `DATA/CLK/LATCH`，只输出低 5bit 到 `D0~D4`。
- 图样层：根据 18 条实测路由枚举每个 5bit 图样实际点亮的段。
- 业务层：`0~100`、百分号、充电图标转换成目标段集合，并优先使用离线固化的最优表。
- 扫描层：使用前后缓冲和安全点切换，避免业务更新时扫描到半更新帧。

扫描状态机固定为：

1. `OFF_PRE`：先输出 `0x00`
2. `OUTPUT_TARGET`：输出当前目标图样
3. `HOLD_TARGET`：保持目标图样
4. `OFF_POST`：再输出 `0x00`
5. `NEXT_ITEM`：切到下一个图样，并在这里提交 back buffer

这样不会直接执行 `目标图样 A -> 目标图样 B`，对 `OE` 不可控的硬件更稳。

## 4. 用户体验优化

为了尽量保证用户观感，新增了一个可配置的 SOC 轻量贴近策略：

- 默认只处理 `10~99`。
- 只有原始显示串亮分数 `>= 4` 时才考虑贴近。
- 默认只允许向相邻值移动 `±1%`。
- 只有相邻值能降低至少 1 个串亮分数时才移动。
- 低电量个位数、`100%` 不贴近，避免误导关键状态。

默认贴近结果如下：

未充电：

`11->10, 12->13, 14->13, 17->18, 21->22, 31->30, 37->36, 41->40, 44->45, 47->46, 71->70, 91->90`

充电：

`11->10, 21->20, 31->30, 41->40, 42->43, 44->45, 47->46`

这会牺牲少量数值精确性，但可以避开肉眼最明显的重串亮值。若业务要求数值绝对精确，可把 `LEDBAR_SOC_DISPLAY_SNAP_ENABLE` 定义为 `0`。

## 5. 可调参数

以下宏可在编译参数或 `LedBar.c` 前部调整：

- `LEDBAR_SOC_DISPLAY_SNAP_ENABLE`：是否启用 SOC 贴近，默认 `1`。
- `LEDBAR_SOC_DISPLAY_SNAP_WINDOW`：允许贴近窗口，默认 `1`。
- `LEDBAR_SOC_DISPLAY_SNAP_MIN_EXTRA`：触发贴近的最低串亮分数，默认 `4`。
- `LEDBAR_SOC_DISPLAY_SNAP_MIN_GAIN`：候选值至少降低多少串亮分数，默认 `1`。
- `LEDBAR_SCAN_HOLD_TICKS`：目标图样保持 tick，默认 `1`。

调优建议：

- 想要数值精确：关闭 `LEDBAR_SOC_DISPLAY_SNAP_ENABLE`。
- 想要更干净的视觉：可把 `LEDBAR_SOC_DISPLAY_SNAP_WINDOW` 调到 `2`，但会出现最多 `±2%` 的显示偏差。
- 想要更亮：可增加 `LEDBAR_SCAN_HOLD_TICKS`，代价是完整扫描周期变长，动态观感需要上板确认。

## 6. 上板验证重点

- 逐段点亮：使用 `LedBar_EnableSingleSegmentTest()` 和 `LedBar_SetSingleSegmentIndex()` 验证 `0~17` 段位与实物丝印对应关系。
- 关键 SOC：优先观察 `0, 8, 60, 64, 68, 69, 80, 85, 86, 88, 89, 100`。
- 重串亮 SOC：重点观察 `11, 31, 41, 47, 91`，确认贴近后观感是否优于精确显示。
- 充电/非充电切换：确认充电图标不会在状态切换时出现明显闪烁。
- 休眠：确认 `Sleep_Mode.bits.b1_ToSleepFlag` 置位后显示立即熄灭，唤醒后能恢复扫描。

## 7. 结论

在不改硬件的前提下，这已经是偏工程可落地的最优方案：不能让所有状态物理完美，但可以把可完美显示的状态完全保留，把重串亮状态通过查表、扫描隔离和轻量贴近压到更可接受的视觉效果。
