# 待测试清单

本文记录当前需要在 Keil/硬件环境继续验证的事项。完成后应把长期有效结论沉淀到对应模块文档。

## SOC 模块优化验证

- [ ] Keil 工程完整编译：使用 `103 + 309/Project/Users/CommomSH367309_16series_103RCT6_C.uvprojx` 的 `FD_Release` / `FD_Debug` 在 Windows + Keil 环境编译。
- [ ] 无 SOC 快照启动：擦除 `0x0801E000/0x0801E800`，确认创建 V2 默认快照，SOC=60%，容量=27Ah，循环次数=3。
- [ ] 旧 V1 快照兼容：写入旧格式 `SocNow/DsgSocInt/CycleTimes`，确认启动后可读取并在下一次保存迁移为 V2。
- [ ] V2 快照恢复：确认 `CapNow/CapFull/LearnPassed/LearnState/Flags/MaxError` 可跨重启恢复。
- [ ] CRC 错误回退：破坏 SOC journal CRC，确认加载默认快照并持续重试保存。
- [ ] Flash 保存失败重试：模拟首次写入失败，确认后续 200ms 监控仍会重新尝试。
- [ ] 小电流死区：`Ichg/Idsg < 0.4A` 时不积分；连续约 5s 后进入 `RELAX`。
- [ ] 充放电方向切换：方向切换时未满 1% 的积分余量和端点计数被清零，不出现反向继承。
- [ ] 样本丢失：AFE 样本序号不变时，`App_SOC()` 不重复积分旧电流。
- [ ] 主机回放：执行 `python3 tools/soc_replay_test.py`，确认满电确认和在线 OCV guard 边界测试通过。
- [ ] 异常电压不校准：模拟 `VCellMax < VCellMin`、单体低于 `2000mV`、单体高于 `5000mV`、压差高于 `1000mV`，确认手动 OCV、满电确认、在线 OCV、静置 OCV 均不修正。
- [ ] 异常状态不校准：注入三级保护故障、AFE1/AFE2/ADC/CBC/温度异常，确认 SOC 电压类校准不动作，故障清除后可恢复正常校准。
- [ ] 满电确认：`CHG`、`VCellMax >= V100`、`VCellMin >= max(化学体系最低门槛, V100 - 80mV)`、单体压差 `<=120mV`、`Ichg <= C/20` 持续约 60s 后才确认 100%。
- [ ] 满电体验：确认前只缓慢上修，确认后对外 SOC 按平滑规则到 100%。
- [ ] 在线 OCV guard：轻载 `0.4A~C/10`、电压稳定 `20s/8mV`、OCV 目标和内部 SOC 持续相差至少 3% 时，按 30s/1% 收敛；重载和 LFP 平台区中段不修正。
- [ ] 重载后恢复抑制：`Idsg >= C/3` 后默认 `180s` 内不做在线 OCV；大电流结束后电压持续回弹未稳定时也不做在线 OCV 下修。
- [ ] 普通低压下修：`V0 + 100mV` 附近按秒级窗口逐步下降，不突然归零。
- [ ] 弱单体临界：`V0 + 30mV` 和 `V0` 附近加速下修，对外 SOC 不再长期高估。
- [ ] 静置 OCV：运行态必须无电流、电压有效且稳定后才小幅修正；当前三元锂默认表不应跳变，切到 `LFP` 时平台区不明显上跳。
- [ ] RTC 唤醒补偿：长时间休眠唤醒后可按 OCV 修正并强制同步对外 SOC。
- [ ] 手动 OCV 刷新：非充电状态只允许下修，不抬高 SOC。
- [ ] 容量/循环初始化：清除学习标志，`FCC` 回到出厂容量，循环累计归零。
- [ ] 设置一次 SOC：内部 SOC、`CapNow`、对外 SOC 同步到设定值。
- [ ] SOC 固定/清零显示：只影响对外发布，不破坏内部积分快照。
- [ ] FCC 首次学习：满到空或空到满跨度达到 90% 后允许学习。
- [ ] FCC 后续学习：已有学习标志后跨度达到 40% 才允许学习。
- [ ] FCC 拒绝条件：跨度不足、容量超出 50%~110%、方向中途反转时拒绝学习。
- [ ] FCC 变化速率：单次 `CapFull` 变化不超过旧容量的 5%，SOH 不跳变。
- [ ] 低压锚点：放电容量归零或弱单体到 `V0` 且 SOC 压到 0 时触发可信空电锚点。
- [ ] 通信兼容：不新增 CAN/RS485 地址，原 SOC 读写地址和 `InitData_SOC()`、`App_SOC()`、`SOC_ApplyRtcRelaxationCompensation()` 签名保持不变。
