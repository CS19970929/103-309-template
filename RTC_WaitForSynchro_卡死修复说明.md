# RTC_WaitForSynchro 卡死修复说明

## 问题

`RTC_WaitForSynchro()` 是库函数里的死循环等待。
当 RTC 时钟源、备份域状态或同步条件异常时，`RSF` 位不会置位，程序会一直卡在这里。

## 修复

- 在 `103 + 309/Project/Source/RTC.c` 中增加有限等待。
- 同步失败时自动走一次 RTC/备份域重建流程，避免启动阶段直接死锁。
- 保留原有初始化逻辑，不改对外接口。

## 影响

- 正常情况下行为不变。
- RTC 时钟链路异常时，不再无限卡死，而是尝试重新初始化 RTC。

