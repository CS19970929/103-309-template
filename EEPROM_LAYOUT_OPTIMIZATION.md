# EEPROM 地址与读写逻辑梳理

本文专门整理外部 EEPROM 的地址布局、开机加载逻辑、后台写入逻辑，以及当前代码里可以优化的点。目标是把实现收敛到“安全、稳定、简单”。

## 1. EEPROM 的角色

当前工程里，外部 EEPROM 不是单纯的配置区，而是主参数仓库。它保存的内容主要包括：

- 保护参数
- RTC 参数
- 校准系数 K/B
- SOC 表
- 铜损补偿
- 故障记录
- 扩展参数
- 产品序列号 / 硬件版本 / 软件版本
- 事件记录
- 启动哨兵位、睡眠状态位、Flash 更新标志位
- 当前偏移量等少量运行相关数据

也就是说，EEPROM 里同时放了“量产默认值”“运行后可持久化参数”和“流程标志位”。

## 2. 当前地址布局

下面按代码中的实际逻辑分块整理。

### 2.1 低地址参数区

| 起始地址 | 区域 | 长度 | 说明 |
|---|---|---:|---|
| `0x0000` | 保护参数 | `65` 个半字 | 单体电压、总压、电流、温度、压差、SOC 保护参数 |
| `0x0082` | RTC 参数 | `12` 个半字 | 年月日时分秒、闹钟等 |
| `0x009A` | 校准 K | `KB_NUM` 个半字 | 各通道校准系数 K |
| `0x00F8` | 校准 B | `KB_NUM` 个半字 | 各通道校准系数 B |
| `0x0156` | SOC 表 | `SOC_TABLE_SIZE` 个半字 | SOC 电压映射表 |
| `0x01AA` | 铜损补偿 | `CompensateNUM` 个半字 | 铜损补偿值 |
| `0x01CA` | 铜损补偿数量 | `CompensateNUM` 个半字 | 铜损分段数量 |
| `0x01EA` | 故障记录区 | 按 `Record_len` 计算 | 故障分段、临时指针、RTC 相关记录 |
| `0x02A4` | `OtherElement1` | `32` 个半字 | 均衡、系统、睡眠、SOC 扩展等 |
| `0x02E4` | 热管理参数 | `24` 个半字 | 加热 / 冷却参数 |
| `0x0316` | SOC 扩展项 | 结构体定义 | 代码里预留的扩展区 |

### 2.2 产品信息与事件区

| 起始地址 | 区域 | 长度 | 说明 |
|---|---|---:|---|
| `0x033E` | 序列号 | 变长 | 首字存长度，后续字符按半字打包 |
| `0x0366` | 硬件版本 | 变长 | 同上 |
| `0x0382` | 软件版本 | 变长 | 同上 |
| `0x03E8` | 事件记录 | `100` 条 | 每条 `2` 字节 |
| `0x04B0` | 事件指针 | `1` 个半字 | 当前事件写入位置 |
| `0x05DC` | 当前偏移量 | `1` 个半字 | 虚拟电流偏移量 |

### 2.3 高地址状态位

| 地址 | 名称 | 用途 |
|---|---|---|
| `0x3FFA` | `EEPROM_ADDR_SLEEP` | 睡眠状态标志 |
| `0x3FFC` | `EEPROM_ADDR_PASS` | 首次上电完成哨兵位 |
| `0x3FFE` | `EEPROM_ADDR_FLASHUPDATE` | Flash 更新触发标志 |
| `2040` | `EEPROM_ADDR_SWITCH_ONOFF` | 开关功能位 |
| `2044` | `EEPROM_ADDR_SYS_FUNC_SELECT` | 系统功能选择位 |

### 2.4 命名上容易误解的点

`FLASH_ADDR_SH367309_VALUE` 这个名字看起来像内部 Flash 地址，但当前代码实际是通过 `WriteEEPROM_Word_NoZone()` 和 `ReadEEPROM_Word_NoZone()` 访问的，也就是 EEPROM 路径。

这个名字建议后续重命名为更明确的 EEPROM 语义，例如：

- `EEPROM_ADDR_SH367309_VALUE`
- 或 `E2P_ADDR_SH367309_VALUE`

这样可以减少后续维护误判。

## 3. 当前读写逻辑

### 3.1 基础 I2C 访问

底层是软件模拟 I2C：

- `ReadEEPROM_Byte()`
- `WriteEEPROM_Byte()`
- `ReadEEPROM_Word_NoZone()`
- `WriteEEPROM_Word_NoZone()`

其中：

- `ReadEEPROM_Byte()` 失败时会触发 `ERROR_EEPROM_COM`
- `WriteEEPROM_Byte()` 负责单字节写入
- `WriteEEPROM_Word_NoZone()` 实际是“写 2 个字节 + 回读校验 + 重试”

### 3.2 开机加载

`InitData_E2prom()` 的流程如下：

1. 先读取 `EEPROM_ADDR_PASS`
2. 如果等于 `EEPROM_VALUE_BEGIN_FLAG`，说明不是第一次上电
3. 调用 `ReadEEPROM_ByteData_StartUp()` 把各参数读回内存
4. 再读取当前偏移量等运行相关值
5. 如果不是第一次上电，就进入默认值初始化流程

### 3.3 首次上电

首次上电时：

1. `EEPROM_ResetData_AllToDefault()` 把内存默认值准备好
2. `WriteEEPROM_ByteData_Circle()` 以“一个标志写一个项”的方式逐步落盘
3. `EEPROM_ResetData_OtherToDefault()` 初始化 AFE、事件记录、系统功能选择等
4. `WriteProID_Default()` 写产品序列号和版本
5. 初始化 AFE
6. 运行电流偏移标定
7. `soc_factory_param_init_first()`
8. 最后写 `EEPROM_ADDR_PASS`
9. 复位 MCU

### 3.4 运行时写入

运行时由 `App_E2promDeal()` 调度，特点是：

- 一次只写一个最小单元
- 优先级大致是 K/B -> 保护参数 -> 扩展参数 -> 热管理 -> 事件清空
- 写标志清空后再写下一个项

这种设计的好处是：

- 不会长时间占用主循环
- 写 EEPROM 的压力比较分散
- 出问题时更容易定位具体写块

## 4. 当前实现的风险点

### 4.1 写保护可能未恢复

原来的 `WriteEEPROM_Byte()` 在 I2C 任一阶段失败时会直接 `return`，`MCUO_E2PR_WP` 可能一直保持为 0。

这会让 EEPROM 长时间处于可写状态，不符合安全要求。

这一点我已经做了小修复：

- 失败后统一走退出分支
- 退出时恢复 `MCUO_E2PR_WP = 1`

### 4.2 读接口没有显式状态返回

当前 `ReadEEPROM_Byte()` 只返回 `UINT8` 数据本身，失败时也会返回 `0`。

问题是：

- `0` 既可能是真实数据
- 也可能是通信失败的退化值

这会让上层判断不够清晰。

### 4.3 `WriteEEPROM_Word_NoZone()` 的原子性不足

它现在是：

1. 写低字节
2. 写高字节
3. 回读校验
4. 最多重试 3 次

优点是简单，缺点是：

- 如果在低字节和高字节之间掉电，可能留下半个旧值
- 仅靠范围校验不能百分百识别这种情况

### 4.4 启动校验主要靠范围校验

`ReadEEPROM_ByteData_StartUp()` 目前更多是做“数值范围检查”。

这能挡住明显非法值，但挡不住：

- 数据错位
- 半写入
- 旧版本数据结构变化
- 设备间不同版本混写

### 4.5 地址命名和实际介质有偏差

`FLASH_ADDR_SH367309_VALUE`、`FLASH_ADDR_*` 这类名字在代码里看起来像内部 Flash，但实际走的是 EEPROM API。

这个问题不影响功能，但会增加维护成本。

## 5. 推荐的优化方向

下面按“收益 / 复杂度”排序。

### 5.1 低成本，建议立即做

1. 统一写函数退出路径，确保写保护一定恢复
2. 给读函数增加“状态返回 + 输出参数”版本，例如：
   - `EEPROM_ReadByte(addr, &val)`
   - `EEPROM_ReadWord(addr, &val)`
3. `WriteEEPROM_Word_NoZone()` 保留回读校验和重试，但把失败信息向上返回得更清楚

### 5.2 中等成本，强烈建议做

1. 给关键配置块加 `magic + version + CRC`
2. 启动时先校验 CRC，再决定是正常加载还是恢复默认值
3. 对“系统功能选择”“产品信息”“AFE 参数”“保护参数”这些关键区做块级完整性检查

推荐优先加 CRC 的块：

- 保护参数
- 校准参数
- 系统扩展参数
- 热管理参数
- 产品信息

### 5.3 较高成本，后续再考虑

1. 为关键参数做双备份
2. 实现“事务提交”机制，先写 shadow 区，再写 commit 标志
3. 统一所有 EEPROM 地址为表驱动结构，减少结构体指针偏移写法

## 6. 我建议的最终形态

如果目标是“安全、稳定、简单”，我建议保留当前分块写入的总体框架，只做以下收敛：

- 底层读写函数加状态返回
- 写保护统一收口
- 关键块加 CRC
- 启动时只接受“校验通过”的配置
- 命名上把 EEPROM 和 Flash 的概念分开

这样比大改架构更稳，也更容易验证。

## 7. 结论

当前 EEPROM 逻辑的核心优点是：

- 分区清晰
- 写入分批
- 有回读校验
- 启动和运行逻辑分离

当前最值得先修的点是：

- `WriteEEPROM_Byte()` 的写保护恢复
- 读接口的错误可见性
- 关键块的 CRC 校验

我已经先修了第一项，后两项建议作为下一步优化。
