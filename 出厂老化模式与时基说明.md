# 出厂老化模式与系统时基说明

## 1. 需求边界

当前需求是：

1. 板子出厂下完程序后，只要 MCU 处于正常运行态，就进入老化模式。
2. 老化模式内调用 `enter_fac_mode(true)`，打开充电管和放电管。
3. 运行态累计满 3 天后，调用 `enter_fac_mode(false)` 退出老化模式。
4. 退出后保持“放电管打开、充电管关闭”，后续必须通过专用带 5V 识别的充电器路径才能允许充电。
5. RTC/STOP 休眠时间不计入 3 天老化时间。
6. MCU 复位、主动进入深度休眠再唤醒后，老化进度不能从 0 重新开始。

这里的“3 天”不是主循环执行次数，也不是 I2C/Delay 累加出来的时间，而是 TIM3 产生的 10ms 运行态 tick。

## 2. 当前系统时基

工程里实际有两类时间源：

| 时间源 | 位置 | 用途 | 是否适合 3 天老化计时 |
| --- | --- | --- | --- |
| `TIM3` 10ms 中断 | `System_Init.c` | 系统调度 tick，生成 10ms/50ms/100ms/200ms/1000ms 标志，并累计 `SysTime_Get10msTickCount()` | 适合 |
| `SysTick` 阻塞延时 | `System_Init.c` | `__delay_us()` / `__delay_ms()` 忙等延时 | 不适合 |
| 软件 I2C 小延时 | `I2C_AFE1.c` / `EEPROM.c` | 位 banging 时序、AFE/EEPROM 访问等待 | 不适合 |
| RTC Alarm | `RTC.c` / `rtc_sleep.c` | STOP 休眠周期唤醒、休眠 SOC 补偿 | 不计入老化 |

`TIM3` 初始化后按 10ms 进入 `TIM3_IRQHandler()`，中断里调用 `SysTime_Post10msTick()`：

- `s_u32Sys10msTickCount++`
- 设置 `b1Sys10msFlag`
- 每 5 个 tick 设置 50ms 标志
- 每 10 个 tick 设置 100ms 标志
- 每 20 个 tick 设置 200ms 标志
- 每 100 个 tick 设置 1000ms 标志

主循环执行慢时，任务标志可能被合并成一次处理，但 `s_u32Sys10msTickCount` 仍按硬件中断累计。因此 I2C 访问、`Delay1ms()`、AFE 采样等前台任务只会拖慢任务处理时刻，不会让运行态 10ms tick 少计。

## 3. 误差与边界

### 3.1 会造成老化计时误差的情况

| 情况 | 影响 |
| --- | --- |
| HSE/系统时钟本身有晶振误差 | TIM3 tick 会跟随系统时钟误差，这是硬件时钟精度边界 |
| 长时间全局关中断超过 10ms | TIM3 中断可能延迟或丢失，运行态计时会偏慢 |
| 进入 STOP/RTC 休眠 | TIM3 停止，老化计时暂停 |
| MCU 复位 | RAM 会清零，但老化进度会从 BKP/Flash journal 恢复，不从 0 重新开始 |
| 完全掉电且 BKP 丢失 | 从最近一次 Flash checkpoint 恢复，最多损失一个 checkpoint 周期 |

### 3.2 不会直接造成老化计时失准的情况

| 情况 | 说明 |
| --- | --- |
| 主循环某个任务执行超过 10ms | 只影响任务处理延迟，不影响 `SysTime_Get10msTickCount()` 累计 |
| AFE I2C bit banging 的 `Delay4us()` | 前台忙等期间 TIM3 中断仍可进入 |
| `MTPWrite()` 内部 `Delay1ms()` | `Delay1ms()` 是忙等，TIM3 中断仍可进入 |
| `SysTime_LatchTaskFlags()` 合并标志 | 任务标志可能只处理一次，但老化计时直接读 10ms tick 总数 |

## 4. 老化状态机

新增状态机在 `main.c` 中运行：

| 状态 | 行为 |
| --- | --- |
| `UNINIT` | 读取 `FLASH_ADDR_FACTORY_AGING_FLAG` |
| 未完成 | 从 Flash journal 和 BKP 恢复累计 tick，调用 `enter_fac_mode(true)`，进入 `RUNNING` |
| `RUNNING` | 通过 `SysTime_Get10msTickCount()` 累计运行态 tick |
| `RUNNING` 持久化 | BKP 约 1 秒更新一次；Flash journal 约 10 分钟 checkpoint 一次；进休眠/主动深睡前强制保存 BKP |
| 累计满 `PROJECT_CFG_FACTORY_AGING_DURATION_SECONDS` | 调用 `enter_fac_mode(false)`，写完成记录；只有写成功才进入 `DONE` |
| 已完成 | 启动后直接保持 `enter_fac_mode(false)` 语义，不再自动进入老化 |

默认配置：

```c
#define PROJECT_CFG_FACTORY_AGING_ENABLE 1
#define PROJECT_CFG_FACTORY_AGING_DURATION_SECONDS 259200
```

`259200s = 3 * 24 * 60 * 60`，即 3 天。

## 5. RTC 休眠策略

老化运行期间不允许自动进入 RTC/STOP 休眠。

原因：

1. 进入 STOP 后 TIM3 停止，板子不再处于连续运行老化状态。
2. RTC 休眠会关闭或重配部分外设，唤醒后初始化路径可能重新改写 AFE MOS 配置。
3. 如果把 RTC 休眠时间也算入 3 天，就不再满足“只要板子运行，没进入休眠”的需求语义。

实现上，`FactoryAging_IsActive()` 为真时，`BQ769x0_SleepMode_Ctrl()` 会清掉低功耗等待计数并返回，不再提交自动 `HICCUP_MODE` / RTC 休眠。已有 AFE 保护、故障采样和故障上报逻辑仍按原路径运行，老化模式不绕过保护逻辑。

用户主动长按进入 `DEEP_MODE`、低压保护进入深睡，或其它路径调用 `SleepDeal_Continue(mode)` 前，会调用 `FactoryAging_SaveProgressBeforeSleep()` 保存当前累计运行 tick。RTC STOP 路径在 `rtc_sleep_prepare_rtc()` 进入 STOP 前也会保存。睡眠期间 TIM3 不运行，因此睡眠时间不计入老化；唤醒或 MCU reset 后从保存点继续累计。

## 6. 持久化标志

`0x0801F400` 现在作为出厂老化 journal 页使用，不再只是一个裸 half-word 标志。

| 地址 | 宏 | 值 | 含义 |
| --- | --- | --- | --- |
| `0x0801F400` | `FLASH_ADDR_FACTORY_AGING_FLAG` | 空白页 `0xFFFF` | 未完成或已擦除 |
| `0x0801F400` | `FLASH_ADDR_FACTORY_AGING_FLAG` | journal record | 保存累计 `elapsed10ms` 和状态 |
| journal state | `FLASH_FACTORY_AGING_STATE_RUNNING` | `0xA931` | 老化进行中 checkpoint |
| journal state | `FLASH_FACTORY_AGING_STATE_DONE` | `0xA93D` | 老化已完成 |

Flash journal 的含义是：不在同一个地址反复擦写，而是在同一页内顺序追加带 magic、sequence、CRC 的记录。启动时扫描 journal，选择 sequence 最新且 CRC 正确的记录。页写满后才擦除整页并继续写入。这样可以显著降低擦页次数，也能识别断电中断造成的半写记录。

为了避免 Flash 高频写：

- BKP `DR6~DR10` 保存最近运行 tick，用于 MCU reset、主动深睡 reset 后快速恢复，BKP 没有 Flash 擦写寿命问题。
- Flash journal 默认约 2 小时写一次 checkpoint。
- 老化完成时强制写一次 `DONE` 记录。
- 主动进入深度休眠前强制保存 BKP；只有达到 Flash checkpoint 周期或完成时才写 Flash。

按 3 天老化估算，2 小时一次最多 36 个周期 checkpoint，加上启动和完成记录，正常一轮不会写满当前 1KB 老化页。即使中途多次 MCU reset，只要未达到 checkpoint 周期，也主要写 BKP，不会把 Flash 当计时器使用。完全掉电且 BKP 丢失时，最多回退到最近一次 Flash checkpoint。

如果返工板需要重新执行出厂老化，必须用受控产线工具把 `0x0801F400` 所在页擦回 `0xFFFF`，并清除 BKP `DR6~DR10` 的老化进度缓存，或确保备份域已重新初始化。不要通过裸写 App bin 到 `0x08000000` 的方式处理，否则可能覆盖 IAP。

## 7. 验收点

1. 首次运行：`enter_fac_mode(true)` 只在老化开始时调用，充放电管打开。
2. 老化期间：自动 RTC 休眠不进入，`sys_time.enter_rtc_delay` 不累计到休眠提交。
3. AFE 重新初始化：若仍处于老化状态，初始化后重新执行 `enter_fac_mode(true)`。
4. MCU reset / 主动深睡唤醒：从 BKP/Flash journal 恢复累计运行 tick，不从 0 重新开始。
5. 3 天运行态 tick 到期：执行 `enter_fac_mode(false)`，充电管关闭、放电管打开；完成记录写成功后才进入 `DONE`。
6. Flash 写失败：不进入 `DONE`，保持可重试状态并上报 `ERROR_EEPROM_STORE`，避免本次运行误判完成、下次重启又重新老化。
7. 老化完成后重启：不再进入老化，保持充电管关闭。
8. 专用 5V 充电器插入后的充电允许逻辑仍走现有充电唤醒/识别路径，不由老化状态机直接打开。
