# 项目自动化检查与发布流程

本文用于把调试、发布、配置检查流程固化下来，减少赶项目时依赖人工记忆切宏、改宏、查宏。

## 1. 管理目标

- 日常调试和正式发布通过 Keil Target 区分，不手工反复改一堆宏。
- 发布安全项由 `Project_BuildGuard.h` 编译期拦截，误开调试/测试宏时直接报错。
- 提交和推送前由 `tools/project_check.py` 做静态自检，提前发现 Keil Target、默认配置、发布保护文件遗漏。
- `.gitignore` 统一过滤 Keil 用户文件、编译产物和 macOS `.DS_Store`，避免无关文件污染提交。

## 2. 设计依据

当前方案没有继续增加大量宏配置，而是采用“Profile + Target + 自动检查”的方式：

- Arm Keil 官方支持在同一工程中维护多个 Target，用于不同编译选项和输出产物：<https://www.keil.com/support/man/docs/uv4/uv4_ca_projtargfilegr.asp>
- Keil `Configuration Wizard` 适合把头文件配置做成可视化配置入口：<https://www.keil.com/support/man/docs/uv4/uv4_ut_configwizard.asp>
- Zephyr Kconfig 的核心思路是集中配置、依赖检查、构建前发现错误：<https://docs.zephyrproject.org/latest/build/kconfig/index.html>
- CMake Presets 的核心思路是把常用构建配置固化成可复用档位：<https://cmake.org/cmake/help/latest/manual/cmake-presets.7.html>

映射到本项目后，`Project_Config.h` 是集中配置源，`FD_Release`/`FD_Debug` 是固定构建档位，`Project_BuildGuard.h` 和 `tools/project_check.py` 负责自动检查。

## 3. 固定入口

| 场景 | 固定入口 | 说明 |
|---|---|---|
| 修改产品/功能配置 | `103 + 309/Project/Source/conf/Project_Config.h` | 在 Keil 中右键 `Configuration Wizard` 修改 |
| 在线调试 | Keil Target `FD_Debug` | 自动定义 `PROJECT_CFG_BUILD_PROFILE=1,_DEBUG_` |
| 正式发布 | Keil Target `FD_Release` | 不额外定义调试宏，走 Release 保护 |
| 发布保护 | `Project_BuildGuard.h` | Release 下禁止危险调试/测试项 |
| 自动检查 | `tools/project_check.py` | 不依赖 Keil，可在 macOS/Windows Git Bash/Python 环境运行 |

## 4. 每日工作流

### 4.1 调试代码

1. 在 Keil 选择 `FD_Debug`。
2. 需要临时观察变量时，优先增加全局观察变量或 `static const` 参数，不新增临时宏开关。
3. 调试完成后切回 `FD_Release`，运行：

```sh
python3 tools/project_check.py
```

### 4.2 发布代码

1. 在 Keil 选择 `FD_Release`。
2. 确认 `Project_Config.h` 中版本号、日期、产品类型正确。
3. 运行自动检查：

```sh
python3 tools/project_check.py
```

4. 在 Windows/Keil 环境执行 `FD_Release` 编译。
5. 若 Release 编译出现 `Release build:` 开头的错误，按错误提示关闭对应调试/测试项，不绕过 `Project_BuildGuard.h`。

## 5. 自动检查内容

`tools/project_check.py` 当前检查：

- Keil 工程必须存在 `FD_Release` 和 `FD_Debug` 两个 Target。
- `FD_Release` 不能定义 `_DEBUG_`、`_DEBUG_CODE`、Flash64K 测试、EasyLogger 输出等发布禁用项。
- `FD_Debug` 必须定义 `PROJECT_CFG_BUILD_PROFILE=1,_DEBUG_`。
- 两个 Target 的输出名必须隔离，避免调试产物覆盖发布产物。
- `Project_Config.h` 的发布安全默认值必须保持安全状态，例如看门狗开启、测试项关闭。
- `conf.h` 和 `elog_cfg.h` 必须 include `Project_BuildGuard.h`。
- `Project_BuildGuard.h` 必须覆盖当前发布风险宏。
- `.gitignore` 必须覆盖 Keil 产物、用户配置文件和 `.DS_Store`。
- `.githooks/pre-commit`、`.githooks/pre-push` 必须调用自动检查脚本。

## 6. Git Hook 启用

本仓库已经提供：

- `.githooks/pre-commit`
- `.githooks/pre-push`

首次在本机启用：

```sh
git config core.hooksPath .githooks
```

启用后：

- `git commit` 前会运行 `tools/project_check.py --quiet`，失败则阻止提交。
- `git push` 前会再次运行检查，并继续提示 `TODO.md`、`TEST_PENDING.md` 中未完成项。

## 7. 后续扩展原则

- 优先扩展 `tools/project_check.py`，不要继续堆宏开关。
- 新增高风险测试功能时，必须同时更新三处：`Project_Config.h`、`Project_BuildGuard.h`、`tools/project_check.py`。
- 若以后接入 CI，只需要在 CI 中执行 `python3 tools/project_check.py` 和 Keil 命令行 Release 构建。
- 若临时需要跳过某项保护，应先在文档中记录原因和恢复条件，不建议在源码中留长期绕过开关。
