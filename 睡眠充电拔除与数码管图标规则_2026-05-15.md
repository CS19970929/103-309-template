# 睡眠充电拔除与数码管图标规则

## 背景

本次修改覆盖两个运行规则：

1. 设备从休眠状态被 5V 充电唤醒后，本次启动只视为充电临时唤醒，不视为用户开机。
2. 数码管平时仍保持熄灭，只有用户按开关或已有显示窗口触发时才显示；在显示期间，放电 MOS 打开时充电图标点亮。

## 休眠态 5V 充电拔除

- `SleepDeal.c` 在睡眠启动流程里记录本次是否由 `GPIO_CHG_IN/PIN_CHG_IN` 充电唤醒。
- 充电唤醒会话使用备份域 `BKP_DR2/BKP_DR3` 的 `FLASH_SLEEP_CHARGER_WAKE_VALUE` 标志保存，不依赖 MCU 复位后的 RAM 变量。
- `DataDeal.c::charger_detect_and_keyLogi_200ms()` 在检测到充电器离线时，若本次是睡眠态充电唤醒，则保存数码管睡眠 SOC 并请求 `NORMAL_MODE` 休眠。
- 该路径不再调用 `open_dsg_close_chg()`，避免充满拔掉 5V 充电器后自动开放电输出。

## 数码管充电图标

- 显示唤醒门控不变：平时熄灭，用户按开关或已有显示窗口才刷新显示。
- 图标点亮条件扩展为：原 5V 充电在线，或 `SystemStatus.bits.b1Status_MOS_DSG` 表示放电 MOS 已打开。
- 放电 MOS 打开只影响充电图标是否点亮，不改变无放电电流时的 `LedBar_Command` 状态。

## 验证要点

- 睡眠态插入 5V 充电，确认充电期间不开放电。
- 充满或中途拔掉 5V 充电器，确认不会执行 `open_dsg_close_chg()`，并进入休眠流程。
- 正常非睡眠启动时，插拔 5V 仍保留原有 `open_chg_close_dsg()` / `open_dsg_close_chg()` 行为。
- 数码管未被按键唤醒时仍熄灭；按键显示时，放电 MOS 打开则充电图标点亮。
