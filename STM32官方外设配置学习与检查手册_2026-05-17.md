# STM32 官方外设配置学习与检查手册

日期：2026-05-17  
适用工程：`103 + 309` BMS 项目  
目标：把当前项目涉及的 STM32 外设、驱动、工程配置、官方知识点、配置方法和后续检查点整理成一份可学习、可复查的长期文档。

## 1. 使用方式

这份文档不替代 ST 官方手册，而是把“官方手册中该看哪里、当前项目怎么配、以后改动前检查什么”串起来。

建议使用顺序：

1. 先看第 2 节，明确官方资料入口。
2. 再看第 3 节，明确当前工程硬件和资源假设。
3. 修改某个外设前，先看对应章节的“官方配置方法”和“项目检查点”。
4. 修改后至少运行 `python3 tools/project_check.py`；涉及低功耗、CAN、Flash、SOC 的改动还必须上板验证。

## 2. 官方资料索引

| 官方资料 | 重点用途 | 当前项目对应内容 |
| --- | --- | --- |
| [RM0008 STM32F10xxx reference manual](https://www.st.com/resource/en/reference_manual/cd00171190-stm32f101xx-stm32f102xx-stm32f103xx-stm32f105xx-and-stm32f107xx-advanced-arm-based-32-bit-mcus-stmicroelectronics.pdf) | STM32F1 外设寄存器和工作机制。重点看 RCC、Flash interface、GPIO/AFIO、EXTI、DMA、ADC、TIM2/3/4、RTC、BKP、PWR、IWDG、USART、bxCAN。 | 绝大多数外设配置判断依据。 |
| [STM32F103C8 product page](https://www.st.com/en/microcontrollers-microprocessors/stm32f103c8.html) | 目标器件官方口径、Flash/SRAM、封装、外设能力。 | 当前 `.uvprojx` Device 是 `STM32F103C8`，必须和真实 BOM 对齐。 |
| [STM32F103x8/xB datasheet DS5319](https://www.st.com/resource/en/datasheet/stm32f103c8.pdf) | 电气特性、封装引脚、alternate function、时钟/ADC/低功耗指标。 | 管脚复用、Flash 容量、ADC 时钟、CAN/USART 引脚检查。 |
| [PM0075 STM32F10xxx Flash memory programming manual](https://www.st.com/resource/en/programming_manual/pm0075-stm32f10xxx-flash-memory-microcontrollers-stmicroelectronics.pdf) | Flash 解锁、页擦除、half-word 编程、IAP/ICP、Option bytes、保护位。 | App/IAP 地址、内部 Flash 参数/SOC/log/aging 持久化。 |
| [PM0056 STM32F10xxx Cortex-M3 programming manual](https://www.st.com/resource/en/programming_manual/pm0056-stm32f10xxx20xxx21xxxl1xxxx-cortexm3-programming-manual-stmicroelectronics.pdf) | Cortex-M3 核心、NVIC、SysTick、异常、`SCB->VTOR`。 | App 向量表偏移、IAP 跳转、SysTick delay、HardFault 保存。 |
| [ES096 STM32F101x8/B, STM32F102x8/B and STM32F103x8/B errata](https://www.st.com/resource/en/errata_sheet/es096-stm32f101x8b-stm32f102x8b-and-stm32f103x8b-mediumdensity-device-limitations-stmicroelectronics.pdf) | 中容量 STM32F103x8/xB 已知限制。 | 量产前对低功耗、Flash、CAN、ADC 等异常行为做最终核对。 |
| [STSW-STM32054 STM32F10x Standard Peripheral Library](https://www.st.com/en/embedded-software/stsw-stm32054.html) | SPL V3.5.0 官方库和示例来源。 | 当前工程使用 `STM32F10x_StdPeriph_Lib_V3.5.0`。 |

## 3. 当前工程硬件与资源假设

| 项目 | 当前配置 | 必须确认 |
| --- | --- | --- |
| MCU | Keil Device：`STM32F103C8`；编译宏：`STM32F10X_MD` | 真实芯片是否确实为 C8 64KB，还是使用了 128KB die/CB 口径。 |
| CPU core | Arm Cortex-M3 | 参考 PM0056 配置 NVIC、SysTick、VTOR、fault。 |
| 时钟 | 源码 `HSE_VALUE=8MHz`，`SYSCLK_FREQ_HSE`，HSE 直通系统时钟 | Keil `CLOCK(12000000)` 和旧 72MHz 注释必须统一，否则后续外设计算会错。 |
| IAP/App | IAP：`0x08000000`；App：`0x08004800`；`VECT_TAB_OFFSET=0x4800` | `.uvprojx` 链接地址仍是 `0x08000000`，需要修为可源控闭环。 |
| 内部 Flash 存储 | `0x0801C000~0x0801FC00` 后 64K 区域 | 只有真实 128KB Flash 时才可靠。 |
| 主要通信 | USART1 PB6/PB7 remap，上位机 19200 8N1；CAN1 PA11/PA12 250kbps | USART2/SPI1 默认不能随便打开，会和现有管脚冲突。 |
| 低功耗 | RTC Alarm + EXTI17 + PWR STOP + IWDG | STOP 后必须恢复 RCC、delay、ADC、SCI、CAN、TIM3。 |

## 4. 工程配置、启动与向量表

### 官方知识

PM0056 说明 Cortex-M3 通过向量表取 reset handler 和中断入口，`SCB->VTOR` 可以重定位向量表。PM0075 说明 IAP 场景中 Flash 可以由 Bootloader 和 App 分区协同使用，但 App 链接地址、向量表地址、Bootloader 跳转地址必须一致。

### 当前项目

- `system_stm32f10x.c`：`VECT_TAB_OFFSET 0x4800`。
- `Flash.h`：`FLASH_ADDR_IAP_START 0x08000000`，`FLASH_ADDR_APP_START 0x08004800`。
- `tools/soc_flash_app_safe.ps1`：只允许 App 烧录到 `0x08004800`。
- `.uvprojx`：`TextAddressRange 0x08000000`，`ScatterFile` 为空。

### 官方配置方法

1. Bootloader 链接到 `0x08000000`。
2. App 链接到 App 起始地址，例如 `0x08004800`。
3. App 的 `SCB->VTOR` 设置为 `FLASH_BASE + 0x4800`。
4. Bootloader 跳 App 前读取 App 向量表首字作为 MSP，第二字作为 reset handler。
5. 烧录脚本必须把 App bin 写到 App 链接地址。

### 项目检查点

- `.uvprojx` Release/Debug target 不能再保留 App `TextAddressRange=0x08000000`。
- 仓库必须有 tracked scatter 文件，不能依赖 `Objects/` 生成产物。
- `project_check.py` 应检查 `0x08004800`、`VECT_TAB_OFFSET=0x4800`、安全烧录脚本三者一致。

## 5. RCC 与系统时钟

### 官方知识

RM0008 的 RCC 章节定义 HSI、HSE、PLL、AHB、APB1、APB2 时钟树。STM32F103 最高 72MHz，但外设时钟不能只看 CPU 主频：ADC、CAN、USART、TIM 都依赖不同总线或分频。

### 当前项目

- `stm32f10x.h`：默认 `HSE_VALUE=8000000`。
- `system_stm32f10x.c`：启用 `SYSCLK_FREQ_HSE`，即 HSE 直通，不启 PLL。
- HCLK/PCLK1/PCLK2 当前都约为 8MHz。
- STOP 唤醒后 `rtc_sleep.c::cpu_frequency_conf()` 调用 `SystemInit()` 和 `SystemCoreClockUpdate()`。

### 官方配置方法

1. 确认真实晶振频率，修改 `HSE_VALUE`。
2. 开 HSE 并等待 `HSERDY`。
3. 配置 Flash latency 和 prefetch。
4. 配置 AHB/APB 分频。
5. 选择 SYSCLK source。
6. 更新 `SystemCoreClock`。
7. 重新计算所有依赖时钟的外设参数。

### 项目检查点

- 当前按 8MHz 核对：CAN 250kbps、TIM2/TIM3 10ms、ADC clock 约 1MHz。
- 若改 72MHz PLL，必须同步重算 CAN bit timing、ADC 分频、TIM prescaler、SysTick delay。
- Keil `CLOCK(12000000)` 和源码 8MHz 不一致，需要统一。

## 6. SysTick 与阻塞延时

### 官方知识

PM0056 的 SysTick 是 Cortex-M3 内核定时器，常用于 RTOS tick 或短延时。它是 24-bit counter，最大 delay 受 `LOAD` 和时钟源限制。

### 当前项目

- `System_Init.c::InitDelay()` 把 SysTick 时钟设置为 HCLK/8。
- `__delay_us()` / `__delay_ms()` 使用 SysTick busy-wait。
- `__delay_ms()` 中会喂 IWDG。
- 系统调度 tick 不是 SysTick，而是 TIM3。

### 官方配置方法

1. 选择 SysTick clock source。
2. 按 `SystemCoreClock` 计算每 us/ms tick 数。
3. 写 `LOAD`，清 `VAL`，使能 counter。
4. 等待 `COUNTFLAG`。
5. 禁用 counter，避免影响其他逻辑。

### 项目检查点

- 不要在主循环新增长时间 `__delay_ms()`，否则会拖慢 AFE/SOC/CAN。
- STOP 唤醒后必须重新 `InitDelay()`。
- 系统长期计时需求用 TIM3，不用 SysTick。

## 7. NVIC、异常与中断优先级

### 官方知识

PM0056 说明 NVIC 支持抢占优先级和响应优先级。异常处理如 HardFault、BusFault、UsageFault 由 Cortex-M3 统一进入异常向量。

### 当前项目

- `System_Init.c::InitNVIC()` 使用 `NVIC_PriorityGroup_1`。
- TIM3：preemption 0，sub 3。
- RTC Alarm：preemption 0，sub 0。
- CAN RX0：preemption 1，sub 1。
- USART：preemption 3，sub 3。
- HardFault 等异常会写 BKP fault snapshot 后 reset。

### 官方配置方法

1. 统一设置 priority group。
2. 每个外设只在初始化 owner 中配置 NVIC。
3. ISR 尽量短，只置 flag 或搬运少量数据。
4. 清外设 pending bit，再退出 ISR。
5. fault handler 不做复杂业务逻辑。

### 项目检查点

- 建议新增 IRQ 优先级表，避免后续 CAN/RTC/USART 互相抢占不清楚。
- fault handler 当前写 `BKP_DR11/DR12`，对 F103C8 中容量资源不可靠，需迁移或压缩。

## 8. GPIO、AFIO、重映射与 JTAG/SWD

### 官方知识

RM0008 的 GPIO/AFIO 章节定义 GPIO 输入、推挽输出、开漏输出、模拟输入、复用推挽、EXTI source 选择、外设 remap、SWJ/JTAG 配置。

### 当前项目

- `conf_gpio.h` 是当前管脚集中定义。
- `PubFunc.c::jtag_disableAndConfIO()` 禁用 JTAG，保留 SWD，让 PA15/PB3/PB4 可作 GPIO。
- USART1 remap 到 PB6/PB7。
- CAN1 使用默认 PA11/PA12。
- LedBar 占用 PA4/PA5/PA6/PB10/PB11。
- SH367309 软件 TWI 占用 PB8/PB9。

### 官方配置方法

1. 先开 AFIO 和 GPIO port clock。
2. 普通输出配置 `GPIO_Mode_Out_PP` 或开漏输出。
3. 模拟输入配置 `GPIO_Mode_AIN`。
4. 外设输出配置 `GPIO_Mode_AF_PP`。
5. 输入中断先配置 GPIO input，再 `GPIO_EXTILineConfig()`。
6. 需要复用时调用 `GPIO_PinRemapConfig()`。
7. 使用 PA15/PB3/PB4 前必须正确配置 SWJ remap。

### 项目检查点

- PA2/PA3：USART2 与 Type-C ADC/2727_EN 冲突。
- PA4/PA5/PA6：SPI1 与 LedBar 冲突。
- PB3/PB4/PA15：依赖 JTAG disable，量产仍需保留 SWD。
- PB6/PB7：USART1 主口，不要切到 `I2C_AFE1.c` 的非活动 `F_TWI_*` 分支。
- 建议把 pin ownership 做成脚本检查。

## 9. EXTI 与唤醒源

### 官方知识

RM0008 EXTI 章节说明每条 EXTI line 可映射到一个 GPIO port source，支持 rising/falling trigger，pending bit 必须清除。RTC Alarm 使用 EXTI17。

### 当前项目

- PA0：充电/插入唤醒。
- PA9：按键唤醒。
- PB12/PB13：通信/MCU wake 相关。
- PB7/PA3：UART wake 可选。
- EXTI17：RTC Alarm。
- `LowPower_ClearWakeupPending()` 清 EXTI0/9/12/13 和 UART wake pending。

### 官方配置方法

1. 开 AFIO clock。
2. 配置 GPIO input。
3. `GPIO_EXTILineConfig(port, pin_source)`。
4. 配置 `EXTI_Line`、mode、trigger。
5. 清 pending bit。
6. 配置 NVIC。
7. ISR 中记录原因并清 pending。

### 项目检查点

- 唤醒极性必须和板级电路一致。
- 退出 STOP 后靠读 GPIO 推断唤醒原因不是最可靠，关键唤醒源应在 ISR 层记录。
- 休眠前必须清 pending，避免刚进 STOP 立即醒。

## 10. PWR STOP 低功耗

### 官方知识

RM0008 PWR 章节说明 STOP mode 可关闭主时钟，保留 SRAM 和寄存器，唤醒后系统时钟通常回到 HSI，需要重新配置时钟树。STOP 可用 EXTI/RTC 等唤醒。

### 当前项目

- `conf.c::Sys_StopMode()` 关闭 TIM3，清 pending，调用 `PWR_EnterSTOPMode(PWR_Regulator_LowPower, PWR_STOPEntry_WFI)`。
- 唤醒后调用 `cpu_frequency_conf()` 重配系统时钟。
- `InitRunAfterStopWakeup()` 恢复 RTC、IO、ADC、SCI、CAN、TIM3、AFE IIC。

### 官方配置方法

1. 进入 STOP 前关闭不需要的外设和外部电源域。
2. 配置所有唤醒 EXTI/RTC。
3. 清 pending。
4. 执行 `WFI` 进入 STOP。
5. 唤醒后重新配置系统时钟。
6. 恢复 GPIO、ADC、USART、CAN、timer、AFE 通信。
7. 再恢复业务状态。

### 项目检查点

- STOP 前必须保存 SOC、CAN、FactoryAging 等核心状态。
- STOP 后必须先重建时钟，再恢复依赖时钟的外设。
- IWDG 在 STOP/低速唤醒期间仍可能计时，RTC 周期必须受 watchdog 安全窗口约束。

## 11. RTC、LSE/LSI 与 BKP

### 官方知识

RM0008 RTC/BKP/PWR 章节说明 RTC 位于 backup domain，访问 RTC/BKP 前需要打开 PWR/BKP 时钟并允许 backup access。RTC clock 可来自 LSE、LSI 或 HSE/128。LSE 精度最好，LSI 精度较差但不依赖外部晶振。

### 当前项目

- `RTC.c` 首选 LSE，失败 fallback 到 LSI。
- `BKP_DR1` 保存 RTC 初始化标记。
- RTC Alarm 配 EXTI17。
- `RTC_GetWakeupPeriodSeconds()` 结合 CAN idle 周期和 IWDG 安全窗口。
- SOC 休眠修正调用 `SOC_ApplyRtcRelaxationCompensation()`。

### 官方配置方法

1. 开 PWR/BKP clock。
2. `PWR_BackupAccessCmd(ENABLE)`。
3. 选择并启动 LSE/LSI。
4. `RCC_RTCCLKConfig()` 选择 RTC clock。
5. `RCC_RTCCLKCmd(ENABLE)`。
6. 等待 RTC 同步和上一次写操作完成。
7. 设置 prescaler，使 RTC counter 以 1s tick 运行。
8. 配置 Alarm 和 EXTI17。

### 项目检查点

- LSI fallback 下不要把 RTC 当高精度时钟。
- 休眠 SOC 校准应继续使用稳定窗口和小步修正，不要因 RTC 秒数直接大幅校准。
- BKP 分配已经紧张：DR1~DR10 被占用，DR11/DR12 对当前 F103C8 口径不可靠。

## 12. IWDG 独立看门狗

### 官方知识

RM0008 IWDG 章节说明 IWDG 使用 LSI，配置 prescaler 和 reload 后需要周期喂狗。LSI 精度会随温度和电压漂移。

### 当前项目

- Release 默认 `PROJECT_CFG_WDOG_ENABLE=1`。
- 普通模式：prescaler 64，reload 800。
- RTC 模式：prescaler 256，reload 4095。
- RTC wake 周期会被限制在 watchdog 安全窗口内。

### 官方配置方法

1. 允许写 IWDG 寄存器。
2. 设置 prescaler。
3. 设置 reload。
4. reload counter。
5. enable IWDG。
6. 在主循环和长等待里按时喂狗。

### 项目检查点

- 长阻塞等待必须喂狗。
- RTC STOP 周期必须按最差 LSI 漂移留余量。
- 调试模式下 DBGMCU freeze 不能带入 Release。

## 13. TIM2/TIM3/TIM4 定时器

### 官方知识

RM0008 TIM2~TIM5 章节说明通用定时器可产生 update event、output compare、PWM、trigger output、interrupt。定时器输入时钟来自 APB，总线分频变化会影响 tick。

### 当前项目

| Timer | 用途 | 配置 |
| --- | --- | --- |
| TIM2 | ADC external trigger | 100kHz counter，Period 999，OC2 pulse，约 10ms trigger。 |
| TIM3 | 系统运行态 tick | 100kHz counter，Period 999，10ms update interrupt。 |
| TIM4 | LedBar 扫描 | 100kHz counter，Period `PROJECT_CFG_LEDBAR_SCAN_TIMER_100KHZ_TICKS - 1`。 |

### 官方配置方法

1. 开 timer clock。
2. `TIM_TimeBaseInit()` 配 prescaler、period、counter mode。
3. 如需比较/触发，配置 OC channel。
4. 清 pending bit。
5. 如需中断，配置 `TIM_ITConfig()` 和 NVIC。
6. `TIM_Cmd(ENABLE)`。

### 项目检查点

- TIM3 是运行态时间基准，不累计 STOP 时间。
- TIM2 只作为 ADC 内部触发，不需要输出到 GPIO。
- TIM4 属于 LedBar owner，低功耗前必须停掉。

## 14. ADC1 与 DMA1

### 官方知识

RM0008 ADC 章节说明 F1 ADC 支持 scan conversion、external trigger、校准、DMA request。DMA 章节说明 DMA channel 从外设寄存器搬运到内存，循环模式适合持续采样。

### 当前项目

- ADC1 independent mode。
- scan mode enable，continuous disable。
- external trigger：TIM2 CC2。
- channels：
  - ADC1_IN9 / PB1：MOS 温度。
  - ADC1_IN2 / PA2：Type-C current。
  - ADC1_IN1 / PA1：VBUS/总压分压。
- DMA1_Channel1：ADC1->DR 到 `g_u16ADCValFilter[]`，halfword circular。
- ADC calibration 有 timeout，失败会置 `ERROR_ADC`。

### 官方配置方法

1. 配 GPIO 为 analog input。
2. 开 ADC clock，并配置 ADC prescaler。
3. 配 DMA：peripheral addr = `ADC1->DR`，memory addr = buffer，halfword，circular。
4. 配 ADC scan、trigger、channel sequence、sample time。
5. enable ADC 和 DMA request。
6. reset calibration，等待完成。
7. start calibration，等待完成。
8. enable external trigger。

### 项目检查点

- `ADC_NbrOfChannel`、DMA buffer size、regular channel 顺序必须一致。
- 不要打开 DMA TC interrupt，除非实现对应 ISR。
- Type-C current 是独立输出侧采样，不能当作 AFE 电池侧电流直接处理。
- ADC 采样公式和硬件分压/分流电阻必须同步文档。

## 15. USART1/2/3 与上位机通信

### 官方知识

RM0008 USART 章节说明 USART 需要配置 baud、word length、stop bits、parity、mode、interrupt。IDLE interrupt 可用于一帧结束辅助判断。

### 当前项目

- USART1：PB6/PB7 remap，19200 8N1，主上位机口。
- USART2：框架存在，PA2/PA3，默认不应启用。
- USART3：可 full remap PD8/PD9，默认不启用。
- `Sci_Upper.c` 使用 RXNE + IDLE 接收，TXE/TC 发送。
- 协议是 Modbus-like：0x03、0x06、0x10，含 CRC16。

### 官方配置方法

1. 开 USART clock 和 GPIO/AFIO clock。
2. 如需 remap，先配置 AFIO remap。
3. TX 配 AF_PP，RX 配 floating input 或上拉输入。
4. `USART_Init()` 配 baud、8N1、无 flow control。
5. 清接收寄存器，打开 RXNE/IDLE interrupt。
6. 发送时按 TXE/TC 状态推进。
7. ISR 处理错误标志 ORE/NE/FE/PE。

### 项目检查点

- 固定上位机方式是 `COM4/19200/slave=1`。
- 量产 `0xD300 supported=0` 是正常结果。
- USART2 与 PA2/PA3 当前用途冲突，不应随意启用。
- 改协议地址时同步 `COMMUNICATION_ADDRESS_INDEX.md` 和测试脚本。

## 16. CAN1 / bxCAN 与低功耗发送

### 官方知识

RM0008 bxCAN 章节说明 CAN bit timing 由 PCLK、prescaler、SJW、BS1、BS2 决定。Normal mode 需要总线上有 ACK 才能成功发送；NART 控制自动重发行为。

### 当前项目

- CAN1 默认 PA11/PA12。
- PCLK1 当前 8MHz。
- `Prescaler=4`，`BS1=5tq`，`BS2=2tq`，`SJW=1tq`。
- bit rate = `8MHz / (4 * (1 + 5 + 2)) = 250kbps`。
- `CAN_NART=ENABLE`，无 ACK 不自动重发。
- PB4 `CMNT_EN` 控制 CAN transceiver power。
- RTC 唤醒后 `Can_RtcWakeService()` bounded window 发送探测/周期帧。

### 官方配置方法

1. 开 AFIO/GPIO/CAN clock。
2. 配 CAN RX input，TX AF_PP。
3. 配 CAN init：mode、ABOM、AWUM、NART、bit timing。
4. 配 filter。
5. 配 RX interrupt。
6. 发送前确认 transceiver 上电稳定。
7. 发送后读取 mailbox status 和 error status。

### 项目检查点

- 只要系统时钟/PCLK1 改动，CAN bit timing 必须重算。
- 低功耗场景下不要把 CAN 发送写成长阻塞 delay。
- 台架必须覆盖无对端、有对端、BusOff、RTC 唤醒后发送成功。
- `CMNT_EN` 电平要结合收发器 datasheet 和实测电流确认。

## 17. Flash、IAP 与内部存储

### 官方知识

PM0075 说明 STM32F10xxx Flash 可 ICP 或 IAP 编程。Flash 擦写通常以页为单位，编程以 half-word 为基本粒度。写 Flash 前要 unlock，操作后 lock，写后应读回校验。

### 当前项目

- IAP：`0x08000000`。
- App：`0x08004800`。
- 后 64K storage：`0x0801C000~0x0801FC00`。
- `Flash.c` 封装 erase verified、program halfword verified、双槽和 journal。
- `StorageFlash_PrintBootCheck()` 会读取 `0x1FFFF7E0` Flash size register 并打印。

### 官方配置方法

1. 确认目标地址属于真实 Flash 容量范围。
2. `FLASH_Unlock()`。
3. 清 `EOP/PGERR/WRPRTERR`。
4. page erase。
5. 检查页是否全 `0xFFFF`。
6. half-word program。
7. 读回校验。
8. `FLASH_Lock()`。
9. 对数据结构加 magic/version/length/sequence/crc。

### 项目检查点

- 真实 Flash 小于 128KB 时禁止写 `0x0801C000+`。
- App bin 禁止写 `0x08000000`。
- `StorageFlash_CalcCrc()` 当前把 length 转成 `UINT8` 传给 CRC，后续 payload 超过 255 字节前必须修。
- Flash 写入频率要受限，不能把高频运行状态直接写 Flash。

## 18. BKP 寄存器分配

### 官方知识

RM0008 BKP 章节说明 backup registers 位于 backup domain。低/中容量与高容量器件可用 backup register 数量不同，不能只看 SPL 头文件是否定义宏。

### 当前项目

| 寄存器 | 当前用途 |
| --- | --- |
| DR1 | RTC init marker |
| DR2/DR3 | Sleep flag / inverse |
| DR4/DR5 | LedBar sleep SOC / inverse |
| DR6~DR10 | FactoryAging progress |
| DR11/DR12 | Fault snapshot |

### 官方配置方法

1. 开 PWR/BKP clock。
2. `PWR_BackupAccessCmd(ENABLE)`。
3. 只写目标器件实际存在的 backup registers。
4. 重要数据用 magic + inverse + crc。
5. 需要清空时明确 backup domain reset 的影响。

### 项目检查点

- 对 F103C8 中容量口径，DR11/DR12 不应作为可靠资源。
- FactoryAging 同时依赖 BKP 和 Flash，返修/重新老化时要同时清。
- 建议新增 `BKP_REGISTER_ALLOCATION.md` 并让脚本检查 DR 最大编号。

## 19. 软件 TWI 与 SH367309 AFE 驱动

### 官方知识

这里没有使用 STM32 硬件 I2C，而是使用 GPIO bit-bang。STM32 官方资料只负责 GPIO 输入/输出模式、电平读写和时序基础；SH367309 的寄存器、CRC、MTP、保护阈值应查 SH367309 官方 datasheet。

### 当前项目

- `I2C_AFE1.h` 启用 `_TWI_COM`。
- PB8：TWI clock。
- PB9：TWI data。
- `I2C_AFE1.c` 实现 start/stop/read/write/CRC8。
- `SH367309_Func.c` 和 `SH367309_DataDeal.c` 负责 AFE 参数、保护状态、MOS 控制。

### 配置方法

1. PB8/PB9 初始为 GPIO 输出高。
2. 发送 start/stop 时按 AFE 协议拉 SDA/SCL。
3. 读数据时切 data pin 为输入。
4. 写数据时切 data pin 为输出。
5. 每次 MTP/寄存器读写做 CRC/ACK 检查。
6. AFE 初始化后再打开 MOS 或进入老化模式。

### 项目检查点

- 非活动 `F_TWI_*` 分支使用 PB6/PB7，会撞 USART1，不要切过去。
- AFE driver 不应继续承担过多业务逻辑；长期应拆 bus、driver、protection adapter。
- MTP 写入失败应有字段级日志，不能只靠总错误计数。

## 20. LedBar Charlieplexing 显示

### 官方知识

Charlieplexing 本质是 GPIO 动态扫描，不是 STM32 专用外设。STM32 官方资料涉及 GPIO mode、输出速度、TIM update interrupt、低功耗前关闭 GPIO/Timer。

### 当前项目

- 5 个 GPIO：PB11、PA4、PA5、PA6、PB10。
- `LedBar.c` 使用 TIM4 扫描。
- 支持普通短按显示 5s、唤醒显示 10s、充电/故障图标、睡前 SOC 保存。
- 当前已删除 74HC595 路径，保留单一路径。

### 配置方法

1. 扫描前把所有 LedBar pin 置为高阻/输入，避免串亮。
2. 每个 route 只驱动一高一低，其余高阻。
3. TIM4 周期中断推进 route。
4. 低功耗前关闭 TIM4 和输出。
5. 唤醒后按状态重新初始化。

### 项目检查点

- 不要重新启用 SPI/74HC595 兼容层。
- PA4/PA5/PA6 与 SPI1 功能冲突。
- 扫描 ISR 只做轻量刷新，业务判断留在 `APP_LedBar()`。

## 21. DBGMCU 与调试配置

### 官方知识

RM0008 DBGMCU 章节允许调试时在 Sleep/Stop/Standby 中保持调试连接，也可 freeze IWDG/WWDG。此功能只适合调试，不适合 Release。

### 当前项目

- `_DEBUG_` 下 `EnableLowPowerDebug()` / `DBGMCU_Config(DBGMCU_STOP, ENABLE)`。
- Release BuildGuard 禁止调试输出和测试模式。

### 项目检查点

- Release 必须 `PROJECT_CFG_BUILD_PROFILE=0`。
- Release 不允许 `_DEBUG_`、`PROJECT_CFG_DEBUG_WATCH_ENABLE=1`、`ELOG_OUTPUT_ENABLE`。
- 低功耗电流测试必须用 Release 口径，不能用 DBGMCU freeze 口径。

## 22. 当前项目外设修改前总检查清单

| 修改内容 | 必查项 |
| --- | --- |
| 改 MCU 型号/Flash 容量 | `.uvprojx` Device、startup、`STM32F10X_*` 宏、Flash storage 地址、BKP 寄存器数量。 |
| 改 App/IAP 地址 | App scatter、`VECT_TAB_OFFSET`、IAP 跳转、烧录脚本、`project_check.py`。 |
| 改系统时钟 | `HSE_VALUE`、RCC、CAN bit timing、ADC clock、TIM2/3/4、SysTick、USART baud。 |
| 开 USART2 | PA2/PA3 与 ADC/2727_EN 冲突。 |
| 开 SPI1 | PA4/PA5/PA6 与 LedBar 冲突。 |
| 改 CAN | PCLK1、bit timing、filter、NART、CMNT_EN、电流和 ACK 测试。 |
| 改 RTC/STOP | EXTI pending、RTC Alarm、IWDG 窗口、STOP 后恢复顺序、SOC 休眠修正。 |
| 改 Flash 存储 | 容量、页大小、擦写频率、CRC 长度、掉电恢复、双槽/journal。 |
| 改 BKP | 目标器件可用 DR 数量、现有 DR 分配、reset/deep sleep 清除策略。 |
| 改 SOC 采样 | AFE 电流、Type-C 电流、ADC 通道顺序、200ms period、host C replay。 |
| 改 LedBar | TIM4、GPIO 高阻/输出切换、低功耗关闭、按键/唤醒显示窗口。 |

## 23. 推荐后续固化到脚本的检查

`tools/project_check.py` 建议继续增加以下硬检查：

1. Keil App 起始地址必须是 `0x08004800`。
2. `VECT_TAB_OFFSET` 必须等于 `0x4800`。
3. 仓库必须存在 tracked App scatter 文件。
4. `STM32F10X_MD` 不能配 `startup_stm32f10x_hd.s`。
5. `STM32F103C8` 口径不能默认写后 64K Flash，除非显式声明真实 128KB。
6. BKP 使用最大编号不能超过目标器件资源。
7. `PROJECT_CFG_SCI2_ROLE != 0` 时禁止 PA2/PA3 继续作为 ADC/2727_EN。
8. LedBar 启用时禁止 SPI1/74HC595 管脚复用。
9. CAN bit timing 与 `SystemCoreClock/PCLK1` 的计算结果必须等于目标波特率。
10. Release target 禁止 debug/watch/test/easylogger 输出。

## 24. 最小验证矩阵

| 验证项 | 方法 |
| --- | --- |
| 静态门禁 | `python3 tools/project_check.py` |
| App 地址 | 检查 scatter/map/bin 烧录 dry-run，确认 App base `0x08004800` |
| Flash 容量 | 上电读取 `0x1FFFF7E0` flash size，确认是否允许后 64K |
| USART | `COM4/19200/slave=1` 读 `0xD000`、`0xD100`、`0xD200`、`0xD300` |
| CAN | 有对端/无对端/BusOff/恢复/RTC 唤醒发送 |
| RTC STOP | STOP 电流、RTC 唤醒周期、IWDG 不复位、唤醒后 ADC/SCI/CAN/TIM3 恢复 |
| ADC | 三通道原始值、Type-C 电流、总压分压、MOS 温度 |
| SOC | 真实 C host test、replay、低压尾段、满电、休眠恢复 |
| LedBar | 短按 5s、唤醒 10s、充电图标、故障显示、睡前 SOC 预览 |
| FactoryAging | 运行态 3 天计时、reset/deep sleep 恢复、Flash/BKP 清除策略 |

## 25. 当前项目源码映射表

这一节用于后续实际改代码时快速定位，避免只知道 STM32 外设理论，不知道本项目真实 owner 在哪里。

| 外设/配置 | 当前项目 owner | 关键函数/宏 | 当前关键值 | 修改前必须同步检查 |
| --- | --- | --- | --- | --- |
| MCU target | `103 + 309/Project/Users/CommomSH367309_16series_103RCT6_C.uvprojx` | `Device`、`Define`、startup file | `STM32F103C8`、`STM32F10X_MD`、但 startup 是 `startup_stm32f10x_hd.s` | Device、startup、Flash size、BKP 寄存器数量必须一致。 |
| App/IAP 地址 | `Flash.h`、`system_stm32f10x.c`、`tools/soc_flash_app_safe.ps1`、`.uvprojx` | `FLASH_ADDR_APP_START`、`VECT_TAB_OFFSET`、`TextAddressRange` | App 需求是 `0x08004800`；Keil 当前仍是 `0x08000000` | 先修 scatter/Keil 链接配置，再烧录。 |
| 系统时钟 | `STM32F10x_StdPeriph_Lib_V3.5.0/drivers/system_stm32f10x.c` | `SYSCLK_FREQ_HSE`、`SetSysClockToHSE()` | HSE 8MHz 直通 | CAN、ADC、TIM、SysTick 全部依赖这个值。 |
| STOP 后时钟恢复 | `rtc_sleep.c`、`conf.c` | `cpu_frequency_conf()`、`Sys_StopMode()` | STOP 后重跑 `SystemInit()` | 改低功耗后必须确认唤醒恢复顺序。 |
| SysTick delay | `System_Init.c` | `InitDelay()`、`__delay_us()`、`__delay_ms()` | HCLK/8，阻塞 delay | 不作为系统调度 tick 使用。 |
| 系统 tick | `System_Init.c`、`Runtime.c` | `InitTimer()`、`TIM3_IRQHandler()`、`SysTime_LatchTaskFlags()` | TIM3 10ms | FactoryAging、LedBar、CAN、ADC 调度都间接依赖。 |
| AFE/SOC 200ms | `System_Init.c`、`DataDeal.c`、`SOC.c`、`SocEnhance.c` | `SysTime_Take200msTaskPeriod()`、`App_AFEGet()`、`App_SOC()` | 200ms pending period，上限 5 个缓存周期 | 不能让主循环长阻塞导致采样积压。 |
| ADC1 + DMA | `ADC.c`、`ADC.h`、`conf_gpio.h` | `InitADC()`、`InitADC_ADC1()`、`InitADC_DMA()` | PB1/PA2/PA1 三通道，TIM2 CC2 触发 | 通道顺序、DMA buffer、`AD_Used_amount` 必须一起改。 |
| Type-C current | `ADC.c`、`SocEnhance.c` | `ADC_Current_Smooth()`、`SOC_GetTypeCBatEquivCurrentA10()` | PA2 独立输出侧采样 | 不能直接当 AFE 电池侧电流。 |
| AFE current | `DataDeal.c`、`I2C_AFE1.c`、`SH367309_Func.c` | `UpdateVoltageFromBqMaximo()`、`DataLoad_Current()` | SH367309 电流寄存器链路 | 和 Type-C current 是两条不同信号链。 |
| 软件 TWI | `I2C_AFE1.h/c` | `_TWI_COM`、`TWI_CLK_*`、`TWI_DAT_*` | PB8/PB9 | 不要误切到 PB6/PB7 的非活动函数分支。 |
| USART1 上位机 | `Sci_Upper.c`、`Sci_Upper.h` | `InitSCI1_CommonUpper()`、`Sci_PortIRQHandler()` | PB6/PB7 remap，19200 8N1 | 默认上位机主口；不要和软件 TWI 冲突。 |
| USART2 | `Sci_Upper.c`、`conf_gpio.h` | `InitSCI2_CommonUpper()`、`PROJECT_CFG_SCI2_ROLE` | PA2/PA3，默认关闭 | 打开会撞 Type-C ADC/2727_EN。 |
| CAN1 | `Can_HDX.c`、`CanFeidaoFrames.c` | `InitCan()`、`Can_RtcWakeService()`、`Can_HDX_Transmit()` | PA11/PA12，250kbps，PB4 控收发器 | 改时钟必须重算 bit timing。 |
| RTC | `RTC.c`、`rtc_sleep.c` | `Init_RTC()`、`RTC_WKTimeConfig()`、`RTC_GetWakeupPeriodSeconds()` | LSE 优先，LSI fallback，Alarm EXTI17 | LSI 下只能做近似计时。 |
| PWR STOP | `conf.c`、`rtc_sleep.c`、`LowPowerSleep.c` | `Sys_StopMode()`、`rtc_sleep_run_hiccup_cycle()`、`LowPowerSleep_SaveCoreState()` | STOP + low power regulator | 进入前保存核心状态，退出后恢复外设。 |
| BKP | `RTC.c`、`SleepDeal.c`、`LedBar.c`、`FactoryAging.c`、`FaultSnapshot.h` | `BKP_DR1~DR12` | DR1~DR10 已占满，DR11/12 有风险 | 对 F103C8 口径应避免依赖 DR11/12。 |
| Flash storage | `Flash.h/c`、`EEPROM.c` | `StorageFlash_Save*()`、`FlashWriteOneHalfWord()` | `0x0801C000~0x0801FC00` | 必须确认真实 128KB Flash。 |
| IWDG | `System_Init.c` | `Init_IWDG()` | Release 打开；RTC 模式更长 reload | RTC 周期必须小于 watchdog 安全窗口。 |
| LedBar | `LedBar.c/h`、`Project_Config.h` | `LedBar_Init()`、`APP_LedBar()`、`TIM4_IRQHandler()` | PB11/PA4/PA5/PA6/PB10，TIM4 扫描 | 不要恢复 SPI/74HC595 兼容路径。 |
| FactoryAging | `FactoryAging.c`、`Flash.h`、`main.c` | `FactoryAging_Task()`、`enter_fac_mode()` | 3 天运行态 tick，Flash + BKP 保存 | reset/deep sleep 边界必须同时覆盖。 |
| BuildGuard | `Project_Config.h`、`Project_BuildGuard.h`、`conf.h` | `PROJECT_CFG_BUILD_PROFILE` 等 | Release 默认 0，SOC test 关闭 | 量产/测试隔离不能靠人工记忆。 |

## 26. 当前项目重点外设调用链

### 26.1 上电初始化链路

当前 `main.c::InitDevice()` 的真实顺序：

1. `SystemInit()`：按 `system_stm32f10x.c` 配系统时钟和 `SCB->VTOR`。
2. `InitDelay()`：初始化 SysTick 阻塞延时。
3. `IsSleepStartUp()`：读取睡眠启动标记。
4. `jtag_disableAndConfIO()`：禁用 JTAG，释放 PA15/PB3/PB4。
5. `InitNVIC()`：设置 NVIC priority group。
6. `InitIO()`：GPIO 默认态。
7. `InitSci()`：USART1 上位机。
8. `InitSystemWakeUp()`：当前为空。
9. `StorageFlash_PrintBootCheck()`：打印后 64K Flash 存储状态。
10. `InitE2PROM()`：当前是内部 Flash 参数 facade。
11. `InitAFE1()`：SH367309 初始化。
12. `InitCan()`：CAN1 与收发器控制。
13. `InitADC()`：ADC1 + DMA + TIM2。
14. `InitData_SOC()`：SOC 初始化。
15. `InitTimer()`：TIM3 10ms 系统 tick。
16. `Init_IWDG()`：Release 下启用 IWDG。

检查意义：如果新增外设，必须考虑它和 AFE、CAN、ADC、SOC、TIM3、IWDG 的初始化先后关系。

### 26.2 主循环运行链路

当前 `Runtime_RunOnce()` 是项目运行主线：

1. `SysTime_LatchTaskFlags()`：把 TIM3 ISR pending flag 搬到主循环。
2. `FactoryAging_Task()`：累计运行态 3 天。
3. `APP_LedBar()`：显示、按键、唤醒显示窗口。
4. `App_AFEGet()`：200ms AFE 主采样、保护状态、SOC sample。
5. `App_Sci()`：上位机协议。
6. `App_AnlogCal()`：ADC 平滑计算。
7. `App_LowPowerProcess()`：RTC/STOP 低功耗。
8. `App_Can()`：CAN 周期发送和低功耗探测。
9. `StorageFlash_AppUseTest_Task()`、`App_FlashUpdate()`、`App_LogRecord()`、`App_ProID_Deal()`。
10. `Feed_IWatchDog`。

检查意义：任何新驱动不能在主循环里长时间阻塞；否则会影响 200ms AFE/SOC、CAN 发送、IWDG。

### 26.3 RTC STOP 链路

当前低功耗 owner 是 `rtc_sleep.c`：

1. `LowPowerSleep_SaveCoreState()` 保存 CAN/SOC/FactoryAging 等核心状态。
2. `before_rtcsleep()` 做睡前准备。
3. `Init_RTC()`、`IOstatus_RTCMode()`、`InitWakeUp_RTCMode()`。
4. `Sys_StopMode()` 进入 STOP。
5. 唤醒后 `RTC_DisableStopWakeup()`，再按 RTC/异常唤醒恢复。
6. RTC 唤醒时 `update_rtc_soc()` 和 `Can_RtcWakeService()`。
7. 非 RTC 异常唤醒时恢复运行态、上报唤醒原因。

检查意义：低功耗改动必须同时看 `RTC.c`、`rtc_sleep.c`、`conf.c`、`Can_HDX.c`、`SocEnhance.c`、`LedBar.c`、`FactoryAging.c`。

### 26.4 Flash 持久化链路

当前内部 Flash 不只是 IAP flag，还承载：

- AFE 参数：`StorageFlash_LoadAfeData()` / `StorageFlash_SaveAfeData()`。
- RW 参数：`StorageFlash_LoadRwParamData()` / `StorageFlash_SaveRwParamData()`。
- SOC 快照：`StorageFlash_LoadSocData()` / `StorageFlash_SaveSocData()`。
- 日志：`StorageFlash_LoadLogData()` / `StorageFlash_SaveLogData()`。
- FactoryAging：`StorageFlash_LoadFactoryAgingData()` / `StorageFlash_SaveFactoryAgingData()`。
- IAP/睡眠/升级标志：`FLASH_ADDR_UPDATE_FLAG` 等。

检查意义：确认真实 Flash 容量之前，不要继续扩大后 64K 存储用途。

## 27. 当前项目最应优先补的工程化资产

| 资产 | 目的 | 建议落点 |
| --- | --- | --- |
| App scatter 文件 | 把 `0x08004800` 从口头规则变成工程真相 | `103 + 309/Project/Users/FD_Release_App_0x08004800.sct` 或同级 tracked 路径 |
| Pin ownership 表 | 防止 USART2/SPI1/TWI/LedBar/ADC 管脚互撞 | `docs/pin_ownership.md` 或 `tools/project_check.py` 内置表 |
| BKP 分配表 | 防止 DR 超出目标器件能力 | `BKP_REGISTER_ALLOCATION.md` |
| Clock ownership 表 | 记录 HSE、SYSCLK、PCLK1/PCLK2、CAN/ADC/TIM 派生关系 | `系统时钟系统梳理.md` 增补或新建 clock check |
| Flash layout 表 | 绑定 App/IAP/storage/flag 地址和真实容量 | `STORAGE_LAYOUT_REPORT.md` 增补 |
| 低功耗恢复清单 | 修改 STOP/RTC 时避免漏恢复外设 | `docs/rtc_low_power_strategy.md` 增补 |
| CAN 台架测试表 | 验证 RTC 唤醒后发送闭环 | `CAN_RTC低功耗广播修复说明.md` 或新增测试报告 |

## 28. 当前项目学习路线

如果目的是学习并能后续独立检查，建议按这个顺序看源码：

1. `main.c`：理解初始化顺序。
2. `Runtime.c`：理解主循环任务编排。
3. `System_Init.c`：理解 TIM3 tick、SysTick delay、IWDG。
4. `conf_gpio.h` + `conf.c`：理解管脚、EXTI、STOP GPIO 状态。
5. `ADC.c`：理解 ADC1/DMA/TIM2 触发和 Type-C 采样。
6. `I2C_AFE1.c` + `SH367309_Func.c` + `DataDeal.c`：理解 AFE 数据链路。
7. `SOC.c` + `SocEnhance.c`：理解 SOC 使用哪些外设数据。
8. `Sci_Upper.c`：理解上位机读写如何影响参数和测试模式。
9. `Can_HDX.c` + `CanFeidaoFrames.c`：理解 CAN 低功耗发送。
10. `RTC.c` + `rtc_sleep.c`：理解 RTC/STOP 和唤醒恢复。
11. `Flash.c` + `EEPROM.c`：理解内部 Flash 持久化。
12. `LedBar.c`：理解显示、按键、休眠 SOC 预览。
13. `Project_Config.h` + `Project_BuildGuard.h`：理解量产和测试隔离。

每看一个模块，都按这 5 个问题复查：

1. 这个模块实际 owner 是谁？
2. 它依赖哪些 STM32 外设和哪些 GPIO？
3. 它的时基来自 TIM3、SysTick、RTC，还是外部事件？
4. 它的数据会不会写 Flash/BKP？
5. 它进入 STOP 前后需要做什么？
