# SOC测试需求梳理

## 你的核心诉求

1. SOC 模块要能解释清楚，不能只靠经验调参。
2. 测试要接近真实骑行，必须模拟真实电压、电流和快变负载。
3. MCU 端也要参与测试，因为只跑 PC 仿真不足以证明固件在板子上可靠。
4. 测试模式不能影响正常出货量产程序。
5. 上位机要有 UI，方便观察 SOC、电压、电流、容量和故障状态。
6. 测试要自动化、可加速、可输出报告，并方便后续项目复用。

## 需要验证的问题

### 安时积分

- 当前频率是多少：已确认 MCU 为 `200ms/次`，即 `5Hz`。
- 快变电流是否准确：需要验证每个 200ms 样本是否代表该窗口平均电流。
- 尖峰电流风险：短于 200ms 且未被 AFE/滤波平均到的尖峰会有混叠风险，需要通过快变电流场景观察误差。

### 电压校准与锚点

- 满电不能只靠安时积分到 100，必须通过满电电压锚点确认。
- 低压不能被大电流压降误判为空，需要 sag holdoff。
- 接近控制器截止电压时，SOC 应能收敛到 0。
- 静置/RTC 恢复后 OCV 只能小步修正，不能突然跳变。

### 板端一致性

- 内部 SOC、显示 SOC、通信输出 SOC 要一致或差异可解释。
- 容量 `cap_now/cap_full/cap_factory` 与 SOH、循环次数要一致。
- RS485 读取值要能反映真实 `g_stCellInfoReport` 和 `SocElement`。

## MCU 端需要提供什么

### 量产固件必须提供

- `0xD000` 实时状态读取：电压、电流、SOC、SOH、容量、循环次数、故障位。
- `0x2318~0x231B` SOC 参数读取：容量、循环次数、V100、V0。
- `0x1005` 单次设置 SOC，供校准和验证使用。
- 正常保护逻辑、采样逻辑、存储逻辑不被测试上位机影响。

### 测试固件建议提供

- `0x2500` 测试样本注入：`enable/vmax/vmin/ichg/idsg/ticks`。
- `0xD300` 测试状态读取：是否支持测试模式、tick、最大加速 tick、最近样本、当前 SOC/SOH。
- 编译期隔离：Release 不能打开测试模式。
- 参数限制：单次加速 tick 有上限，电压/电流范围非法时拒绝。

## 上位机需要提供什么

- 串口扫描和连接参数设置。
- 在线监控：实时表格、曲线、CSV 保存。
- 参数读取：容量、循环次数、V100、V0。
- 安全写入：设置一次 SOC 必须确认并读回。
- 离线仿真：不用板子即可跑真实骑行模型。
- MCU 加速测试：测试固件下把同一批真实骑行样本送入 MCU。
- 报告输出：Markdown 汇总和 CSV 明细。
- 复用能力：场景库、协议函数、测试脚本可迁移到新项目。

## 当前状态

已完成：

- PC 侧真实骑行场景仿真。
- `200ms/5Hz` 安时积分和快变电流脉冲测试。
- Tkinter 测试上位机 UI。
- RS485 在线监控。
- MCU 测试模式协议设计和固件入口。
- Release 构建保护，测试模式默认关闭。
- 文档和自动化测试脚本。

待硬件闭环：

- 烧录测试固件后执行 `0x2500` MCU 注入加速测试。
- CAN 输出一致性测试。
- 断电恢复和长时间骑行实测。
