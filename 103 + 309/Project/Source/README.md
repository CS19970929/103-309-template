# 旧应用层源码已退役

本目录原来的 `*.c` / `*.h` 应用层源码已在 `codex/full-rewrite-2026-05-12` 分支删除。

当前有效实现位于仓库根目录：

- `firmware_rewrite/`
- `PROJECT_REWRITE_REQUIREMENTS_2026-05-12.md`
- `tools/run_rewrite_host_tests.py`

保留本文件只是为了让历史路径有明确说明，避免后续继续把旧 `main.c`、旧 SOC、旧低功耗、旧 CAN 调度作为真相源。
