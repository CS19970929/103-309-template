# 存储迁移实现说明

## 1. 本次已实现范围

本次已经把项目从“外部 EEPROM 依赖”迁移到“内部 Flash + AFE 内部 MTP/BKP”的组合方案，且只保留你明确要求的存储对象：

1. `SOC` 相关参数
2. `IAP` 升级标志
3. `AFE` 参数
4. 休眠备份域寄存器
5. `100` 条事件日志

其余历史 EEPROM 持久化对象，本次不再落盘，只保留运行期默认值或 RAM 行为，避免继续维护已经废弃的存储路径。

## 2. 存储介质分工

### 2.1 MCU 内部 Flash

用于替代外部 EEPROM，承载以下数据：

1. `AFE` 参数镜像
2. `SOC` 持久化参数
3. `100` 条日志及日志写指针
4. `IAP` 升级标志

### 2.2 AFE 内部 MTP/EEPROM

仍然保留为 AFE 自身硬件配置的真实存储区。  
本次对 AFE 参数写入做了“双重校验”：

1. 先把参数写入 MCU 内部 Flash，并回读校验
2. 后台再把参数写入 AFE 内部 MTP，并再次回读校验

任一步失败都视为写失败。

### 2.3 BKP 备份域寄存器

休眠/唤醒标志继续走备份域寄存器，不迁移到 Flash。  
这样可以保持原有低功耗唤醒语义，也避免无意义擦写。

## 3. Flash 地址规划

### 3.1 固定不变地址

`IAP` 升级标志地址保持不变：

- `FLASH_ADDR_UPDATE_FLAG = 0x0801F800`

本次没有改这个地址。

### 3.2 新增内部 Flash 存储区

| 数据 | 地址 A | 地址 B | 说明 |
| --- | --- | --- | --- |
| AFE 参数 | `0x0803C000` | `0x0803C800` | 双槽快照 |
| 日志 | `0x0803D000` | `0x0803D800` | 双页追加日志 |
| SOC | `0x0803E000` | `0x0803E800` | 双页追加日志 |

说明：

1. `SOC` 继续使用原先 `0x0803E000/0x0803E800` 这一段地址。
2. `SOC` 和 `LOG` 没有采用“每次整页擦写”的低寿命方案，而是改成“页内追加记录，页满后切页”的热数据策略。
3. `AFE` 参数属于低频配置，仍采用双槽快照。

## 4. 数据结构和写策略

### 4.1 AFE 参数

MCU Flash 中保存 `24` 个 AFE 参数的 `curValue` 镜像。

写入流程：

1. 串口改参时，先修改 RAM 中的 `curValue`
2. 立即把 `24` 个参数整体保存到 MCU Flash
3. 保存后回读 MCU Flash，校验内容一致
4. 只有 MCU Flash 校验成功，才置位 `AFE_PARAM_WRITE_Flag`
5. 后台任务检测到 `AFE_PARAM_WRITE_Flag` 后，写入 AFE 内部 MTP
6. 写完再次整包回读 AFE 内部 MTP
7. 回读一致才算成功，否则报错并重新置位写标志

这保证了：

1. 主控 Flash 镜像可靠
2. AFE 真正生效的内部参数可靠
3. 现场掉电后，仍能从 MCU Flash 恢复最近一次确认成功的参数集

### 4.2 SOC 参数

当前持久化字段：

1. `SOC`
2. `DSG_SOC_Int`
3. `CycleTimes`

存储策略：

1. 上电优先从 Flash 恢复
2. 如果无效或越界，回落到默认初始化值并重新写回 Flash
3. 运行中当这 3 个字段有变化时写入新记录
4. 写入采用“页内追加 + 双页轮换”，降低擦写频率

### 4.3 100 条日志

当前持久化字段：

1. `BMS_LOG_POINT`
2. `BMS_LOG_RECORD[100][2]`

存储策略：

1. 每次新增事件后，保存当前完整日志快照
2. Flash 底层采用“页内追加 + 双页轮换”
3. 上电先恢复最新有效快照
4. 若日志区损坏或索引越界，则清空并重建

## 5. 校验和容错

所有 MCU Flash 存储记录都带以下头信息：

1. `magic`
2. `version`
3. `length`
4. `sequence`
5. `crc`

读取时只接受：

1. `magic` 正确
2. `version` 正确
3. `length` 正确
4. `crc` 校验通过

写入时：

1. Flash 页擦除后会检查整页是否真的为 `0xFFFF`
2. HalfWord 编程后会立即回读校验
3. 记录写完后会再次按完整记录回读校验

AFE 参数额外还有一次 AFE MTP 回读校验，因此是双链路校验。

## 6. 代码层面的改动

### 6.1 已删除/废弃

1. 外部 EEPROM 的 I2C bit-bang 物理层实现已移除
2. `conf.c` 中 EEPROM 引脚初始化调用已移除
3. 启动阶段重新恢复 `InitE2PROM()`，但其职责已改成“内部存储初始化/恢复”

### 6.2 已保留但不再持久化

以下对象当前只保留运行期默认值，不再写 Flash：

1. Protect 参数
2. K/B 校准
3. OtherElement
4. Heat/Cool
5. Product ID
6. 其他旧 EEPROM 参数区

原因很直接：你已经明确这批对象暂时不用实现持久化，本次只保留运行逻辑，不再维护旧 EEPROM 存储链。

### 6.3 兼容处理

旧代码中仍有少量非必须功能会走 `WriteEEPROM_*` 接口。  
当前处理方式是：

1. 旧 EEPROM 读写接口变成空实现/默认返回
2. 后台旧脏标志持续清零
3. 避免旧逻辑卡住主流程

这使得非本次范围内的老功能不会再真的写存储，但也不会因为等待 EEPROM 完成而堵塞运行。

## 7. 当前行为边界

### 7.1 本次保证的

1. `IAP` 标志地址不变
2. `AFE` 参数支持掉电恢复
3. `AFE` 参数写入具备 MCU Flash 与 AFE MTP 双重回读校验
4. `SOC` 参数支持掉电恢复
5. `100` 条日志支持掉电恢复
6. 休眠相关标志继续使用 BKP 备份域寄存器

### 7.2 本次明确未做

1. Protect 参数持久化
2. K/B 持久化
3. OtherElement 持久化
4. Heat/Cool 持久化
5. Product ID 持久化
6. 历史故障记录持久化扩展

如果后续你决定恢复其中某一类，只需要继续在 `Flash.c` 下新增对应对象的存储布局，不需要再回到外部 EEPROM 方案。

## 8. 编译验证

工程：

- `103 + 309/Project/Users/CommomSH367309_16series_103RCT6_C.uvprojx`

目标：

- `Target 1`

工具链：

- `Keil MDK 5.32`
- `ARMCC 5.06 update 7`

构建结果：

- `0 Error(s), 0 Warning(s)`
- `Code=39192`
- `RO-data=2964`
- `RW-data=1232`
- `ZI-data=6048`

产物：

1. `103 + 309/Project/Users/Objects/CommomSH367309_16series_103RCT6_C.axf`
2. `103 + 309/Project/Users/Objects/CommomSH367309_16series_103RCT6_C.bin`

## 9. 后续建议

如果后面继续裁剪，我建议按这个顺序做：

1. 明确 `Protect/KB/OtherElement` 里哪些字段最终还要掉电保持
2. 把真正永久废弃的旧 EEPROM 地址宏和旧接口声明继续清理掉
3. 若日志非常频繁，可把日志区从 `2` 页扩成 `4` 页，进一步提高寿命
