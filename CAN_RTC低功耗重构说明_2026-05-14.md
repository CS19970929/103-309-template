# CAN 与 RTC 低功耗重构说明

## 需求梳理

本次重构按以下运行目标收敛 CAN 通信和 RTC 休眠逻辑：

1. 正常运行态 CAN 仍按现有飞道协议周期发送：电压/电流和 SOC 为 1s 周期，容量、SOH、版本、状态、出厂时间为 5s 周期。
2. 准备进入 RTC 低功耗时，CAN 周期发送不能长期阻塞休眠入口；即使当前 CAN 正在上电等待、发送 pending 或 mailbox 残留，也要在入睡前统一收口。
3. RTC 周期唤醒后，如果 CAN 总线有可 ACK 的设备，板端应打开 CAN 窗口，发送一次 1s 周期帧，然后重新关闭 CAN 收发器并进入下一轮 RTC STOP。
4. 如果长时间无 ACK，则降低功耗，退回 10s RTC 探测周期；下次探测到 ACK 或收到有效 CAN 帧后，再恢复 1s 周期。
5. 调试时必须能直接看到 RTC 入口阻塞原因、CAN 休眠阻塞原因、当前 CAN 是否认为总线活跃、RTC 唤醒服务次数和 CAN 发送窗口次数。

## 代码入口

| 模块 | 入口 | 说明 |
| --- | --- | --- |
| `rtc_sleep.c` | `BQ769x0_SleepMode_Ctrl()` | 只判断电流、加热、MCU_WK、老化模式、外部通信计数、AFE 休眠条件，不再用普通 CAN 忙态阻塞 RTC 入口。 |
| `rtc_sleep.c` | `rtc_sleep_prepare_rtc()` | 每次进入 STOP 前关闭 CAN 窗口；第一轮 RTC 会话额外调用 `Can_BeginRtcSleepSession()`。 |
| `rtc_sleep.c` | `rtc_sleep_run_hiccup_cycle()` | RTC 唤醒且无异常时调用 `Can_RtcWakeService()`，发完 CAN 窗口后继续下一轮 STOP。 |
| `Can_HDX.c` | `Can_PrepareSleep()` | 清 pending mask、取消 3 个 TX mailbox、必要时 `CAN_DeInit(CAN1)` 清硬件 mailbox，再关闭 CAN 收发器。 |
| `Can_HDX.c` | `Can_BeginRtcSleepSession()` | 开始 RTC 会话时强制保留 6 个 1s 活跃探测周期。 |
| `Can_HDX.c` | `Can_GetIdleRtcPeriodSeconds()` | 总线活跃或强制活跃探测剩余时返回 1s，否则返回 10s。 |

## 行为变化

旧逻辑中，`isErr_enterRTC()` 会因为 `Can_IsSleepBlocked()` 返回忙而重置 RTC 进入计时，CAN 周期发送每 1s 到期一次时容易反复推迟低功耗入口。

新逻辑中，`Can_IsSleepBlocked()` 仍保留，但只用于调试和状态观察。RTC 入口不再被普通 CAN 忙态阻塞；当系统真正准备进入 RTC STOP 时，统一由 `Can_PrepareSleep()` 收口 CAN 状态。

RTC 会话开始后，即使之前 CAN 状态被判为不活跃，也会先按 1s 周期探测 6 次。任一发送收到 ACK 或收到有效 CAN 帧后，`Can_IsBusActive()` 变为 1，后续 RTC 周期保持 1s。连续无 ACK 后，周期退回 10s 以降低功耗。

## 调试观察点

新增 `g_stCanLowPowerStatus`：

| 字段 | 含义 |
| --- | --- |
| `u8SleepBlockReason` | CAN 休眠阻塞原因：0 无阻塞，1 状态机未空闲，2 软件跟踪 mailbox 未空，3 硬件 mailbox 未空。 |
| `u8BusActive` | CAN 是否认为总线活跃。 |
| `u8NoAckCnt` | 连续无 ACK 计数。 |
| `u8RtcForceActiveCycles` | RTC 会话剩余强制 1s 探测次数。 |
| `u16RtcWakeServiceCnt` | RTC 唤醒后执行 CAN 服务次数。 |
| `u16RtcWakeTxWindowCnt` | RTC 唤醒后打开 CAN 发送窗口次数。 |
| `u16PrepareSleepCnt` | CAN 进入休眠准备次数。 |

新增 `g_stLowPowerRtcStatus`：

| 字段 | 含义 |
| --- | --- |
| `u8BlockReason` | RTC 入口阻塞原因：0 无，1 电流，2 加热，3 MCU_WK，4 老化模式，5 外部通信，6 AFE 未空闲。 |
| `u8CanBlockReason` | 同步记录 CAN 休眠原因码。 |
| `u8SleepModeSelect` | 当前休眠模式选择。 |
| `u8StateSleep` | 当前休眠状态机阶段。 |
| `u16EnterRtcDelay` | RTC 入口已累计秒数。 |
| `u16EnterRtcTarget` | RTC 入口目标秒数。 |
| `u32RtcElapsedSeconds` | 当前 RTC 会话累计休眠秒数。 |

`rtc_sleep_dump_state()` 日志中同步打印 `period/can/cblk/rblk`，用于判断当前 RTC 周期、CAN 活跃判断、CAN 原因码和 RTC 原因码。

## 验证建议

1. 无 CAN 设备：进入 RTC 后先观察最多 6 次 1s 探测，连续无 ACK 后应退回 10s 周期。
2. 有可 ACK 的 CAN 设备：RTC 唤醒后 `u8BusActive=1`，`Can_GetIdleRtcPeriodSeconds()` 应保持 1，`u16RtcWakeTxWindowCnt` 每次 RTC 唤醒递增。
3. 故意让 CAN mailbox pending：触发 RTC 入口后 `Can_PrepareSleep()` 应递增 `u16PrepareSleepCnt`，最终 `u8SleepBlockReason` 回到 0。
4. MCU_WK 或外部通信变化：`u8BlockReason` 应分别显示 `3` 或 `5`，RTC 入口计时清零。
5. 用示波器看 `GPIO_CMNT_EN`：每次 RTC CAN 发送窗口应短时打开，发送完成或超时后关闭。
