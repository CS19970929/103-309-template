# CAN 与 RTC 低功耗极简策略说明

## 本次目标

这次继续删减 CAN 通信和 RTC 低功耗之间的耦合，按更直接的规则运行：

1. RTC 是否能进入低功耗，只由电流、加热、MOS 状态、MCU_WK、老化模式、外部通信变化和 AFE 空闲状态决定。
2. CAN 不再参与 RTC 入睡准入判断，不再提供 `Can_IsSleepBlocked()`、`Can_GetSleepBlockReason()` 这类容易误用的入口。
3. 进入 RTC STOP 前统一调用 `Can_PrepareSleep()`，清 pending、取消 mailbox、必要时 `CAN_DeInit(CAN1)`，然后关闭 CAN 收发器。
4. RTC 唤醒后只做一次 CAN 服务窗口：总线活跃则补发周期帧，总线不活跃则发送 1s 探测帧；服务完成或超时后再次关闭 CAN。
5. CAN 连续无 ACK 后认为总线不活跃，RTC 周期退回 10s；任一发送 ACK 成功或收到 CAN 帧后恢复 1s RTC 周期。

## 保留的核心入口

| 模块 | 入口 | 说明 |
| --- | --- | --- |
| `rtc_sleep.c` | `BQ769x0_SleepMode_Ctrl()` | 直线判断 RTC 阻塞原因；无阻塞时累计 `enter_rtc_delay`，到时进入 `HICCUP_MODE`。 |
| `rtc_sleep.c` | `rtc_sleep_prepare_rtc()` | 每次 STOP 前保存 SOC/老化进度并调用 `Can_PrepareSleep()`。 |
| `rtc_sleep.c` | `rtc_sleep_run_hiccup_cycle()` | RTC 唤醒且无异常时更新 SOC，然后调用 `Can_RtcWakeService()`，之后继续下一轮 STOP。 |
| `Can_HDX.c` | `Can_PrepareSleep()` | CAN 休眠收口的唯一入口。 |
| `Can_HDX.c` | `Can_GetIdleRtcPeriodSeconds()` | CAN 活跃返回 1s，不活跃返回 10s。 |
| `Can_HDX.c` | `Can_RtcWakeService()` | RTC 唤醒后的唯一 CAN 发送窗口。 |

## 已删除的复杂逻辑

- 删除 `Can_IsSleepBlocked()` / `Can_GetSleepBlockReason()`，避免 RTC 再因为 CAN 状态机忙、软件 mailbox 或硬件 mailbox 残留而反复推迟入睡。
- 删除 `Can_BeginRtcSleepSession()` / `Can_EndRtcSleepSession()`，RTC 不再维护 CAN 会话。
- 删除 RTC 强制活跃探测计数和发送窗口计数，改成“活跃 1s、不活跃 10s、唤醒只服务一次”的单一规则。
- `g_stLowPowerRtcStatus` 不再记录 CAN 阻塞原因，只记录 RTC 自己的阻塞原因和计时状态。

## 当前调试观察点

`g_stCanLowPowerStatus` 保留最少必要字段：

| 字段 | 含义 |
| --- | --- |
| `u8BusActive` | CAN 是否认为总线活跃。 |
| `u8NoAckCnt` | 连续无 ACK 计数。 |
| `u8ProbeActive` | 当前是否在发送不活跃总线探测帧。 |
| `u16PendingMask` | 待发送帧掩码。 |
| `u16RtcWakeServiceCnt` | RTC 唤醒后执行 CAN 服务次数。 |
| `u16PrepareSleepCnt` | CAN 休眠收口次数。 |

`g_stLowPowerRtcStatus.u8BlockReason` 原因码：

| 值 | 含义 |
| --- | --- |
| 0 | 无阻塞 |
| 1 | 充/放电电流存在 |
| 2 | 加热中 |
| 3 | 同口 MOS 关闭 |
| 4 | MCU_WK 唤醒脚有效 |
| 5 | 出厂老化模式 |
| 6 | 外部通信变化 |
| 7 | AFE 未空闲 |

`rtc_sleep_dump_state()` 只打印 `period/can/rblk`，分别表示下一次 RTC 周期、CAN 活跃判断和 RTC 阻塞原因。

## 验证建议

1. 无 CAN 设备：RTC 唤醒后发送探测帧，连续无 ACK 后 `u8BusActive=0`，下一次 RTC 周期为 10s。
2. 有可 ACK 的 CAN 设备：RTC 唤醒后 `u8BusActive=1`，`Can_GetIdleRtcPeriodSeconds()` 返回 1，每次 RTC 唤醒只打开一次 CAN 服务窗口。
3. 故意制造 CAN mailbox pending：进入 RTC 前应由 `Can_PrepareSleep()` 收口，`u16PrepareSleepCnt` 递增，不再阻塞 `enter_rtc_delay`。
4. MCU_WK、外部通信变化、AFE 未空闲：只看 `u8BlockReason`，对应原因出现时 `enter_rtc_delay` 清零。
5. 示波器观察 `GPIO_CMNT_EN`：RTC 唤醒服务窗口短时打开，发送完成或 1.5s 超时后关闭。
