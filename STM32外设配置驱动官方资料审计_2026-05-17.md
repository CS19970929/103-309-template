# STM32 外设、配置、驱动官方资料审计

日期：2026-05-17  
工程路径：`/Users/cs/Downloads/work/todo/103-309-template`  
审计对象：`103 + 309/Project/Users/CommomSH367309_16series_103RCT6_C.uvprojx`、`103 + 309/Project/Source`、`STM32F10x_StdPeriph_Lib_V3.5.0`、`tools/soc_flash_app_safe.ps1`

## 1. 结论摘要

当前工程是 STM32F103 + SH367309 AFE 的 BMS 保护板应用，外设主线已经比较完整：`TIM3` 提供 10ms 系统 tick，`ADC1 + DMA1 + TIM2` 做 Type-C/总压/MOS 温度采样，`USART1` 做上位机 Modbus-like 通信，`CAN1` 做飞道帧与低功耗探测，`RTC + PWR STOP + BKP` 做周期唤醒和休眠状态保存，内部 Flash 做参数、SOC、日志和老化进度持久化。

按 ST 官方资料核对后，当前最大风险不是单个外设初始化 API 写法，而是工程配置和资源假设没有闭环：

| 优先级 | 问题 | 判断 |
| --- | --- | --- |
| P0 | App 链接地址没有源控闭环 | 代码和烧录脚本要求 App 在 `0x08004800`，但 `.uvprojx` `TextAddressRange` 仍是 `0x08000000`，且仓库没有可追踪的 App scatter 文件。 |
| P0 | 后 64K Flash 存储依赖真实 128KB 器件 | 工程 Device 是 `STM32F103C8`，ST 产品页口径为 64KB Flash；代码持久化地址使用 `0x0801C000~0x0801FC00`，必须确认真实芯片是否为 128KB 口径。 |
| P1 | `BKP_DR11/DR12` 不适合作为 F103C8 中容量可靠资源 | 当前 DR1~DR10 已被 RTC/Sleep/LedBar/FactoryAging 占满，Fault snapshot 用 DR11/DR12；按 RM0008 中低/中容量 backup register 资源口径，应优先改为 Flash 或重新压缩 DR 分配。 |
| P1 | startup 文件与编译密度宏不一致 | `.uvprojx` 使用 `startup_stm32f10x_hd.s`，但 C 宏是 `STM32F10X_MD`，目标 Device 也是 `STM32F103C8`。建议统一为 medium-density startup。 |
| P1 | 时钟假设分散 | 源码 `HSE_VALUE=8MHz`，`system_stm32f10x.c` 走 HSE 直通；Keil target 写 `CLOCK(12000000)`，部分注释仍写 72MHz。当前运行计算多数基于 `SystemCoreClock`，但后续改 PLL/晶振会直接影响 CAN bit timing、ADC clock、TIM tick。 |

## 2. 官方资料依据

本次只采用 ST 官方资料作为 STM32 判断依据，第三方文章不作为结论依据。

| 官方资料 | 本次使用范围 |
| --- | --- |
| [RM0008 STM32F10xxx reference manual](https://www.st.com/resource/en/reference_manual/cd00171190-stm32f101xx-stm32f102xx-stm32f103xx-stm32f105xx-and-stm32f107xx-advanced-arm-based-32-bit-mcus-stmicroelectronics.pdf) | RCC、GPIO/AFIO、EXTI、DMA、ADC、TIM2/3/4、RTC、IWDG、BKP/PWR、bxCAN、USART、Flash interface 的寄存器与外设行为依据。 |
| [STM32F103C8 product page](https://www.st.com/en/microcontrollers-microprocessors/stm32f103c8.html) 与 [STM32F103x8/xB datasheet DS5319](https://www.st.com/resource/en/datasheet/stm32f103c8.pdf) | 目标器件资源、Flash/SRAM、封装引脚、外设数量、电气和时钟限制依据。 |
| [PM0075 STM32F10xxx Flash memory programming manual](https://www.st.com/resource/en/programming_manual/pm0075-stm32f10xxx-flash-memory-microcontrollers-stmicroelectronics.pdf) | 内部 Flash 解锁、页擦除、half-word 编程、页大小和写入约束依据。 |
| [PM0056 Cortex-M3 programming manual](https://www.st.com/resource/en/programming_manual/pm0056-stm32f10xxx20xxx21xxxl1xxxx-cortexm3-programming-manual-stmicroelectronics.pdf) | `SCB->VTOR`、NVIC、SysTick、异常向量行为依据。 |
| [ES096 STM32F101x8/B, STM32F102x8/B and STM32F103x8/B errata](https://www.st.com/resource/en/errata_sheet/es096-stm32f101x8b-stm32f102x8b-and-stm32f103x8b-mediumdensity-device-limitations-stmicroelectronics.pdf) | 中容量 F103x8/xB 器件限制核对。 |
| [STSW-STM32054 STM32F10x Standard Peripheral Library](https://www.st.com/en/embedded-software/stsw-stm32054.html) | 当前工程 SPL V3.5.0 API 来源和示例库依据。 |

## 3. 工程目标与配置真相

| 项目 | 当前源码/工程值 | 官方资料核对与判断 |
| --- | --- | --- |
| Keil Device | `.uvprojx`：`STM32F103C8` | ST 产品页给 `STM32F103C8` 的 Flash 口径是 64KB；若实际使用 128KB die，应在 BOM、工程 Device、文档中改成明确的 xB/CB 口径。 |
| 编译宏 | `STM32F10X_MD, USE_STDPERIPH_DRIVER` | 中容量宏和 F103C8 口径一致。 |
| startup | `startup_stm32f10x_hd.s` | 与 `STM32F10X_MD` 不一致，建议换成 `startup_stm32f10x_md.s`。 |
| App 起始地址 | `Flash.h`：`0x08004800`，`system_stm32f10x.c`：`VECT_TAB_OFFSET 0x4800`，烧录脚本也固定 `0x08004800` | 这是符合 IAP + App 分区需求的方向。 |
| Keil 链接地址 | `.uvprojx`：`TextAddressRange 0x08000000`，`ScatterFile` 为空 | 与 App 起始地址冲突。按 PM0056 的 `VTOR` 语义，向量表基址和链接地址必须一致，否则 IAP 跳 App 后可能异常。 |
| Flash 存储地址 | `0x0801C000~0x0801FC00` | 这些地址只有在真实 128KB Flash 可用时成立；若目标真是 64KB C8，则越界。 |
| 系统时钟 | `SYSCLK_FREQ_HSE = HSE_VALUE`，`HSE_VALUE=8MHz` | RM0008 RCC 配置允许 HSE 作为 SYSCLK。当前 CAN/ADC/TIM 计算应按 8MHz 核对，而不是旧注释里的 72MHz。 |

## 4. STM32 外设 inventory 与官方核对

| 外设 | 当前配置/驱动位置 | 官方资料依据 | 判断 |
| --- | --- | --- | --- |
| RCC/Clock | `system_stm32f10x.c` 使用 `SYSCLK_FREQ_HSE`，`SetSysClockToHSE()`，HCLK/PCLK1/PCLK2 不分频；`rtc_sleep.c::cpu_frequency_conf()` 在 STOP 后重跑 `SystemInit()` | RM0008 RCC；DS5319 时钟限制 | 用法方向正确。风险是 Keil `CLOCK(12000000)`、旧 72MHz 注释和 8MHz 源码不一致。 |
| SysTick | `System_Init.c::InitDelay()` 只用于阻塞 `__delay_us/ms`，`__delay_ms()` 中喂 IWDG | PM0056 SysTick | 可用。不要把 SysTick 当系统调度 tick；主 tick 是 TIM3。 |
| NVIC | `NVIC_PriorityGroup_1`；TIM3、TIM4、CAN、USART、RTC/RTCAlarm、EXTI 分别初始化 | PM0056 NVIC；RM0008 各外设中断 | 基本可用。建议补一张 IRQ 优先级表，避免后续改 CAN/RTC 时互相抢占不清楚。 |
| GPIO/AFIO | `conf_gpio.h` 管脚定义，`conf.c` 初始化；`jtag_disableAndConfIO()` 禁用 JTAG 保留 SWD；USART1 remap 到 PB6/PB7 | RM0008 GPIO/AFIO；DS5319 pinout | 当前能支撑硬件。关键是必须维护 pin ownership，尤其 PA2/PA3、PB3/PB4/PA15、PA4/PA5/PA6/PB10/PB11。 |
| EXTI | PA0 charger、PA9 key、PB12/PB13、UART wake、RTC Alarm EXTI17 | RM0008 EXTI/AFIO；PWR/RTC 章节 | 低功耗唤醒链路方向正确。部分唤醒原因仍靠退出 STOP 后读 GPIO 推断，建议加 IRQ 层记录。 |
| TIM3 | `System_Init.c::InitTimer()`：`SystemCoreClock/100000 - 1`，Period 999，形成 10ms tick | RM0008 general-purpose timers TIM2-TIM5 | 符合需求。它是运行态时间基准，3 天老化、200ms AFE/SOC 调度都应继续以它为准。 |
| TIM2 | `ADC.c::InitADC_TIMER()` 产生 TIM2 CC2 event，驱动 ADC external trigger | RM0008 TIM2 + ADC external trigger | 配置合理。当前没有把 TIM2_CH2 remap 输出到 PB3，这是正确的，避免和 `AD_EN` 冲突。 |
| TIM4 | `LedBar.c` 用 TIM4 update interrupt 扫描 Charlieplexing | RM0008 TIM2-TIM5 | 可用。低功耗前应关闭 TIM4，当前已有 stop/enable 逻辑。 |
| ADC1 | `ADC.c`：scan mode，external trigger `T2_CC2`，通道 PB1/PA2/PA1，采样时间 239.5/55.5/239.5 cycles，ADC clock `PCLK2/8` | RM0008 ADC；DS5319 ADC clock/electrical limits | 当前 8MHz PCLK2 下 ADC clock 约 1MHz，安全；若以后改 72MHz，`PCLK2/8=9MHz` 仍低于常见 F1 ADC 上限，但必须重新核对采样周期和计算节拍。 |
| DMA1 Channel1 | `ADC.c::InitADC_DMA()`，ADC1 DR -> `g_u16ADCValFilter[]`，halfword，circular，不开 TC IRQ | RM0008 DMA + ADC DMA request | 用法合理。不要启用被注释的 DMA TC interrupt，除非同时实现 `DMA1_Channel1_IRQHandler()`。 |
| USART1 | `Sci_Upper.c::InitSCI1_CommonUpper()`，PB6/PB7 remap，19200 8N1，RXNE + IDLE 收帧，TXE/TC 按发送过程打开 | RM0008 USART + AFIO remap | 符合上位机需求。当前是主通信口。 |
| USART2 | 默认 runtime 框架存在，PA2/PA3 | RM0008 USART2 默认引脚 | 默认关闭是正确的；若打开会和 PA2 Type-C ADC、PA3 `2727_EN` 冲突。 |
| USART3 | 可选 full remap 到 PD8/PD9 | RM0008 USART3 remap | 当前未用。若启用要确认封装和板上引脚真实引出。 |
| CAN1/bxCAN | `Can_HDX.c`：PA11/PA12 默认映射，normal mode，Prescaler=4，BS1=5tq，BS2=2tq，SJW=1tq，`NART=ENABLE`；PB4 `CMNT_EN` 控制收发器供电 | RM0008 bxCAN；DS5319 alternate function pins | 在当前 8MHz PCLK1 下 bit rate = `8MHz / (4 * (1+5+2)) = 250kbps`，与代码意图一致。若系统时钟改为 72MHz/36MHz PCLK1，必须同步重算 CAN bit timing。 |
| RTC | `RTC.c`：优先 LSE，失败 LSI；BKP_DR1 标记初始化；Alarm + EXTI17 周期唤醒；`RTC_GetWakeupPeriodSeconds()` 结合 CAN idle 和 IWDG window | RM0008 RTC/BKP/PWR；DS5319 LSE/LSI；PM0056 NVIC/EXTI | 方向正确。LSI 精度低，适合低功耗唤醒和看门狗窗口，不适合长期精确日历；SOC 休眠 OCV 只能用稳定窗口，不应信任 LSI 绝对时间过细。 |
| PWR/STOP | `conf.c::Sys_StopMode()`：关 TIM3，清 pending，`PWR_EnterSTOPMode(PWR_Regulator_LowPower, WFI)`，唤醒后 `SystemInit()` | RM0008 PWR STOP mode | 符合 STOP 后重建时钟要求。建议在文档中固定“STOP 后必须重建 RCC/Delay/ADC/SCI/CAN/TIM3”。 |
| BKP | DR1 RTC，DR2/3 Sleep flag，DR4/5 LedBar SOC，DR6~10 FactoryAging，DR11/12 Fault snapshot | RM0008 BKP backup data registers | DR1~DR10 分配清楚；DR11/12 对 F103C8 中容量目标不应依赖，见 P1 问题。 |
| IWDG | `Init_IWDG()`：Release 打开；RTC 模式 prescaler 256 reload 4095 | RM0008 IWDG；DS5319 LSI | 当前 RTC wake 最大 10s，按 nominal 40k LSI 有余量。仍建议实板测 LSI/IWDG 最短超时，避免温度漂移造成边界。 |
| Flash controller | `Flash.c`：unlock、page erase、halfword program、读回校验；双槽和 journal | PM0075；RM0008 Flash interface | 写法符合 Flash 编程基本流程。最大风险是地址/容量闭环，而不是 API 顺序。 |
| 硬件 I2C/SPI | 主链路未用硬件 I2C/SPI；SH367309 用 PB8/PB9 软件 TWI；LedBar 占用 PA4/PA5/PA6/PB10/PB11 | RM0008 I2C/SPI/GPIO | 当前无硬件 I2C/SPI 初始化冲突。源码里 `F_TWI_*` 的 PB6/PB7 分支是非活动分支，不能误切，否则会撞 USART1。 |
| DBGMCU | `_DEBUG_` 下启用 STOP/IWDG debug | RM0008 DBGMCU | 仅调试可用，Release 不应打开。 |

## 5. 关键管脚 ownership

| 管脚 | 当前 owner | 风险点 |
| --- | --- | --- |
| PA0 | `CHG_IN` / EXTI0 | 充电器插拔唤醒，需确认下降沿极性和外部上拉/下拉。 |
| PA1 | `ADC_VBUS` / ADC1_IN1 | 总压分压采样。 |
| PA2 | `ADC_CUR` / ADC1_IN2 / Type-C current | 与 USART2_TX 默认脚冲突。 |
| PA3 | `2727_EN` | 与 USART2_RX 默认脚冲突。 |
| PA4/PA5/PA6 | LedBar Charlieplexing | 与 SPI1 NSS/SCK/MOSI 习惯功能冲突，后续不要恢复 74HC595/SPI。 |
| PA8 | `MCC_C` | MOS/充放控制相关输出。 |
| PA9 | `SW` / EXTI9 | 按键唤醒。 |
| PA10 | `DC_EN` | 电源控制输出。 |
| PA11/PA12 | CAN1 RX/TX | 当前 CAN 默认映射，未 remap。 |
| PA15/PB3/PB4 | `M_STB` / `AD_EN` / `CMNT_EN` | 依赖 JTAG disable，必须保留 SWD 下载策略。 |
| PB1 | `ADC_NMOS` / ADC1_IN9 | MOS 温度/模拟采样。 |
| PB6/PB7 | USART1 TX/RX remap | 上位机主口；不要切到软件 TWI dead branch。 |
| PB8/PB9 | SH367309 软件 TWI | 不走 STM32 硬件 I2C。 |
| PB10/PB11 | LedBar Charlieplexing | PB11 同时是 USART3_RX 默认脚，当前 USART3 未用。 |
| PB12/PB13 | `INT_WK_CMNT` / `MCU_WK` | 休眠唤醒/显示阻断相关输入。 |
| PB14/PB15 | `AFE1_CTL` / debug LED | AFE 控制和调试输出。 |

## 6. 重点问题与建议

### P0：App 链接地址必须固定到 `0x08004800`

需求侧规则很明确：IAP/Bootloader 是 `0x08000000`，正常 App 是 `0x08004800`，不能把 App 裸写到 `0x08000000`。当前代码层有三处是对的：

- `Flash.h` 定义 `FLASH_ADDR_APP_START 0x08004800`。
- `system_stm32f10x.c` 定义 `VECT_TAB_OFFSET 0x4800`。
- `tools/soc_flash_app_safe.ps1` 拒绝非 `0x08004800` 烧录。

但 Keil 工程层仍不可靠：

- `.uvprojx` `TextAddressRange` 是 `0x08000000`。
- `.uvprojx` `ScatterFile` 为空，`useFile=0`。
- 仓库中没有 `103 + 309/Project/Users/Objects/FD_Release.sct` 或其他可追踪 `.sct` 文件。

按 PM0056 的 `SCB->VTOR` 行为，App 链接地址、向量表偏移、IAP 跳转地址必须一致。只靠烧录脚本把 bin 写到 `0x08004800` 不够；如果 bin 是按 `0x08000000` 链接的，IAP 跳转后仍可能异常。

建议：

1. 新增源控 scatter：例如 `103 + 309/Project/Users/FD_Release_App_0x08004800.sct`。
2. Release/Debug target 都设置 `useFile=1` 并指向该 scatter，或至少把 `TextAddressRange` 改到 `0x08004800`。
3. `tools/project_check.py` 增加硬检查：`VECT_TAB_OFFSET=0x4800`、App scatter/IROM 起点 `0x08004800`、禁止 App `TextAddressRange=0x08000000`。

### P0：后 64K Flash 存储必须和真实芯片容量绑定

当前持久化地址：

- AFE/RW_PARAM：`0x0801C000~0x0801CC00`
- LOG：`0x0801D000~0x0801D800`
- SOC：`0x0801E000~0x0801E800`
- Upgrade/FactoryAging/Update/Sleep flag：`0x0801F000~0x0801FC00`

这些地址要求 Flash 至少 128KB。问题是 `.uvprojx` Device 是 `STM32F103C8`，ST 产品页对 `STM32F103C8` 的口径是 64KB Flash。`StorageFlash_PrintBootCheck()` 会在 `flash_size_kb < 128` 时跳过打印检查，但保存接口没有统一 runtime gate。

建议：

1. 确认 BOM 和实物丝印。如果实际就是 128KB，工程 Device、文档和量产资料应统一改成对应 128KB 口径，避免继续叫 C8。
2. 如果仍按 C8 管理，则后 64K 存储必须迁移到真实可用区域，不能写 `0x0801C000+`。
3. 在所有 `StorageFlash_Save*()`、`FlashWriteOneHalfWord()` 后 64K 调用入口前统一检查 `flash_size_kb >= 128`。
4. `project_check.py` 增加 `FLASH_ADDR_STORAGE_*` 与目标器件 Flash 容量匹配检查。

### P1：BKP 寄存器已超出中容量资源边界

当前分配：

| BKP 寄存器 | 用途 |
| --- | --- |
| DR1 | RTC 初始化标记 |
| DR2/DR3 | Sleep flag + inverse |
| DR4/DR5 | LedBar sleep SOC + inverse |
| DR6~DR10 | FactoryAging magic/inverse/elapsed low/high/crc |
| DR11/DR12 | Fault snapshot reason + inverse |

按 RM0008 的 backup register 资源口径，中容量 F103C8 不应把 DR11/DR12 当成可靠资源。当前最危险的是异常处理里 `HardFault/MemManage/BusFault/UsageFault` 会写 DR11/DR12 后 reset；如果寄存器不存在，故障原因保存不可靠，甚至可能让 fault handler 更复杂。

建议：

1. 不要继续新增 BKP 用户。
2. Fault snapshot 改到 Flash journal 或压缩复用已有 DR 空间；如果必须留在 BKP，先裁决哪一类 BKP 状态优先级最低。
3. 新增 `BKP_REGISTER_ALLOCATION.md` 或加入本文件索引，并让 `project_check.py` 检查 DR 编号不超过目标器件可用范围。

### P1：startup 文件应与 `STM32F10X_MD` 一致

当前 `.uvprojx` 使用 `startup_stm32f10x_hd.s`，但 target 和宏是 `STM32F103C8 + STM32F10X_MD`。ST SPL 对不同容量器件提供不同 startup 文件；使用 HD startup 会让向量表和目标中断资源口径不一致。

建议：

1. 切换到 `startup_stm32f10x_md.s`。
2. 若仓库没有 MD startup，从同版本 `STM32F10x_StdPeriph_Lib_V3.5.0` 官方包补入。
3. `project_check.py` 检查：`Device=STM32F103C8` 或 `STM32F10X_MD` 时禁止使用 `startup_stm32f10x_hd.s`。

### P1：时钟配置必须从 8MHz HSE 口径收口

当前真正生效逻辑是 HSE 8MHz 直通。基于这个口径：

- TIM3：100kHz counter、Period 999，10ms tick，正确。
- TIM2：100kHz counter、Period 999，10ms ADC trigger，正确。
- CAN：PCLK1=8MHz，`Prescaler=4`，`1+BS1+BS2=8tq`，250kbps，正确。
- ADC：PCLK2=8MHz，`RCC_PCLK2_Div8`，ADC clock 1MHz，安全但偏慢。

风险在于 `.uvprojx` 写 `CLOCK(12000000)`，注释里还有 72MHz。后续任何人按 72MHz 改 CAN/ADC/TIM，都会破坏当前节拍。

建议：

1. 统一文档：当前量产时钟为 8MHz HSE 直通。
2. 如果未来要切 72MHz PLL，必须同步重算 CAN bit timing、ADC clock、SysTick delay、TIM2/TIM3/TIM4。
3. 删除或修正“72MHz”旧注释。

### P1：USART2、SPI1、软件 TWI 的隐藏冲突要写成机器可检查规则

当前默认配置是安全的：

- USART1 remap PB6/PB7。
- SH367309 软件 TWI 用 PB8/PB9。
- USART2 默认关闭。
- SPI1 未用，PA4/PA5/PA6 给 LedBar。

潜在冲突：

- 打开 USART2 会占 PA2/PA3，撞 Type-C ADC 和 `2727_EN`。
- 恢复 SPI/74HC595 会撞 LedBar Charlieplexing。
- `I2C_AFE1.c` 里非活动 `F_TWI_*` 分支使用 PB6/PB7，若误切会撞 USART1。

建议：新增 pin ownership 检查脚本，把 `PROJECT_CFG_SCI2_ROLE`、SPI/74HC595、LedBar、TWI 分支纳入互斥。

## 7. 和当前 BMS 需求的匹配度

| 需求 | 当前匹配度 | 说明 |
| --- | --- | --- |
| IAP/Bootloader 不能被 App 覆盖 | 不够 | 烧录脚本安全，但 Keil 链接配置没有闭环。 |
| SOC 体验稳定，休眠后可恢复 | 基本匹配 | RTC/SOC/Flash/LedBar 有联动，但 Flash 容量和 BKP 资源风险会影响恢复可靠性。 |
| RTC 唤醒后 CAN 能发送，再回到休眠 | 基本匹配 | `Can_RtcWakeService()` 有 bounded window；仍需台架验证 ACK/BusOff/无对端电流曲线。 |
| 量产与 SOC 测试隔离 | 匹配 | `PROJECT_CFG_BUILD_PROFILE` 和 `PROJECT_CFG_SOC_TEST_MODE_ENABLE` 有 BuildGuard。 |
| 长期维护、可扩展 | 部分匹配 | 外设 owner 基本清楚，但 App 地址、Flash 容量、BKP 分配、pin ownership 还没有脚本化门禁。 |

## 8. 建议落地顺序

1. 先做 App 地址闭环：tracked scatter + `.uvprojx` 指向 + `project_check.py` 检查。
2. 确认真实 Flash 容量：若 128KB，改 Device/BOM 文档口径；若 64KB，迁移后 64K 存储。
3. 修 BKP 分配：DR11/DR12 不再用于 F103C8 中容量关键 fault snapshot。
4. 切换 medium-density startup，并加入检查。
5. 增加 pin ownership 与 clock ownership 检查。
6. 台架补测 CAN/RTC/STOP：无对端、有对端、BusOff、RTC 唤醒发送成功率、IWDG 不复位、STOP 电流。

## 9. 本次未覆盖

- 未运行 Keil/ARMCC 编译。
- 未烧录实板。
- 未用示波器/逻辑分析仪验证 CAN、USART、ADC trigger、STOP 电流。
- 未修改源码；本文件是审计结论和后续修改依据。

