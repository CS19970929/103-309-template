# SOC 板端自耗补偿说明

## 背景

当前 SOC 积分只处理 AFE 采到的充放电电流，以及 Type-C 输出折算到电池侧的放电电流。BMS 板自身工作电流不一定经过电流采样链路，长期静置时会造成容量缓慢减少但 SOC 不下降。

## 配置项

板端自耗通过 `103 + 309/Project/Source/conf/Project_Config.h` 配置：

```c
#define PROJECT_CFG_SOC_BOARD_SELF_CONSUMPTION_MA 30
```

- 单位：mA。
- 默认值：30mA。
- 允许范围：0 到 1000mA，`Project_BuildGuard.h` 会在编译期检查。
- 后续若实测板耗不同，只需要修改该宏，不需要改 SOC 算法。

## 积分方式

SOC 增强模块现在按 mA 精度计算净电流：

- 放电：`AFE/Type-C 放电电流 + 板端自耗电流`。
- 充电：`AFE 充电电流 - 板端自耗电流`。
- 静置：只扣除板端自耗电流。

容量内部单位仍沿用原来的 `A*10*s`。30mA 这类小电流不会被 0.1A 电流单位直接截断，而是通过 `mA*ms` 余量累计，长期积分结果按平均 30mA 生效。

## 单位和溢出边界

SOC 对外和存储字段不改单位，只有积分中间量改为 mA 精度：

- `u16_Ichg/u16_Idsg`：仍为 `A*10`，来自原有采样和 Type-C 折算后的净电流入口。
- `SOC_MA_PER_A10`：`1 A*10 = 100mA`。
- `SOC_MAMS_PER_AS10`：`100000 mA*ms = 1 A*10*s`。
- `s_soc.rem_mams`：保存未凑满 `1 A*10*s` 的 `mA*ms` 余量，原先的 `rem_ms` 已改名，避免把余量误解成毫秒。
- `delta_as10`：仍为容量积分使用的 `A*10*s` 增量。

按当前约束不会溢出：`u16_Idsg` 最大为 `65535 A*10`，折算约 `6553500mA`；再加最大板耗 `1000mA`，单个 `200ms` tick 的 `acc_mams` 约 `1310900000`，低于 `UINT32` 上限 `4294967295`。`current_ma_signed` 使用 `int32_t`，当前最大正负电流也远低于 `int32_t` 边界。只有将来把电流输入扩展到 `UINT32` 或把 SOC tick 改得很长时，才需要重新评估 `current_ma * SOC_TICK_MS` 的范围。

## 行为边界

- 板端自耗会减少 `CapacityNow`，并计入放电累计和循环统计。
- 静置时扣除板耗不会把 SOC 外部模式强制改成放电，因此不会因为 30mA 板耗阻断静置 OCV 修正。
- 低压尾段、压降延迟和满电锚点仍按原有外部充放电模式判断，板耗只影响容量积分。

## 验证建议

1. 修改 `PROJECT_CFG_SOC_BOARD_SELF_CONSUMPTION_MA` 后先编译，确认编译期范围检查通过。
2. 静置长时间台架验证时，按 `30mA * 时间` 折算理论容量下降，与 `0xD000` SOC/容量字段对比。
3. 充电台架验证时确认小板耗只降低净充电速度，不影响满电确认逻辑。

## 2026-05-13 补充验证

- 运行态静置：只要 `PROJECT_CFG_SOC_BOARD_SELF_CONSUMPTION_MA > 0`，容量 `CapacityNow` 会按 `mA * 时间` 扣减；SOC 百分比只有跨过容量百分比取整边界后才变化。
- 高电压但未达到满电确认阈值：自耗仍会扣容量。测试覆盖 `4050mV` 静置，`0mA/30mA/1000mA` 均按理论容量变化。
- 达到满电确认阈值：满电锚点优先级高，会把 SOC 逐步锚到 `100%` 并刷新满容量。因此在 `4180mV` 这类满电电压下，板耗可能被满电锚点覆盖，表现为 SOC 不下降。这是满电锚点逻辑，不是自耗积分失效。
- 充电电流小于板耗：净积分方向会变成放电。例如 `2 A*10` 充电电流配 `1000mA` 板耗时，按 `800mA` 净放电扣容量。
- 放电电流：板耗与放电电流相加后扣容量。
- RTC/STOP 休眠：补偿路径已按 `rest_seconds` 扣除板耗，并修复首次建立静置电压参考时返回值覆盖导致快照不保存的问题。
- 测试覆盖：
  - `tools/soc_replay_test.py`：47 项通过，包含 `0mA/30mA/1000mA` 自耗矩阵。
  - `tools/run_soc_host_c_test.py`：当前配置、`0mA`、`30mA`、`1000mA` 四档，每档普通版和 debug watch 版均通过。
