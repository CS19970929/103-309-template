# SOC 板端自耗补偿说明

## 背景

当前 SOC 积分只处理 AFE 采到的充放电电流，以及 Type-C 输出折算到电池侧的放电电流。BMS 板自身工作电流不一定经过电流采样链路，长期静置时会造成容量缓慢减少但 SOC 不下降。

## 配置项

板端自耗通过 `103 + 309/Project/Source/conf/Project_Config.h` 配置：

```c
#define PROJECT_CFG_SOC_BOARD_SELF_CONSUMPTION_MA 30
```

- 单位：mA。
- 默认值：30mA。
- 允许范围：0 到 1000mA，`Project_BuildGuard.h` 会在编译期检查。
- 后续若实测板耗不同，只需要修改该宏，不需要改 SOC 算法。

## 积分方式

SOC 增强模块现在按 mA 精度计算净电流：

- 放电：`AFE/Type-C 放电电流 + 板端自耗电流`。
- 充电：`AFE 充电电流 - 板端自耗电流`。
- 静置：只扣除板端自耗电流。

容量内部单位仍沿用原来的 `A*10*s`。30mA 这类小电流不会被 0.1A 电流单位直接截断，而是通过 `mA*ms` 余量累计，长期积分结果按平均 30mA 生效。

## 行为边界

- 板端自耗会减少 `CapacityNow`，并计入放电累计和循环统计。
- 静置时扣除板耗不会把 SOC 外部模式强制改成放电，因此不会因为 30mA 板耗阻断静置 OCV 修正。
- 低压尾段、压降延迟和满电锚点仍按原有外部充放电模式判断，板耗只影响容量积分。

## 验证建议

1. 修改 `PROJECT_CFG_SOC_BOARD_SELF_CONSUMPTION_MA` 后先编译，确认编译期范围检查通过。
2. 静置长时间台架验证时，按 `30mA * 时间` 折算理论容量下降，与 `0xD000` SOC/容量字段对比。
3. 充电台架验证时确认小板耗只降低净充电速度，不影响满电确认逻辑。