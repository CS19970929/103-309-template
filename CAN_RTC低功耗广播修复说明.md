# CAN RTC低功耗广播修复说明

## 背景

现象：CAN总线上接入设备后，仍然收不到BMS周期广播。结合当前工程时钟和CAN低功耗策略，主要风险点有三个：

1. 当前工程 `system_stm32f10x.c` 选择 HSE 8MHz 作为 SYSCLK/PCLK1，而 `Can_HDX.c` 里一度使用 `BS1=5tq, BS2=2tq, Prescaler=4`，实际波特率为 250kbit/s；如果对端按常用 500kbit/s 接收，就无法ACK和解码。
2. 有设备/未知状态下，首帧失败后原逻辑会直接放弃本轮所有待发业务帧，电源刚恢复、第一帧偶发失败时容易表现为“整轮没有广播”。
3. RX过滤器只覆盖旧的标准帧请求范围，无法用任意总线报文作为“总线上有其它设备”的辅助判定。

## 本次修改

文件：`103 + 309/Project/Source/Can_HDX.c`

1. CAN位时序改为按当前 8MHz PCLK1 计算的 500kbit/s：

```text
PCLK1 = 8MHz
Prescaler = 4
BS1 = 2tq
BS2 = 1tq
bitrate = 8MHz / 4 / (1 + 2 + 1) = 500kbit/s
```

2. 有设备/未知状态下，单个发送窗口内如果第一帧无ACK，不再立即清空整轮待发；会继续尝试另一个 1s 探测/业务帧。任一帧成功ACK后，恢复本轮完整业务发送。两帧都失败时才放弃剩余5s业务帧并断电。

3. CAN RX过滤器改为32位全接收掩码。只要收发器上电窗口内收到任意有效CAN报文，RX0中断都会调用现有 `feidao_can_mark_bus_active()`，把RTC周期保持或恢复到1s。

## 状态收敛

- 上电和未知状态默认按“可能有设备”运行，RTC周期为1s。
- 有设备时，1s/5s到期报文会在RTC唤醒服务窗口内上电、等待稳定、发送，成功ACK后继续完整业务帧。
- 无ACK窗口连续达到阈值后，切换到“无设备”，RTC周期为10s。
- 无设备10s周期内仍发送两帧轻量探测；任一探测帧ACK成功或RX收到报文，立即恢复“有设备”状态。
- 发送完成、失败或超时后仍会关闭 `GPIO_CMNT_EN`，保持低功耗目标。

## 实测验证

建议同时观察 `GPIO_CMNT_EN`、CAN_TX、CANH/CANL：

1. 接入500kbit/s CAN设备：应看到约1s一次短上电窗口，CAN分析仪能收到 `0x14F80200/02` 等扩展帧。
2. 断开对端：连续多个无ACK窗口后，应切换为约10s一次探测窗口。
3. 无设备10s周期中途接入对端：下一次探测帧被ACK后，应恢复1s周期和完整业务广播。
4. 如果 `GPIO_CMNT_EN` 有脉冲但 CAN_TX 没有波形，优先检查 `CAN_Transmit()` 是否返回无邮箱、CAN是否处于BusOff/初始化模式。
5. 如果 CAN_TX 有波形但对端不ACK，优先确认实际对端波特率、终端电阻、CANH/CANL接线和收发器供电极性。
