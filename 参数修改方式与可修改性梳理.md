# 参数修改方式与可修改性梳理

本文按当前 `103 + 309` 工程源码梳理：哪些参数可以改、通过什么方式改、改哪里才会生效、哪些不能改或不建议改。

## 1. 总结论

当前工程已经基本去掉外部 EEPROM 持久化：`EEPROM.c` 里的 `ReadEEPROM_*()` / `WriteEEPROM_*()` 为空实现。旧 EEPROM 地址表仍保留给历史代码和协议命名使用，但不能再认为所有写 EEPROM 的路径都会掉电保存。

当前真正能掉电保存的主要是：

| 类型 | 当前保存位置 | 修改方式 |
| --- | --- | --- |
| `PRT_E2ROMParas` 保护参数 | 内部 Flash RW 参数双槽：`0x0801C400` / `0x0801CC00` | 上位机 `0x10` 写 `0x2100~0x2140`；或 `0x06/0x1002` 恢复默认；或升级策略 |
| `OtherElement` 其他/睡眠/SOC 基础/系统参数 | 同上 | 上位机 `0x10` 写 `0x2300~0x231F`；或 `0x06/0x1003` 恢复默认；或升级策略 |
| `Heat_Cool_Element` 加热/冷却参数 | 同上 | 上位机 `0x10` 写 `0x2320~0x2337`；或 `0x06/0x1004` 恢复默认 |
| AFE 保护参数 | 内部 Flash AFE 双槽：`0x0801C000` / `0x0801C800` | 上位机 `0x10` 写 `0x2400~0x2417`；或 `0x06/0x1006` 恢复默认；或升级策略 |
| SOC 快照 | 内部 Flash SOC journal：`0x0801E000` / `0x0801E800` | SOC 模块自动保存；`0x06/0x1005` 设置一次 SOC；升级策略可重置为默认 60% |
| 事件日志 | 内部 Flash log journal：`0x0801D000` / `0x0801D800` | 日志模块自动写；`0x06/0x1007` 可清空 |
| 升级策略已执行版本 | 内部 Flash：`0x0801F000` | `UpgradeParamPolicy_ApplyOnce()` 自动写 |
| IAP 更新标志 | 内部 Flash：`0x0801F800` | 上位机 `0x10` 写 `0xFFFD` 触发 |

当前可写但不会掉电保存，或只在本次运行有效的主要是：校准 K/B、SOC 曲线表、铜损补偿、RTC 结构体、SN/硬件版本/软件版本、系统功能开关位、旧故障记录。

## 2. 上电初始化决定了“改默认值是否生效”

`InitE2PROM()` 当前顺序是：

1. 加载编译期默认值到 RAM：保护参数、校准、`OtherElement`、`Heat_Cool_Element`、SOC 表、铜损。
2. 从内部 Flash RW 参数双槽加载 `PRT_E2ROMParas`、`OtherElement`、`Heat_Cool_Element`，校验成功后覆盖 RAM 默认值。
3. 从内部 Flash AFE 双槽加载 AFE 参数，越界则回默认。
4. 从内部 Flash log journal 加载事件日志。
5. 执行升级参数策略 `UpgradeParamPolicy_ApplyOnce()`。

因此：

- 只改 `Fault.h` / `DataDeal.h` / `Heat_Cool.h` 默认值，对已有设备不一定生效，因为 RW 参数 Flash 会覆盖默认值。
- 对已有设备要改保护参数、`OtherElement`、热管理、AFE，优先用上位机写入或升级策略。
- 当前 SOC 表、铜损、校准没有被当前 Flash RW 参数块保存，改源码默认值会在下次启动重新装载，但上位机运行时写入会在重启后丢失。

## 3. 通信修改方式

通信是类 Modbus 协议：

- `0x03`：读寄存器。
- `0x06`：写单寄存器命令，主要用于恢复默认、设置一次 SOC、功能开关、清日志。
- `0x10`：写多个寄存器，主要用于参数写入。
- 默认从机地址：`0x01`，广播地址：`0x00`。
- 寄存器值按高字节在前传输，CRC 由上位机工具按 Modbus CRC 计算。

建议所有参数修改流程都按下面做：

1. `0x03` 读当前参数。
2. 按地址和字段含义修改目标值。
3. `0x10` 或 `0x06` 写入。
4. `0x03` 立即回读确认。
5. 对需要掉电保存的参数，断电/复位后再次回读确认。

## 4. `0x10` 可写参数

### 4.1 校准 K/B：`0x2000~0x205D`

可改内容：

- 电芯电压 `VC1~VC32` 校准 K/B。
- AFE1/AFE2、总压、电流、温度校准 K/B。

具体改法：

- 起始地址必须是某个 K 地址，例如 `0x2000`、`0x2002`、`0x2046`。
- 一次必须写 2 个寄存器：第 1 个是 K，第 2 个是 B。
- K 范围：`512~1536`，`1024` 表示 1.0。
- B 范围：`-30000~30000`，协议里 B 使用符号位格式：bit15 为 1 表示负数，低 15 位是绝对值。

当前限制：

- 只改 RAM，不会掉电保存。
- `0x06/0x1000` 的校准恢复默认也调用了空 EEPROM 写接口，所以当前同样不能持久化。

### 4.2 保护参数：`0x2100~0x2140`

可改内容：

- 单体过压/欠压。
- 总压过压/欠压。
- 充电/放电过流。
- 充电/放电高低温。
- MOS 高温。
- 压差过大。
- SOC 低电量告警。

具体改法：

- `0x2100 + struct PRT_E2ROM_PARAS` 字段偏移就是地址。
- 支持从块内任意地址开始写 1 个或多个连续寄存器。
- 不允许跨出 `0x2100~0x2140`。
- 写入值会按 `E2P_PROTECT_MIN_PRT` / `E2P_PROTECT_MAX_PRT` 校验。
- 校验通过后立即写内部 Flash RW 参数双槽；失败会回滚 RAM 并返回负响应。

常用例子：

- 单体过压一级：`0x2100`。
- 单体过压二级：`0x2101`。
- 单体过压三级：`0x2102`。
- 单体过压恢复：`0x2103`。
- 单体过压滤波：`0x2104`。

### 4.3 SOC 表、铜损、RTC：`0x2200~0x2255`

可改内容和限制：

| 地址 | 内容 | 写法 | 当前持久化 |
| --- | --- | --- | --- |
| `0x2200~0x2229` | SOC 曲线表，21 组电压/SOC | 必须从 `0x2200` 一次写满 42 个寄存器 | 不保存，重启丢失 |
| `0x222A~0x2249` | 铜损补偿值 16 个 + 编号 16 个 | 必须从 `0x222A` 一次写满 32 个寄存器 | 不保存，重启丢失 |
| `0x224A~0x2255` | RTC 时间和闹钟结构体 | 必须从 `0x224A` 一次写满 12 个寄存器 | 不保存；当前只是写 `RTC_time` 结构体 |

注意：

- 这些函数当前没有像 RW 参数块那样做完整 min/max 校验和 Flash 保存。
- 如果要让 SOC 表、铜损、RTC 设置长期有效，需要补内部 Flash 存储逻辑。

### 4.4 `OtherElement`：`0x2300~0x231F`

这块是当前最常用的可配置参数块，会掉电保存。实际字段以 `struct OTHER_ELEMENT` 顺序为准，部分历史宏名和实际字段含义不完全一致。

| 地址 | 实际字段 | 含义 |
| --- | --- | --- |
| `0x2300` | `u16Balance_OpenVoltage` | 均衡开启电压，mV |
| `0x2301` | `u16Balance_OpenWindow` | 均衡开启窗口，mV |
| `0x2302` | `u16Balance_CloseWindow` | 均衡关闭窗口，mV |
| `0x2303~0x2307` | `u16Balance_Res1~Res5` | 预留 |
| `0x2308` | `u16CS_Cur_CHGmax` | 最大充电电流，A*10 |
| `0x2309` | `u16CS_Cur_DSGmax` | 最大放电电流，A*10 |
| `0x230A` | `u16CBC_DelayT` | CBC 延时 |
| `0x230B` | `u16CBC_Cur_DSG` | CBC 放电电流，A*10 |
| `0x230C` | `u16Soc_TableSelect` | SOC 表选择 |
| `0x230D` | `u16Password_Always` | 历史字段，当前无明确业务 |
| `0x230E` | `u16CurLimit_Vdelta` | 限流压差，mV |
| `0x230F` | `u16CurLimit_Cur` | 限流电流，A*10 |
| `0x2310` | `u16Sleep_VNormal` | 普通休眠电压，mV |
| `0x2311` | `u16Sleep_TimeNormal` | 普通休眠时间，min |
| `0x2312` | `u16Sleep_Vlow` | 低压休眠电压，mV |
| `0x2313` | `u16Sleep_TimeVlow` | 低压休眠时间，min |
| `0x2314` | `u16Sleep_VirCur_Chg` | 休眠虚拟充电电流，A*10 |
| `0x2315` | `u16Sleep_VirCur_Dsg` | 休眠虚拟放电电流，A*10 |
| `0x2316` | `u16Sleep_RTC_WakeUpTime` | RTC 唤醒时间 |
| `0x2317` | `u16Sleep_TimeRTC` | RTC 休眠时间 |
| `0x2318` | `u16Soc_Ah` | 额定容量，10*Ah |
| `0x2319` | `u16Soc_Cycle_times` | 循环次数 |
| `0x231A` | `u16Soc_V_100` | SOC 100% 电压 |
| `0x231B` | `u16Soc_V_0` | SOC 0% 电压 |
| `0x231C` | `u16Sys_SeriesNum` | 串数 |
| `0x231D` | `u16Sys_CS_Res` | 采样电阻，mΩ |
| `0x231E` | `u16Sys_CS_Res_Num` | 采样电阻并联数量 |
| `0x231F` | `u16Sys_PreChg_Time` | 预充时间，s |

具体改法：

- 支持块内任意地址开始写 1 个或多个连续寄存器。
- 不允许跨出 `0x2300~0x231F`。
- 写入值按 `OtherElement_min` / `OtherElement_max` 校验。
- 保存成功后会更新运行时副作用，例如 `SeriesNum`、`g_u32CS_Res_AFE`，必要时触发 `InitData_SOC()`。

例子：

- 修改均衡开启电压：写 `0x2300`，1 个寄存器。
- 修改 SOC 容量：写 `0x2318`，1 个寄存器。
- 修改串数/采样电阻/并联数：写 `0x231C~0x231E`，3 个连续寄存器。

### 4.5 加热/冷却参数：`0x2320~0x2337`

这块会掉电保存。实际字段以 `struct HEAT_COOL_ELEMENT` 顺序为准。

| 地址 | 实际字段 | 含义 |
| --- | --- | --- |
| `0x2320` | `u16Heat_OpenTemp` | 加热开启温度，`(T+40)*10` |
| `0x2321` | `u16Heat_CloseTemp` | 加热关闭温度，`(T+40)*10` |
| `0x2322` | `u16Heat_OpenCur` | 加热开启电流，A*10 |
| `0x2323` | `u16Cool_OpenTemp` | 冷却开启温度，`(T+40)*10` |
| `0x2324` | `u16Cool_CloseTemp` | 冷却关闭温度，`(T+40)*10` |
| `0x2325~0x2337` | `u16Heat_Res3~Res10`、`u16Cool_Res1~Res11` | 预留 |

具体改法：

- 支持块内任意地址开始写 1 个或多个连续寄存器。
- 不允许跨出 `0x2320~0x2337`。
- 写入值按 `HeatCoolElement_Min` / `HeatCoolElement_Max` 校验。

### 4.6 AFE 参数：`0x2400~0x2417`

可改内容：

| 地址 | AFE 字段 |
| --- | --- |
| `0x2400` | 单节过压 |
| `0x2401` | 单节过压恢复 |
| `0x2402` | 单节过压延时 |
| `0x2403` | 单节低压 |
| `0x2404` | 单节低压恢复 |
| `0x2405` | 单节低压延时 |
| `0x2406` | 一级充电过流 |
| `0x2407` | 一级充电过流延时 |
| `0x2408` | 二级充电过流 |
| `0x2409` | 二级充电过流延时 |
| `0x240A` | 一级放电过流 |
| `0x240B` | 一级放电过流延时 |
| `0x240C` | 二级放电过流 |
| `0x240D` | 二级放电过流延时 |
| `0x240E` | 充电高温 |
| `0x240F` | 充电高温恢复 |
| `0x2410` | 充电低温 |
| `0x2411` | 充电低温恢复 |
| `0x2412` | 放电高温 |
| `0x2413` | 放电高温恢复 |
| `0x2414` | 放电低温 |
| `0x2415` | 放电低温恢复 |
| `0x2416` | CBC 放电电流 |
| `0x2417` | CBC 延时 |

具体改法：

- 支持从 `0x2400~0x2417` 内任意地址开始写连续寄存器。
- 不允许超过 `0x2417`。
- 写入后保存到 AFE Flash 双槽，并置 `AFE_PARAM_WRITE_Flag = 1`，后续 AFE 配置更新流程会处理。
- AFE 参数结构中每个字段都有 `cur/default/max/min`。当前 `0x10` 写入函数没有立即按 max/min 拒绝超范围值，但启动加载会校验，超范围会回默认并报存储错误。因此实际使用时必须遵守 `SH367309_DataDeal.h` 中的 min/max。

### 4.7 产品 ID：`0xFFF0~0xFFF2`

可改内容：

- `0xFFF0`：序列号。
- `0xFFF1`：硬件版本。
- `0xFFF2`：软件版本。

具体改法：

- 用 `0x10` 写，寄存器数 `N` 表示 `2*N` 字节字符串。
- 最大长度 `PRODUCT_ID_LENGTH_MAX = 32` 字节。

当前限制：

- 只改 RAM，不会掉电保存。
- 下次上电 `InitProID()` 会重新加载 `DataDeal.h` 里的默认字符串。

### 4.8 IAP 触发：`0xFFFD`

这不是普通参数。

具体改法：

- 用 `0x10` 写 `0xFFFD`，寄存器数量必须是 1。
- 固件会向 `FLASH_ADDR_UPDATE_FLAG = 0x0801F800` 写 `FLASH_TO_IAP_VALUE`，用于后续跳转 IAP。

## 5. `0x06` 命令

| 地址 | 数据 | 作用 | 当前是否保存 |
| --- | --- | --- | --- |
| `0x1000` | `0x55AA` | 恢复一组校准默认值 | 当前不持久 |
| `0x1000` | `0x55AB` | 恢复 AFE1 电压校准 | 当前不持久 |
| `0x1000` | `0x55AC` | 恢复 AFE2 电压校准 | 当前不持久 |
| `0x1000` | `0x55AD` | 恢复总压校准 | 当前不持久 |
| `0x1000` | `0x55AE` | 恢复温度校准 | 当前不持久 |
| `0x1000` | `0x55AF` | 恢复放电电流校准 | 当前不持久 |
| `0x1000` | `0x55B0` | 恢复充电电流校准 | 当前不持久 |
| `0x1001` | `0x0001` | 清旧保护记录 RAM 数据 | 当前不持久 |
| `0x1002` | `0x0001` | 保护参数恢复 `E2P_PROTECT_DEFAULT_PRT` | 保存到 RW 参数 Flash |
| `0x1003` | `0x0001` | `OtherElement` 恢复 `OtherElement_default` | 保存到 RW 参数 Flash |
| `0x1004` | `0x0001` | 热管理恢复 `HeatCoolElement_Default` | 保存到 RW 参数 Flash |
| `0x1005` | `0~100` | 设置一次 SOC | 运行期动作，SOC 模块后续处理 |
| `0x1006` | `0x0001` | AFE 参数恢复 `AFE_PARAMETERS_RS485_STRUCTION_DEFAULT` | 保存到 AFE Flash |
| `0x1007` | `0x0001` | 清事件日志 | 保存到 log Flash |
| `0x1100` | `1~32` | `SwitchON`，当前函数只校验参数 | 当前无实际动作 |
| `0x1101` | `1~32` | `SwitchOFF`，当前函数只校验参数 | 当前无实际动作 |
| `0x1102` | `1~32` | 打开系统功能位 | 运行期有效，旧 EEPROM 保存路径无效 |
| `0x1103` | `1~32` | 关闭系统功能位 | 运行期有效，旧 EEPROM 保存路径无效 |

系统功能位当前可识别的常用位：

- `1`：均衡。
- `2`：BMS source。
- `3`：MOS/继电器。
- `5`：SOC fixed。
- `6`：加热。
- `7`：冷却。
- `8`：AFE1。
- `9`：AFE2。
- `10`：Sleep；`0x1102` 写 `0x000A` 会调用 `entersleep(DEEP_MODE)`。
- `11`：SOC zero，源码里明确不保存。

## 6. 通过源码/编译配置修改

### 6.1 产品和功能配置

优先改：

- `103 + 309/Project/Source/conf/Project_Config.h`

建议在 Keil 里右键该文件，选择 `Configuration Wizard` 修改：

- 电池类型：`PROJECT_CFG_BAT_TYPE`。
- 电芯体系：`PROJECT_CFG_BAT_CHEMISTRY`。
- 固件日期和版本：`PROJECT_CFG_FD_YEAR/MONTH/DAY/VERSION`。
- 电流规格：`PROJECT_CFG_LEVEL_CURR`。
- AFE 类型：`PROJECT_CFG_AFE_TYPE`。
- 看门狗、RTC、UART 唤醒、RS485 唤醒、虚拟电流、IAP、LedBar、SCI 角色等功能开关。

这些是编译期配置，不能通过上位机运行时改。修改后必须重新编译烧录。

### 6.2 默认参数表

| 要改的默认值 | 文件 |
| --- | --- |
| 保护参数默认/上下限 | `103 + 309/Project/Source/Fault.h`：`E2P_PROTECT_*_PRT` |
| `OtherElement` 默认/上下限 | `103 + 309/Project/Source/DataDeal.h`：`OtherElement_*` |
| 热管理默认/上下限 | `103 + 309/Project/Source/Heat_Cool.h`：`HeatCoolElement_*` |
| AFE 默认/上下限 | `103 + 309/Project/Source/SH367309_DataDeal.h`：`AFE_PARAMETERS_RS485_STRUCTION_DEFAULT` |
| SOC 曲线默认表 | `103 + 309/Project/Source/SOC.c`：`SOC_Table_Default` |
| 产品 ID 默认字符串 | `103 + 309/Project/Source/DataDeal.h`：`BMS_*_DEFAULT` |

对已有设备发布升级包时，不能只改默认表。已经存在内部 Flash 数据的模块会继续使用现场保存值，必须配合上位机写入、`0x06` 恢复默认命令，或升级参数策略。

## 7. 升级参数策略该怎么用

入口：

- `103 + 309/Project/Source/conf/Project_Config.h`
- `103 + 309/Project/Source/UpgradeParamPolicy.h`

标准流程：

1. 修改本次需要的新默认参数。
2. 打开 `PROJECT_CFG_UPGRADE_PARAM_POLICY_ENABLE`。
3. 递增 `PROJECT_CFG_UPGRADE_PARAM_POLICY_VERSION`，不能用 `0xFFFF`。
4. 只打开需要覆盖现场参数的开关。
5. 保持 `PROJECT_CFG_UPGRADE_PARAM_FORCE_REAPPLY = 0`。
6. 编译升级包。

当前支持的策略：

| 开关 | 作用 |
| --- | --- |
| `PROJECT_CFG_UPGRADE_PARAM_RESET_AFE` | AFE 参数恢复为当前固件默认值，并保存 AFE Flash |
| `PROJECT_CFG_UPGRADE_PARAM_RESET_PROTECT` | 保护参数恢复默认，并保存 RW 参数 Flash |
| `PROJECT_CFG_UPGRADE_PARAM_RESET_BALANCE_OPEN_VOLTAGE` | 只覆盖 `OtherElement.u16Balance_OpenVoltage`，并保存 RW 参数 Flash |
| `PROJECT_CFG_UPGRADE_PARAM_RESET_SOC_TABLE` | 把 SOC 表 RAM 重新加载为当前固件默认表；当前没有单独持久化 |
| `PROJECT_CFG_UPGRADE_PARAM_RESET_SOC_CONFIG` | 只覆盖 SOC 基础参数：表选择、容量、循环次数、100% 电压、0% 电压，并保存 RW 参数 Flash |
| `PROJECT_CFG_UPGRADE_PARAM_RESET_SOC_SNAPSHOT` | SOC 历史快照重置为默认 60% |
| `PROJECT_CFG_UPGRADE_PARAM_RESET_EVENT_RECORD` | 清空事件日志 |

注意当前 `Project_Config.h` 中 `PROJECT_CFG_UPGRADE_PARAM_RESET_BALANCE_OPEN_VOLTAGE` 是 1，但 `PROJECT_CFG_UPGRADE_PARAM_POLICY_ENABLE` 是 0，所以策略整体不会执行。

## 8. 不能修改或不建议修改的内容

### 8.1 协议只读区不能写

这些地址只能通过 `0x03` 读，不能作为参数写入：

- `0xD000`：实时电芯电压、总压、温度、电流、SOC、故障位、均衡位。
- `0xD100`：RTC、故障记录、系统错误位、系统状态、开关状态。
- `0xD200`：扩展只读区。
- `0xC000`：LCD 汇总数据。
- `0xC001`：RTC 快速读取。
- `0xC002`：SN/版本读取。
- `0xC008`：事件记录读取。

测量值、电流、电压、温度、故障状态、系统状态都由采样和业务逻辑产生，不应直接写。

### 8.2 不能跨块写

保护参数、`OtherElement`、热管理、AFE 都支持块内任意连续写，但不能跨出本块地址范围。跨块请求会返回负响应。

### 8.3 整块写参数不能只写中间一项

这些入口当前只识别固定起始地址：

- SOC 表只能从 `0x2200` 写满 42 个寄存器。
- 铜损只能从 `0x222A` 写满 32 个寄存器。
- RTC 只能从 `0x224A` 写满 12 个寄存器。
- 校准只能从 K 地址开始写 K/B 两个寄存器。

### 8.4 预留字段不要当业务参数用

`OtherElement`、`Heat_Cool_Element` 中有多个 `Res` 字段。协议可能接受写入，但没有明确业务逻辑，不应作为有效参数给客户开放。

### 8.5 存储地址、IAP 地址、Flash magic 不建议随意改

这些只能源码修改，并且必须同步链接、IAP、存储迁移和掉电保护验证：

- `FLASH_ADDR_APP_START`
- `FLASH_ADDR_STORAGE_*`
- `FLASH_ADDR_UPDATE_FLAG`
- `FLASH_ADDR_UPGRADE_PARAM_FLAG`
- `FLASH_STORAGE_MAGIC_*`
- `FLASH_STORAGE_VERSION`
- Keil IROM 起始地址和大小

### 8.6 板级硬件参数不能通过上位机改

以下属于源码/硬件配置，不是运行参数：

- GPIO 引脚映射。
- ADC 通道和采样序列。
- AFE I2C 地址和底层寄存器位定义。
- CAN 过滤器、收发器供电 GPIO、底层时序。
- USART/SCI 角色和中断使能。
- 系统时钟、定时器中断频率。

这些修改必须重新编译，并做硬件验证。

## 9. 常见修改场景

### 修改已有设备的均衡开启电压

推荐方式：

1. 上位机 `0x03` 读 `0x2300`。
2. `0x10` 写 `0x2300`，寄存器数量 1，值为目标 mV。
3. `0x03` 回读 `0x2300`。
4. 断电重启后再回读确认。

批量升级方式：

1. 改 `DataDeal.h` 中 `OtherElement_default` 第一个值。
2. `Project_Config.h` 中打开升级策略。
3. 设置 `PROJECT_CFG_UPGRADE_PARAM_RESET_BALANCE_OPEN_VOLTAGE = 1`。
4. 递增 `PROJECT_CFG_UPGRADE_PARAM_POLICY_VERSION`。
5. 保持 `PROJECT_CFG_UPGRADE_PARAM_FORCE_REAPPLY = 0`。

### 修改已有设备的保护参数

单台调试：

- 用 `0x10` 写 `0x2100~0x2140` 中对应字段。

批量升级：

- 改 `Fault.h` 默认表。
- 打开 `PROJECT_CFG_UPGRADE_PARAM_RESET_PROTECT`。
- 递增升级策略版本。

### 修改已有设备的 AFE 参数

单台调试：

- 用 `0x10` 写 `0x2400~0x2417` 对应字段，值必须在 `SH367309_DataDeal.h` 的 min/max 内。

批量升级：

- 改 `SH367309_DataDeal.h` 中默认值。
- 打开 `PROJECT_CFG_UPGRADE_PARAM_RESET_AFE`。
- 递增升级策略版本。

### 修改 SOC 基础参数

单台调试：

- 容量写 `0x2318`。
- 循环次数写 `0x2319`。
- 100% 电压写 `0x231A`。
- 0% 电压写 `0x231B`。

批量升级：

- 改 `DataDeal.h` 中 `OtherElement_default` 对应字段。
- 打开 `PROJECT_CFG_UPGRADE_PARAM_RESET_SOC_CONFIG`。
- 需要同时让历史 SOC 回到默认 60% 时，再打开 `PROJECT_CFG_UPGRADE_PARAM_RESET_SOC_SNAPSHOT`。

### 修改 SOC 曲线表

当前状态：

- 可以通过 `0x10` 写 `0x2200`，一次写满 42 个寄存器。
- 但当前不会保存到 Flash，重启后会回到 `SOC.c` 的 `SOC_Table_Default`。

如果要客户长期使用自定义 SOC 曲线，需要新增 SOC 表内部 Flash 持久化，或改源码默认表后发布固件。

