# 项目运行流程与时序源码梳理

日期：2026-05-16

本文以当前源码为准，梳理整机启动、主循环、时基、低功耗、通信、SOC、显示、存储和测试隔离的真实运行流程。若旧文档与本文冲突，以源码和本文为准。

## 1. 总体结论

当前工程是 STM32F103 + SH367309 AFE 的裸机轮询架构，没有 RTOS。系统主时基由 `TIM3` 产生 10ms tick，主循环每轮先把中断 pending flag 锁存到 `g_st_SysTimeFlag`，再按固定顺序调度业务模块。

主链路已经比较清晰：

```text
main()
  -> InitDevice()
  -> InitVar()
  -> Init_RTC()
  -> while(1) Runtime_RunOnce()
```

当前可确认的主要问题是量产配置曾经默认成 Debug profile 且关闭 IWDG，已经改回 `PROJECT_CFG_BUILD_PROFILE=0`、`PROJECT_CFG_WDOG_ENABLE=1`。这属于量产安全问题，不是普通文档问题。

## 2. 启动初始化流程

源码入口在 `103 + 309/Project/Source/main.c`。

```text
InitDevice()
  -> SystemInit()
  -> InitDelay()
  -> IsSleepStartUp()
  -> jtag_disableAndConfIO()
  -> InitNVIC()
  -> InitIO()
  -> InitSci()
  -> StorageFlash_PrintBootCheck()
  -> InitE2PROM()
  -> InitAFE1()
  -> InitCan()
  -> InitADC()
  -> InitData_SOC()
  -> InitTimer()
  -> Init_IWDG()
```

说明：

- `InitSci()` 当前只保留一次，避免同一串口初始化路径重复进入。
- `InitData_SOC()` 在 `InitE2PROM()` 后执行，确保 `OtherElement`、SOC 表、容量和阈值参数已加载。
- `InitTimer()` 最后启动 `TIM3`，之后主循环才开始消费 10ms/50ms/100ms/200ms/1000ms 任务标志。

## 3. 主循环调度顺序

源码入口在 `Runtime_RunOnce()`。量产路径当前分成三段，只做结构收口，不改变任务顺序。

```text
Runtime_RunOnce()
  -> Runtime_RunFrontTasks()
     -> SysTime_LatchTaskFlags()
     -> FactoryAging_Task()
     -> APP_LedBar()
     -> App_AFEGet()
  -> Runtime_RunIoAndPowerTasks()
     -> App_Sci()
     -> App_AnlogCal()
     -> App_LowPowerProcess()
     -> App_Can()
  -> Runtime_RunBackgroundTasks()
     -> StorageFlash_AppUseTest_Task()
     -> App_Heat_Cool_Ctrl()
     -> App_FlashUpdate()
     -> App_LogRecord()
     -> App_ProID_Deal()
     -> Feed_IWatchDog
```

关键点：

- `App_SOC()` 不在主循环独立空跑，正常只在 `App_AFEGet()` 采到新 AFE 样本后调用。
- `App_AnlogCal()` 自己按 `SysTime_Get10msTickCount()` 补算 elapsed tick，因此外层不必强行 10ms 调用。
- `App_LowPowerProcess()` 放在 `App_Can()` 前；如果本轮进入 STOP，CAN 由低功耗唤醒服务负责发一次探测/周期帧。

## 4. 系统时基

`TIM3` 配置在 `System_Init.c`：

| 项目 | 当前源码 |
| --- | --- |
| `TIM3` 输入基准 | `SystemCoreClock / 100000`，得到 100kHz |
| `TIM3 Period` | `999` |
| 中断周期 | 10ms |
| 派生 flag | 10ms / 50ms / 100ms / 200ms / 1000ms |
| 200ms 任务 | `SysTime_Post200msTaskPeriod()` 累加 pending counter |
| pending 上限 | 5 个 200ms 周期 |
| 溢出观测 | `SysTime_Get200msTaskOverflowCount()` |

`SysTime_LatchTaskFlags()` 每轮把 pending flag 拷贝到 `g_st_SysTimeFlag` 并清 pending。模块读取的是同一轮快照，避免同一 10ms tick 内不同模块看到不一致 flag。

## 5. AFE / 数据采样 / SOC

真实链路：

```text
TIM3 每 200ms post period
  -> App_AFEGet()
     -> SysTime_Take200msTaskPeriod()
     -> MonitorAFE(0, UpdateVoltageFromBqMaximo())
     -> DataLoad_CellVolt()
     -> DataLoad_CellVoltMaxMinFind()
     -> DataLoad_Temperature()
     -> DataLoad_TemperatureMaxMinFind()
     -> DataLoad_Current()
     -> ++g_u32AfeCurrentSampleSeq
     -> App_SH367309()
     -> new_todo_logi()
     -> App_SOC()
```

`App_SOC()` 先比较 `g_u32AfeCurrentSampleSeq`，只有新样本才执行 `SOC_UpdateSampleData()` 和 `SOC_IntEnhance_Ctrl()`。如果其他路径误调用 `App_SOC()`，只会发布已有 SOC，不会重复积分旧电流。

SOC 主状态机在 `SocEnhance.c`：

```text
SOC_IntEnhance_Ctrl()
  -> soc_integrate()
  -> soc_update_sag_hold()
  -> soc_low_tail_config()
  -> soc_mid_tail_config()
  -> soc_apply_full_empty()
  -> soc_apply_mid_tail()
  -> soc_apply_deferred_ocv_step()
  -> soc_update_rest_timer()
  -> soc_save_if_needed()
  -> soc_publish()
```

当前 SOC 规则重点：

- 自动校准单步最多 `1%`。
- Type-C PA2 电流是输出侧电流，先折算为电池侧等效放电电流再进入 SOC。
- 静置 OCV 先记录 deferred target，不在静置时快速跳变；后续充放电或久置慢速消化差值。
- 睡前通过 `SOC_SaveSnapshotBeforeSleep()` 保存 Flash SOC 快照。

## 6. ADC / Type-C / 总压

`ADC.c` 使用 `TIM2_CC2` 触发 ADC1，DMA 循环搬运三路采样：

| 通道 | 当前用途 |
| --- | --- |
| PB1 / ADC9 | MOS NTC |
| PA2 / ADC2 | Type-C 输出电流 |
| PA1 / ADC1 | VBC 总压分压 |

`InitADC_ADC1()` 的 ADC reset calibration / calibration 等待已经改成有界等待，超时会关闭 ADC、置 `ERROR_ADC` 并退出初始化，避免 ADC 外设异常时在 IWDG 启动前卡死。

`App_AnlogCal()` 不依赖 `g_st_SysTimeFlag.bits.b1Sys10msFlag`，而是读取 10ms tick 差值并循环补算 `ADC_TTC()`、`ADC_Vbc()`、`ADC_Current_Smooth()`。为避免长时间 STOP 恢复、调试暂停或主循环阻塞后一次性补算过多 tick，当前每轮最多处理 `ADC_ANALOG_CAL_MAX_CATCHUP_TICKS` 个 10ms tick，未处理的 backlog 留到后续主循环继续消化。

## 7. RS485 / 上位机通信

通信入口：

```text
USARTx_IRQHandler()
  -> Sci*_CommonUpper_IRQHandler()
Runtime_RunOnce()
  -> App_Sci()
     -> App_CommonUpper()
        -> Sci_PortService()
        -> Sci_ModbusProcessFrame()
```

关键地址：

- `0xD000`：运行状态只读窗口，来源主要是 `g_stCellInfoReport`。
- `0xD300`：SOC 测试状态窗口。量产固件 `supported=0` 是正确结果。
- `0x1005`：一次性设置 SOC。
- `0x2200`：SOC 表写入，当前更新 RAM 表，不等同于独立跨重启保存自定义表。
- `0x2318/0x2319`：容量和循环次数等 SOC 参数。

串口中断收到字节会递增 `RTC_ExtComCnt`，低功耗判断会用这个计数延后 RTC 休眠。

`printf` / `fputc()` 当前仍是阻塞式调试输出，但等待 `USART_FLAG_TC` 已加入 `SCI_DEBUG_UART_TX_WAIT_LOOP` 上限；UART 异常时会放弃本字符并继续运行，避免日志输出导致整机卡死。

## 8. CAN / RTC 低功耗闭环

CAN 主路径在 `Can_HDX.c`：

```text
App_Can()
  -> feidao_can_update_logical_tick(SysTime_Get10msTickCount())
  -> Can_BusOFF_Monitor()
  -> feidao_can_send()
  -> feidao_can_update_low_power_status()
```

CAN 发送状态机：

```text
IDLE
  -> POWER_WAIT_STABLE: 打开收发器，等待 100ms
  -> TX_WAIT: 等 mailbox 成功 / failed / 200ms timeout
  -> IDLE: 无 pending 后关闭收发器
```

RTC 唤醒时的 CAN 闭环：

```text
rtc_sleep_run_hiccup_cycle()
  -> STOP 醒来且仍满足继续睡
  -> update_rtc_soc()
  -> Can_RtcWakeService(elapsed_seconds)
     -> InitCan()
     -> queue RTC wake frames 或 idle probe
     -> rtc_service_active = 1，禁止服务窗口内自动生成新周期帧
     -> feidao_can_service_until_idle(150 * 10ms)
     -> 记录 last_rtc_wake_tx_acked / last_rtc_wake_timeout
     -> Can_PrepareSleep()
  -> 继续下一轮 STOP
```

`Can_GetIdleRtcPeriodSeconds()` 根据 CAN bus active 状态返回 1s 或 10s，影响 RTC 醒来频率，但不直接决定 SOC 校准步长。

边界说明：

- 有 CAN 对端 ACK 时，RTC 唤醒窗口内至少一帧发送成功，`g_stCanLowPowerStatus.u8LastRtcWakeTxAcked=1`。
- 无 CAN 对端或物理层异常时，源码不能“凭空保证物理 ACK”，但会在 1.5s 有界窗口内完成发送尝试、统计 failed/timeout/no mailbox，并关闭收发器后继续低功耗，避免卡死。
- RTC 服务窗口跨过 1s 周期时，不再额外生成新一批 1s/5s 周期帧，避免“为了补发 CAN 导致无法再次入睡”。

## 9. 低功耗 / 休眠

低功耗入口：

```text
App_LowPowerProcess()
  -> rtc_sleep()
     -> 每 1000ms 执行一次 BQ769x0_SleepMode_Ctrl()
     -> LowPower_Request(mode)
     -> HICCUP_MODE: rtc_sleep_run_hiccup_cycle()
     -> NORMAL/DEEP: SleepDeal_Continue(mode) 后复位进入对应休眠路径
```

进入 STOP 前：

```text
LowPowerSleep_SaveCoreState()
  -> Can_PrepareSleep()
  -> SOC_SaveSnapshotBeforeSleep()
  -> FactoryAging_SaveProgressBeforeSleep()
```

如果是复位式休眠，还会额外 `LedBar_SaveSleepSoc()`，用于唤醒短显示时读取 BKP 中的 SOC 预览值。

RTC STOP 醒来后：

```text
InitRtcWakeupCheck() 或 InitRunAfterStopWakeup()
  -> updataData_rtc()
  -> isHaveCurrent() / rtc_monitor() / vmin <= 2750mV
  -> 满足异常则退出运行；否则补 SOC + CAN service 后继续睡
```

## 10. LedBar / 数码管

显示模块有两个节拍：

- `TIM4_IRQHandler()` 每 1ms 调 `LedBar_Scan1ms()`，只负责动态扫描输出。
- `APP_LedBar()` 在主循环中运行，按 10ms/100ms flag 处理按键、`MCU_WK` 滤波、显示窗口和显示内容；充电图标只按放电 MOS 状态组合，不再做 `GPIO_CHG_IN` 滤波。

显示来源是 `g_stCellInfoReport.SocElement.u16Soc`，由 SOC 发布。内部 SOC、显示 SOC 和数码管运行窗口是分层的：

- 复位/上电显示窗口默认 10s。
- 短按显示窗口独立，不应和唤醒 10s 混在一起。
- 休眠前 BKP SOC 只用于预览，不替代 Flash SOC 快照。

## 11. 存储 / Flash / BKP

当前后 64K 存储按数据域拆分：

| 数据域 | 地址 |
| --- | --- |
| AFE 参数 | `0x0801C000 / 0x0801C800` |
| RW 参数 | `0x0801C400 / 0x0801CC00` |
| 日志 | `0x0801D000 / 0x0801D800` |
| SOC 快照 | `0x0801E000 / 0x0801E800` |
| 升级参数标志 | `0x0801F000` |
| 出厂老化 | `0x0801F400` |
| IAP 跳转标志 | `0x0801F800` |
| 睡眠标志 | `0x0801FC00` |

SOC、日志和出厂老化使用 journal 风格写入；参数类使用双槽。烧录 App 必须从 `0x08004800` 起烧，不能把 App bin 写到 `0x08000000` 覆盖 IAP。

## 12. 出厂老化

`FactoryAging_Task()` 在主循环前段执行，只累计 MCU 正常运行态 `TIM3` 10ms tick。STOP/RTC 时间不计入老化时间。

状态保存：

- BKP：约 1s 保存一次进度。
- Flash：约 2h 保存一次，完成时强制保存。
- 进入休眠前：`FactoryAging_SaveProgressBeforeSleep()` 保存当前进度。

运行期间 `FactoryAging_IsActive()` 会阻断 RTC 休眠，避免老化模式被低功耗中断。

## 13. 已修问题

| 问题 | 影响 | 本次处理 |
| --- | --- | --- |
| `PROJECT_CFG_BUILD_PROFILE` 默认是 `1` | 默认配置不是量产 Release，`project_check.py` 失败 | 改为 `0` |
| `PROJECT_CFG_WDOG_ENABLE` 默认是 `0` | Release 下未启用 IWDG，量产安全门禁失败 | 改为 `1` |
| `InitSci()` 初始化两次 | 启动流程重复，不利于后续定位串口初始化副作用 | 保留一次 |
| `Runtime_RunNormalOnce()` 单函数堆叠所有任务 | 主循环职责不够清楚 | 拆为 front / IO power / background 三段，保持顺序不变 |
| ADC 校准等待无超时 | ADC 外设异常时可能在启动阶段卡死，且此时 IWDG 尚未初始化 | 加 `ADC_CALIBRATION_WAIT_LOOP`，失败置 `ERROR_ADC` 后退出 |
| `App_AnlogCal()` 一次性补算全部 elapsed tick | STOP 恢复、调试暂停或主循环阻塞后可能长时间占用 CPU | 每轮最多处理 `ADC_ANALOG_CAL_MAX_CATCHUP_TICKS` 个 tick，backlog 分轮消化 |
| `fputc()` 等待 `USART_FLAG_TC` 无超时 | UART/日志异常时可能卡死在调试输出 | 加 `SCI_DEBUG_UART_TX_WAIT_LOOP`，超时放弃当前字符 |
| Fault handler 永久 `while(1)` | Release 下 HardFault/BusFault/UsageFault 可能表现为死机 | Debug 保持停住便于调试，非 Debug 直接 `NVIC_SystemReset()` |
| `LedBar_GetPinIndex()` 未防 `pin == 0` | 后续改显示引脚表时可能引入死循环 | 对 0 引脚直接返回，非 0 引脚限制最多扫描 15 位 |
| `rtc_sleep.c` 同时维护模式和 armed 标志 | `g_sleepModeSelect` / `state_sleep` 分散，状态边界不直观 | 收敛为 `LOW_POWER_RUNTIME_STATE { mode, armed }`，`LowPower_Request()` 后统一刷新观测状态 |
| 低功耗同时保留 `Sleep_Mode` 位图和运行态 mode | 请求入口、显示熄屏、复位式休眠提交存在两套状态来源 | 删除旧 `Sleep_Mode` / `Sleep_Status` / `App_SleepDeal()` 状态机，统一为 `LowPower_Request()`、`LowPower_IsToSleepPending()`、`SleepDeal_Continue(mode)` |
| Fault handler 复位前无现场原因 | 上板复位后无法区分 HardFault / MemManage / BusFault / UsageFault | 复位前写 `BKP_DR11` 原因码和 `BKP_DR12` 反码，并映射到 `0xD200/0xD201` 只读寄存器 |
| `Can_HDX.c` 与飞道协议组包混在一起 | 运行时、电源状态机和报文内容边界不清晰 | 飞道帧已拆到 `CanFeidaoFrames.c/.h`，`Can_HDX.c` 保留运行时、BusOff、低功耗 CAN 服务 |
| `Sci_Upper.c` 地址映射和写入副作用难追踪 | 后续拆协议文件容易影响上位机兼容 | `COMMUNICATION_ADDRESS_INDEX.md` 已补 `0xD200/0xD201`，`0x10` 写副作用在文档和 `project_check.py` 中固化 |

## 14. 原需关注风险的处理结论

第 14 节原列出的源码级风险已经按源码收口：

1. 低功耗请求入口统一：旧 `Sleep_Mode` 位图、`Sleep_Status` 枚举和 `App_SleepDeal()` 状态机已删除。外部进入休眠只走 `LowPower_Request(mode)` / `entersleep(mode)`；显示侧只读 `LowPower_IsToSleepPending()`；复位式提交只走 `SleepDeal_Continue(mode)`。
2. `SleepDeal.c` 只保留启动休眠标志、BKP 校验、唤醒后 STOP 入口和 `SleepDeal_Continue(mode)`，不再保留未启用的 normal/hiccup 旧状态机函数。
3. Fault snapshot 已形成闭环：Fault handler 写 `BKP_DR11/BKP_DR12`，RS485 `0xD200` 读 fault reason，`0xD201` 读 inverse，定义集中在 `FaultSnapshot.h`。
4. CAN 边界已经文档化并部分拆分：协议帧在 `CanFeidaoFrames.c/.h`，运行时和低功耗服务在 `Can_HDX.c`，对外低功耗 API 为 `Can_PrepareSleep()`、`Can_RtcWakeService()`、`Can_IsBusActive()`、`Can_GetIdleRtcPeriodSeconds()`。
5. SCI 地址表和副作用清单已更新：`COMMUNICATION_ADDRESS_INDEX.md` 记录 `0xD200/0xD201` 和 `0x10` 写入副作用；`project_check.py` 会检查 SOC 参数写入、fault snapshot、低功耗接口和 CAN 文档关键约束。
6. 当前仍不能在 Mac 侧替代 Windows + Keil 完整链接和上板验证；这不是源码流程风险，而是验证环境边界。Mac 侧结论只覆盖静态检查、host C、Python replay 和 clang 语法检查。

## 15. 源码编码收口

当前 git 跟踪的源码、脚本、Keil 工程文件和 Markdown 文档默认使用 UTF-8 无 BOM；`103 + 309/Project/Source/conf/Project_Config.h` 是例外，必须保持 GBK/ANSI，保证 Keil `Configuration Wizard` 中文可视化配置正常显示。未跟踪参考工程不纳入本规则。

Keil 影响边界：

- 除 `Project_Config.h` 外，C/H/S 文件中的 ASCII 代码、宏、寄存器名、路径和 Keil 工程 XML 结构不变。
- `Project_Config.h` 的中文只用于 Keil 配置向导注释，保存为 GBK/ANSI 后不改变宏值和编译逻辑。
- 少量中文日志字符串仍随普通源码保持 UTF-8；这只影响调试串口中文字显示编码，不影响保护、SOC、CAN、RTC 等控制逻辑。
- `tools/project_check.py` 已增加编码检查：普通文本拦截非 UTF-8 回退，`Project_Config.h` 单独检查 GBK/ANSI 和配置向导标记。

## 16. 本次验证命令

```bash
python3 tools/project_check.py
python3 tools/soc_replay_test.py
python3 tools/run_soc_host_c_test.py
clang -fsyntax-only 103\ +\ 309/Project/Source/ADC.c
clang -fsyntax-only 103\ +\ 309/Project/Source/Sci_Upper.c
clang -fsyntax-only 103\ +\ 309/Project/Source/LedBar.c
clang -fsyntax-only 103\ +\ 309/Project/Source/SleepDeal.c
clang -fsyntax-only 103\ +\ 309/Project/Source/rtc_sleep.c
git diff --check
```

说明：`stm32f10x_it.c` 含 ARMCC 风格 `__asm void wait()`，Mac 自带 clang 不能直接解析该语法；该文件需要通过 Keil/Armclang 做最终编译验证。

SOC 当前实测覆盖：

- `tools/soc_replay_test.py`：47 项通过。
- `tools/run_soc_host_c_test.py`：每档 19 项通过，覆盖当前配置、debug watch 版以及 0mA/30mA/1000mA 板端自耗配置。
