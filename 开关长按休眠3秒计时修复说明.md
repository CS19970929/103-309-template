# 开关长按休眠 3 秒计时修复说明

## 背景

现场需求是开关长按 3 秒进入休眠。原 `LedBar_ServiceSwitch()` 使用主循环里捕获到的 `b1Sys10msFlag` 对 `s_ledbar_key_hold_10ms` 自增，理论阈值是 `300 * 10ms = 3s`，但实际触发时间会受到主循环调度影响。

当主循环被 AFE 采样、通信处理、Flash/日志任务或低功耗流程拖慢时，`LedBar_ServiceSwitch()` 不一定每 10ms 都被执行一次。这样计数器虽然按 10ms 命名，实际是在按“服务函数执行次数”累计，表现为用户感觉长按休眠时间偏长。

## 本次修改

修改文件：

- `103 + 309/Project/Source/LedBar.c`

关键逻辑：

- `LEDBAR_KEY_LONG_PRESS_10MS` 保持 `300u`，即 3 秒。
- 按键按下沿记录 `s_ledbar_key_press_start_10ms = SysTime_Get10msTickCount()`。
- 按住期间用无符号差值计算真实经过的 10ms tick：

```c
s_ledbar_key_hold_10ms = now_10ms - s_ledbar_key_press_start_10ms;
```

这样主循环偶发延迟不会把长按时间拉长。只要系统 10ms tick 正常运行，长按达到约 3 秒就会触发 `entersleep(DEEP_MODE)` 和 `SleepDeal_Continue(mode)`。

## 适用范围

这种写法适合运行态中需要按真实经过时间判断的逻辑，例如：

- 按键长按、短按窗口和防抖。
- 显示保持时间。
- 通信超时。
- 保护延时。
- 进入休眠前的运行态倒计时。

不适合跨 STOP/RTC 休眠计时。进入 STOP 后普通 10ms tick 会停止，跨休眠时长应继续使用 RTC 秒计数、备份域或已有休眠标志。

## 风险控制

- 使用 `uint32_t` tick 差值，支持无符号回绕。
- 长按一次性动作仍由 `s_ledbar_key_long_handled` 防止重复触发。
- 松手后清零长按计时和处理标志，保持下一次按键独立判定。
- SOC 显示窗口递减仍保留 `b1Sys10msFlag` 门控，避免主循环空转时过快递减。

## 验证

已执行 Keil 命令行构建：

```powershell
& 'C:\Keil_v5\UV4\UV4.exe' -b 'E:\TODO\103 + 309 - 副本\103 + 309\Project\Users\CommomSH367309_16series_103RCT6_C.uvprojx' -t 'FD_Release' -o 'E:\TODO\103 + 309 - 副本\build\keil_fd_release.log'
```

结果：

- 目标：`FD_Release`
- 工具链：ARMCC 5.06 update 7 build 960
- 结果：`0 Error(s), 0 Warning(s)`
- 产物：`103 + 309/Project/Users/Objects/FD_Release.bin`

烧录 App 时必须继续使用安全脚本，避免覆盖 IAP：

```powershell
.\tools\soc_flash_app_safe.ps1 -Bin "103 + 309\Project\Users\Objects\FD_Release.bin" -Flash
```
