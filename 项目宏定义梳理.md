# 项目宏定义梳理

本文按当前工程源码梳理 `#define` 宏的功能、使用方式和维护边界。统计范围为：

- Keil 工程编译宏：`103 + 309/Project/Users/CommomSH367309_16series_103RCT6_C.uvprojx`
- 业务源码宏：`103 + 309/Project/Source`
- EasyLogger 配置宏：`103 + 309/Project/Source/easylogger`
- STM32 标准外设库宏：`103 + 309/Project/STM32F10x_StdPeriph_Lib_V3.5.0`

说明：STM32 标准外设库自身包含约 7700 个寄存器、外设参数和断言宏，不建议复制成业务文档逐项维护；本文按“库宏族群”说明其功能和使用边界。业务源码约 870 个宏，已按模块和同类连续宏合并覆盖。

## 1. 当前有效编译入口

当前已新增 Keil 可视化配置入口：`103 + 309/Project/Source/conf/Project_Config.h`。在 Keil 工程树的 `conf` 分组中打开该文件，右键选择 `Configuration Wizard`，优先通过其中的开关和选择项修改产品配置、功能启用、SCI 角色、LedBar 模式和升级参数策略。

| 来源 | 宏 | 当前值/状态 | 功能 | 怎么用 |
|---|---|---:|---|---|
| Keil `uvprojx` | `STM32F10X_MD` | 已定义 | 选择 STM32F10x Medium-density 设备族，影响标准库寄存器、启动文件、Flash page size 等条件编译 | 不在源码内重复定义；通过 Keil target 的 `Define` 维护 |
| Keil `uvprojx` | `USE_STDPERIPH_DRIVER` | 已定义 | 启用 STM32 标准外设库驱动路径，并使 `stm32f10x_conf.h` 生效 | 保持在工程预定义宏中；`main.h` 中同名注释宏不要再打开 |
| `Project_Config.h` | `PROJECT_CFG_BUILD_PROFILE` | `0` / Release | 构建档位，控制发布保护规则 | 日常正式包保持 Release；调试包可切 Debug；产测/破坏性测试切 Factory/Test |
| `Project_Config.h` -> `conf/conf.h` | `BAT_TYPE` | `BAT_SLAVE` | 当前产品形态选择，从机/40A 档；影响采样电阻数量、容量默认值、CAN 数据等 | 在 Keil Wizard 中修改 `PROJECT_CFG_BAT_TYPE` |
| `Project_Config.h` -> `conf/conf.h` | `TERNARYLI` | 已定义 | 选择三元锂参数表 | 在 Keil Wizard 中修改 `PROJECT_CFG_BAT_CHEMISTRY`，与 `LIFEPO` 二选一 |
| `Project_Config.h` -> `conf/conf.h` | `AFE_TYPE` | `sh36xx` | 选择 AFE 类型 | 在 Keil Wizard 中修改 `PROJECT_CFG_AFE_TYPE`；切 `bq76xx_afe` 需要验证相关条件分支 |
| `Project_Config.h` -> `conf/conf.h` | `_IAP` | 已定义 | 启用跳转 IAP/升级标志相关逻辑 | 在 Keil Wizard 中修改 `PROJECT_CFG_IAP_ENABLE` |
| `Project_Config.h` -> `conf/conf.h` | `_COMMOM_UPPER_SCI1` | 已定义 | 启用 SCI1 通用上位机协议 | 在 Keil Wizard 中修改 `PROJECT_CFG_SCI1_ROLE`；每个串口只选一种角色 |
| `conf/conf.h` | `wdog_enable` | 已定义 | 启用 IWDG 喂狗和 RTC 等长等待中的喂狗处理 | 调试死循环/单步时可临时关闭；量产建议打开 |
| `conf/conf.h` | `__FUNC_RTC__` | 已定义 | 启用 RTC 功能路径 | 关闭后 `System_Init.c` 会跳过 RTC 初始化，休眠/唤醒策略需同步调整 |
| `conf/conf.h` | `UART1_WAKEUP_ENABLE` | 已定义 | 允许 UART1 作为低功耗唤醒源 | 修改后检查 `rtc_sleep.c` 中 wake source 初始化 |
| `conf/conf.h` | `RS485_WAKEUP_ENABLE` | 已定义 | 允许 RS485/通信唤醒逻辑 | 与上位机通信低功耗唤醒策略配套 |
| `conf/conf.h` | `__VIRTURE_CURRENT__` | 已定义 | 休眠或无电流采样时启用虚拟电流补偿 | 修改后验证 `DataDeal.c` 与 SOC 累计逻辑 |
| `conf/conf.h` | `_DI_SWITCH_longKEY_ONOFF` | 已定义 | 启用 DI1 长按开关机 | 影响 `LedBar.c` / `DataDeal.c` 的按键逻辑 |

## 1.1 调试/正式发布保护

当前新增 `Project_BuildGuard.h`，用于把“发布前不能打开的调试/测试开关”变成编译期错误，避免赶项目时靠人工记忆检查。

| 档位 | `PROJECT_CFG_BUILD_PROFILE` | 允许用途 | 保护策略 |
|---|---:|---|---|
| Release | `0` | 正式发布、现场升级包 | 强制看门狗开启；禁止 `_DEBUG_`、`_DEBUG_CODE`、Flash 64K 测试、LedBar GPIO 测试、升级策略强制重刷、EasyLogger 输出 |
| Debug | `1` | 在线调试、单步跟踪、临时日志 | 不做发布保护拦截；调试完成后必须切回 Release |
| Factory/Test | `2` | 产测、破坏性存储测试、专项验证 | 允许打开测试项；生成固件不能作为正式包外发 |

发布保护检查位置：

- `conf/conf.h` 末尾 include `Project_BuildGuard.h`，检查项目配置、Keil `_DEBUG_`、旧式派生宏。
- `easylogger/inc/elog_cfg.h` 末尾 include `Project_BuildGuard.h`，确保 Release 档打开 `ELOG_OUTPUT_ENABLE` 时也会报错。

Release 档下若误开危险项，编译会直接失败，不再依赖人工发布清单。

Keil 工程当前拆成两个目标：

| Keil Target | 预定义宏差异 | 用途 |
|---|---|---|
| `FD_Release` | `STM32F10X_MD,USE_STDPERIPH_DRIVER` | 正式发布默认目标，走 Release 保护 |
| `FD_Debug` | 额外定义 `PROJECT_CFG_BUILD_PROFILE=1,_DEBUG_` | 在线调试目标，允许低功耗调试和调试日志路径 |

后续日常操作优先切 Keil Target，不建议为了调试反复手改 `Project_Config.h` 的多个开关。

自动化检查入口：

```sh
python3 tools/project_check.py
```

本仓库新增 `.githooks/pre-commit` 和 `.githooks/pre-push`，启用 `git config core.hooksPath .githooks` 后，提交和推送前会自动运行上述检查。详细流程见 `项目自动化检查与发布流程.md`。

## 2. 产品级配置宏

| 宏/宏族 | 定义位置 | 功能 | 怎么用 |
|---|---|---|---|
| `EEPROM_VALUE_BEGIN_FLAG` | `conf/conf.h` | 参数初始化标志值。改变该值会触发默认参数重新落库 | 需要强制刷新现场参数时才改；改完烧录一次后不要随意反复变动 |
| `T3MAX`、`T3` | `conf/conf.h` | 温度/NTC 档位常量 | 作为状态/索引常量使用，不建议分散改名 |
| `BAT_MASTER`、`BAT_SLAVE`、`BAT_TYPE` | `conf/conf.h` | 产品主从/电流档位选择 | 只通过 `BAT_TYPE` 选择；不要在模块内硬编码主从 |
| `FD_YEAR`、`FD_MONTH`、`FD_DAY` | `conf/conf.h` | 出厂/固件日期字段 | 发布版本前更新；CAN/上位机读取日期时依赖这些值 |
| `VERSION` | `conf/conf.h` | 固件版本号 | 协议或发布识别用；升级包发布时递增 |
| `CURR_80A`~`CURR_250A`、`LEVEL_CURR` | `conf/conf.h` | 电流规格枚举和当前规格 | 当前为 `CURR_150A`；保护、电流显示或参数模板若按规格分档，应引用 `LEVEL_CURR` |
| `bq76xx_afe`、`sh36xx`、`AFE_TYPE` | `conf/conf.h` | AFE 类型枚举和当前选择 | 只在平台配置层改；业务代码用 `#if AFE_TYPE == ...` 分支 |
| `CHG_LOWTEMP_PARAM`、`HEAT_OPEN_CURR` | `conf/conf.h` | 加热功能相关低温充电阈值和开启电流 | 当前 `__FUNC__HEAT__` 已开启，使用加热相关默认值；需结合 `Heat_Cool.c` 上板验证 |

## 3. 当前关闭但可切换的功能宏

| 宏 | 定义位置 | 当前状态 | 功能 | 打开前检查 |
|---|---|---|---|---|
| `LIFEPO` | `conf/conf.h` | 注释关闭 | 磷酸铁锂参数体系 | 必须关闭 `TERNARYLI`，并检查 `Fault.h`、`DataDeal.h`、`SH367309_Func.h` 的阈值 |
| `__FUNC__HEAT__` | `conf/conf.h` | 已定义 | 加热功能 | 检查 `Heat_Cool.c`、`Fault.h`、`SH367309_DataDeal.h` 低温阈值和 IO；重点验证 CHG MOS 不绕过保护链路 |
| `UART2_WAKEUP_ENABLE` | `conf/conf.h` | 注释关闭 | UART2 唤醒 | 打开后配置 USART2/RS485 引脚和 `rtc_sleep.c` 唤醒源 |
| `_SECOND_CURR_PROTECT_FUNC_` | `conf/conf.h` | 注释关闭 | 二级电流保护相关条件编译 | 打开后验证 `IO_Control.c` 保护出口 |
| `FLASH64K_APP_QUICK_TEST_ENABLE` | `conf/conf.h` | 注释关闭 | 后 64KB 存储破坏性快速测试 | 仅产测/开发用，不能用于量产包 |
| `FLASH64K_APP_USE_TEST_ENABLE` | `conf/conf.h` | 注释关闭 | App 运行时 SOC/AFE 存储验证 | 打开后配合打印周期和加速周期宏；注意 Flash 擦写寿命 |
| `_DI_SWITCH_SYS_ONOFF`、`_DI_SWITCH_DSG_ONOFF` | `conf/conf.h`、`main.h` | 注释关闭 | DI 控制系统或放电通断 | 与 `_DI_SWITCH_longKEY_ONOFF` 不能随意叠加，需定义优先级 |
| `_COMMOM_UPPER_SCI2`、`_COMMOM_UPPER_SCI3` | `main.h` | 注释关闭 | 启用 SCI2/SCI3 通用上位机协议 | 每个串口只选一种角色；SCI3 还涉及重映射和中断 |
| `_CLIENT_SCIx`、`_LCD_SCIx` | `main.h` | 注释关闭 | 串口客户端或 LCD 协议角色 | 与 `_COMMOM_UPPER_SCIx` 互斥 |
| `_CHARGER_LOAD` | `ChargerLoadFunc.h` | 注释关闭 | 充电器/负载检测功能路径 | 打开后检查 `ChargerLoadFunc.c` 中入口是否被主循环调度 |

## 4. GPIO 与位带宏

| 宏/宏族 | 定义位置 | 功能 | 怎么用 |
|---|---|---|---|
| `__STM32F1__` | `conf/conf_gpio.h` | 选择 STM32F1 GPIO 配置路径 | 当前有效；不要同时打开 `__STM32F0__` |
| `BITBAND`、`MEM_ADDR`、`BIT_ADDR` | `System_Init.h` | Cortex-M3 位带地址换算 | 仅用于 `PAout/PBin` 等位操作宏，不建议业务层直接写复杂表达式 |
| `GPIOx_ODR_Addr`、`GPIOx_IDR_Addr` | `System_Init.h` | GPIO 输出/输入寄存器位带基址 | 与 `PAout/PBin` 等配套，不直接作为外设 API |
| `PAout/PBout/.../PGout` | `System_Init.h` | 单 bit 输出左值宏 | 用法：`PBout(15)=1`；参数必须是 0~15 |
| `PAin/PBin/.../PGin` | `System_Init.h` | 单 bit 输入读取宏 | 用法：`if (PBin(12)) ...`；参数必须是 0~15 |
| `MCUO_*`、`MCUI_*` | `System_Init.h`、`conf_gpio.h` | 业务 IO 别名，例如 LED、AFE 控制、DI 输入 | 上层逻辑优先使用业务名，不直接散落 `PBout(n)` |
| `GPIO_*`、`PIN_*` | `conf/conf_gpio.h` | 外设和信号的端口/引脚映射 | 初始化和驱动函数应使用这组宏；改板级引脚时集中改这里 |
| `GPIO_LED595_*`、`PIN_LED595_*` | `conf/conf_gpio.h` | 74HC595 LED 驱动引脚别名 | 当前映射到 SPI 相关 GPIO；切 Charlieplexing 时仍保留兼容 |
| `GPIO_ADC_*`、`PIN_ADC_*` | `conf/conf_gpio.h` | ADC 信号引脚映射 | 改 ADC 通道时同步检查 `ADC.c` 的通道序列 |

## 5. 通用工具宏

| 宏 | 定义位置 | 功能 | 怎么用 |
|---|---|---|---|
| `UPDNLMT16(Var,Max,Min)` | `main.h`、`IODrivers.c` | 将变量限制在 `[Min, Max]` 范围 | 宏会对 `Var` 赋值两次，参数不要传带副作用表达式 |
| `S2U(x)` | `main.h` | 将对象地址按 `volatile UINT16*` 解释读取 | 用于底层数据转换；注意对齐和别名风险 |
| `MCU_RESET()` | `main.h` | 调用 `NVIC_SystemReset()` 软件复位 | 升级、严重错误恢复时使用 |
| `CRC_KEY` | `main.h` | 项目 CRC/校验相关常量 | 修改会影响通信或存储校验兼容性 |
| `I2C_RW_W`、`I2C_RW_R` | `main.h` | I2C 读写方向常量 | I2C 驱动内部使用，避免魔法值 `0/1` |
| `DELAYB10MS_*` | `main.h`、`IODrivers.c` | 10ms tick 换算的延时常量 | 用在以 10ms 为周期的计数器中；不要用于真实阻塞延时 |
| `Feed_IWatchDog` | `System_Init.h` | `IWDG_Feed()` 的兼容别名 | 老代码可继续调用，新代码建议直接调用函数接口 |
| `SET_BIT`、`CLEAR_BIT`、`READ_BIT`、`CLEAR_REG`、`WRITE_REG`、`READ_REG`、`MODIFY_REG`、`POSITION_VAL` | `rtc_sleep.h` | HAL 风格寄存器操作兼容宏 | 低功耗/寄存器代码中使用；传入参数应是寄存器左值 |
| `enumToStr(WEEK)` | `rtc_sleep.h` | 将 token 字符串化 | 调试打印使用 |

## 6. ADC 与采样宏

| 宏/宏族 | 定义位置 | 功能 | 怎么用 |
|---|---|---|---|
| `AD_Used_amount` | `ADC.h` | 当前 ADC 规则组使用数量，当前为 3 | 增减采样信号时同步修改 ADC 初始化和缓存处理 |
| `AD_CalNum`、`AD_CalNum_2` | `ADC.h` | 普通 ADC 滤波平均次数及右移位数，8 次平均 | 两者必须保持 `AD_CalNum == 1 << AD_CalNum_2` |
| `AD_CalNum_Cur`、`AD_CalNum_Cur_2` | `ADC.h` | 电流采样平均次数及右移位数，32 次平均 | 两者必须保持一致 |
| `AD_CurOffsetCalNum`、`AD_CurOffsetCalNum_2` | `ADC.h` | 电流零点校准平均次数和位移 | 零漂校准时使用 |
| `AD_CurZeroDeadband` | `ADC.h` | 电流零点死区 | 减少小信号抖动；过大可能吞掉微小电流 |
| `LENGTH_TBLTEMP_PORT_10K` | `ADC.h` | 10K NTC 查表长度 | 修改温度表时同步更新 |
| `Vbc_scale` | `ADC.h` | 母线电压分压比例 | 改硬件分压电阻后必须校准 |
| `MONITOR_AFE_*` | `DataDeal.c` | AFE 监控失败、恢复、唤醒重试和周期参数 | 调整 AFE 异常恢复速度时改这组宏，单位见宏名：`*_MS`、`*_SEC`、`*_TICKS` |

## 7. AFE 与 I2C 宏

| 宏/宏族 | 定义位置 | 功能 | 怎么用 |
|---|---|---|---|
| `AFE_ID` | `I2C_AFE1.h` | SH367309 AFE I2C 地址，当前 `0x34` | AFE 通信统一使用该地址 |
| `E2PROM_ID` | `I2C_AFE1.h` | 外部 EEPROM I2C 地址，当前 `0xA0` | 外部 EEPROM 驱动使用；当前参数主路径已转内部 Flash |
| `MTP_OTC`~`MTP_RSTSTAT` | `I2C_AFE1.h` | SH367309 MTP/状态/采样寄存器地址 | 调用 `MTPRead/MTPWrite` 时使用，避免硬编码寄存器地址 |
| `_TWI_COM` | `I2C_AFE1.h` | 启用软件 TWI 通信路径 | `I2C_AFE1.c` 中按该宏编译 bit-bang 通信 |
| `TWI_CLK_OUT/IN/HIGH/LOW`、`TWI_DAT_OUT/IN/HIGH/LOW`、`TWI_RD_CLK/DAT` | `I2C_AFE1.h` | 软件 I2C 时钟/数据线方向、输出和输入宏 | 当前直接操作 PB8/PB9；移植硬件 I2C 或函数封装时切换 `#if 0` 备用实现 |
| `VAL_CELL_OVP/OVP_REC/UVP/UVP_REC/BAL_OPEN` | `SH367309_Func.h` | AFE 初始化时的单体过压/欠压/均衡阈值 | 由 `TERNARYLI` 或 `LIFEPO` 选择当前值；不要在寄存器字节宏里改阈值 |
| `VAL_CHG_*`、`VAL_DSG_*` | `SH367309_Func.h` | AFE 充放电温度保护和恢复值，编码为 `(T+40)` 或相关 8 bit 值 | 修改温度策略时改这组阈值，再由 `BYTE_11H`~`BYTE_18H` 写入 |
| `BIT_*` | `SH367309_Func.h` | AFE 配置寄存器位域，例如 `BIT_ENMOS`、`BIT_BAL`、`BIT_OCPM` | 只表达单个位域，不单独写入；由 `BYTE_xxH_*` 组合 |
| `BYTE_00H_*`~`BYTE_19H_*` | `SH367309_Func.h` | SH367309 初始化寄存器字节值 | AFE 初始化表使用；修改时需对照芯片手册位定义 |
| `E2P_ADDR_E2POS_AFE_Parameters` | `SH367309_DataDeal.h` | AFE 参数旧 EEPROM 起始地址 | 当前兼容历史地址；内部 Flash 已承接主要参数存储 |
| `RS485_CMD_ADDR_AFE_ROM_PARAMETERS_START/END`、`RS485_ADDR_RW_AFE_PARAMETER` | `SH367309_DataDeal.h` | 上位机读写 AFE 参数的地址窗口 | 与 `Sci_Upper.h` 地址表保持一致 |
| `AFE_PARAMETES_TOTAL_LENGTH` | `SH367309_DataDeal.h` | AFE 参数寄存器数量，当前 24 | 改 AFE 参数结构时同步更新存储和通信长度 |
| `AFE_COV/CUV/OTC/UTC/OTD/UTD/OCC/ODC*` | `SH367309_DataDeal.h` | AFE 保护参数默认值、恢复值和滤波时间 | 当前 `LIFEPO` 未定义，走三元/非磷酸铁锂过压欠压默认值；用于生成 `AFE_PARAMETERS_RS485_STRUCTION_DEFAULT` |
| `AFE_PARAMETERS_RS485_STRUCTION_DEFAULT` | `SH367309_DataDeal.h` | AFE 参数默认表，包含当前值、默认值、最大值、最小值 | 参数初始化和恢复默认时使用；增减字段必须同步 `AFE_PARAMETES_TOTAL_LENGTH` |
| `DSG_CHG_OCP_DELAY_TIME` | `SH367309_DataDeal.c` | 充放电过流相关延时，当前 `30 * 100` | AFE 数据处理内部使用，单位按调用处计数周期理解 |
| `OFF`、`ON` | `SH367309_DataDeal.c` | 本地开关常量，0/1 | 仅该模块内部使用，避免扩散成全局状态定义 |

## 8. 保护参数宏

| 宏/宏族 | 定义位置 | 功能 | 怎么用 |
|---|---|---|---|
| `CurOverFaultDelay` | `Fault.h` | 电流过流故障延迟，单位 10ms，当前 3000 即 30s | 用于故障判定延时；调小会更敏感 |
| `COV_*`、`CUV_*` | `Fault.h` | 单体过压/欠压一二三级、恢复、滤波 | 作为保护参数默认表的来源；单位 mV 或计数见结构字段 |
| `BOV_*`、`BUV_*` | `Fault.h` | 总压过压/欠压一二三级、恢复、滤波 | 当前按 `SNum` 计算，总压单位为 `0.1V` 风格常量 |
| `OCC_*`、`ODC_*` | `Fault.h` | 充电/放电过流一二三级、恢复、滤波 | 默认表使用；和采样电阻、电流档位联动 |
| `OTC_*`、`UTC_*`、`OTD_*`、`UTD_*` | `Fault.h` | 充放电高低温保护阈值、恢复、滤波 | 温度编码为 `(T + 40) * 10` |
| `mos_*` | `Fault.h` | MOS 高温保护阈值、恢复、滤波 | 与 MOS 温度采样通道绑定 |
| `VDELTER_*` | `Fault.h` | 单体压差过大阈值、恢复、滤波 | 保护和告警判断使用 |
| `socLow_*` | `Fault.h` | SOC 低电量告警阈值、恢复、滤波 | SOC 模块输出异常时会影响告警 |
| `E2P_PROTECT_MIN_PRT`、`E2P_PROTECT_DEFAULT_PRT`、`E2P_PROTECT_MAX_PRT` | `Fault.h` | 保护参数最小/默认/最大表 | 参数初始化、上位机写入边界检查和恢复默认使用；字段顺序必须与 `struct PRT_E2ROM_PARAS` 一致 |
| `Record_len` | `Fault.h` | 故障记录长度，当前 10 | 修改后同步 EEPROM/Flash 记录布局和数组声明 |
| `OTP_UTP_VirCur_Chg`、`OTP_UTP_VirCur_Dsg` | `Fault.c` | 温度保护/恢复相关虚拟充放电电流标志 | 故障判断内部使用，修改后需验证温度保护解除路径 |

## 8.1 产品参数默认值宏

| 宏/宏族 | 定义位置 | 功能 | 怎么用 |
|---|---|---|---|
| `CompensateNUM` | `DataDeal.h` | 铜损补偿点数，当前 16 | 与 `CopperLoss[]`、`CopperLoss_Num[]` 和 EEPROM 参数数量一致 |
| `CopperLoss_*`、`CopperLossNum_*` | `DataDeal.h` | 铜损补偿值和补偿点编号的最小/默认/最大值 | 上位机写入铜损表时做边界检查 |
| `SYSKMAX/SYSKDEFAULT/SYSKMIN` | `DataDeal.h` | 校准 K 系数范围，1024 表示 1.0 | 电压、电流、温度校准 K 值使用 |
| `SYSBMAX/SYSBDEFAULT/SYSBMIN` | `DataDeal.h` | 校准 B 偏移范围 | 校准偏置使用，注意有符号范围 |
| `BMS_HARDWARE_VERDION_DEFAULT`、`BMS_SOFTWARE_VERDION_DEFAULT`、`BMS_SERIAL_NUMBER_DEFAULT` | `DataDeal.h` | 硬件版本、软件版本、序列号默认字符串 | 出厂信息未写入时使用；修改版本字符串要考虑上位机长度限制 |
| `SNum` | `DataDeal.h` | 电芯串数，当前 10 | 总压保护、默认参数和显示都依赖该值 |
| `CS_Cur_CHGmax`、`CS_Cur_DSGmax` | `DataDeal.h` | 基于采样电阻和并联数量计算的最大充/放电电流 | 修改 `CS_Res` 或 `CS_Res_Num` 后自动变化 |
| `CBC_DelayT`、`CBC_Cur_DSG` | `DataDeal.h` | CBC 延时和放电电流阈值默认值 | 与 CBC 保护/恢复逻辑配套 |
| `CS_Res`、`CS_Res_Num`、`BMS_CAPCITY` | `DataDeal.h` | 采样电阻、并联数量和额定容量 | 由 `BAT_TYPE` 条件编译选择；当前 `BAT_SLAVE` 下为 `2`、`6`、`270` |
| `OtherElement_min/default/max` | `DataDeal.h` | 其他参数块的最小/默认/最大表 | 当前 `TERNARYLI` 分支默认使用 `SOC_TABLE_TERNARYLI`；字段顺序必须与 `struct OTHER_ELEMENT` 一致 |

## 9. 参数、EEPROM 与 Flash 存储宏

| 宏/宏族 | 定义位置 | 功能 | 怎么用 |
|---|---|---|---|
| `DELAY_US_IIC_EEPROM` | `EEPROM.h` | 软件 I2C EEPROM 延时，当前 2us 近似 150kHz | 仅外部 EEPROM bit-bang 路径使用 |
| `sEEAddress` | `EEPROM.h` | 外部 EEPROM 地址，当前 `0xA0` | 历史外部 EEPROM 驱动使用 |
| `AZONE/BZONE/CZONE` | `EEPROM.h` | EEPROM 分区起始地址 | 旧参数分区布局使用 |
| `SDA_IN_SEE/SDA_OUT_SEE`、`IIC_SCL_SEE/IIC_SDA_SEE/READ_SDA_SEE` | `EEPROM.h` | 外部 EEPROM 软件 I2C IO 宏 | 当前 EEPROM 读写已大量转向内部 Flash，保留兼容 |
| `EEPROM_ADDR_*`、`EEPROM_VALUE_*` | `EEPROM.h` | EEPROM 中密码、休眠、升级等标志地址和值 | 新逻辑多数转向 Flash/BKP；修改前确认是否仍被调用 |
| `E2P_PARA_NUM_*` | `EEPROM.h` | 各参数块字数：保护、RTC、校准、SOC、铜损、故障、其他、热管理 | 与结构体字段数量和 Flash word count 必须一致；`EEPROM.c` 已有编译期检查部分参数块 |
| `E2P_ADDR_*` | `EEPROM.h` | EEPROM 参数地址表和起始地址 | 保持与上位机地址、结构体顺序一致；新增参数只能在确认空洞后分配 |
| `FLASH_ADDR_IAP_START`、`FLASH_ADDR_APP_START` | `Flash.h` | IAP 和 App 起始地址 | 与 Keil IROM 和 Bootloader 链接配置一致 |
| `FLASH_ADDR_STORAGE_*` | `Flash.h` | 内部 Flash 参数/AFE/日志/SOC 双槽地址 | 当前 64KB 工程使用后部页存储；改地址必须检查页大小和擦除边界 |
| `FLASH_STORAGE_PAGE_SIZE`、`FLASH_STORAGE_SLOT_SIZE` | `Flash.h` | Flash 页大小和槽大小 | `STM32F10X_MD` 下为 1KB，否则 2KB |
| `FLASH_STORAGE_*_WORD_COUNT` | `Flash.h` | 各参数块写入字数 | 与 `E2P_PARA_NUM_*` 保持一致 |
| `FLASH_STORAGE_MAGIC_SOC/AFE/RW_PARAM/LOG` | `Flash.c` | 内部 Flash 存储块 magic，用于识别 SOC、AFE、RW 参数、日志块 | 修改会导致旧数据无法被识别，通常只在存储格式大改时变更 |
| `FLASH_STORAGE_VERSION` | `Flash.c` | 内部 Flash 存储格式版本，当前 `0x0001` | 存储结构不兼容时递增，并提供迁移/默认策略 |
| `FLASH_SIZE_REG_ADDR` | `Flash.c` | STM32 Flash size 寄存器地址 | 用于运行时判断芯片容量 |
| `FLASH_309_*` | `Flash.h` | 309/RTC/正常启动组合标志值 | 启动路径和 AFE/RTC 判断使用 |
| `FLASH_TO_IAP_VALUE`、`FLASH_TO_APP_VALUE` | `Flash.h` | App/IAP 跳转标志 | 升级流程写入/读取该值 |
| `FLASH_NORMAL_SLEEP_VALUE`、`FLASH_DEEP_SLEEP_VALUE`、`FLASH_HICCUP_SLEEP_VALUE`、`FLASH_SLEEP_RESET_VALUE` | `Flash.h` | 休眠类型标志值 | 当前休眠启动标志主要写 BKP，Flash 宏保留兼容 |
| `BOOT_FLAG_RESET_VALUE` | `Flash.h` | 启动标志复位值，等同 `FLASH_SLEEP_RESET_VALUE` | `SleepDeal.c` 中启动标志清理语义使用 |
| `STORAGE_FLASH_SOC_API_DECLARED` | `Flash.h` | 防止 SOC 模块重复声明 Flash SOC API | 不作为业务开关，属于声明保护宏 |

## 10. 升级参数策略宏

| 宏 | 定义位置 | 功能 | 怎么用 |
|---|---|---|---|
| `UPGRADE_PARAM_POLICY_ENABLE` | `UpgradeParamPolicy.h` | 启用升级后参数重置策略 | 量产默认 0；客户包需要强制刷新参数时设 1 |
| `UPGRADE_PARAM_POLICY_VERSION` | `UpgradeParamPolicy.h` | 参数策略版本 | 每次发布新的强制策略必须递增，不能为 `0xFFFF` |
| `UPGRADE_PARAM_RESET_AFE` | `UpgradeParamPolicy.h` | 升级后重置 AFE 参数 | 与客户包默认 AFE 参数同步 |
| `UPGRADE_PARAM_RESET_PROTECT` | `UpgradeParamPolicy.h` | 升级后重置保护参数 | 会覆盖现场保护参数，需谨慎 |
| `UPGRADE_PARAM_RESET_SOC_TABLE` | `UpgradeParamPolicy.h` | 升级后重置 SOC 表 | 用于更换 SOC OCV 表 |
| `UPGRADE_PARAM_RESET_SOC_CONFIG` | `UpgradeParamPolicy.h` | 升级后重置 SOC 配置 | 会影响容量、循环、SOC 配置字段 |
| `UPGRADE_PARAM_RESET_SOC_SNAPSHOT` | `UpgradeParamPolicy.h` | 升级后重置 SOC 快照 | 会清除运行累计快照 |
| `UPGRADE_PARAM_RESET_EVENT_RECORD` | `UpgradeParamPolicy.h` | 升级后清空历史事件日志 | 需要客户升级后丢弃历史日志时设 1，默认保持 0 |
| `UPGRADE_PARAM_FORCE_REAPPLY` | `UpgradeParamPolicy.h` | 测试用，强制重复执行策略 | 发布包必须保持 0 |
| `UPGRADE_PARAM_POLICY_HAS_ACTION` | `UpgradeParamPolicy.h` | 汇总是否存在任何重置动作 | 由策略宏自动计算，不手工改 |

## 11. 通信协议宏

| 宏/宏族 | 定义位置 | 功能 | 怎么用 |
|---|---|---|---|
| `RS485_BROADCAST_ADDR`、`RS485_SLAVE_ADDR` | `Sci_Upper.h` | RS485 广播地址和本机地址 | 协议解析入口使用 |
| `SCI_TX_BUF_LEN`、`RS485_MAX_BUFFER_SIZE` | `Sci_Upper.h` | SCI/RS485 缓冲区长度，当前 251 | 修改后检查最大帧长度、栈/全局 RAM 占用 |
| `RS485_STA_*` | `Sci_Upper.h` | 接收/发送状态机状态 | SCI 状态机内部使用 |
| `RS485_ACK_*` | `Sci_Upper.h` | 正/负应答类型 | 上位机协议响应使用 |
| `RS485_ERROR_*` | `Sci_Upper.h` | 地址、CRC、数据、命令、权限等错误码 | 负应答时填入 `ErrorType` |
| `RS485_ADDR_RW_*` | `Sci_Upper.h` | 可读写参数区起始地址，例如校准、保护、其他参数、CAN 地址 | 上位机读写寄存器时使用这些地址段 |
| `RS485_ADDR_RO_*` | `Sci_Upper.h` | 只读数据区起始地址，例如实时信息、RTC、LCD、事件记录 | 上位机读实时数据时使用 |
| `RS485_ADDR_SN_*` | `Sci_Upper.h` | 序列号、硬件版本、软件版本地址 | 产品身份信息读写 |
| `RS485_CMD_ADDR_FLASH_CONNECT` | `Sci_Upper.h` | MCU 升级/连接命令地址 | 升级握手使用 |
| `RS485_CMD_ADDR_*` 枚举 | `Sci_Upper.h` | 复位命令、校准参数、保护参数、SOC 表、RTC、热管理、其他参数等完整寄存器地址 | 新增上位机寄存器时优先扩展该枚举，并同步处理函数和文档 |
| `_COMMOM_UPPER_SCI3` | `Sci_Upper.h`、`Sci_Upper.c` | 条件编译 SCI3 变量、处理函数和调度 | 当前关闭；打开需要同时打开 `main.h` 的 SCI3 角色宏 |
| `SCI_USART_ERROR_FLAGS` | `Sci_Upper.c` | USART ORE/NE/FE/PE 错误位集合 | 串口中断中统一清错 |
| `debug_uart` | `Sci_Upper.c` | printf/debug 输出串口选择 | 由条件编译选择 USART1/USART2，避免多处改串口 |

## 12. CAN 宏

| 宏/宏族 | 定义位置 | 功能 | 怎么用 |
|---|---|---|---|
| `CANID_TX_Test` | `Can_HDX.h` | CAN 测试发送 ID | 调试帧使用 |
| `CANID_CHECK_0x00`~`CANID_CHECK_0x11` | `Can_HDX.h` | CAN 周期帧/检查项编号 | 与发送状态 bit 对应，便于按帧类型置位 |
| `CAN_ADRESS_STD_ID` | `Can_HDX.h` | CAN 标准 ID 地址字段 | 参与接收过滤 ID 计算 |
| `CANID_RX_COMMON_MSG_FILTER`、`CANID_RX_COMMON_MSG_MASK` | `Can_HDX.h` | CAN 接收过滤器 ID 和 mask | 初始化 CAN filter 时使用；改协议 ID 分配时同步修改 |
| `FEIDAO_CAN_POWER_ON_LEVEL`、`FEIDAO_CAN_POWER_OFF_LEVEL` | `Can_HDX.c` | CAN 收发器电源控制电平 | 适配硬件电源控制极性 |
| `FEIDAO_CAN_POWER_STABLE_TICKS` | `Can_HDX.c` | CAN 上电稳定等待时间，当前 100ms | 调整收发器唤醒时序 |
| `FEIDAO_CAN_TX_DONE_TIMEOUT_TICKS` | `Can_HDX.c` | RTC 唤醒后发送完成兜底超时，当前 200ms | 防止低功耗发送卡死 |
| `FEIDAO_CAN_PERIOD_1000MS_TICKS`、`FEIDAO_CAN_PERIOD_5000MS_TICKS` | `Can_HDX.c` | CAN 周期帧调度周期 tick | 100Hz 逻辑 tick 下分别为 1s/5s |
| `FEIDAO_CAN_TICKS_PER_SECOND` | `Can_HDX.c` | CAN 调度 tick 频率，当前 100 | 改系统 tick 后同步 |
| `FEIDAO_CAN_RTC_ACTIVE_PERIOD_SECONDS`、`FEIDAO_CAN_RTC_IDLE_PERIOD_SECONDS` | `Can_HDX.c` | RTC 低功耗 CAN 活跃/空闲周期 | 决定休眠中 CAN 探测频率 |
| `FEIDAO_CAN_RTC_SERVICE_TIMEOUT_TICKS` | `Can_HDX.c` | RTC CAN 服务超时 | 避免服务窗口过长 |
| `FEIDAO_CAN_NO_ACK_INACTIVE_LIMIT` | `Can_HDX.c` | 连续无 ACK 后判定总线不活跃次数 | 现场无 CAN 设备时降低功耗 |
| `FEIDAO_CAN_MSG_*` | `Can_HDX.c` | 各 CAN 消息的调度 bit mask | 调度器通过 mask 组合待发帧 |
| `FEIDAO_CAN_RTC_PROBE_MSG_MASK` | `Can_HDX.c` | RTC 探测时发送的帧集合 | 当前只发电压电流和 SOC |
| `FEIDAO_CAN_TXMAILBOX_*`、`FEIDAO_CAN_ABORT_WAIT_LOOP` | `Can_HDX.c` | CAN mailbox 编号和 abort 等待循环 | 底层发送取消/清理使用 |

## 13. SOC 宏

| 宏/宏族 | 定义位置 | 功能 | 怎么用 |
|---|---|---|---|
| `SOC_TABLE_SIZE` | `SOC.h` | SOC 表长度，当前 42 | 与 SOC 电压/百分比成对表长度一致 |
| `SOC_VOL_MIN/MAX`、`SOC_VALUE_MIN/MAX` | `SOC.h` | SOC 表电压和百分比边界 | 上位机写表边界检查 |
| `SOC_Size_TableCanSet`、`SOC_Size_LiFePO`、`SOC_Size_TernaryLi`、`SOC_Size_LiFePO2` | `SocEnhance.h` | 可配置表和内置 SOC 表长度 | 修改表点数时同步数组和通信地址 |
| `SOC_DEFAULT_STARTUP_PERCENT` | `SocEnhance.h` | SOC 默认启动值，当前 60% | 无有效快照/OCV 时兜底 |
| `SOC_VIRTUAL_CURRENT_CHG/DSG` | `SocEnhance.c` | 虚拟充放电电流，单位 A*10 | `__VIRTURE_CURRENT__` 路径使用，避免小电流为 0 导致状态卡住 |
| `SOC_REST_BUCKET_*_SECONDS` | `SocEnhance.c` | 静置补偿时间分桶 | RTC 休眠后 OCV 补偿使用 |
| `SOC_TICKS_PER_SECOND` | `SocEnhance.c` | SOC 内部 tick 换算，当前 5 | 主循环调度周期变化时同步 |
| `SOC_WEAK_CELL_GUARD_WINDOW_MV`、`SOC_WEAK_CELL_CRITICAL_WINDOW_MV` | `SocEnhance.c` | 弱单体保护窗口 | 防止 SOC 显示高于弱单体实际状态 |
| `SOC_CYCLE_PERCENT_PER_COUNT` | `SocEnhance.c` | 循环次数累计阈值，当前 80% 放电算一次 | 影响循环次数增长 |
| `SOC_INTEGRATE_DIRECTION_*` | `SocEnhance.c` | SOC 安时积分方向枚举 | 内部状态机使用 |

## 14. LED 显示宏

| 宏/宏族 | 定义位置 | 功能 | 怎么用 |
|---|---|---|---|
| `LEDBAR_PIN_COUNT` | `LedBar.h` | Charlieplexing 使用的 GPIO 数量，当前 5 | 改硬件脚数时同步路线表 |
| `LEDBAR_DRIVER_GPIO_CHARLIE` | `LedBar.h` | 选择 GPIO Charlieplexing 驱动，当前默认 1 | 可通过编译参数或头文件覆盖；0 时使用 74HC595 路径 |
| `LEDBAR_SLEEP_ENABLE` | `LedBar.h` | 启用 LED 休眠 SOC 保存/恢复 | 关闭后休眠预览路径被裁剪 |
| `LEDBAR_LONG_PRESS_GPIO_TOGGLE_TEST` | `LedBar.h` | 长按 GPIO 翻转测试 | 仅调试用，量产保持 0 |
| `LEDBAR_GPIO_Px`、`LEDBAR_PIN_Px` | `LedBar.h` | Charlieplexing 引脚顺序映射 | 改硬件接线时只改这里和 `conf_gpio.h` |
| `LEDBAR_ICON_*_MASK` | `LedBar.h` | 充电图标、百分号图标 mask | `LedBar_SetIndicators()` 传入 mask |
| `LEDBAR_SINGLE_SEG_ID_MIN/MAX` | `LedBar.h` | 单段测试 ID 范围 | 产测逐段点亮使用 |
| `LEDBAR_SOC_DISPLAY_10MS` | `LedBar.h` | 普通 SOC 显示持续时间，10ms 单位，当前 500 即 5s | 主循环 10ms tick 下使用，短按显示窗口走这个值 |
| `LEDBAR_WAKEUP_DISPLAY_10MS` | `LedBar.h` | 休眠后 MCU 复位唤醒的 SOC 显示持续时间，当前 1000 即 10s | `SleepDeal_IsBootFromSleepStartup()` 置位后，`APP_LedBar()` 只触发一次唤醒显示窗口 |
| `LEDBAR_595_FRAME_PATTERN_COUNT`、`LEDBAR_FRAME_PATTERN_COUNT` | `LedBar.c` | 595/Charlieplexing 帧模式数量 | 由驱动模式自动选择 |
| `LEDBAR_SOC_DISPLAY_SNAP_*` | `LedBar.c` | SOC 显示贴近/吸附策略参数 | 当前 `LEDBAR_SOC_DISPLAY_SNAP_ENABLE` 被显式设为 0，不启用吸附 |
| `LEDBAR_SCAN_TIMER_100KHZ_TICKS` | `LedBar.c` | LED 扫描定时器周期，当前 50 个 100kHz tick | 调整扫描刷新频率时使用，需兼顾亮度和 CPU/中断负载 |
| `LEDBAR_MCU_WK_*_FILTER_10MS`、`LEDBAR_CHARGE_*_FILTER_100MS` | `LedBar.c` | MCU 唤醒输入和充电显示输入的消抖/滤波时间 | 用于抑制显示状态抖动；单位分别是 10ms 和 100ms tick |
| `LEDBAR_DIGIT_BIT_A`~`G` | `LedBar.c` | 七段码 bit mask | 数字字形表内部使用 |
| `LEDBAR_PATTERN_MASK_MAX`、`LEDBAR_SCAN_HOLD_TICKS` | `LedBar.c` | 扫描 mask 上限和保持 tick | 调整亮度/扫描节奏时使用 |
| `LEDBAR_595_GPIO_*`、`LEDBAR_595_PIN_*` | `LedBar.c` | 74HC595 数据、时钟、锁存引脚 | 非 Charlieplexing 模式使用 |
| `LEDBAR_KEY_LONG_PRESS_10MS` | `LedBar.c` | 长按判定时间，当前 300 即 3s | 与 `_DI_SWITCH_longKEY_ONOFF` 逻辑配合 |
| `LEDBAR_SLEEP_SOC_*` | `LedBar.c` | 休眠 SOC BKP 保存 magic、mask、寄存器 | 用 `BKP_DR4/BKP_DR5` 保存和校验休眠前 SOC |

## 15. RTC 与休眠宏

| 宏/宏族 | 定义位置 | 功能 | 怎么用 |
|---|---|---|---|
| `RTC_BKP_DATA` | `RTC.h` | RTC 初始化备份标志 | 判断备份域是否已初始化 |
| `LSE_START_TIMEOUT`、`LSE_FREQUENT` | `RTC.h` | LSE 启动超时和频率 | RTC 时钟初始化使用 |
| `TIME_ZOOM` | `RTC.h` | 时区偏移，当前 8 小时 | Unix 时间换算使用 |
| `UNIX_TIME_YEAR`、`SEC_DAY` | `RTC.h` | Unix 起始年份和一天秒数 | 时间转换使用 |
| `ALARM_TIME_SEC` | `RTC.h` | RTC 闹钟间隔默认值，当前 10s | 唤醒周期配置使用 |
| `Leapyear(year)`、`Days_in_year(a)`、`Days_in_month(a)` | `RTC.h`、`RTC.c` | 日期计算宏 | 仅支持当前项目年份范围；`Days_in_month` 依赖 `month_days` 数组 |
| `RTC_element_min/default/max` | `RTC.h` | RTC 参数最小/默认/最大表 | 参数初始化和上位机写入边界检查 |
| `RTC_CLOCK_*` | `RTC.c` | RTC 时钟状态码：正常、使用 LSI、需重初始化、初始化失败 | RTC 初始化内部状态返回 |
| `RTC_WAIT_TIMEOUT` | `RTC.c` | RTC 寄存器等待超时 | 防止 RTC 同步/写入等待死循环 |
| `RTC_WT_Protect/Normal/Force` | `SleepDeal.h` | 保护、正常、强制休眠 RTC 唤醒时间 | 休眠策略使用 |
| `SleepInitOC/CBC/Normal1/Normal2` | `SleepDeal.h` | 休眠进入前的延时判定计数 | 以 200ms 周期为基础 |
| `Sleep_min/default/max` | `SleepDeal.h` | 休眠参数最小/默认/最大表 | 参数初始化和通信写入边界 |
| `DI1_LONG_PRESS_WAKE_10MS` | `SleepDeal.c` | DI1 长按唤醒判定时间，当前 300 即 3s | 与长按开关机逻辑配套，单位是 10ms tick |
| `SLEEP_BKP_FLAG_REG`、`SLEEP_BKP_INV_REG` | `SleepDeal.c` | 启动/休眠标志及反码备份寄存器 | 写入时同时写反码，读取时校验异或为 `0xFFFF` |
| `VcellMin`、`VcellMax`、`SOC_DISP`、`sOC_REAL` | `rtc_sleep.h` | 全局数据字段别名 | 低功耗文件内部快速访问；不建议扩散到其他模块 |
| `SOC_ERROR` | `rtc_sleep.c` | 休眠前后 SOC OCV 校验误差阈值 | 当前位于 `#if 0` 旧逻辑中，仅作历史参考 |

## 16. IO、MOS、继电器与热管理宏

| 宏/宏族 | 定义位置 | 功能 | 怎么用 |
|---|---|---|---|
| `_RELAY_*`、`_MOS_*` | `IODrivers.c` | 编译不同继电器/MOS 控制模型 | 当前文件内多个模型都定义并编译；实际入口选择需要看调用路径，后续建议收敛为单一配置 |
| `PreRelayCLOSE_MODET`、`PreDsgMOSCLOSE_MODET` | `IODrivers.c` | 预充继电器/MOS 关闭延时 | 单位按 10ms 计数，防止主路和预充同时打开 |
| `CHG_MOS_OPEN_CUR`、`DSG_MOS_OPEN_CUR` | `IODrivers.c` | MOS 同口充放电开启电流阈值 | 以 A*10 表示，当前 20 即 2A |
| `PreRelayCloseT`、`PreDsgMOSCloseT` | `IO_Control.h` | 新控制框架预充关闭时间 | 与 `IODrivers.c` 中旧宏语义接近，维护时注意不要双轨不一致 |
| `_MOS_SAME_DOOR_NO_PRECHG` | `IO_Control.h` | 当前 IO 控制模型选择 | 该头文件内只启用此模型；自动派生 `_MOS` |
| `_MOS`、`_RELAY` | `IO_Control.h` | 根据模型选择自动生成的驱动类型宏 | 不手工定义，依赖上方模型宏 |
| `HeatCoolElement_Min/Default/Max` | `Heat_Cool.h` | 热管理参数最小/默认/最大表 | 需与 `struct HEAT_COOL_ELEMENT` 字段顺序一致 |
| `HEAT_CLOSE_CUR` | `Heat_Cool.c` | 加热关闭电流阈值 | 当前为 0，避免加热膜电流波动导致频繁开关 |

## 17. 日志、生产信息与测试宏

| 宏/宏族 | 定义位置 | 功能 | 怎么用 |
|---|---|---|---|
| `EVENT_RECORD_LENGTH` | `LogRecord.c` | 事件日志记录条数，当前 100 | 与 Flash 日志槽容量 `FLASH_STORAGE_LOG_RECORD_COUNT` 保持一致；上位机写 `Sci_WrReg_0x06_Reset_EventRecord` 的 `0x0001` 清空、`0x0000` 保留 |
| `PRODUCT_ID_LENGTH_MAX` | `ProductionID.h` | 产品 ID 最大长度，当前 32 | 读写序列号/硬件版本/软件版本时使用 |
| `FLASH64K_SIZE_REG_ADDR` | `Flash64KAppTest.c` | STM32 Flash size 寄存器地址 | 测试代码读取芯片 Flash 容量 |
| `FLASH64K_TEST_*_SLOT_*` | `Flash64KAppTest.c` | 后 64KB 测试使用的 SOC/AFE 双槽地址 | 直接复用 `Flash.h` 存储槽地址 |
| `FLASH64K_APP_QUICK_TEST_CYCLES` | `conf/conf.h`、`Flash64KAppTest.c` | 快速测试循环次数，默认 96 | 未在 `conf.h` 定义时使用 `.c` 内默认值 |
| `FLASH64K_APP_USE_TEST_PRINT_PERIOD_SEC` | `conf/conf.h`、`Flash64KAppTest.c` | 运行时测试打印周期 | 仅 `FLASH64K_APP_USE_TEST_ENABLE` 打开时有效 |
| `FLASH64K_APP_USE_TEST_ACCEL_*` | `conf/conf.h`、`Flash64KAppTest.c` | 运行时测试加速和 SOC/AFE 周期 | 测试 Flash 写入稳定性时使用 |
| `FLASH64K_APP_QUICK_TEST_ENABLE`、`FLASH64K_APP_USE_TEST_ENABLE` | `conf/conf.h` | 测试入口开关 | 当前关闭；打开会改变运行行为 |

## 18. EasyLogger 宏

| 宏/宏族 | 定义位置 | 功能 | 怎么用 |
|---|---|---|---|
| `ELOG_OUTPUT_LVL` | `easylogger/inc/elog_cfg.h` | 全局日志输出等级，当前 `ELOG_LVL_WARN` | 低于该等级的日志编译为空或不输出 |
| `ELOG_ASSERT_ENABLE` | `easylogger/inc/elog_cfg.h` | 启用 EasyLogger assert | 断言失败会调用日志 assert 处理 |
| `ELOG_LINE_BUF_SIZE` | `easylogger/inc/elog_cfg.h` | 单行日志缓冲长度，当前 100 | 打印长日志时可能截断；增大会占 RAM |
| `ELOG_LINE_NUM_MAX_LEN`、`ELOG_FILTER_TAG_MAX_LEN`、`ELOG_FILTER_KW_MAX_LEN`、`ELOG_FILTER_TAG_LVL_MAX_NUM` | `easylogger/inc/elog_cfg.h` | 行号、tag、关键字、tag 等级过滤长度 | 按 RAM 和过滤需求调整 |
| `ELOG_NEWLINE_SIGN` | `easylogger/inc/elog_cfg.h` | 换行符，当前 `"\r\n"` | 串口日志保持 CRLF |
| `ELOG_COLOR_ENABLE`、`ELOG_COLOR_*` | `easylogger/inc/elog_cfg.h` | ANSI 彩色日志 | 串口终端不支持颜色时可关闭 |
| `ELOG_LVL_*`、`ELOG_FILTER_LVL_*`、`ELOG_LVL_TOTAL_NUM` | `easylogger/inc/elog.h` | 日志等级枚举宏 | 应用代码使用 `log_e/log_w/log_i/...`，不要直接比较魔法值 |
| `elog_*`、`log_*` | `easylogger/inc/elog.h` | 带 tag 或默认 `LOG_TAG` 的日志输出宏 | 每个 `.c` 可定义 `LOG_TAG`、`LOG_LVL` 覆盖默认 tag/等级 |
| `ELOG_FMT_ALL` | `easylogger/inc/elog.h` | 日志格式字段全集 | 初始化日志格式时使用 |
| `ELOG_ASYNC_*`、`ELOG_BUF_*` | `easylogger/inc/elog_cfg.h`、`src` | 异步/缓冲输出配置 | 当前未看到 `ELOG_ASYNC_OUTPUT_ENABLE` 或 `ELOG_BUF_OUTPUT_ENABLE` 主开关打开，相关默认宏多为库内部备用 |
| `ELOG_FLASH_*` | `plugins/flash` | Flash 日志插件配置 | `ELOG_FLASH_BUF_SIZE` 当前为空占位，启用插件前必须给值 |
| `ELOG_FILE_*` | `plugins/file` | 文件日志插件配置 | 嵌入式当前通常不用；启用前必须定义文件名、大小、轮转数量 |
| `likely(x)`、`unlikely(x)`、`SUFFIX_LEN`、`OUTPUT_LVL`、`OUTPUT_BUF_SIZE` | EasyLogger 源码 | 分支预测、文件后缀长度、异步输出内部默认值 | 属于库内部实现宏，业务代码不直接调用 |
| `ELOG_FLASH_SW_VERSION`、`ELOG_FILE_SW_VERSION` | EasyLogger 插件头文件 | 插件版本字符串 | 仅用于日志插件版本识别 |
| `LOG_TAG` | 多个 `.c` | 模块日志 tag | 定义在包含 `elog.h` 前生效 |

## 19. STM32 标准外设库宏族

| 宏族 | 位置 | 功能 | 使用边界 |
|---|---|---|---|
| `STM32F10X_*` | `drivers/stm32f10x.h`、Keil Define | 设备密度/型号选择 | 当前工程为 `STM32F10X_MD`；不要同时定义多个密度 |
| `USE_STDPERIPH_DRIVER` | Keil Define、`stm32f10x.h` | 标准外设库入口开关 | 当前已在工程定义；不要在多个头文件重复维护 |
| `HSE_VALUE`、`HSI_VALUE`、`SYSCLK_FREQ_*` | `stm32f10x.h`、`system_stm32f10x.c` | 系统时钟源和目标频率 | 改晶振/PLL 时必须系统性验证 RCC、Flash latency、CAN、USART、ADC |
| `assert_param`、`IS_*` | 各 `inc/stm32f10x_*.h` | 参数合法性检查宏 | Debug 可打开断言定位参数错误；Release 通常为空 |
| `RCC_*`、`GPIO_*`、`USART_*`、`CAN_*`、`ADC_*`、`TIM_*` 等 | 标准库头文件 | 外设寄存器位、参数枚举、flag/IT 定义 | 业务代码调用标准库 API 时使用，不在项目文档逐项复制 |
| `__IO`、`__I`、`__O`、`FlagStatus` 等 | CMSIS/标准库 | 寄存器访问限定和类型定义 | 底层寄存器结构使用 |
| `VECT_TAB_RAM` | `Can_HDX.c` 条件引用、标准库 | 向量表位置选择 | 当前未定义，默认 Flash 向量表 |

## 20. 头文件防重复包含宏

以下宏只用于 include guard，不承载业务含义：`MAIN_H`、`CONF_H`、`CONF_GPIO_H`、`ADC_H`、`CAN_HDX_H`、`CHARGERLOADFUNC_H`、`DATADEAL_SYS_H`、`EEPROM_H`、`FAULT_H`、`PROJECT_STORAGE_FLASH_H`、`FLASH64K_APP_TEST_H`、`HEAT_COOL_H`、`I2C_AFE1_H`、`IODRIVERS_H`、`IO_CONTROL_H`、`LEDBAR_H`、`LOG_RECORD_H`、`PRODUCTIONID_H`、`PUBFUNC_H`、`RTC_H`、`_SH367309_DATADEAL_H`、`SH367309_FUNC_H`、`SOC_H`、`SOCENHANCE_H`、`SCI_H`、`SHORTFUNC_H`、`SLEEPDEAL_H`、`SYSTEM_INIT_H`、`SYSTEM_MONITOR_H`、`UPGRADE_PARAM_POLICY_H`、`__RTC_SLEEP__`、以及 EasyLogger 的 `__ELOG_H__`、`_ELOG_CFG_H_` 等。

使用方式：不要在业务代码中判断这些 guard 宏；它们只由头文件自身维护。

## 21. 按文件宏数量索引

| 文件 | 宏数量 | 主要宏类型 |
|---|---:|---|
| `Fault.h` | 100 | 保护阈值、保护参数默认表、故障记录长度 |
| `SH367309_Func.h` | 81 | AFE 初始化阈值、寄存器 bit/byte 拼装 |
| `I2C_AFE1.h` | 63 | AFE 寄存器地址、软件 I2C IO 操作 |
| `conf/conf_gpio.h` | 59 | 板级 GPIO/PIN 映射 |
| `EEPROM.h` | 56 | EEPROM 参数地址、参数数量、旧 I2C EEPROM IO |
| `SH367309_DataDeal.h` | 41 | AFE 参数通信地址、默认参数表 |
| `Sci_Upper.h` | 39 个 `#define` + 大量地址枚举 | RS485 状态、错误码、地址段 |
| `System_Init.h` | 38 | 位带、GPIO 快速访问、系统 IO 别名 |
| `conf/conf.h` | 36 | 产品配置、功能开关、测试开关 |
| `Flash.h` | 36 | Flash 地址、槽大小、存储块长度、启动标志 |
| `DataDeal.h` | 32 | 校准边界、产品默认参数、铜损/容量配置 |
| `LedBar.c` / `LedBar.h` | 54 | LED 驱动模式、显示 bit、输入滤波、BKP 保存、扫描参数 |
| `main.h` | 26 | 通用工具、延时常量、IAP/SCI 开关 |
| `Can_HDX.h` / `Can_HDX.c` | 46 | CAN ID、filter、低功耗 CAN 调度 |
| `IODrivers.c` / `IO_Control.h` | 23 | MOS/继电器模型、预充时序 |
| `rtc_sleep.h` / `rtc_sleep.c` | 16 | 寄存器工具、低功耗字段别名 |
| `SocEnhance.c` / `SocEnhance.h` | 19 | SOC 表长度、补偿、积分方向 |
| `RTC.h` / `RTC.c` | 19 | RTC 时间换算、默认表、时钟状态 |
| `SleepDeal.h` / `SleepDeal.c` | 14 | 休眠等待、默认表、BKP 启动标志 |
| `ADC.h` | 11 | ADC 平均、校准、温度表、分压比例 |
| `UpgradeParamPolicy.h` | 11 | 升级参数重置策略 |
| `Flash64KAppTest.c` | 9 | 后 64KB 存储测试 |
| 其他头/源文件 | 少量 | guard、长度、局部调试常量 |
| EasyLogger | 约 135 | 日志等级、输出宏、插件配置 |
| STM32 标准外设库 | 约 7721 | 外设寄存器、参数检查、设备配置 |

## 22. 维护建议

1. 产品级开关只放在 `conf/conf.h`、`main.h` 或 Keil Define 中维护，业务 `.c` 文件不要再新增同类全局开关。
2. 板级 IO 只改 `conf/conf_gpio.h` 和必要的初始化代码，业务模块使用 `GPIO_xxx/PIN_xxx` 或 `MCUO_/MCUI_` 别名。
3. 参数表宏必须和结构体字段顺序、EEPROM/Flash word count、RS485 地址表同步修改。
4. `#if 0`、注释掉的候选宏只能作为历史参考；真正启用前必须做编译验证、参数初始化验证、上位机读写验证和一次低功耗唤醒验证。
5. 当前 `IODrivers.c` 同时定义多个 `_RELAY_*` / `_MOS_*` 模型，长期建议收敛成单一配置入口，避免多个实现同时编译带来维护风险。
