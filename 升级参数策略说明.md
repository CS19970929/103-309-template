# 升级参数策略说明

## 目的

给客户发布一个带新参数的升级包时，可以让设备在升级后首次启动时，按模块独立决定是否把现场参数重置为本固件内置的新默认值。执行完成后会写入版本标记，避免每次开机重复重置。

## 配置入口

策略开关在 `103 + 309/Project/Source/UpgradeParamPolicy.h`：

| 宏 | 作用 |
| --- | --- |
| `UPGRADE_PARAM_POLICY_ENABLE` | 总开关，`1` 启用升级参数策略，`0` 完全关闭。 |
| `UPGRADE_PARAM_POLICY_VERSION` | 策略版本号。每次需要再次执行参数重置时必须递增，不能使用 `0xFFFF`。 |
| `UPGRADE_PARAM_RESET_AFE` | 是否把 SH367309 AFE 保护参数重置为 `AFE_PARAMETERS_RS485_STRUCTION_DEFAULT`。 |
| `UPGRADE_PARAM_RESET_PROTECT` | 是否把系统保护参数重置为 `E2P_PROTECT_DEFAULT_PRT`。 |
| `UPGRADE_PARAM_RESET_SOC_TABLE` | 是否把 SOC 曲线表重置为 `SOC_Table_Default`。 |
| `UPGRADE_PARAM_RESET_SOC_CONFIG` | 是否只重置 `OtherElement_default` 中的 SOC 基础参数：表选择、容量、循环次数、100% 电压、0% 电压。 |
| `UPGRADE_PARAM_FORCE_REAPPLY` | 测试用强制重复执行开关，客户发布包必须保持 `0`。 |

执行标记存放在内部 Flash `0x0801F000` 页，和 `0x0801F800` 的 IAP 跳转标志、`0x0801FC00` 的休眠标志分开。

## 客户升级包制作流程

1. 修改本次客户需要的新默认参数：
   - AFE 保护参数：`103 + 309/Project/Source/SH367309_DataDeal.h` 中的 `AFE_PARAMETERS_RS485_STRUCTION_DEFAULT`
   - 系统保护参数：`103 + 309/Project/Source/Fault.h` 中的 `E2P_PROTECT_DEFAULT_PRT`
   - SOC 曲线：`103 + 309/Project/Source/SOC.c` 中的 `SOC_Table_Default`
   - SOC 基础参数：`103 + 309/Project/Source/DataDeal.h` 中的 `OtherElement_default`
2. 在 `UpgradeParamPolicy.h` 中只打开需要覆盖现场参数的模块开关。
3. 将 `UPGRADE_PARAM_POLICY_VERSION` 递增一个新值。
4. 编译生成升级固件，按现有 IAP 流程发给客户。

## 执行时序

`InitE2PROM()` 先装载现有参数，再调用 `UpgradeParamPolicy_ApplyOnce()`。如果打开 AFE 重置开关，固件会先把 AFE 参数写入内部 Flash；随后 `InitAFE1()` 会调用 `SH367309_UpdataAfeConfig()`，检测到差异后更新 SH367309 MTP。

SOC 相关开关在 `InitData_SOC()` 之前执行，因此 SOC 模块初始化时会直接使用重置后的 SOC 表和基础参数。

## 注意事项

- 某个模块开关保持 `0` 时，该模块参数不因升级策略被覆盖。
- 如果客户包需要再次执行同一类重置，必须修改 `UPGRADE_PARAM_POLICY_VERSION`。
- `UPGRADE_PARAM_FORCE_REAPPLY` 只用于台架验证，发布给客户前保持 `0`，否则每次启动都会重新覆盖参数。
- IAP 擦写策略应保留 `0x0801C000 ~ 0x0801FFFF` 的内部 Flash 存储页；如果升级过程擦除了该区域，升级策略会因为版本标记丢失而再次执行。
- 当前分支的普通保护参数、SOC 表、SOC 基础参数由固件默认值初始化；AFE 参数会通过内部 Flash 持久保存。
