# 升级参数策略说明

## 目的

给客户发布一个带新参数的升级包时，可以让设备在升级后首次启动时，按模块独立决定是否把现场参数重置为本固件内置的新默认值。执行完成后会写入版本标记，避免每次开机重复重置。

## 配置入口

策略开关在 `103 + 309/Project/Source/UpgradeParamPolicy.h`：

| 宏 | 作用 |
| --- | --- |
| `UPGRADE_PARAM_POLICY_ENABLE` | 总开关，`1` 启用升级参数策略，`0` 完全关闭。 |
| `UPGRADE_PARAM_POLICY_VERSION` | 策略版本号。每次需要再次执行参数重置时必须递增，不能使用 `0xFFFF`。 |
| `UPGRADE_PARAM_RESET_AFE` | 是否把 SH367309 AFE 保护参数重置为 `AFE_PARAMETERS_RS485_STRUCTION_DEFAULT`。 |
| `UPGRADE_PARAM_RESET_PROTECT` | 是否把系统保护参数重置为 `E2P_PROTECT_DEFAULT_PRT`。 |
| `UPGRADE_PARAM_RESET_SOC_TABLE` | 是否把 SOC 曲线表重置为 `SOC_Table_Default`。 |
| `UPGRADE_PARAM_RESET_SOC_CONFIG` | 是否只重置 `OtherElement_default` 中的 SOC 基础参数：表选择、容量、循环次数、100% 电压、0% 电压。 |
| `UPGRADE_PARAM_RESET_SOC_SNAPSHOT` | 是否把历史 SOC 快照重写为默认启动快照：SOC=60%、放电累计=0、循环次数=当前 SOC 配置循环次数。 |
| `UPGRADE_PARAM_FORCE_REAPPLY` | 测试用强制重复执行开关。`0` 表示同一版本只执行一次，`1` 表示每次启动都执行，客户发布包必须保持 `0`。 |

执行标记存放在内部 Flash `0x0801F000` 页，和 `0x0801F800` 的 IAP 跳转标志、`0x0801FC00` 的休眠标志分开。

## 整体执行逻辑

`UpgradeParamPolicy_ApplyOnce()` 的判断顺序如下：

1. 如果 `UPGRADE_PARAM_POLICY_ENABLE` 为 `0`，直接退出，不处理任何参数。
2. 如果所有模块重置开关都是 `0`，直接退出，不处理任何参数。
3. 如果 `UPGRADE_PARAM_FORCE_REAPPLY` 为 `0`，会读取 `0x0801F000` 中保存的已执行版本号：
   - 已执行版本号等于当前 `UPGRADE_PARAM_POLICY_VERSION` 时，说明这版策略已经执行过，直接跳过。
   - 已执行版本号不等于当前 `UPGRADE_PARAM_POLICY_VERSION` 时，继续执行已打开的模块重置。
4. 如果 `UPGRADE_PARAM_FORCE_REAPPLY` 为 `1`，跳过版本号判断，每次启动都执行已打开的模块重置。
5. AFE、系统保护参数、SOC 曲线表、SOC 基础参数、历史 SOC 快照按各自开关独立执行。
6. 如果 SOC 基础参数需要保存，会先写入 RW 参数区；随后才会按需重写 SOC journal。
7. 全部执行成功后，把当前 `UPGRADE_PARAM_POLICY_VERSION` 写入 `0x0801F000`。

## `UPGRADE_PARAM_FORCE_REAPPLY` 用法

正常客户发布包必须配置为：

```c
#define UPGRADE_PARAM_FORCE_REAPPLY        0
```

这种模式下，同一个 `UPGRADE_PARAM_POLICY_VERSION` 只会执行一次。客户升级后首次启动会覆盖指定模块参数，后续重启不会再次覆盖，现场再次通过上位机修改的参数可以保留。

`UPGRADE_PARAM_FORCE_REAPPLY` 配置为 `1` 只适合台架调试：

```c
#define UPGRADE_PARAM_FORCE_REAPPLY        1
```

这种模式会忽略 `0x0801F000` 中的已执行版本号，每次启动都重新执行已打开的模块重置。如果客户现场改了参数，下次重启又会被固件覆盖回默认值，所以不能用于正式客户包。

示例：只希望客户升级后首次重置 AFE 参数和 SOC 曲线表，可以这样配置：

```c
#define UPGRADE_PARAM_POLICY_ENABLE        1
#define UPGRADE_PARAM_POLICY_VERSION       0x0002

#define UPGRADE_PARAM_RESET_AFE            1
#define UPGRADE_PARAM_RESET_PROTECT        0
#define UPGRADE_PARAM_RESET_SOC_TABLE      1
#define UPGRADE_PARAM_RESET_SOC_CONFIG     0
#define UPGRADE_PARAM_RESET_SOC_SNAPSHOT   0

#define UPGRADE_PARAM_FORCE_REAPPLY        0
```

这样设备升级后第一次启动会执行 AFE 参数和 SOC 曲线表重置；执行成功后写入版本号 `0x0002`，之后再重启不会重复执行。

## 客户升级包制作流程

1. 修改本次客户需要的新默认参数：
   - AFE 保护参数：`103 + 309/Project/Source/SH367309_DataDeal.h` 中的 `AFE_PARAMETERS_RS485_STRUCTION_DEFAULT`
   - 系统保护参数：`103 + 309/Project/Source/Fault.h` 中的 `E2P_PROTECT_DEFAULT_PRT`
   - SOC 曲线：`103 + 309/Project/Source/SOC.c` 中的 `SOC_Table_Default`
   - SOC 基础参数：`103 + 309/Project/Source/DataDeal.h` 中的 `OtherElement_default`
2. 在 `UpgradeParamPolicy.h` 中只打开需要覆盖现场参数的模块开关；如果升级后要让历史 SOC 回到默认 60%，打开 `UPGRADE_PARAM_RESET_SOC_SNAPSHOT`。
3. 将 `UPGRADE_PARAM_POLICY_VERSION` 递增一个新值。
4. 编译生成升级固件，按现有 IAP 流程发给客户。

## 执行时序

`InitE2PROM()` 先装载现有参数，再调用 `UpgradeParamPolicy_ApplyOnce()`。如果打开 AFE 重置开关，固件会先把 AFE 参数写入内部 Flash；随后 `InitAFE1()` 会调用 `SH367309_UpdataAfeConfig()`，检测到差异后更新 SH367309 MTP。

SOC 相关开关在 `InitData_SOC()` 之前执行，因此 SOC 模块初始化时会直接使用重置后的 SOC 表、基础参数和历史快照。`UPGRADE_PARAM_RESET_SOC_SNAPSHOT` 会重写 `0x0801E000/0x0801E800` 的 SOC journal；如果同时打开 `UPGRADE_PARAM_RESET_SOC_CONFIG`，快照中的循环次数会使用重置后的 `OtherElement.u16Soc_Cycle_times`。

## 注意事项

- 某个模块开关保持 `0` 时，该模块参数不因升级策略被覆盖。
- `UPGRADE_PARAM_RESET_SOC_CONFIG` 和 `UPGRADE_PARAM_RESET_SOC_SNAPSHOT` 相互独立：只改基础参数时保留现场 SOC；需要升级后首次 SOC 变为默认 60% 时，必须额外打开快照开关。
- 如果客户包需要再次执行同一类重置，必须修改 `UPGRADE_PARAM_POLICY_VERSION`。
- `UPGRADE_PARAM_FORCE_REAPPLY` 只用于台架验证，发布给客户前保持 `0`，否则每次启动都会重新覆盖参数。
- IAP 擦写策略应保留 `0x0801C000 ~ 0x0801FFFF` 的内部 Flash 存储页；如果升级过程擦除了该区域，升级策略会因为版本标记丢失而再次执行。
- 当前分支的普通保护参数、SOC 表、SOC 基础参数由固件默认值初始化；AFE 参数会通过内部 Flash 持久保存。
