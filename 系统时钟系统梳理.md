# 系统时钟系统梳理

本文档基于当前主工程 `103 + 309/Project` 梳理 MCU 与外设时钟。未把 `.worktrees/zero-bms-rebuild` 和 `C030v1.0` 作为当前系统依据。

## 结论摘要

当前固件实际不是 72 MHz 运行。`system_stm32f10x.c` 里启用的是 `SYSCLK_FREQ_HSE`，所以系统时钟直接来自外部 HSE，频率按 `HSE_VALUE = 8 MHz` 计算。PLL 的 24/36/48/56/72 MHz 配置都没有启用。

当前正常运行时钟树为：

| 节点 | 当前来源/分频 | 实际频率 |
| --- | --- | --- |
| HSE | 外部高速晶振，`HSE_VALUE` | 8 MHz |
| SYSCLK | HSE 直连，非 PLL | 8 MHz |
| HCLK | SYSCLK / 1 | 8 MHz |
| PCLK1 | HCLK / 1 | 8 MHz |
| PCLK2 | HCLK / 1 | 8 MHz |
| TIM2/TIM3 内核时钟 | APB1 未分频，等于 PCLK1 | 8 MHz |
| SysTick 延时源 | HCLK / 8 | 1 MHz |
| ADC 时钟 | PCLK2 / 8 | 1 MHz |
| RTC | 优先 LSE，失败后 LSI | 1 Hz 计数目标 |
| IWDG | LSI | 约 40 kHz 标称 |

目前最大的风险不是 8 MHz 本身，而是代码和注释里仍残留多处“72 MHz/PLL”假设。TIM3 的 1 ms 系统节拍当前刚好按 8 MHz 改成了可用配置，但 ADC 触发、低功耗恢复、HSE 失败处理、BKP/PWR 时钟等处还有明显隐患。

## 依据文件

| 内容 | 位置 |
| --- | --- |
| 启动文件调用 `SystemInit()` | `STM32F10x_StdPeriph_Lib_V3.5.0/drivers/startup_stm32f10x_hd.s` |
| MCU 系统时钟树 | `STM32F10x_StdPeriph_Lib_V3.5.0/drivers/system_stm32f10x.c` |
| 主初始化流程 | `Source/main.c` |
| 延时、TIM3、IWDG | `Source/System_Init.c` |
| GPIO/低功耗 IO/STOP | `Source/conf/conf.c` |
| RTC/LSE/LSI/闹钟 | `Source/RTC.c` |
| RTC 打嗝休眠流程 | `Source/rtc_sleep.c` |
| 旧睡眠标志重启路径 | `Source/SleepDeal.c` |
| ADC/DMA/TIM2 触发 | `Source/ADC.c` |
| CAN1 | `Source/Can_HDX.c` |
| USART1/2/3 | `Source/Sci_Upper.c` |
| AFE 软件 I2C | `Source/I2C_AFE1.c/.h` |
| EEPROM 软件 I2C | `Source/EEPROM.c/.h` |
| Keil 目标配置 | `Users/CommomSH367309_16series_103RCT6_C.uvprojx` |

## MCU 主时钟

### 启动顺序

1. 复位后硬件默认使用 HSI 8 MHz。
2. 启动汇编 `startup_stm32f10x_hd.s` 在进入 C 库 `__main` 之前调用 `SystemInit()`。
3. `main()` 进入 `InitDevice()` 后又手动调用一次 `SystemInit()`。
4. 第二次调用发生在外设初始化之前，因此正常上电阶段不会破坏已经配置的外设，但它说明当前工程把 `SystemInit()` 当成可重复调用的频率配置入口。

### `SystemInit()` 当前配置

`system_stm32f10x.c` 当前有效宏为：

```c
#define SYSCLK_FREQ_HSE HSE_VALUE
```

对应执行 `SetSysClockToHSE()`：

1. 开 HSE。
2. 等待 `HSERDY`，带 `HSE_STARTUP_TIMEOUT` 超时。
3. Flash 开预取，0 wait state。
4. `HCLK = SYSCLK`。
5. `PCLK2 = HCLK`。
6. `PCLK1 = HCLK`。
7. 选择 HSE 作为 SYSCLK。

因此当前不是注释里说的“默认倍频到 72 MHz”，而是 HSE 8 MHz 直连。

### PLL 当前状态

正常上电路径没有配置 PLL，也没有选择 PLL 作为 SYSCLK。`SYSCLK_FREQ_72MHz` 被注释，`SetSysClockTo72()` 不会编译进有效路径。

`rtc_sleep.c` 里的 `cpu_frequency_conf()` 会：

1. 再次调用 `SystemInit()`。
2. 打开 HSE。
3. 直接使能 PLL。
4. 选择 PLL 作为 SYSCLK。

但因为当前 `SystemInit()` 并没有配置 PLL 倍频，调用后 PLL 仍是复位默认配置：HSI/2 * 2，频率仍约 8 MHz。也就是说这个函数名像是在恢复外部 PLL 主频，但实际更可能切到 HSI 派生的 8 MHz PLL。频率相同，时钟源不同。

## 外设时钟梳理

### GPIO 与 AFIO

`InitIO()` 打开：

| 总线 | 外设 |
| --- | --- |
| APB2 | AFIO |
| APB2 | GPIOA |
| APB2 | GPIOB |
| APB2 | GPIOC |
| APB2 | GPIOD |
| APB2 | GPIOE |

低功耗前的 `IOstatus_Base()`/`IOstatus_RTCMode()` 也会重新打开 GPIOA-E，然后把大部分 IO 配成模拟输入以降功耗。注意：这些函数只是配置 IO 状态，没有统一关闭外设时钟。

GPIO 时钟频率来自 PCLK2，即当前 8 MHz。GPIO 输出速度多处配置为 2 MHz 或 10 MHz，这是 IO 驱动速度档位，不是 RCC 时钟频率。

### SysTick 延时

`InitDelay()` 选择 SysTick 外部时钟源，即 HCLK/8。当前 HCLK 为 8 MHz，所以 SysTick 计数频率为 1 MHz：

```c
fac_us = SystemCoreClock / 8000000;
fac_ms = fac_us * 1000;
```

当前 `SystemCoreClock = 8000000` 时：

| 参数 | 值 |
| --- | --- |
| `fac_us` | 1 |
| `fac_ms` | 1000 |
| `__delay_us(1)` | 约 1 us |
| `__delay_ms(1)` | 约 1 ms |

如果以后真的切到 72 MHz，`fac_us = 9`，延时逻辑也能按比例工作，但必须保证 `SystemCoreClock` 被同步更新。

### TIM3 系统节拍

`InitTimer()` 打开 APB1 的 TIM3。当前 APB1 未分频，所以 TIM3 时钟为 8 MHz。

当前 HSE 正常分支：

```c
TIM_Prescaler = 80 - 1;
TIM_Period = 99;
```

计算：

```text
8 MHz / 80 / (99 + 1) = 1000 Hz
```

所以 TIM3 中断周期约 1 ms。`TIM3_IRQHandler()` 再派生 2 ms、10 ms、200 ms、1000 ms 等软件节拍。

问题点：`System_ErrFlag.u8ErrFlag_HSE != 0` 分支使用 `TIM_Prescaler = 8 - 1`。在 HSI 8 MHz 下会得到：

```text
8 MHz / 8 / 100 = 10 kHz
```

这会让系统节拍快 10 倍。当前看不到 `SystemInit()` 在 HSE 失败时设置 `u8ErrFlag_HSE`，所以这个分支平时可能不会走，但一旦走就是明显错误。

### TIM2 + ADC 触发

ADC 使用 TIM2 CH2 外部触发，TIM2 打开 APB1 时钟。当前 TIM2 时钟为 8 MHz。

`InitADC_TIMER()` 当前 HSE 正常分支：

```c
TIM_Prescaler = 72 - 1;
TIM_Period = 999;
```

按当前 8 MHz 计算：

```text
8 MHz / 72 / 1000 = 111.11 Hz
```

也就是 ADC 触发周期约 9 ms，并不是 1 kHz。这个配置明显来自 72 MHz 假设：若 TIM 时钟为 72 MHz，才是 1 kHz。

### ADC1 与 DMA1

`InitADC_ADC1()` 打开 APB2 的 ADC1，`InitADC_DMA()` 打开 AHB 的 DMA1。

当前配置：

| 项 | 配置 |
| --- | --- |
| ADC 模式 | 独立模式 |
| 扫描 | ENABLE |
| 连续转换 | DISABLE |
| 外部触发 | TIM2_CC2 |
| DMA | DMA1_Channel1 循环模式 |
| ADC 时钟 | `RCC_PCLK2_Div8` |
| 实际 ADC 时钟 | 8 MHz / 8 = 1 MHz |

代码注释写“9 MHz”，这是 72 MHz PCLK2 / 8 的结果，和当前真实时钟不符。1 MHz 不超规格，但转换速度和滤波更新时间会明显低于 72 MHz 方案。

ADC 通道：

| 通道 | 引脚 | 用途 |
| --- | --- | --- |
| ADC_Channel_9 | PB1 | `GPIO_ADC_NMOS` |
| ADC_Channel_2 | PA2 | `GPIO_ADC_CUR` |
| ADC_Channel_1 | PA1 | `GPIO_ADC_VBUS` |

### USART1/USART2/USART3

`InitUSART_CommonUpper()` 根据宏只初始化 SCI1 和 SCI2：

```c
#define _COMMOM_UPPER_SCI1
#define _COMMOM_UPPER_SCI2
// #define _COMMOM_UPPER_SCI3
```

| 串口 | 时钟总线 | 当前外设时钟 | 引脚 | 当前是否启用 | 波特率 |
| --- | --- | --- | --- | --- | --- |
| USART1 | APB2 | 8 MHz | PB6/PB7，USART1 remap | 是 | 19200 |
| USART2 | APB1 | 8 MHz | PA2/PA3 | 是 | 19200 |
| USART3 | APB1 | 8 MHz | PD8/PD9，full remap | 否，宏未打开 | 19200 |

标准库会按实际 RCC 时钟计算 BRR，所以当前 8 MHz 下 19200 波特率本身可以成立。风险在于低功耗唤醒后如果只唤醒 RTC 周期并未完整恢复串口，通信外设不应该被假定处于正常工作状态。

### CAN1

CAN1 打开 APB1 时钟，当前 PCLK1 为 8 MHz。引脚使用 PA11/PA12 默认映射。

当前配置：

| 项 | 值 |
| --- | --- |
| CAN 模式 | Normal |
| SJW | 1 tq |
| BS1 | 2 tq |
| BS2 | 1 tq |
| Prescaler | 4 |

按 STM32 CAN 公式：

```text
bitrate = PCLK1 / Prescaler / (1 + BS1 + BS2)
        = 8 MHz / 4 / (1 + 2 + 1)
        = 500 kbit/s
```

所以当前 8 MHz 配置下，CAN 波特率反而是合理的 500 kbit/s。代码旁边的 72 MHz 注释不准确，不能作为依据。如果未来切回 72 MHz 并按 APB1=36 MHz 运行，当前 Prescaler=4 会导致 CAN 波特率错误。

### RTC

`Init_RTC()` 打开 PWR 时钟，然后调用 `RTC_ClockConfig()`。RTC 时钟优先使用 LSE：

| 路径 | 时钟源 | 分频 | 目标 |
| --- | --- | --- | --- |
| 正常 | LSE | `RTC_SetPrescaler(32767)` | 1 Hz |
| LSE 失败 | LSI | `RTC_SetPrescaler(40000 - 1)` | 1 Hz 标称 |

RTC 闹钟由 `RTC_WKTimeConfig()` 设置：

```c
RTC_SetAlarm(RTC_GetCounter() + OtherElement.u16Sleep_TimeRTC);
```

也就是以秒为单位，而不是 `OtherElement.u16Sleep_RTC_WakeUpTime` 所表达的分钟值。`rtc_sleep_get_period_seconds()` 用的是 `u16Sleep_RTC_WakeUpTime * 60`，这和实际闹钟使用的字段不是同一个，需要确认业务意图。

问题点：

1. `RTC_ClockConfig()` 使用 `BKP_DeInit()`、`BKP_ReadBackupRegister()`、`BKP_WriteBackupRegister()`，但当前只看到打开 `RCC_APB1Periph_PWR`，没有看到打开 `RCC_APB1Periph_BKP`。
2. LSE 启动失败后切 LSI，但没有记录 `System_ErrFlag.u8ErrFlag_LSE`，现场只能通过行为推断。
3. LSI 标称 40 kHz 误差很大，用作 RTC 会导致长期时间漂移。
4. 每次 `Init_RTC()` 里 `BKP_DeInit()` 会复位备份域，这会清掉 RTC 备份寄存器，等价于每次重配 RTC。之前已有“WaitForSynchro 卡死修复说明”，这块需要结合实际硬件继续验证。

### IWDG

`wdog_enable` 当前在 `conf.h` 中被注释，所以 IWDG 默认不启用。

如果打开 `wdog_enable`，IWDG 使用独立 LSI 时钟，不依赖 SYSCLK/HCLK：

| 宏状态 | Prescaler | Reload | 粗略超时 |
| --- | --- | --- | --- |
| `__FUNC_RTC__` 未定义 | 64 | 160 | 约 256 ms |
| `__FUNC_RTC__` 已定义 | 256 | 0x0FFF | 约 26.2 s |

注释里关于 28 ms、6.28 s 的说法和当前参数不一致，建议以后按公式重新写。

### 软件 I2C：AFE 与 EEPROM

工程没有使用硬件 I2C 外设时钟，AFE 与 EEPROM 都是 GPIO 模拟 I2C。

| 设备 | 引脚 | 时钟依赖 |
| --- | --- | --- |
| SH367309 AFE | PB8/PB9 | GPIOB 时钟 + 软件延时 |
| EEPROM | PB10/PB11，PB13 WP | GPIOB 时钟 + 软件延时 |

AFE 的 `initAFE1_IIC()` 和 EEPROM 的 `InitE2PROM_i2c()` 本身没有打开 GPIOB 时钟，依赖前面 `InitIO()` 已经打开 GPIOB。当前主初始化路径满足这一点；如果以后单独调用这些函数，需要先确保 GPIOB 时钟打开。

软件 I2C 的时序会受 `__delay_us()` 影响，因此会间接受 `SystemCoreClock` 和 SysTick 配置影响。

### 74HC595/LED 与 SPI 命名

`GPIO_SPI_MOSI`、`GPIO_SPI1_NSS`、`GPIO_SPI1_SCK` 实际在 `InitIO()` 中配置为普通 GPIO 输出，用于 LED595 数据、锁存和时钟。

当前没有启用 SPI1 外设时钟，也没有调用 `SPI_Init()`。这里的 `SPI` 是引脚命名/逻辑含义，不是硬件 SPI 外设。

### FLASH

Flash 控制器没有单独 RCC 外设时钟配置。系统时钟相关点主要是 `SystemInit()` 设置 Flash 预取与 wait state。

当前 8 MHz 下 0 wait state 可用。如果未来切到 48/72 MHz，必须同步配置正确的 Flash latency。

### PWR/STOP/DBGMCU

STOP 入口为：

```c
PWR_EnterSTOPMode(PWR_Regulator_LowPower, PWR_STOPEntry_WFI);
```

`main.c` 初始化末尾调用：

```c
DBGMCU_Config(DBGMCU_STOP, ENABLE);
```

问题点：

1. `Sys_StopMode()` 里打开 PWR 时钟的语句被注释。RTC 打嗝休眠路径在进入 STOP 前会调用 `Init_RTC()`，因此 PWR 时钟通常已打开；但旧的 `IsSleepStartUp()` 标志路径可能在未打开 PWR 时钟的情况下直接进入 `Sys_StopMode()`。
2. `DBGMCU_STOP` 会让调试器在 STOP 下保持调试能力，量产低功耗测试时可能显著影响功耗，应确认是否仅用于调试版本。

## 工作模式时钟行为

### 正常上电/复位模式

流程：

1. 复位后 HSI 8 MHz。
2. 启动文件调用 `SystemInit()`，切到 HSE 8 MHz。
3. `InitDevice()` 再调用 `SystemInit()`，仍为 HSE 8 MHz。
4. 初始化延时、GPIO、外设、TIM3。

正常模式下所有主要外设都工作在 8 MHz 派生时钟上。

### RTC 打嗝休眠模式

当前 `sleep()` 直接调用 `rtc_sleep()`。每 1 s 软件标志到达后判断是否进入 HICCUP_MODE。

单次打嗝周期：

1. `rtc_sleep_prepare_rtc()`：
   - `Init_RTC()`：准备 RTC/LSE/LSI。
   - `IOstatus_RTCMode()`：IO 转模拟输入降功耗。
   - `InitWakeUp_RTCMode()`：配置外部唤醒 EXTI，并设置 RTC alarm。
2. `Sys_StopMode()` 进入 STOP。
3. RTC alarm 或外部 EXTI 唤醒。
4. 如果是 RTC 唤醒，`is_rtc_wakekup = true`。
5. `Init()` 被调用。

关键行为：RTC 唤醒后 `Init()` 看到 `is_rtc_wakekup == true`，会跳过 `cpu_frequency_conf()`、`InitDelay()`、`InitIO()`、`InitADC()`、`InitSci()`、`InitCan()`、`InitTimer()` 等完整恢复，只执行后面的 `initAFE1_IIC()` 和 `InitE2PROM_i2c()`。这意味着 RTC 周期内只做最小采样/判断，不应假设所有外设都恢复为正常通信状态。

由于 STOP 唤醒后系统时钟按 STM32F1 行为回到 HSI，当前 HSI 和 HSE 都是 8 MHz，所以频率上不明显出错。但时钟源从 HSE 变成 HSI，精度和漂移会变化。

### RTC 打嗝退出/异常唤醒

如果 RTC 周期检测到异常，代码会：

1. 清 `is_rtc_wakekup`。
2. 再调用一次 `Init()`。
3. 这次会走 `cpu_frequency_conf()` 和完整外设恢复。

但 `cpu_frequency_conf()` 当前会把 SYSCLK 切到 PLL，而 PLL 未按目标频率配置。按复位默认，结果仍约 8 MHz，但源自 HSI/2*2 的 PLL，不是 HSE 直连。

### 旧睡眠标志 + 复位路径

`SleepDeal_Continue()` 会写 Flash 睡眠标志，然后让 AFE 休眠并 `MCU_RESET()`。

下次上电/复位后 `IsSleepStartUp()` 根据 Flash 标志进入：

| 标志 | IO 状态 | 唤醒配置 | 进入模式 | 恢复 |
| --- | --- | --- | --- | --- |
| HICCUP | `IOstatus_RTCMode()` | `InitWakeUp_RTCMode()` | STOP | `MCU_RESET()` |
| NORMAL | `IOstatus_NormalMode()` | `InitWakeUp_NormalMode()` | STOP | `MCU_RESET()` |
| DEEP | `IOstatus_DeepMode()` | `InitWakeUp_DeepMode()` | STOP | `MCU_RESET()` |

这个路径唤醒后不是原地继续，而是再次复位，因此最终回到正常上电时钟配置。

### Sleep-on-exit

`Sys_SleepOnExitMode()` 设置 `SLEEPONEXIT` 并执行 WFI。当前没有看到主流程使用它。若未来启用，这属于 Cortex-M 睡眠，不会关闭 HSE/PLL，外设时钟保持，和 STOP 不是同一个功耗等级。

## 明确问题与风险

### 1. 真实主频是 8 MHz，不是 72 MHz

影响范围：

| 模块 | 当前结果 |
| --- | --- |
| TIM3 系统节拍 | 目前按 8 MHz 配置，可用 |
| CAN1 | 目前按 8 MHz 计算为 500 kbit/s，可用 |
| ADC TIM2 触发 | 目前约 111 Hz，疑似低于预期 |
| ADC 时钟 | 目前 1 MHz，注释 9 MHz 错误 |
| 注释/维护判断 | 大量 72 MHz 注释会误导后续配置 |

建议先确认产品真实目标：到底要 8 MHz 低功耗运行，还是要 72 MHz 满速运行。两种方案的 CAN、TIM、ADC、USART、Flash latency 都需要成套配置，不能只改一个宏。

### 2. HSE 失败没有形成可靠错误状态

`SetSysClockToHSE()` 超时后只保留 HSI，未设置 `System_ErrFlag.u8ErrFlag_HSE`，也没有降级重配外设参数。后续代码却有 `u8ErrFlag_HSE` 分支。

建议：

1. 在 HSE 失败时显式记录错误。
2. 调用 `SystemCoreClockUpdate()`。
3. 按实际时钟重新配置 TIM/USART/CAN/ADC，或统一保持 8 MHz 参数。

### 3. `cpu_frequency_conf()` 名称和实际行为不一致

它看起来像“恢复 HSE/PLL”，但当前实际可能切到默认 PLL 8 MHz。建议要么删除 PLL 切换，要么明确配置 PLL 来源和倍频，并更新所有外设参数。

### 4. ADC 采样链路仍按 72 MHz 思路配置

TIM2 触发在当前 8 MHz 下约 111 Hz；ADC 时钟为 1 MHz。若业务要求快速电流/母线采样，这会影响响应速度。

建议按目标采样周期重算：

```text
TIM2触发频率 = TIM2CLK / (PSC + 1) / (ARR + 1)
ADC转换时间 = (采样周期 + 12.5) / ADCCLK
```

### 5. RTC/BKP 时钟配置不完整

RTC 代码使用 BKP 外设，但未看到 `RCC_APB1Periph_BKP` 使能。建议 `Init_RTC()` 中统一打开：

```c
RCC_APB1PeriphClockCmd(RCC_APB1Periph_PWR | RCC_APB1Periph_BKP, ENABLE);
```

同时需要验证 `BKP_DeInit()` 是否必须每次执行。它能避免之前的同步卡死，但也会重置备份域，可能导致 RTC 每次重新初始化。

### 6. STOP 入口对 PWR 时钟有路径依赖

`Sys_StopMode()` 内部没有确保 PWR 时钟打开。RTC 打嗝路径间接满足，但旧的睡眠标志路径不一定满足。

建议 `Sys_StopMode()` 自己打开 PWR 时钟，降低调用顺序风险。

### 7. RTC alarm 字段存在语义不一致

实际 alarm 使用 `OtherElement.u16Sleep_TimeRTC`，而睡眠累计周期计算使用 `OtherElement.u16Sleep_RTC_WakeUpTime * 60`。如果一个是秒、一个是分钟，命名和使用需要统一，否则 SOC 休眠累计时间会偏。

### 8. 调试 STOP 配置可能影响功耗

`DBGMCU_Config(DBGMCU_STOP, ENABLE)` 适合调试，不适合直接作为低功耗量产默认。建议受 `_DEBUG_` 或专用宏控制。

### 9. Keil 目标信息存在不一致

Keil 工程显示：

| 项 | 当前值 |
| --- | --- |
| Device | `STM32F103C8` |
| Define | `STM32F10X_MD,USE_STDPERIPH_DRIVER` |
| Startup | `startup_stm32f10x_hd.s` |
| 工程名 | `103RCT6` |
| 代码使用 | GPIOA-E、CAN、较多外设 |

`STM32F103C8`、`103RCT6`、HD 启动文件和 GPIOE 使用并不完全一致。若硬件实际是 RCT6，应修正 Keil Device/Flash/RAM/Startup/启动向量设置；若实际是 C8，则 GPIOE/GPIOD/存储布局要重新核对。

## 建议整理方向

### 如果目标保持 8 MHz

1. 把 `main.c`、`ADC.c`、`Can_HDX.c` 等处 72 MHz 注释全部改为 8 MHz 实际配置。
2. 把 TIM2 ADC 触发重算到业务需要的采样频率。
3. 删除或改名 `cpu_frequency_conf()` 中误导性的 PLL 切换。
4. `Sys_StopMode()` 内部确保 PWR 时钟使能。
5. `Init_RTC()` 同时打开 PWR 和 BKP 时钟。
6. 明确 RTC alarm 使用秒还是分钟。

### 如果目标恢复 72 MHz

1. 在 `system_stm32f10x.c` 启用 `SYSCLK_FREQ_72MHz`，确认 HSE 为 8 MHz。
2. 配置 Flash 2 wait state。
3. 配置 `PCLK1 = HCLK / 2`，保证 APB1 不超过 36 MHz。
4. 重算 TIM3：72 MHz TIMCLK 下，1 ms 可用 `PSC=72-1, ARR=1000-1`。
5. 重算 CAN：PCLK1=36 MHz 时，500 kbit/s 不能继续用当前 Prescaler=4。
6. 重算 ADC：PCLK2=72 MHz 时 `PCLK2/8=9 MHz` 可用，但要确认 ADC 最大时钟与采样时间。
7. 低功耗 STOP 唤醒后必须完整恢复 HSE/PLL，并调用 `SystemCoreClockUpdate()`。

## 推荐优先级

| 优先级 | 项目 | 原因 |
| --- | --- | --- |
| P0 | 确认目标主频是 8 MHz 还是 72 MHz | 决定所有外设参数 |
| P0 | 修正 STOP 后时钟恢复策略 | 低功耗路径频繁执行，影响可靠性 |
| P1 | 修正 RTC 的 PWR/BKP 时钟和备份域策略 | RTC 是当前低功耗核心 |
| P1 | 重算 ADC 触发频率 | 当前很可能低于预期 |
| P1 | 清理 72 MHz 错误注释 | 防止后续误改 |
| P2 | Keil Device/Startup/Flash 配置统一 | 避免烧录、链接、向量表和芯片型号不一致 |
| P2 | 调试 STOP 宏隔离 | 避免功耗测试误判 |

