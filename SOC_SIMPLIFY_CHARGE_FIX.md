# SOC 简化与充电校准修正说明

## 背景

现场现象是放电 SOC 基本可信，但充电 SOC 不准。原 `SocEnhance.c` 同时叠加了库仑积分、在线 OCV、静置补偿、弱单体保护、端点校准、容量学习等策略，状态交叉较多；充电到满时还要求进入很小尾电流，容易因为充电器策略、电流校准或采样波动导致 SOC 长时间不能锚定到 100%。

## 本次调整

1. SOC 主路径简化为库仑积分：
   - 充电电流增加 `CapNow`。
   - 放电电流减少 `CapNow`。
   - SOC 直接由 `CapNow / CapFull` 四舍五入计算。

2. 容量基准固定为参数容量：
   - 移除运行中容量学习状态机。
   - `CapFull` 使用配置容量，避免学习误差影响充电 SOC。
   - 保留原 Flash SOC 数据格式，保证已有存储接口兼容。

3. 充电端点改为电压锚定：
   - 充电中单体最高电压达到 `u16_SOC_100_Vol`。
   - 单体最低电压达到按化学体系约束后的满电确认电压。
   - 单体压差满足 `PROJECT_CFG_SOC_FULL_CONFIRM_MAX_CELL_DELTA_MV`。
   - 条件持续 60 秒后 SOC 直接校准到 100%。
   - 不再要求电流必须低于 C/20，减少“实际已满但 SOC 不到满”的情况。

4. 增加充电高压追赶：
   - 充电且所有单体进入满电附近区间后，若 SOC 低于 95%，每 20 秒最多上调 1%。
   - 该逻辑只把 SOC 追到 95%，真正 100% 仍由满充端点锚定完成。

5. 保留放电安全相关策略：
   - 放电库仑积分和循环次数累计保留。
   - 低压弱单体保护保留，低压区可把 SOC 快速压低。
   - 放电到 `u16_SOC_0_Vol` 并持续约 4 秒后锚定到 0%。

6. 静置校准收敛：
   - 只保留 RTC 静置唤醒路径的 OCV 校准。
   - 静置 30 分钟以上才根据 OCV 表有限步进修正，避免在线运行时反复拉扯 SOC。

## 删除的复杂策略

- 在线轻载 OCV 纠偏。
- 重载放电后的 OCV holdoff 状态机。
- 容量学习锚点和 SOH 自学习。
- 运行中静置桶补偿和电压稳定窗口状态机。

## 调试关注点

1. `OtherElement.u16Soc_Ah` 必须与实际容量匹配，否则库仑积分比例仍会偏。
2. `OtherElement.u16Soc_V_100` 必须与电芯体系和充电器终止电压匹配。
3. 若充满仍不到 100%，优先检查：
   - `g_stCellInfoReport.u16Ichg` 是否识别为充电。
   - `u16VCellMax` 是否能达到 `u16_SOC_100_Vol`。
   - `u16VCellMin` 是否高于满电确认电压。
   - 单体压差是否超过配置阈值。
4. 若充电中 SOC 上升过慢，优先检查充电电流标定 `MDL_ICHG`。
