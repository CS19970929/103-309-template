# BMS 工程 O2 优化检查与修复说明

日期：2026-09-03

## 检查范围

- 工程：`103 + 309/Project/Users/CommomSH367309_16series_103RCT6_C.uvprojx`
- MCU：STM32F103C8
- 编译器：Keil ARMCC V5.06 update 7 (build 960)
- `FD_Release`：`<Optim>3</Optim>`，对应 Keil 优化等级 O2
- `FD_Debug`：保留原有调试配置，并验证可编译链接

## 已修复问题

1. `PubFunc.c` 的 `Delay1ms()` 和 `I2C_AFE1.c` 的 `Delay4us()` 原本只有空循环。O2 可能删除没有可观察副作用的循环，导致软件 I2C 时序和 AFE 初始化延时失效；已改为使用现有 `SysTick` 延时接口 `__delay_ms()`、`__delay_us()`。
2. `SleepDeal_IsWakeupValid()` 和 `rtc_sleep_has_wakeup_exception()` 存在非 `void` 函数路径缺少返回值，已补齐确定的 `0/false` 返回。
3. CAN 接收计数、RTC alarm 计数、SCI 外部通信计数以及 SCI 升级状态和收发状态均存在 ISR/主循环共享，已补充 `volatile`。
4. `Sci_PortService()` 对帧待处理标志的检查和清零原先不是原子操作，已使用 `PRIMASK` 临界区完成原子取出，避免 ISR 在窗口内提交的帧被清除。
5. 补齐 `LogEvent_Record()`、`Sci2_CommonUpper_IRQHandler()`、`Sci3_CommonUpper_IRQHandler()` 声明，消除 ARMCC 隐式声明风险。
6. 启动汇编无条件调用 `IrqDebug_RecordUnhandledVector()`，而 Debug 配置原先因条件编译缺少该符号；已保留空实现，使 Debug 目标能够正常链接。
7. 删除完全无调用且缺少实现的 SCI 老化时长占位函数；老化状态函数当前仍是既有空实现，未在本次 O2 适配中擅自改变协议行为。

## 编译验证

本轮死代码告警清理后的 `FD_Release` 完整重编译：

- 0 errors
- 0 warnings
- Program Size：Code 32556，RO-data 3156，RW-data 1336，ZI-data 5520

本轮死代码告警清理后的 `FD_Debug` 完整重编译：

- 0 errors
- 0 warnings
- Program Size：Code 40644，RO-data 3156，RW-data 1336，ZI-data 5520

本轮处理方式遵循模板项目约束：`__SOC_5_PROTECT_` 代码、CAN 老化辅助代码、SOC Type-C 辅助代码、ADC Type-C/VBC 辅助代码和 AFE OCD2V 表均保留在源码中，仅通过对应条件编译块避免当前配置产生 warning；只移除了当前无任何读取用途的 SOC 历史变量和 TIM3 旧状态变量。

## 未完成的硬件验证

编译无法替代实机验证。烧录后仍应重点回归软件 I2C AFE 通信、SCI 收发、CAN 接收/发送、升级状态、RTC/Stop 唤醒和看门狗喂狗时序。
