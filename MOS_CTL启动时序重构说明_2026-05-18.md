# MOS CTL 启动时序重构说明

## 目标

本次调整的目标是把 AFE `CTL` 作为开机 MOS 输出的总闸门使用，避免从深度休眠或普通休眠唤醒后的 AFE 初始化阶段出现充电 MOS / 放电 MOS 乱动作。

新的启动原则：

1. `CTL` 先关闭。
2. 完整配置 AFE 寄存器。
3. 按优先级写好目标 MOS 控制位。
4. 完成启动电流零点校准。
5. 最后打开 `CTL`，让目标 MOS 状态真正生效。

## 启动 MOS 优先级

启动阶段统一使用 `MosStartup_ApplyInitialState()` 决定目标 MOS 状态：

| 优先级 | 条件 | 目标状态 |
| --- | --- | --- |
| 1 | 检测到 5V 充电信号，`GPIO_CHG_IN` 有效 | 打开充电 MOS，关闭放电 MOS |
| 2 | 未检测到 5V 充电信号，且工厂老化未完成/应进入老化 | 打开充电 MOS 和放电 MOS |
| 3 | 以上都不满足 | 关闭充电 MOS，打开放电 MOS |

5V 充电信号优先级高于工厂老化模式。也就是说，哪怕工厂老化未完成，只要启动时检测到 5V 充电信号，也只允许打开充电 MOS，不打开放电 MOS。

## 关键源码入口

- `InitAFE1()`：启动 AFE 初始化主入口。
- `MosStartup_ApplyInitialState()`：启动 MOS 优先级策略。
- `MosStartup_Is5vChargeActive()`：5V 充电信号判断，当前按 `GPIO_CHG_IN == Bit_RESET` 为有效。
- `FactoryAging_ShouldStartOnBoot()`：启动阶段判断工厂老化是否应启动，不依赖 `FactoryAging_Task()` 已经运行。
- `AfeCurrent_StartupZeroCal()`：启动电流零点校准，内部保持 `CTL` 关闭，完成后再打开 `CTL`。

## 新时序

```text
InitAFE1()
  -> initAFE1_IIC()
  -> close_ctlc()
  -> AfeCurrent_PrepareStartupZero()
  -> AFE_IsReady()
  -> SH367309_UpdataAfeConfig()
  -> MosStartup_ApplyInitialState()
       -> 5V charging ? open_chg_close_dsg()
       -> factory aging ? enter_fac_mode(true)
       -> otherwise open_dsg_close_chg()
  -> AfeCurrent_StartupZeroCal()
       -> close_ctlc()
       -> current zero calibration
       -> open_ctlc()
```

如果本次启动不需要重新做电流零点校准，则在目标 MOS 寄存器写好后直接 `open_ctlc()`。

## 工厂老化模式边界

`FactoryAging_Start()` 也遵循 5V 充电优先级：

- 有 5V 充电信号：保持充电 MOS 开、放电 MOS 关。
- 无 5V 充电信号：进入正常工厂老化，充放电 MOS 都打开。

这样可以避免启动时先因为工厂老化打开充放电，再被充电识别改回充电单开造成时序抖动。

## 验收点

1. 深度休眠唤醒后，AFE 初始化早期 `CTL` 必须为关闭。
2. 5V 充电信号存在时，启动目标 MOS 必须是充电开、放电关。
3. 无 5V 充电信号且工厂老化未完成时，启动目标 MOS 才允许充放都开。
4. 非充电、非工厂老化时，默认只打开放电 MOS。
5. 电流零点校准完成前不打开 `CTL`。
6. `CTL` 打开前，AFE 参数和目标 MOS 寄存器已经配置完成。
