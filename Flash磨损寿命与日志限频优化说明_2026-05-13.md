# Flash 磨损寿命与日志限频优化说明

日期：2026-05-13

## 当前 Flash 持久化布局

当前工程按 `STM32F10X_MD` 的 1KB Flash 页布局后 16KB 持久化区：

- `0x0801C000/0x0801C800`：AFE 参数 A/B 双槽
- `0x0801C400/0x0801CC00`：RW 参数 A/B 双槽
- `0x0801D000/0x0801D800`：事件日志双页 journal
- `0x0801E000/0x0801E800`：SOC 快照双页 journal
- `0x0801F000`：升级参数标志页
- `0x0801F400`：工厂老化进度页
- `0x0801F800`：IAP 更新标志页
- `0x0801FC00`：睡眠标志保留页

量产前必须确认实物 Flash 至少 128KB，且页大小与当前 1KB 页配置一致。若实物是 64KB Flash，后 16KB 存储区不可用；若实物按 2KB 页擦除，则当前 1KB 间隔的槽地址会互相影响。

## 寿命估算

按内部 Flash 每页 10000 次擦除保守估算：

| 数据区 | 写入机制 | 估算保存次数 | 主要风险 |
|---|---:|---:|---|
| AFE 参数 | A/B 双槽，每次保存擦 1 页 | 约 20000 次 | 低，通常只在调参/默认恢复时写 |
| RW 参数 | A/B 双槽，每次保存擦 1 页 | 约 20000 次 | 低，避免上位机循环写参 |
| SOC 快照 | 双页 journal，约 36 条一轮 | 约 360000 次 | 中低，正常 SOC 百分比变化触发 |
| 事件日志 | 双页 journal，约 8 条一轮 | 约 80000 次 | 最高，故障抖动会快速磨损 |
| 工厂老化 | 单页 journal，约 42 条一轮 | 约 420000 次 | 低，当前 2 小时保存一次 |
| IAP/升级标志 | 单页半字写，擦整页 | 约 10000 次 | 低，避免误循环触发升级 |

## 本次优化

事件日志是当前最高磨损风险点。本次新增同类事件限频：

```c
#define PROJECT_CFG_LOG_RECORD_REPEAT_MIN_INTERVAL_SEC 3600
```

配置位置：`103 + 309/Project/Source/conf/Project_Config.h`

行为：

- 默认同一事件 3600 秒内只保存一次。
- `0` 表示关闭限频。
- 启动事件 `BMS_START_UP` 和睡眠事件 `BMS_SLEEP` 不限频，仍立即保存。
- 首次故障仍立即保存；只有同一事件在配置窗口内反复恢复/触发时才抑制重复写 Flash。
- 日志 Flash 存储格式未变化，已有日志记录兼容。
- `StorageFlash_SaveLogData()` 增加内容相同跳过写入，避免重复清日志或无变化保存消耗 journal。

连续同一故障抖动时，日志写入频率从最高每秒一次降为每小时一次。按日志区约 80000 次保存寿命估算，单一故障持续抖动时从约 1 天级提升到约 9 年级。

## 后续建议

- 若现场仍有长期故障抖动，可把 `PROJECT_CFG_LOG_RECORD_REPEAT_MIN_INTERVAL_SEC` 提高到 `7200` 或更大。
- 若需要完整记录每次故障恢复/重触发，可改为 `0` 关闭限频，但会恢复原来的 Flash 磨损风险。
- 量产固件保持 `PROJECT_CFG_FLASH64K_QUICK_TEST_ENABLE 0` 和 `PROJECT_CFG_FLASH64K_USE_TEST_ENABLE 0`，避免测试逻辑高频写后 64KB。
- 现场评估寿命时，优先统计日志页 sequence 增长速度，而不是只看 SOC 快照写入。
