# RTC STOP 低功耗配置修复说明

日期：2026-05-14

## 背景

本次针对 STM32F103 RTC STOP 休眠前后配置做代码修复。前一轮复核结论是：项目使用 `RTC_IT_ALR + EXTI_Line17 + RTCAlarm_IRQn` 唤醒 STOP 的方向正确，但 STOP 前 pending 清理、秒中断处理和唤醒后恢复路径不够收敛。

参考依据：

- ST AN2629：STM32F101/102/103 low-power modes，要求进入 STOP 前清 EXTI pending，WFI 唤醒依赖配置为 interrupt mode 的 EXTI 线和 NVIC。
- ST RM0008：STM32F10xxx reference manual，STOP 唤醒后系统时钟回到 HSI，需要软件恢复系统时钟；RTC Alarm 通过 EXTI17 唤醒 STOP。

## 本次代码变更

### RTC STOP 前配置

`103 + 309/Project/Source/RTC.c`

- `RTC_WKTimeConfig()` 进入 STOP 前先关闭并清理 `RTC_IT_SEC`。
- 设置 Alarm 前关闭 `RTC_IT_ALR`，清 `RTC_IT_ALR`、`RTC_FLAG_ALR` 和 `EXTI_Line17`。
- 设置下一次 Alarm 后只重新开启 `RTC_IT_ALR`。
- 新增 `RTC_DisableStopWakeup()`，STOP 返回后统一关闭 Alarm 唤醒并清 pending。
- 新增 `RTC_RestoreRunInterrupts()`，完整恢复运行态时关闭 Alarm、清 pending、重新开启秒中断。

### 外部唤醒 pending 清理

`103 + 309/Project/Source/conf/conf.c`

- 新增 `LowPower_ClearWakeupPending()`，统一清 PA0、PA9、PB7、PB12、PB13 等外部唤醒 EXTI pending，并清对应 NVIC pending。
- `Sys_StopMode()` 在 `PWR_EnterSTOPMode()` 前调用 `LowPower_ClearWakeupPending()`。
- 新增 `LowPower_DisableWakeupExti()`，STOP 返回后统一关闭外部唤醒 EXTI，避免 `rtc_sleep.c` 分散关闭线号。

### 唤醒后恢复路径

`103 + 309/Project/Source/rtc_sleep.c`

- 删除运行期 HICCUP 路径里的重复通用 `Init()`。
- 新增 `rtc_sleep_restore_after_stop()`：RTC Alarm 唤醒时只做短恢复检查，外部唤醒直接完整恢复。
- 新增 `rtc_sleep_restore_for_run()`：只有 RTC Alarm 唤醒但检测到异常、需要退出低功耗时，才补完整运行恢复。
- STOP 返回后通过 `LowPower_DisableWakeupExti()` 和 `RTC_DisableStopWakeup()` 收口唤醒配置。

## 当前架构状态

本次没有重写完整低功耗框架，而是先把风险最高、最影响 STOP 稳定性的路径收敛：

- RTC 周期唤醒路径：`RTC_IT_ALR + EXTI17`。
- 运行态秒中断：只在完整运行态打开，STOP 周期前关闭。
- 外部唤醒 pending：统一在 `Sys_StopMode()` 进 WFI 前清理。
- RTC 短唤醒服务：只恢复 Delay、IIC、EEPROM 等检查所需资源。
- 完整运行恢复：只在外部唤醒或异常退出时执行。

仍建议后续继续统一 HICCUP、NORMAL、DEEP 的唤醒源采样和有效唤醒判定。

## 验证建议

1. 静置无电流、无通信时进入 RTC HICCUP，确认可连续 1s/10s RTC 唤醒并回睡。
2. RTC 唤醒后无异常时，确认不会完整恢复 ADC/CAN/TIM/SCI 后再回睡。
3. RTC 唤醒时制造电流或 AFE 异常，确认只做一次完整运行恢复并退出低功耗。
4. 充电 PA0、按键 PA9、RS485/MCU_WK 唤醒，确认不会因旧 pending 导致刚进 STOP 就醒。
5. 完整唤醒后确认 `RTC_IT_SEC` 重新打开，`sys_time.rtc_sec_cnt` 能继续递增。
6. 用电流表对比 STOP 电流，重点确认秒中断关闭、ADC/TIM2/DMA 停止、LED 扫描关闭、CAN 收发器关闭后电流符合预期。
