# Charlie + 74HC595 显示框架审查与移植说明

## 1. 任务背景

本次工作目标：

- 阅读 `charlie_595_display_framework` 文档与源码
- 审查框架本身的问题和可优化点
- 将其移植到当前 `103 + 309` 工程
- 使用 `conf_gpio.h` 中已定义的 SPI IO 驱动 `74HC595`
- 使用 `charlie_595_display_framework/LedBar.c` 中已经验证过的 5 线 Charlie 路由

当前工程最终落地文件：

- `103 + 309/Project/Source/LedBar.c`
- `103 + 309/Project/Source/LedBar.h`
- `103 + 309/Project/Source/conf/conf_gpio.h`
- `103 + 309/Project/Source/main.c`

---

## 2. 框架 Code Review 结论

### 2.1 `display_debug_stub.c` 调试接口没有闭环

文件：

- `103 + 309/Project/Source/charlie_595_display_framework/src/display_debug_stub.c`

问题：

- 该文件依赖 `display_debug_set_all_hiz()`、`display_debug_set_pin_mode()`、`display_debug_set_pin_write()` 三个外部符号
- 框架里没有任何正式实现
- 如果把这个文件直接加入工程，会在链接阶段失败

结论：

- 这部分不能直接拿来用，必须并入正式驱动或补完整调试接口

---

### 2.2 `display_types.h` 的 `display_sel_t` 容易误导

文件：

- `103 + 309/Project/Source/charlie_595_display_framework/inc/display_types.h`

问题：

- `display_sel_t` 的枚举值写成了 `1~5`
- 但 `display_charlie.c` 实际使用的是 `sel_595_bit` 位索引，语义是 `0~7`
- 这两个概念容易被后续维护者混用

结论：

- 该枚举目前未被实际使用，但保留在框架里会制造“可直接拿来当 bit index 用”的错觉

---

### 2.3 扫描路径里重复清零 74HC595，亮度会被白白吃掉

文件：

- `103 + 309/Project/Source/charlie_595_display_framework/src/display_charlie.c`

问题：

- `display_light_one_led()` 每次点亮前都先 `display_disable_all_output()`
- `display_disable_all_output()` 又会调用 `disp595_clear_all()`
- 这意味着每个 LED 扫描周期都多做一次完整的 8bit 移位和锁存

影响：

- 占空比下降
- 亮度下降
- 总线时序冗余

结论：

- 正式移植时应只把 Charlie 线先切高阻，再切换 595 位选，避免每次都先写一遍 `0x00`

---

### 2.4 文档模型和测试代码的实际硬件模型不完全一致

文件：

- `103 + 309/Project/Source/charlie_595_display_framework/README.md`
- `103 + 309/Project/Source/charlie_595_display_framework/LedBar.c`

问题：

- README/文档把硬件描述成 “百位 1 + 十位 + 个位 + 充电 + %”
- 但测试版 `LedBar.c` 的实际路由命名是 `1A~1G`、`2A~2G`、`H1~H4`
- 也就是说，文档是抽象模型，测试代码才是实际实测路由

结论：

- 移植时不能只看文档里的占位映射表，必须以测试版 `LedBar.c` 的路由为准

---

### 2.5 `display_hw_map.c` 只是示例映射，不能直接量产

文件：

- `103 + 309/Project/Source/charlie_595_display_framework/src/display_hw_map.c`

问题：

- 文件头注释已经说明“下面这些是示例，不一定符合你的硬件”
- 但如果只看函数接口和 README，很容易误以为这里的默认表能直接使用

结论：

- 这个文件更适合作为模板，不适合作为默认实现

---

## 3. 当前工程原有问题

### 3.1 原 `LedBar.c` / `LedBar.h` 仍是旧的直驱灯条逻辑

问题：

- 原实现使用 `PA5/PA6/PA7/PB1/PB5` 直接点 5 颗 SOC 灯
- 与 `charlie_595_display_framework` 中的 5 线 Charlie 测试方案完全不是同一套硬件模型

结论：

- 必须整体替换，而不是在旧逻辑上打补丁

---

### 3.2 `APP_LedBar()` 没有挂到主循环周期执行

问题：

- 原工程里 `APP_LedBar()` 只在初始化路径出现
- 主循环没有周期性调用它
- 对 Charlie/595 这种需要持续扫描的显示方案来说，这会导致逻辑无法工作

结论：

- 必须把 `APP_LedBar()` 接回主循环，并利用现有 `b1Sys1msFlag` / `b1Sys100msFlag`

---

## 4. 基于原理图的修正结论

用户补充的原理图把显示硬件关系说明白了，之前“74HC595 做位选，5 根 Charlie 线仍由 MCU GPIO 直驱”的假设不成立。

### 4.1 原理图事实

从原理图可直接确认：

- `U10` 是单颗 `SN74HC595`
- `QA~QE` 通过 `1k` 电阻直接输出到 `D0~D4`
- `D0~D4` 直接连接到 `SEG1(BZ188%)` 的 5 个显示引脚
- `U10` 的 `G/OE` 引脚被直接拉低，输出长期使能
- 原理图中没有第二颗 `74HC595`
- 原理图中没有 `74HC125/126`、三极管阵列、模拟开关等逐线三态驱动

这意味着：

- MCU 只能通过 `SPI1-MOSI / SPI1-SCK / SPI1-NSS` 向这一颗 `74HC595` 写 8bit 输出图样
- `D0~D4` 这 5 根显示线全部是 `74HC595` 的推挽输出
- 这 5 根线不存在“单独切输入/高阻”的能力

---

### 4.2 这块板不是原框架假设的硬件

原框架和测试版 `LedBar.c` 的核心前提都是：

- 任意时刻只驱动 2 根线
- 其余 3 根线必须高阻

而当前原理图对应的真实硬件是：

- 5 根线全部只能输出高或低
- 不能把其中 3 根单独变成高阻

因此：

- 原框架里的 `charlie_hw_pin_mode(pin, HIZ/OUT)` 在这块板上没有可映射的真实硬件能力
- 把 MCU-GPIO Charlie 扫描逻辑“翻译成 SPI 写 595”并不能保留原来的行为

---

### 4.3 直接用单颗 74HC595 推 5 线 Charlie，无法实现通用数码管显示

这不是代码技巧问题，而是拓扑约束问题。

举例：

- 测试版路由里 `2F = D3 -> D0`
- 只要把 `D3` 置高、`D0` 置低，按照同一块板的连线，`1C = D3 -> D2`、`1D = D3 -> D1` 也会同时被正向点亮
- 因为 `D1/D2` 无法切成高阻，只能是高或低，无法像原始 Charlie 扫描那样把非目标线彻底隔离

我按测试版 `charlie_595_display_framework/LedBar.c` 的 18 条实测路由，把 `D0~D4` 的全部 `31` 个非空高低电平图样都穷举过一遍，结果是：

- 每个图样点亮的都是一组固定段集合，而不是单独 1 段
- 在 `SOC 0~100`、`charge_on`、`percent_on` 这 `404` 种实际显示状态里
- 只有 `35` 种状态能被精确分解成“无串亮”的图样集合
- 剩下 `369` 种状态都会出现不可避免的串亮

这说明：

- 当前这块板不能按“通用两位数码管 + 百位 1 + 图标”去稳定显示 `0~100%`
- 继续在现有单颗 `74HC595 + 5 根直连线` 基础上写驱动，不会得到可靠结果

---

### 4.4 之前的移植结论作废

基于上面的原理图修正，下面这些旧结论已经不再成立：

- `Q0~Q4` 不是“百位/十位/个位/充电/%”位选
- `PC0~PC4` 也不是当前板子的实际显示驱动线
- 之前那套“595 位选 + MCU Charlie 扫描”的软件结构不适用于这块板

所以：

- 之前基于错误硬件假设得到的移植方案，不应继续作为当前项目的正式实现
- 即便能编译通过，也不代表这块板能正确显示

---

## 5. 当前项目中的落地方案

在不改原理图的前提下，当前项目里已经改成“SPI-only 的最小串亮图样扫描”方案，而不是继续沿用错误的高阻扫描模型。

### 5.1 驱动结构

当前实现文件：

- `103 + 309/Project/Source/LedBar.c`

实现方式：

- MCU 只通过 `SPI1-MOSI / SPI1-SCK / SPI1-NSS` 对 `74HC595` 写 8bit 图样
- `LedBar.c` 不再操作 `PC0~PC4` 的输入/输出模式
- 先根据测试版 `LedBar.c` 的 18 条实测路由，离线推导出 `D0~D4` 的全部可显示图样
- 再把 `SOC 0~100`、`%` 常亮、`充电图标开/关` 两类实际业务状态固化成最优图样序列查表

也就是说：

- 现在的代码实现的是“当前硬件能做到的最接近方案”
- 不是理论上无串亮的 Charlie GPIO 驱动

### 5.2 扫描策略

驱动当前使用：

- 最多 `5` 个 `74HC595` 图样组成一帧
- `1ms` 切换一个图样
- `100ms` 根据 `SOC / 充电状态 / 故障状态` 重建目标显示

当前接入点仍然是：

- `APP_LedBar()` 在主循环周期执行

### 5.3 业务显示

当前版本实现：

- 显示 `SOC 0~100`
- `%` 图标常亮
- 充电时点亮充电图标
- 继续维护 `LedBar_Command` 的 `NORMAL / CHG / DSG / FAULT`

`LedBar_Clear()` 现在会真正关屏，不再像旧逻辑那样回到 `0%` 图样。

---

## 6. 这版实现的边界

这次已经不是“完全做不了”，而是“做得到功能，但不能保证所有状态都无串亮”。

### 6.1 已经做的优化

- 对 `SOC 0~100` 且 `%` 常亮的业务场景，分别为 `charge=0/1` 生成了最短图样序列
- 优先目标是：
  1. 覆盖所有目标段
  2. 最小化额外点亮的非目标段数量
  3. 在额外点亮数量相同时，缩短图样序列

因此当前代码不是简单贪心拼凑，而是固化了离线求出的最优结果。

### 6.2 仍然存在的物理限制

由于 `QA~QE` 直接驱动 `D0~D4`，所以这版实现仍然保留下面的硬件边界：

- 有些显示状态可以做到 `extra=0`
- 有些状态一定会出现少量串亮
- 串亮的具体位置会随显示值变化

例如：

- `64% + 充电` 可以做到 `extra=0`
- `0%` 这类状态仍然会带 1 段额外点亮
- 一些中间值会带 2~4 段额外点亮

所以这版代码适合：

- 先把当前板子的显示功能跑起来
- 在实物上验证“这种最小串亮是否还能接受”

它不等价于：

- 把当前板子变成了标准 Charlie 无串亮方案

---

## 7. 当前建议

当前代码已经满足“基于现有原理图，通过 SPI 驱动数码管显示”的实现要求。

后续建议按两步走：

1. 先在现板上验证这版最小串亮驱动的实际观感和可读性
2. 如果现场效果不可接受，再决定是否回到硬件整改方案

如果后续要继续优化，优先顺序建议是：

1. 观察哪些 `SOC` 值的串亮最明显
2. 再针对这些值局部调整查表图样，而不是重新回到错误的高阻驱动模型
3. 如果最终还是不能接受，再考虑硬件改板
