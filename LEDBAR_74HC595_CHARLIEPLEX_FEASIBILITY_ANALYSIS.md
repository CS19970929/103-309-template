# LedBar 74HC595 + 5Pin Charlieplexing 可实现性完整分析

## 1. 文档目的

本文回答一个非常具体的问题：

- 在**硬件已经固定**为 `74HC595 + 5pin Charlieplexing 数码管` 的前提下，
- 对固定业务显示 `0~100 + 百分号 + 充电图标`，
- **哪些值可以完美显示，哪些不可以，为什么不可以，肉眼会看到什么现象**。

本文不是主观判断，而是基于当前仓库历史 `LedBar` 的 `74HC595` 路由定义，对全部显示值做了**穷举分析**。

## 2. 结论摘要

先给结论：

- 如果“完美显示”的定义是：
  - 目标段全部点亮；
  - 非目标段**任何时隙都不允许亮**；
  - `0~100` 全范围成立；
  - 再叠加 `百分号`、`充电图标` 也成立；
  - 那么当前固定硬件**做不到**。

- 如果“可实现”的定义是：
  - 允许通过查表扫描；
  - 允许部分值出现轻微鬼影/伴亮；
  - 以“人眼可读”为目标；
  - 那么当前硬件**可以做近似实现**。

- 基于历史 `74HC595` 路由模型穷举后的结果：
  - `SOC + 百分号`：只有 `7/101` 个值可逻辑完美显示
  - `SOC + 百分号 + 充电图标`：只有 `12/101` 个值可逻辑完美显示

- 即使某些值在“无额外段”意义下可以逻辑完美显示，也**不代表视觉上完美**：
  - 多数“可完美值”仍然存在**占空比不均**
  - 表现为某些目标段更亮、某些目标段更暗

一句话总结：

- **全范围完美显示：不行**
- **查表近似显示：可以**
- **少量特定值逻辑精确显示：可以**

## 3. 分析依据

本分析采用的是仓库历史 `74HC595` 方案中的 `LedBar.c` 路由模型，而不是当前工作区里直接 GPIO 三态控制版。

历史方案的关键特征是：

- `74HC595` 的 `QA~QE` 直接驱动 `5` 根 Charlie 线
- `OE` 不可做每线独立高阻控制
- 输出只有 `高/低` 两态，没有“每 pin 独立高阻”
- 因此不能按理想 Charlieplexing 的方式做：
  - `1 根高`
  - `1 根低`
  - `其余 3 根高阻`

历史代码注释里其实已经明确写了这一点：目标是“**minimizing unavoidable extra lit segments**”，也就是“尽量减少不可避免的额外点亮段”，而不是保证零串亮。

## 4. 完美显示的判定标准

为了避免“能看”与“完美”混淆，这里把“完美”拆成两层。

### 4.1 逻辑完美

定义：

- 对某个目标显示 `T`
- 存在一组时序输出 `P1, P2, ..., Pk`
- 满足：
  - 每个时隙实际点亮集合 `Lit(Pi)` 都是 `T` 的子集
  - 即 `Lit(Pi) ⊆ T`
  - 且所有时隙的并集正好覆盖目标
  - 即 `Lit(P1) ∪ Lit(P2) ∪ ... ∪ Lit(Pk) = T`

这意味着：

- 不会出现任何非目标段
- 所有目标段最终都会被扫到

本文中“哪些值可以完美显示”，默认指的是这个标准。

### 4.2 视觉完美

定义更严格：

- 先满足逻辑完美
- 再要求目标段的占空比接近一致

如果某些目标段在 5 帧里亮 `3` 次，而另一些只亮 `1` 次，那么虽然没有鬼影，但亮度不均，视觉上也不能算真正完美。

本文结论里会特别区分：

- `逻辑完美`
- `视觉完美`

## 5. 为什么这个判定标准是对的

关键物理事实是：

- LED 亮度是**非负叠加**
- 某个非目标段只要在任何一个时隙里被点亮，它就会贡献正亮度
- 这个亮度无法被“抵消”

因此：

- 不能说“虽然某个额外段偶尔亮一下，但平均后等于不亮”
- 真实效果只能是：
  - 这个额外段以较低占空比出现
  - 人眼看到的结果是**暗亮段 / 鬼影段 / 伴亮段**

所以“任一子帧中出现非目标段”就足以判定“不完美”。

## 6. 分析模型是怎么建立的

### 6.1 Charlie 路由的定义

历史方案把逻辑显示拆成 `18` 条有向发光路由：

- 百位“1”两段：`H1_upper`, `H1_lower`
- 十位数码管 `7` 段
- 个位数码管 `7` 段
- 图标两段：`ICON_CHARGE`, `ICON_PERCENT`

每条路由都对应一对：

- `low_pin`
- `high_pin`

当且仅当：

- `high_pin = 1`
- `low_pin = 0`

这条 LED 路由就会导通。

### 6.2 74HC595 可输出的模式

`74HC595` 的 `QA~QE` 直接对应 `5` 根控制线，所以一个输出模式就是一个 `5-bit` 值。

因为全零没有点亮意义，因此需要枚举的实际模式数是：

- `2^5 - 2 = 30`

也就是：

- `00001`
- `00010`
- ...
- `11110`

这里按历史代码模型，实际枚举并使用的是：

- `1~30`

这 `30` 个 pattern。

### 6.3 每个 5-bit pattern 实际会点亮哪些路由

对任意 pattern `P`，定义：

```text
Lit(P) = { route r | P[high(r)] = 1 且 P[low(r)] = 0 }
```

这一步的含义是：

- 我们不再从“我要点亮哪个段”思考
- 而是从“当 595 输出某个 5-bit 组合时，硬件会**被动地点亮哪些段**”思考

这是整个分析最核心的一步。

### 6.4 目标显示集合的构建

对任意业务显示值，例如：

- `0`
- `41`
- `64`
- `100`

再加上图标条件：

- 是否显示 `百分号`
- 是否显示 `充电图标`

都可以得到一个目标集合 `T(value, charge, percent)`。

例如：

- `1 + 百分号`
  - 目标只有：个位 `B/C` + `ICON_PERCENT`
- `100 + 百分号`
  - 目标包括：百位“1”、十位 `0`、个位 `0`、`ICON_PERCENT`

### 6.5 完美显示判定

对每个目标集合 `T`，做穷举：

- 从 30 个 pattern 中选择不超过 5 个 pattern
- 判断是否存在一个 pattern 序列，使得：
  - 每个 pattern 点亮集合都不超出目标
  - 所有 pattern 的并集刚好覆盖目标

如果存在，则该值可**逻辑完美显示**；
如果不存在，则该值必然带鬼影或伴亮。

### 6.6 为什么采用“最多 5 个 pattern”

因为仓库历史 `74HC595` 方案就是按：

- `LEDBAR_FRAME_PATTERN_COUNT = 5`

设计的。

因此本文统计结果是按**真实历史实现能力上限**来做的，不是理论无限帧的乐观估计。

### 6.7 穷举判定伪代码

可复现的分析流程可以简化成下面这段伪代码：

```text
routes = 历史 74HC595 Charlie 路由表
patterns = 所有 5-bit 输出模式（1~30）

for pattern in patterns:
    Lit(pattern) = {}
    for route in routes:
        if pattern[route.high] == 1 and pattern[route.low] == 0:
            Lit(pattern).add(route)

for value in 0..100:
    for scene in {SOC only, SOC+percent, SOC+percent+charge}:
        T = BuildTarget(value, scene)
        perfect = false

        for every pattern sequence P1..Pk where k <= 5:
            if Lit(Pi) ⊆ T for all i:
                if Union(Lit(P1)..Lit(Pk)) == T:
                    perfect = true
                    break

        if perfect:
            标记该 value 可逻辑完美显示
        else:
            继续寻找“额外段最少”的近似序列
```

这里最关键的判断只有一句：

```text
Lit(Pi) ⊆ T
```

一旦某个 `Pi` 点亮了目标外路由，就说明这个值只能做近似显示，不能算完美显示。

## 7. 全量穷举结果

## 7.1 不同业务场景下，可逻辑完美显示的数量

| 场景 | 可逻辑完美值数量 |
| --- | --- |
| `SOC only` | `8 / 101` |
| `SOC + 百分号` | `7 / 101` |
| `SOC + 百分号 + 充电图标` | `12 / 101` |

## 7.2 `SOC + 百分号` 可逻辑完美显示的值

仅有：

```text
64, 68, 69, 85, 86, 88, 89
```

也就是说，若百分号常亮，而充电图标按需显示，那么：

- `0~100` 中绝大多数值都无法做到零鬼影

## 7.3 `SOC + 百分号 + 充电图标` 可逻辑完美显示的值

仅有：

```text
0, 8, 60, 64, 68, 69, 80, 85, 86, 88, 89, 100
```

可以看出：

- 充电图标并不是纯粹“让问题更难”
- 有些场景里，充电图标反而把原本不可避免的额外点亮段“吸收”为合法目标段

例如：

- `0 + 百分号` 不完美
- `0 + 百分号 + 充电图标` 却变成完美

## 7.4 最少额外点亮段数量分布

下面的“额外点亮段数量”指的是：

- 在最优 pattern 组合下
- 仍然不可避免会被点亮的**非目标路由数**

### `SOC + 百分号`

| 最少额外段数 | 个数 |
| --- | --- |
| `0` | `7` |
| `1` | `17` |
| `2` | `32` |
| `3` | `31` |
| `4` | `10` |
| `5` | `3` |
| `6` | `1` |

### `SOC + 百分号 + 充电图标`

| 最少额外段数 | 个数 |
| --- | --- |
| `0` | `12` |
| `1` | `22` |
| `2` | `41` |
| `3` | `19` |
| `4` | `6` |
| `5` | `1` |

这个分布非常重要，因为它说明：

- 当前硬件不是“只有少数边界值不完美”
- 而是**绝大多数值至少会多亮 1~3 段**

## 8. 典型值是怎么分析出来的

这一节给出几个代表性例子，说明“为什么有的值行，有的不行”。

### 8.1 `0 + 百分号`：不完美

历史方案选中的 pattern 序列是：

```text
[1, 14]
```

目标段：

- 个位 `0` 的六段：`ONES_A/B/C/D/E/F`
- `ICON_PERCENT`

实际除了目标段外，还会额外点亮：

```text
ICON_CHARGE
```

所以肉眼现象是：

- `0%` 能看出来
- 但**充电图标会误亮**

这类问题在产品上通常表现为：

- 明明没有充电，图标却弱亮或常亮

### 8.2 `1 + 百分号`：不完美，而且鬼影明显

历史方案 pattern 序列：

```text
[2, 3]
```

目标段：

- 个位 `1`：`ONES_B`, `ONES_C`
- `ICON_PERCENT`

实际额外点亮：

```text
H1_lower, TENS_A, ONES_E
```

也就是：

- 百位“1”的下半段
- 十位 A 段
- 个位 E 段

肉眼现象：

- 主体仍像 `1%`
- 但会伴随几根**不该出现的暗段**
- 尤其在低环境光下，鬼影会很明显

### 8.3 `41 + 百分号`：不完美，且失真严重

历史方案 pattern 序列：

```text
[4, 11, 18]
```

目标段：

- 十位 `4`
- 个位 `1`
- `ICON_PERCENT`

实际额外点亮：

```text
H1_upper, H1_lower, TENS_A, ONES_D, ONES_G, ICON_CHARGE
```

这已经不是“轻微伴亮”，而是：

- 百位“1”都会被带出来
- 十位会多一段
- 个位会多两段
- 充电图标还会误亮

肉眼现象：

- 显示形状明显变形
- 不再是“只有专业人士才能看出的鬼影”
- 而是普通用户也会直接觉得显示不对

### 8.4 `64 + 百分号`：逻辑完美，但视觉不完全完美

这是一个可以逻辑完美显示的值。

历史方案 pattern 序列：

```text
[10, 8, 16, 11]
```

额外点亮：

```text
无
```

所以从“有没有鬼影”这个角度，它是完美的。

但目标段的占空比并不均匀，例如：

- 某些目标段只亮 `1/4` 帧
- 某些目标段亮 `2/4` 帧
- `TENS_C` 甚至达到 `3/4` 帧

肉眼现象：

- 没有额外错误段
- 但各段亮度不完全一致
- 某些笔画会比其他笔画更亮

因此：

- `64` 是**逻辑完美**
- 但不是严格意义上的**视觉完美**

### 8.5 `100 + 百分号` 与 `100 + 百分号 + 充电图标`

这是一个很有代表性的反例。

#### `100 + 百分号`

历史方案 pattern：

```text
[2, 5, 25, 12]
```

额外点亮：

```text
ICON_CHARGE
```

现象：

- `100%` 主体能显示
- 但会带出**充电图标误亮**

#### `100 + 百分号 + 充电图标`

同样的 pattern：

```text
[2, 5, 25, 12]
```

此时 `ICON_CHARGE` 已经是合法目标段，所以：

- 额外段变为 `0`
- 该场景可逻辑完美显示

这个例子说明：

- 有些“额外段”不是绝对坏事
- 当业务需求把它纳入目标时，原本不完美的显示可能转为完美

## 9. 为什么有些值可以完美，有些值不可以

本质原因是：

- `74HC595` 给的是**5 根线同时的高低电平模式**
- 而不是“单段选择”
- 所以每个 pattern 天然就会点亮一组**耦合路由**

某个目标值能否完美显示，取决于：

- 它的目标段集合，是否能写成若干个“安全 pattern 路由集合”的并集

换句话说：

- 不是数字本身决定能不能显示
- 而是“这个数字需要的路由组合”是否刚好吻合已有 pattern 的自然耦合关系

这也解释了为什么可完美值会集中在：

```text
64, 68, 69, 85, 86, 88, 89
```

这些值的段组合，恰好比较接近某些 pattern 的天然并集。

## 10. 图标为什么有时让结果更好，有时更差

图标有双重作用：

### 10.1 图标会增加目标集合

新增目标段意味着：

- 要额外覆盖更多路由
- 这可能迫使系统引入新的 pattern
- 从而带出更多额外段

例如：

- `8` 在 `SOC only` 场景可逻辑完美显示
- 但加上 `百分号` 后，反而不再完美

### 10.2 图标也可能“吸收”额外段

如果某个 pattern 原本不可避免会带出 `ICON_CHARGE`，
而当前业务恰好又要求显示 `充电图标`，那么这个“额外段”就不再是问题。

例如：

- `0 + 百分号` 不完美
- `0 + 百分号 + 充电图标` 反而完美

因此：

- 图标不是单向增加难度
- 它有时是负担，有时是“补偿项”

## 11. 肉眼会看到什么现象

结合穷举结果和历史 pattern 表，当前硬件上的实际现象可以分为四类。

### 11.1 假图标

表现：

- 不该亮的 `充电图标` 或 `百分号` 被带亮

典型例子：

- `0 + 百分号` 会误亮 `充电图标`
- `100 + 百分号` 也会误亮 `充电图标`

### 11.2 暗亮段 / 鬼影段

表现：

- 某些本不属于当前数字的段，以较低亮度出现

典型例子：

- `1 + 百分号`
- `67 + 百分号`

这种现象肉眼看起来像：

- 数字旁边多了几根淡淡的线
- 或某个数码管轮廓被“拖尾”

### 11.3 形状失真

表现：

- 多亮段数量较多
- 最终显示形状已经明显变形

典型例子：

- `41 + 百分号`

当最少额外段达到 `4~6` 时，已经不建议再用“轻微鬼影”来形容，而应认为是**显示失真**。

### 11.4 亮度不均

即使是逻辑完美值，也常见：

- 某些目标段亮度更高
- 某些目标段亮度更低

原因是：

- 不同目标段在 frame 中出现次数不同

典型例子：

- `64 + 百分号`
- `100 + 百分号 + 充电图标`

其中只有极少数场景能做到目标段占空比一致。按历史表看：

- `0 + 百分号 + 充电图标`

是少数接近“逻辑完美 + 占空一致”的例子。

## 12. 工程含义

基于以上分析，当前硬件能做到的事情和做不到的事情可以明确分开。

### 12.1 做不到的

- 保证 `0~100` 全范围都零鬼影
- 同时支持 `百分号`、`充电图标`，仍然零鬼影
- 保证全范围亮度均匀

### 12.2 能做到的

- 做一个“查表近似显示”版本
- 对大多数值显示出可辨识数字
- 在少量特定值上做到逻辑精确
- 在部分业务场景下把“不可避免的额外段”转化为合法图标

### 12.3 如果业务要求是“产品级完美”

那当前硬件不满足。

这里的“不满足”不是软件未完成，而是：

- **输出自由度不足**
- `74HC595` 的 5 根两态输出不能提供理想 Charlieplex 所需的每线独立高阻

### 12.4 如果业务要求是“可读、可量产、但允许一定妥协”

那当前硬件仍然可以尝试：

- 做近似查表版
- 优先优化常见电量区间
- 对鬼影最严重的值做特殊降级策略

## 13. 最终结论

最终结论可以压缩成四句话：

1. 当前 `74HC595 + 5pin Charlieplexing` 组合，**不能**保证 `0~100 + 百分号 + 充电图标` 全范围完美显示。
2. “是否能完美显示”可以通过**穷举 30 个 5-bit pattern 与目标路由集合**严格判定，不是经验判断。
3. 在 `SOC + 百分号` 场景下，只有：
   - `64, 68, 69, 85, 86, 88, 89`
   可以逻辑完美显示。
4. 在 `SOC + 百分号 + 充电图标` 场景下，只有：
   - `0, 8, 60, 64, 68, 69, 80, 85, 86, 88, 89, 100`
   可以逻辑完美显示。

如果把“亮度均匀”也纳入“完美”的定义，那么能称得上真正视觉完美的值会更少。
