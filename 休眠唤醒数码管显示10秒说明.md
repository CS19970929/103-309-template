# 上电复位与休眠唤醒数码管显示 10 秒说明

## 需求

MCU 上电或复位后重新运行主程序时，数码管需要主动显示 SOC 10 秒；如果 10 秒内没有按键、`MCU_WK` 有效等其他显示事件，再关闭显示。休眠后有效唤醒最终也会通过 `MCU_RESET()` 重新进入主程序，因此同样走这条启动显示规则。

## 当前实现

- 普通短按显示窗口保持 `PROJECT_CFG_LEDBAR_SOC_DISPLAY_10MS = 500`，即 5 秒。
- 启动/复位显示窗口使用 `PROJECT_CFG_LEDBAR_WAKEUP_DISPLAY_10MS = 1000`，即 10 秒；代码侧通过 `LEDBAR_STARTUP_DISPLAY_10MS` 表达其启动显示语义。
- `APP_LedBar()` 正常运行后，首次通过 `LedBar_ServiceStartupDisplayWindow()` 只触发一次 10 秒 SOC 显示窗口，不再依赖 `SleepDeal_IsBootFromSleepStartup()`。
- 显示窗口请求采用“只延长不缩短”策略，10 秒启动窗口尚未结束时，普通短按不会把剩余显示时间缩短成 5 秒。
- 10 秒窗口结束后，如果没有 `MCU_WK` 有效、按键仍按下、短按显示窗口等其他显示请求，`APP_LedBar()` 会调用 `LedBar_Clear()` 关闭显示。

## 修改范围

- `103 + 309/Project/Source/LedBar.h`
- `103 + 309/Project/Source/LedBar.c`
- `休眠唤醒数码管显示10秒说明.md`

## 验收点

1. 冷上电、手动复位、休眠唤醒后重新进入主循环，数码管显示 SOC 约 10 秒。
2. 10 秒内如果没有新的按键、`MCU_WK` 或其他显示请求，显示自动关闭。
3. 普通短按显示仍保持约 5 秒，不被唤醒显示窗口改成 10 秒。
