# 休眠唤醒数码管显示 10 秒说明

## 需求

休眠后通过开关有效唤醒时，MCU 会按休眠启动标志走一次复位后的启动恢复流程。用户要求这次唤醒后数码管显示 SOC 10 秒；如果 10 秒内没有其他显示事件，再关闭显示。

## 当前实现

- 普通短按显示窗口保持 `PROJECT_CFG_LEDBAR_SOC_DISPLAY_10MS = 500`，即 5 秒。
- 休眠后 MCU 复位唤醒新增独立窗口 `PROJECT_CFG_LEDBAR_WAKEUP_DISPLAY_10MS = 1000`，即 10 秒。
- `IsSleepStartUp()` 识别到 `FLASH_HICCUP_SLEEP_VALUE`、`FLASH_NORMAL_SLEEP_VALUE` 或 `FLASH_DEEP_SLEEP_VALUE` 后，会让 `SleepDeal_IsBootFromSleepStartup()` 返回有效。
- `APP_LedBar()` 首次看到 `SleepDeal_IsBootFromSleepStartup()` 有效时，只触发一次 10 秒 SOC 显示窗口。
- 显示窗口请求采用“只延长不缩短”策略，10 秒唤醒窗口尚未结束时，普通短按不会把剩余显示时间缩短成 5 秒。
- 10 秒窗口结束后，如果没有 `MCU_WK` 有效、按键仍按下、短按显示窗口等其他显示请求，`APP_LedBar()` 会调用 `LedBar_Clear()` 关闭显示。

## 修改范围

- `103 + 309/Project/Source/conf/Project_Config.h`
- `103 + 309/Project/Source/LedBar.h`
- `103 + 309/Project/Source/LedBar.c`
- `项目宏定义梳理.md`

## 验收点

1. 休眠后长按开关达到有效唤醒条件，进入主循环后数码管继续显示 SOC 约 10 秒。
2. 10 秒内如果没有新的按键、`MCU_WK` 或其他显示请求，显示自动关闭。
3. 普通短按显示仍保持约 5 秒，不被唤醒显示窗口改成 10 秒。
