# 13 — 后续维护建议

## 优先级 1：立即处理（安全相关）

### 1.1 修复 SW OVP 阈值与 AFE 配置脱钩

```
文件: DataDeal.c:1135-1153
问题: 硬编码 4280mV 应改为读取 AFE_Parameters_RS485_Struction.u16VcellOvp.curValue
影响: LiFePO4 电池下保护行为不一致
预估工作量: 30 分钟
```

### 1.2 统一化学体系阈值定义

```
当前: Fault.h, SH367309_Func.h, SH367309_DataDeal.h, DataDeal.h 各自 #ifdef
建议: 创建 BatteryChemistry.h 作为唯一来源，其他文件引用
预估工作量: 2-4 小时 (需要仔细验证每个阈值的一致性)
```

### 1.3 修复均衡电压硬编码

```
文件: SH367309_DataDeal.c:75
修改: temp = (4160 + 10) / 20; → temp = (OtherElement.u16Balance_OpenVoltage + 10) / 20;
预估工作量: 5 分钟
```

### 1.4 确认熔丝触发条件

```
文件: DataDeal.c:1097-1121
待确认: PEC 错误计数阈值 (2次) 和 5 秒确认时间是否保守
建议: 增加确认次数或延长确认时间，防止误触发
```

## 优先级 2：短期优化（功能改进）

### 2.1 HICCUP 阻塞改为状态机

```
文件: rtc_sleep.c:285-287
问题: while(rtc_sleep_run_hiccup_cycle()) 阻塞主循环
建议: 改为状态机，每轮主循环执行一个 HICCUP 步骤，不阻塞
预估工作量: 4-8 小时
```

### 2.2 64 位运算替换为 32 位

```
文件: DataDeal.c:773, SOC.c:34-38
问题: int64_t/uint64_t 在 Cortex-M3 上极慢
建议: 验证数据范围后用 uint32_t 替代
预估工作量: 1-2 小时 + 测试验证
```

### 2.3 清理死代码

```
文件: DataDeal.c, Sci_Upper.c, SH367309_Func.c, EEPROM.c, I2C_AFE1.c
内容: 删除 #if 0 块、空函数、未调用函数
预估工作量: 1 小时
```

### 2.4 统一 AFE 数据访问接口

```
当前: DataLoad_CellVolt() 直接访问 SH367309_Read_AFE1.u16VCell[]
建议: 通过 AFE_GetCellVoltage(index) 函数访问
收益: AFE 类型切换时只需修改一处
预估工作量: 2-4 小时
```

### 2.5 补充 HardFault 启动诊断

```
当前: HardFault_Handler 保存原因码到 BKP_DR16，启动后未读取
建议: 在 InitE2PROM() 之前读取 BKP_DR16，记录到事件日志
预估工作量: 1 小时
```

## 优先级 3：中期优化（架构改进）

### 3.1 重命名遗留函数

| 当前名称 | 建议名称 | 文件 |
|---------|---------|------|
| `UpdateVoltageFromBqMaximo()` | `UpdateVoltageFromAFE1()` | `I2C_AFE1.c:719` |

### 3.2 Flash 存储槽位选择逻辑去重

```
文件: Flash.c
问题: LoadPair/SavePair/LoadJournal/SaveJournal 中槽位选择逻辑4处重复
建议: 提取为 StorageFlash_SelectSlot() 静态函数
预估工作量: 2-3 小时
```

### 3.3 简化 Sci_Upper 地址窗口解析

```
文件: Sci_Upper.c:293 (Sci_Deal_ReadRegs_0x03)
问题: 7+ 级 if-else 链
建议: 改为查表结构
预估工作量: 4-8 小时
```

### 3.4 拆分 new_todo_logi()

```
文件: DataDeal.c:1052-1190
问题: 140 行, 5 层嵌套, 混合 MOS 保护、熔丝保护、SW OVP、温度保护
建议: 拆分为 MosOTP_Monitor(), Fuse_Protect(), SW_OVP_Monitor() 等独立函数
预估工作量: 3-4 小时
```

### 3.5 拆分 DataDeal.c 中的 GPIO 操作

```
问题: open_ctlc(), close_ctlc(), GPIO_RF_EN 等于数据层直接控制硬件
建议: 移到 MosStartup 或新建 SafetyActuator 模块
预估工作量: 3-4 小时
```

### 3.6 SOC 空尾插值改为查表

```
文件: SocEnhance.c:130 (soc_empty_tail_interpolate())
问题: 11 分支 if-else 硬编码断点
建议: 改为 const struct {offset_mV, target_dsg, target_relax}[] 查表
预估工作量: 1 小时
```

## 优先级 4：长期维护

### 4.1 完善编译时检查

- 增加 AFE 配置参数的静态范围断言
- 增加 `sizeof(FLASH_STORAGE_*)` 不超页面的编译断言
- 增加化学体系阈值一致性的编译检查

### 4.2 建立参数变更影响分析

- 每当修改保护阈值时，输出影响矩阵（影响哪些文件/模块）
- 建立参数版本变更日志

### 4.3 考虑自动化测试方案

- 当前完全依赖硬件测试
- 建议：利用现有 RS485 协议编写 Python 测试脚本
  - 保护参数边界值测试
  - SOC 校准逻辑单元测试（离线数据注入）
  - Flash 存储读写循环测试

### 4.4 与 `todo.md` 同步

`todo.md` 中有 5 个未完成项 (`- [ ]`)，建议逐项评估：

| 项 | 内容 | 评估 |
|-----|------|------|
| item 1 | 使用备份域实现休眠唤醒，测试 AFE 均衡功耗、电压 | 已在 RTC 休眠系统中部分实现 |
| item 2 | 分口、低功耗逻辑 | 待确认 "分口" 含义 |
| item 3 | 全局 debug 变量统一 | debug_hub 已实现集中管理 |
| item 4 | 调研 STM32F0、F1 的 RTC、备份域、低功耗 | 研究性质 |
| item 5 | 梳理完善 rtc_sleep | 已在本文档中梳理 |

## 维护建议优先级总结

```
P1 (安全) ───── 4 items, ~3-6 小时
  ├── SW OVP 阈值修复
  ├── 化学体系阈值统一
  ├── 均衡电压修复
  └── 熔丝条件确认

P2 (功能) ───── 5 items, ~8-16 小时
  ├── HICCUP 状态机
  ├── 64 位运算替换
  ├── 死代码清理
  ├── AFE 接口统一
  └── HardFault 诊断

P3 (架构) ───── 6 items, ~16-30 小时
  ├── 遗留命名重命名
  ├── Flash 去重
  ├── Sci 查表
  ├── new_todo_logi 拆分
  ├── GPIO 操作分层
  └── SOC 查表

P4 (长期) ──── 4 items, 持续
  ├── 编译时检查
  ├── 参数变更分析
  ├── 自动化测试
  └── todo.md 同步
```

## 与 `/simplify` 审查的关系

本文档的部分建议来源于之前运行的 `/simplify` 审查 (4-agent 并行 scan)，经过手工过滤去除了误报后采纳。详细的逐行发现见 `/simplify` 运行结果。
