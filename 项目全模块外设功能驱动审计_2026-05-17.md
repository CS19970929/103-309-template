# 项目全模块、外设、功能、驱动审计

日期：2026-05-17

## 1. 审计范围与结论

本次审计范围覆盖 `103 + 309/Project/Source` 业务源码、`STM32F10x_StdPeriph_Lib_V3.5.0` 中当前工程引用的启动/外设库、Keil 工程 `103 + 309/Project/Users/CommomSH367309_16series_103RCT6_C.uvprojx`、根目录已有架构文档和 `tools/project_check.py` 静态门禁。

结论分三层：

| 层级 | 判断 |
| --- | --- |
| 主运行架构 | 已经形成“初始化 + TIM3 10ms tick + 主循环拉取任务 flag + 200ms AFE/SOC 主采样”的稳定架构。 |
| 核心功能 | SOC、CAN 低功耗、RTC STOP、Flash 双槽/日志、LedBar、测试模式隔离已经有明确边界，整体比早期代码更可维护。 |
| 主要风险 | Keil App 起始地址/Flash 容量配置仍存在源控不一致；后 64K Flash 存储依赖真实 128KB Flash；部分旧保护/驱动代码未完全退出；`DataDeal.c` 仍承担过多业务决策。 |

本次只做源码和文档审计，没有烧录实板，没有运行 Keil/ARMCC 编译。已执行：

```bash
python3 tools/project_check.py
```

结果：`OK: 100, Warnings: 0, Errors: 0`。

## 2. 官方资料依据

STM32 外设判断主要按以下 ST 官方资料核对：

| 资料 | 用途 |
| --- | --- |
| [RM0008 STM32F10xxx reference manual](https://www.st.com/resource/en/reference_manual/cd00171190-stm32f101xx-stm32f102xx-stm32f103xx-stm32f105xx-and-stm32f107xx-advanced-arm-based-32-bit-mcus-stmicroelectronics.pdf) | GPIO/AFIO、RCC、EXTI、DMA、ADC、TIM、CAN、USART、RTC、PWR/BKP、IWDG、Flash 控制器寄存器依据。 |
| [STM32F103x8/xB datasheet DS5319](https://www.st.com/resource/en/datasheet/stm32f103c8.pdf) | STM32F103C8/CB 资源、Flash/SRAM、外设数量、低功耗能力、封装/引脚能力依据。 |
| [PM0075 STM32F10xxx Flash programming manual](https://www.st.com/resource/en/programming_manual/cd00283419-stm32f10xxx-flash-memory-microcontrollers-stmicroelectronics.pdf) | 内部 Flash 擦写、页大小、写入/擦除约束依据。 |
| [PM0056 Cortex-M3 programming manual](https://www.st.com/resource/en/programming_manual/pm0056-stm32f10xxx20xxx21xxxl1xxxx-cortexm3-programming-manual-stmicroelectronics.pdf) | NVIC、SysTick、异常/中断行为依据。 |
| [ES096 STM32F10xx8/B errata](https://www.st.com/resource/en/errata_sheet/es096-stm32f101x8b-stm32f102x8b-and-stm32f103x8b-mediumdensity-device-limitations-stmicroelectronics.pdf) | 中等容量 STM32F103x8/B 器件限制核对。 |
| [STSW-STM32054 STM32F10x Standard Peripheral Library](https://www.st.com/en/embedded-software/stsw-stm32054.html) | 当前 SPL V3.5.0 工程库来源和 API 使用依据。 |

## 3. 工程入口与运行主线

### 3.1 启动入口

主入口在 `main.c`：

1. `main()` 调用 `InitDevice()`。
2. 调用 `InitVar()` 初始化系统功能、事件记录、Boot flag。
3. 调用 `Init_RTC()` 初始化 RTC。
4. 进入 `while (1) Runtime_RunOnce();`。

`InitDevice()` 当前主要初始化顺序：

1. `SystemInit()`：配置系统时钟。
2. `InitDelay()`：基于 `SystemCoreClock/8` 配置 SysTick 延时参数。
3. `IsSleepStartUp()`：判断睡眠启动状态。
4. `jtag_disableAndConfIO()`：禁用 JTAG，保留 SWD，使 PA15/PB3/PB4 可作 GPIO。
5. `InitNVIC()`：配置 NVIC 分组。
6. `InitIO()`：GPIO 初始状态。
7. `InitSci()`：SCI/RS485 上位机通信。
8. `InitSystemWakeUp()`：唤醒源配置。
9. `StorageFlash_PrintBootCheck()`：启动时打印 Flash 存储区状态。
10. `InitE2PROM()`：加载默认/Flash 参数。
11. `InitAFE1()`：初始化 SH367309 AFE。
12. `InitCan()`：初始化 CAN。
13. `InitADC()`：初始化 ADC1 + TIM2 触发 + DMA。
14. `InitData_SOC()`：初始化 SOC。
15. `InitTimer()`：启动 TIM3 10ms 系统 tick。
16. `Init_IWDG()`：量产打开 IWDG。

### 3.2 主循环调度

主循环入口在 `Runtime.c`。当前 `Runtime_RunOnce()` 顺序：

| 阶段 | 调用 | 说明 |
| --- | --- | --- |
| tick 归并 | `SysTime_LatchTaskFlags()` | 将 TIM3 ISR 置位的 pending flag 原子搬到主循环使用。 |
| 出厂老化 | `FactoryAging_Task()` | 只累计 MCU 正常运行态 tick，不累计 RTC/STOP 时间。 |
| 显示 | `APP_LedBar()` | LedBar 逻辑和显示窗口管理。 |
| AFE 主采样 | `App_AFEGet()` | 消费 200ms pending period，完成 AFE 数据、保护状态和 SOC 更新。 |
| 通信/模拟量/低功耗 | `App_Sci()`、`App_AnlogCal()`、`App_LowPowerProcess()`、`App_Can()` | 上位机、ADC 平滑、RTC/STOP、CAN 发送。 |
| 后台 | `StorageFlash_AppUseTest_Task()`、`App_FlashUpdate()`、`App_LogRecord()`、`App_ProID_Deal()` | 存储测试、IAP 标志、事件记录、生产信息。 |
| 看门狗 | `Feed_IWatchDog` | 每轮喂狗。 |

关键点：`App_SOC()` 不在主循环独立跑，而是在 `App_AFEGet()` 成功产生新 AFE sample 后触发，避免 SOC 使用重复采样。

## 4. 源码模块逐项分析

### 4.1 `main.c / Runtime.c`

定位：系统入口、初始化顺序、主循环任务编排。

现状：

- 初始化顺序基本合理，AFE、CAN、ADC、SOC、TIM3 的依赖关系清楚。
- `Runtime_RunOnce()` 已经从散乱 `while(1)` 收口成一个可读任务序列。
- AFE/SOC 使用 200ms pending period，避免主循环卡顿时完全丢采样。

问题：

- `Runtime.c` 中 `App_WarnCtrl()` 仍被注释，说明旧软件保护链路已经不再作为主保护路径，但源码仍大面积保留。
- `Runtime_RunOnce()` 仍直接串行调用所有任务，没有任务耗时统计。某个任务阻塞时，只能靠 200ms pending overflow 间接发现。

建议：

- 增加轻量 `runtime_task_watch` 或调试计数，记录各任务最大耗时和 `SysTime_Get200msTaskOverflowCount()`。
- 将“主动运行模块”和“历史兼容模块”在文档或配置中明确标记。

### 4.2 `System_Init.c`

定位：TIM3 系统 tick、SysTick 延时、NVIC、IWDG。

现状：

- `InitTimer()` 使用 TIM3，目标 100kHz 计数、Period 999，形成 10ms tick。
- ISR 只做 flag 和计数，主循环通过 `SysTime_LatchTaskFlags()` 拉取。
- 50ms、100ms、200ms、1000ms flag 都由 TIM3 tick 派生。
- 200ms 使用 `s_u8Sys200msPendingPeriods`，最大缓存 5 个周期，超过后记录 overflow。
- IWDG 在普通模式约 1.28s，RTC 功能打开时配置为更长窗口，适配 STOP/RTC 周期。

问题：

- 注释里仍有“非 PCLK1 所以 72MHz”等旧假设，但当前 `system_stm32f10x.c` 实际选的是 `SYSCLK_FREQ_HSE HSE_VALUE`，即 8MHz HSE 路径。
- `.uvprojx` 中 `CLOCK(12000000)` 与源码 `HSE_VALUE=8000000` 不一致，容易误导 Keil 调试/时钟理解。

建议：

- 保留动态 `SystemCoreClock` 计算方式，但清理 72MHz 旧注释。
- 将 Keil target clock、`HSE_VALUE`、硬件晶振频率统一成一个明确规则，并加入 `project_check.py`。

### 4.3 `conf/Project_Config.h / Project_BuildGuard.h / conf.h`

定位：产品配置、编译档位、量产/测试隔离。

现状：

- `PROJECT_CFG_BUILD_PROFILE` 明确区分 Release/Debug/Factory-Test。
- SOC 测试模式、LedBar always-on、Flash64K 测试、debug watch、升级参数强制重置等都被 BuildGuard 阻断。
- Release 默认 `PROJECT_CFG_WDOG_ENABLE=1`、`PROJECT_CFG_SOC_TEST_MODE_ENABLE=0`。
- `conf.h` 将可视化配置转换为旧宏，例如 `__FUNC_RTC__`、`_IAP`、`SOC_TEST_MODE_ENABLE`。

问题：

- BuildGuard 目前主要检查功能宏，没有检查 App 起始地址、Flash 容量、startup 文件密度和实际器件型号。
- `PROJECT_CFG_HEAT_ENABLE=0` 时，热管理编译关闭，但 `System_Monitor.c` 默认功能位仍会把 Heat 置 1，容易造成通信状态理解混乱。

建议：

- `project_check.py` 增加：Keil IROM/TextAddressRange、scatter 文件、Flash 存储地址、startup 文件与 `STM32F10X_MD` 一致性检查。
- 对 Heat/Cool 这类硬件可选功能，运行状态位也应从 `PROJECT_CFG_HEAT_ENABLE` 派生，避免“编译无功能但状态位默认开启”。

### 4.4 `conf.c / conf_gpio.h`

定位：GPIO 初始态、低功耗 GPIO 态、EXTI 唤醒源、STOP 恢复。

现状：

- `conf_gpio.h` 集中定义主要 IO：
  - PA0 `CHG_IN`
  - PA1 `ADC_VBUS`
  - PA2 `ADC_CUR`
  - PA3 `2727_EN`
  - PA8 `MCC_C`
  - PA9 `SW`
  - PA10 `DC_EN`
  - PA11/PA12 CAN
  - PA15 `M_STB`
  - PB0 `AFE1_PRO_EN`
  - PB1 `ADC_NMOS`
  - PB3 `AD_EN`
  - PB4 `CMNT_EN`
  - PB5 `ADC_BUS_EN`
  - PB6/PB7 USART1 remap
  - PB8/PB9 software I2C for SH367309
  - PB10/PB11/PA4/PA5/PA6 LedBar Charlieplexing
  - PB12 `INT_WK_CMNT`
  - PB13 `MCU_WK`
  - PB14 `AFE1_CTL`
  - PB15 debug LED
- `IOstatus_Base()` 和 `IOstatus_RTCMode()` 会在低功耗前将大多数 GPIO 转模拟输入，并关闭电源控制脚。
- STOP 唤醒后通过 `InitRunAfterStopWakeup()` / `RestoreRunPeripheralsAfterStop()` 重建 ADC、SCI、CAN、TIM3 等。

问题：

- GPIO 复用风险主要来自 PA2/PA3：默认 SCI2 关闭时 PA2 可作为 Type-C ADC，PA3 可作 `2727_EN`；若未来启用 USART2，会与现有硬件定义冲突。
- PA15/PB3/PB4 依赖 JTAG disable，必须保证 SWD 下载策略和量产调试方式一致。

建议：

- 增加一份机器可读的 pin ownership 表，用脚本检查 USART2、TIM2 remap、LedBar、ADC 与 GPIO 电源脚的冲突。
- 当前 PB3 的 TIM2_CH2 remap 代码已被 `#if 0` 关闭，这是正确方向；ADC 外部触发不需要把 TIM2_CH2 引脚输出到 PB3。

### 4.5 `ADC.c`

定位：ADC1 + DMA1 + TIM2 触发，负责 MOS 温度、Type-C 电流、总压分压采样。

现状：

- ADC1 独立扫描模式，外部触发源 `ADC_ExternalTrigConv_T2_CC2`。
- TIM2 使用 `SystemCoreClock/100000 - 1` 和 Period 999，形成约 10ms 触发。
- DMA1_Channel1 循环搬运 3 个 halfword：PB1 `ADC_Channel_9`、PA2 `ADC_Channel_2`、PA1 `ADC_Channel_1`。
- DMA 中断配置块已被注释掉，因此不会触发缺省 `DMA1_Channel1_IRQHandler` 死循环。
- `App_AnlogCal()` 按 10ms tick 追赶处理，并把单轮 catch-up 限制在 10 个 tick。
- Type-C 电流独立按 PA2 分流电阻压降计算，再给 SOC 做电池侧等效电流换算。

问题：

- `ADC.c` 中旧注释仍提到 DMA 中断“后面看看是否需要”，容易被误打开；一旦打开但不实现 ISR，启动文件弱中断会 `B .` 卡死。
- ADC 通道含义与 `ADC.h` 旧枚举命名有历史包袱，`ADC_CURR`、`ADC_CUR_AMP`、`ADC_VBC` 需要继续保持文档同步。

建议：

- 明确写注释：DMA 只用于循环搬运，不启用 TC interrupt。
- 对 `ADC_NUM`、DMA buffer size、RegularChannelConfig 顺序增加脚本检查，防止后续通道顺序改错。

### 4.6 `I2C_AFE1.c / SH367309_Func.c / SH367309_DataDeal.c`

定位：SH367309 AFE 软件 I2C/MTP 读写、参数映射、硬件保护状态、MOS 控制。

现状：

- AFE 通信用 PB8/PB9 软件 TWI，`I2C_AFE1.h` 中 `_TWI_COM` 生效。
- `InitAFE1()` 初始化 AFE、执行启动零点、应用 AFE 参数、根据出厂老化/普通模式打开 MOS。
- `UpdateVoltageFromBqMaximo()` 周期读取 AFE 寄存器，更新 cell voltage、temperature、current raw。
- `SH367309_UpdataAfeConfig()` 将上位机参数转换到 AFE MTP 配置，有差异时写入。
- `SH367309_Func.c` 会把 AFE 硬件保护状态映射到 `g_stCellInfoReport.unMdlFault_Third`，并做故障记录。

问题：

- AFE 层同时承担“通信驱动、参数转换、保护状态映射、MOS 控制”，模块边界偏宽。
- MTP 参数写入失败后的恢复策略主要靠错误计数和后续重试，缺少“本次写入字段/失败字段”的可观测记录。

建议：

- 拆成三层：`afe_bus`、`afe_sh367309_driver`、`afe_protection_adapter`。
- 对 MTP 写入增加版本/字段级日志，避免现场只看到 `ERROR_AFE1` 或 `ERROR_EEPROM_STORE`。

### 4.7 `DataDeal.c`

定位：AFE 周期采样后的数据装载、温度/电流计算、业务状态联动。

现状：

- `App_AFEGet()` 是 200ms 主链路：读取 AFE、装载 cell/temperature/current、执行 AFE 状态处理、调用 SOC。
- 电流链路包含启动零点、运行时 auto-zero、200mA 输出 deadband。
- `g_u32AfeCurrentSampleSeq` 用于保证 SOC 只消费新样本。

问题：

- `new_todo_logi()` 混合了充电器检测、RF_EN、MOS 温度保护、故障记录、老化/认证等逻辑，已经超出“DataDeal”的职责。
- `DataLoad_Temperature()` 中部分环境温度被固定成默认值，长期看应明确是“未接传感器”还是“临时屏蔽”。
- `DataDeal.c` 仍调用 `App_DI1_Switch()`，显示/按键逻辑已经迁到 LedBar 后，这里仍有历史耦合。

建议：

- `DataDeal.c` 只保留采样数据装载和归一化。
- 将 `new_todo_logi()` 拆为 `charger_state`、`mos_thermal_guard`、`rf_power_ctrl`、`cert_mode` 等小模块。
- `App_DI1_Switch()` 如果只服务 LedBar/按键显示，应归 LedBar 或 input manager。

### 4.8 `SOC.c / SocEnhance.c`

定位：SOC 初始化、OCV 表、安时积分、满空锚定、低压尾段、静置/RTC OCV 小步修正、Flash 快照。

现状：

- `SOC.c` 是外部入口和通信参数适配，`SocEnhance.c` 是核心算法。
- 默认三元表可到 100 和 0。
- `SOC_CURRENT_ACTIVE_A10=2`，0.2A 以上进入活跃积分。
- Type-C 输出电流按电池侧等效电流并入净电流。
- 自动校准单次步长由 `PROJECT_CFG_SOC_CALIBRATION_STEP_PERCENT=1` 限制。
- 大电流压降后有 `PROJECT_CFG_SOC_SAG_HOLDOFF_SECONDS` 和 offset 门控。
- 静置 OCV 已从“固定时间立刻校准”演进为“稳定窗口 + deferred target + 小步消化”。
- RTC/STOP 唤醒通过 `SOC_ApplyRtcRelaxationCompensation()` 处理休眠期间 OCV 修正。

问题：

- SOC 逻辑已经较完整，但 `SocEnhance.c` 仍然承担积分、校准、显示、存储、RTC 补偿多个职责。
- `PROJECT_CFG_SOC_CALIBRATION_BLOCK_PROTECTION_FAULT=0`、`PROJECT_CFG_SOC_CALIBRATION_BLOCK_SYSTEM_FAULT=0` 较宽松，体验更顺，但异常工况误校准风险更高。

建议：

- 当前不建议继续叠加复杂算法，优先保持“单 tick 一条主路径”。
- 将 SOC 文档和 host C test 持续绑定，每次参数调整都跑真实 C 源码回放。
- 如果现场出现异常采样或保护中校准问题，再考虑把两个 block fault 开关按产品档位打开。

### 4.9 `Can_HDX.c / CanFeidaoFrames.c`

定位：CAN 初始化、飞道协议帧、低功耗发送、BusOff/ACK 状态。

现状：

- CAN1 默认 PA11/PA12，不启用 CAN remap。
- `GPIO_CMNT_EN/PB4` 控制 CAN 收发器供电，发送前上电，空闲后断电。
- 当前时钟树按 PCLK1=8MHz，Prescaler=4、BS1=5tq、BS2=2tq，目标 250kbps。
- `CAN_NART=ENABLE`，无 ACK 时不自动重发，用于降低未接设备时功耗。
- 周期帧分 1000ms 和 5000ms，RTC 唤醒后 `Can_RtcWakeService()` 有 150 * 10ms bounded window。
- `Can_GetIdleRtcPeriodSeconds()` 根据 bus active 状态返回 1s 或 10s。

问题：

- BusOff 自动恢复是手动拉 INQ/退出 INQ，策略可用，但缺少台架日志说明恢复时间和错误计数阈值。
- RX 目前只标记 bus active，没有协议解析。

建议：

- 保持低功耗发送状态机，不要回到阻塞 `delay + send + off`。
- 增加 CAN 台架验证表：无对端、有对端、BusOff、RTC 唤醒后发送成功率、CMNT_EN 电流曲线。

### 4.10 `Sci_Upper.c / Sci_Upper.h`

定位：USART 上位机协议、Modbus-like 读写、参数配置、SOC 测试入口。

现状：

- SCI1 默认上位机，USART1 remap 到 PB6/PB7，19200 8N1。
- SCI2/SCI3 有通用 runtime 框架，但默认关闭。
- RXNE + IDLE 中断收帧，主循环 `App_Sci()` 处理 frame pending。
- 地址窗口覆盖 `0xD000`、`0xD100`、`0xD200`、`0xD300` 等只读状态，写路径覆盖保护参数、OtherElement、HeatCool、校准、RTC、Flash/IAP、SOC test。
- SOC 注入测试入口受 `PROJECT_CFG_SOC_TEST_MODE_ENABLE` 和 build profile 隔离，量产 `0xD300 supported=0` 是正常状态。

问题：

- `Sci_Upper.c` 文件过大，协议解析、地址校验、参数写入副作用混在一个文件。
- 如果未来打开 SCI2，PA2/PA3 会和 Type-C ADC/2727_EN 冲突。

建议：

- 把 `0x03 read map`、`0x06 single write`、`0x10 multi write` 拆表驱动。
- 对 USART role 与 pin ownership 增加 BuildGuard 或 `project_check.py` 检查。

### 4.11 `RTC.c / rtc_sleep.c / LowPowerSleep.c`

定位：RTC 初始化、STOP 休眠、RTC 周期唤醒、休眠前快照、唤醒恢复。

现状：

- RTC 首选 LSE，失败可走 LSI。
- 使用 Backup domain 标记 RTC 是否已初始化。
- RTC alarm 走 EXTI17 和 `RTCAlarm_IRQHandler()`。
- `RTC_GetWakeupPeriodSeconds()` 根据 CAN idle/active 自适应，同时考虑 IWDG 安全窗口。
- `rtc_sleep.c` 主导 HICCUP STOP 周期：保存状态、配置 RTC/IO/EXTI、进入 STOP、唤醒后恢复 ADC/SCI/CAN/TIM3。
- 休眠前通过 `LowPowerSleep_SaveCoreState()` 保存 CAN、SOC、FactoryAging 状态。

问题：

- `rtc_sleep.c` 体量较大，低功耗条件、异常唤醒判断、CAN 服务、SOC 修正都在一个文件内。
- `isException()` 的条件包含电流、外部通信、低压等，需要长期和产品体验保持同步，否则容易出现误休眠/频繁唤醒。

建议：

- 保持一个低功耗状态机 owner，但拆出 condition evaluator。
- 增加 RTC wake trace：上次阻断原因、进入 STOP 次数、RTC 唤醒次数、异常退出原因，通过 `0xD100` 或 Debug watch 可读。

### 4.12 `Flash.c / Flash.h / EEPROM.c`

定位：内部 Flash 持久化、双槽/日志、升级标志、FactoryAging、旧 EEPROM facade。

现状：

- IAP 起始 `0x08000000`，App 起始宏 `0x08004800`。
- 后 64K 存储区从 `0x0801C000` 开始。
- AFE/RW_PARAM 使用双槽；SOC/LOG/FactoryAging 使用 journal 或 page append。
- Header 包含 magic/version/length/sequence/crc。
- `EEPROM.c` 中外部 EEPROM 读写已基本变成 stub，真实参数持久化走内部 Flash。
- `tools/soc_flash_app_safe.ps1` 固定检查 App 烧录地址为 `0x08004800`。

问题：

1. Keil 工程源控配置与安全规则不一致：
   - `.uvprojx` 中 IROM 和 `TextAddressRange` 仍是 `0x08000000`，`ScatterFile` 为空。
   - ignored 的 `Users/Objects/CommomSH367309_16series_103RCT6_C.sct` 显示 `0x08004800`，但它不是可靠源控真相。
   - 仓库规则指定的 `103 + 309/Project/Users/Objects/FD_Release.sct` 当前不存在；源控里也没有可作为 App scatter 真相源的 `.sct` 文件。
   - 项目规则要求 App 地址 `0x08004800`，这一点必须进入可检查的工程文件或脚本。
2. Flash 容量依赖风险：
   - `.uvprojx` Device 是 `STM32F103C8`，ST datasheet 中 x8/xB 是 64/128KB 系列，真实 C8 常按 64KB 管理。
   - 当前存储地址 `0x0801C000` 到 `0x0801FC00` 明确依赖 128KB Flash。
   - `StorageFlash_PrintBootCheck()` 会在 flash size <128KB 时跳过打印检查，但实际 load/save API 没有统一阻止写入后 64K。
3. `StorageFlash_CalcCrc()` 将 length 转为 `UINT8` 传给 `Sci_CRC16RTU()`，当前 payload 小于 255 字节暂时安全，但后续扩展 RW_PARAM/LOG/SOC 会埋雷。

建议：

- 把 App scatter 文件作为源控文件固定，例如 `103 + 309/Project/Users/Objects/FD_Release.sct` 或更合适的 tracked 路径，并让 `.uvprojx` `useFile=1` 指向它。
- `project_check.py` 强制检查 `0x08004800`、禁止 `0x08000000` App TextAddressRange、检查安全烧录脚本。
- 上电读取 Flash size 后，所有后 64K load/save API 都应统一 gate；如果不是 128KB 器件，不允许写 `0x0801C000+`。
- 将 CRC 函数升级为支持 `UINT16 length` 的实现，去掉 255 字节隐性限制。

### 4.13 `Fault.c / System_Monitor.c / FaultSnapshot.h`

定位：软件保护判断、系统错误标志、故障记录和 BKP snapshot。

现状：

- `System_ERROR_UserCallback()` 是全局错误计数入口，支持 set/remove/status 三类命令。
- `Fault.c` 保留了 first/second/third 大量软件保护检查函数。
- 当前主循环 `App_WarnCtrl()` 注释，主要保护来源已经转到 SH367309 硬件状态映射和业务逻辑直接记录。
- `FaultSnapshot.h` 使用 BKP 保存故障原因。

问题：

- 旧软件保护代码仍大面积存在，且参数写路径仍能修改相关阈值，容易让维护者误判“软件保护仍是主路径”。
- `System_Monitor.c` 中部分功能开关默认与编译宏不完全一致，例如 Heat 默认状态与 `PROJECT_CFG_HEAT_ENABLE`。

建议：

- 明确保护所有权：SH367309 硬件保护为主，MCU 软件保护仅做补充/记录。
- 对未调用的 `Fault.c` first/second check 和旧记录数组标记 deprecated，后续分阶段删除。

### 4.14 `IO_Control.c / IODrivers.c / ShortFunc.c`

定位：历史 MOS/Relay 驱动框架、外部 DI 控制、短路保护参数。

现状：

- `IODrivers.c` 体量很大，覆盖同口/异口、有无预充、Relay/MOS、bootstrap 等多个历史硬件形态。
- 当前 `InitMosRelay_DOx()` 注释，`App_MOS_Relay_Ctrl()` 注释，主 MOS 控制实际更多走 `SH367309_DriverMos_Ctrl()`。
- `App_DI1_Switch()` 仍被 `DataDeal.c` 调用，用于 DI/按键相关逻辑。
- `ShortFunc.c` 写 AFE 短路/过流寄存器，但当前主保护策略更偏 AFE 硬件配置。

问题：

- 代码保留了大量当前产品不用的历史驱动路径，维护成本高。
- 同一 MOS 控制可能存在多个入口：`SH367309_DriverMos_Ctrl()`、旧 `Drivers_Ctrl()`、业务层直接调用。

建议：

- 当前产品先定义唯一 MOS 控制接口，其他旧 driver 仅保留参数参考，避免被误调用。
- 如果不再需要 Relay/Precharge 多形态，建议拆出归档文档后删除未用分支。

### 4.15 `LedBar.c / LedBar.h`

定位：5 pin GPIO Charlieplexing 数码管/灯条显示、按键显示窗口、休眠 SOC 备份。

现状：

- 当前是单一路径 GPIO Charlieplexing，不再依赖 74HC595。
- 使用 TIM4 扫描，扫描周期由 `PROJECT_CFG_LEDBAR_SCAN_TIMER_100KHZ_TICKS` 控制。
- LedBar 使用 PB11、PA5、PA4、PA6、PB10 作为 Charlieplexing pins。
- 支持普通短按显示 5s、休眠唤醒显示 10s、充电/故障指示、BKP 休眠 SOC 保存。
- TIM4 IRQ 已有 `TIM4_IRQHandler()`。

问题：

- LedBar 同时处理显示、按键过滤、充电过滤、故障显示、休眠 SOC，功能已经比较满。
- PA4/PA5/PA6 是 SPI 习惯命名脚，如果未来恢复 SPI/74HC595，要重新做 pin ownership 检查。

建议：

- 保持当前单一路径，避免重新引入 74HC595 兼容层。
- 如果要继续扩展，优先拆出 input filter 和 display renderer，不要把更多业务判断直接塞进扫描层。

### 4.16 `Heat_Cool.c / Heat_Cool.h`

定位：加热/冷却控制。

现状：

- 默认 `PROJECT_CFG_HEAT_ENABLE=0`，运行主循环中 `App_Heat_Cool_Ctrl()` 被编译关闭。
- `Heat_Cool.c` 内部实现了简化加热逻辑，但 `App_Heat_Cool_Ctrl()` 只调用 `Heat_Control()`，没有调用 `Cool_Control()`。
- `MCUO_RELAY_HEAT` 和 `MCUO_RELAY_COOL` 都映射到 PC6。
- 文件中存在“明天回来补”“后续需要修改”等临时注释。

问题：

- 该模块当前更像未完成/备用功能，不适合作为量产关键功能直接启用。
- Heat 和 Cool 共用 PC6，如果硬件只有一个输出，应改名为单一 thermal relay；如果硬件有两路，当前代码不够。

建议：

- 若本产品无加热/冷却，继续保持编译关闭，并清理通信可配置入口。
- 若要启用，先重写为 table-driven 状态机，明确硬件输出、温度阈值、电流门槛、最大加热时间、故障恢复策略。

### 4.17 `FactoryAging.c`

定位：出厂老化 3 天运行态计时、BKP/Flash 进度保存、完成后退出老化。

现状：

- `PROJECT_CFG_FACTORY_AGING_ENABLE=1`，默认 259200 秒，即 3 天。
- 只累计 TIM3 10ms 运行态 tick，STOP/RTC 时间不累计。
- BKP 每秒保存，Flash 每 2 小时或强制节点保存。
- 休眠前 `FactoryAging_SaveProgressBeforeSleep()` 保存当前进度。
- 完成后 `enter_fac_mode(false)` 并写入 Flash done 状态。

问题：

- 功能依赖后 64K Flash 存储，因此同样受 128KB Flash 前提约束。
- 如果 Flash 保存失败，当前按 1s retry，需评估失败时 MOS 老化状态是否符合安全策略。

建议：

- 将 FactoryAging done 状态纳入 Flash size guard。
- 通过 CAN frame ch8 和 RS485 状态暴露更完整的 aging state/elapsed/done/save fail。

### 4.18 `LogRecord.c`

定位：事件记录、重复故障限频、Flash 持久化。

现状：

- 100 条事件记录，每条 event + delta。
- `PROJECT_CFG_LOG_RECORD_REPEAT_MIN_INTERVAL_SEC=3600` 抑制重复故障写 Flash。
- 记录启动、睡眠、加热、冷却、三级故障、AFE/EEPROM/CBC 等。

问题：

- 时间压缩规则 `LogTime_Map()` 超过 168 小时记为 170，短时间 <=60s 记为 171，语义需要文档同步给上位机。
- 事件 record 依赖 Flash journal，仍受 Flash 容量/CRC 长度限制影响。

建议：

- 通信文档中增加事件 delta 编码表。
- 如果售后要精确时间，建议结合 RTC fault snapshot，而不是只靠 event delta。

### 4.19 `ProductionID.c`

定位：硬件版本、软件版本、序列号默认值。

现状：

- 每次启动第一次 `App_ProID_Deal()` 调用 `InitProID()`，当前只加载默认字符串。
- 写默认和初始化共用 `InitProID_DefaultData()`。

问题：

- 目前看不到从 Flash/上位机持久化 SN 的完整闭环，更多像默认占位。

建议：

- 若 SN 要量产写入，应明确存储地址、写保护、读取上报和重置策略。

### 4.20 `SleepDeal.c / ChargerLoadFunc.c / easylogger`

定位：历史休眠/充电检测/日志组件。

现状：

- `SleepDeal.c` 仍存在，但主动低功耗 owner 已经转到 `rtc_sleep.c`。
- `ChargerLoadFunc.c` 初始化入口在 `main.c` 中注释。
- easylogger 仍在工程中，但 Release 禁止 `ELOG_OUTPUT_ENABLE`。

问题：

- 这些模块需要明确“活跃/历史/调试”状态，否则后续维护者会误读。

建议：

- 对历史模块加文件头说明，或者移入 `legacy/`。
- Release 中继续保持 easylogger 输出关闭。

## 5. STM32 外设逐项分析

| 外设 | 当前使用 | 代码位置 | 判断 |
| --- | --- | --- | --- |
| RCC/Clock | HSE 8MHz 路径，`SystemCoreClock` 动态使用 | `system_stm32f10x.c`、`System_Init.c` | 运行逻辑可用，但 Keil `CLOCK(12000000)` 与源码不一致。 |
| SysTick | 阻塞 delay，`__delay_ms()` 中喂狗 | `System_Init.c` | 可用，注意不要在主任务新增长阻塞。 |
| NVIC | PriorityGroup_1，TIM3/CAN/USART/RTC/TIM4 等 IRQ | `System_Init.c`、各模块 | 可用。DMA IRQ 未启用，保持现状。 |
| GPIO/AFIO | 多数控制脚、JTAG disable、USART1 remap | `conf.c`、`conf_gpio.h` | 需要 pin ownership 表防冲突。 |
| EXTI | PA0、PA9、PB12、PB13、UART wake 等 | `conf.c`、`stm32f10x_it.c` | 低功耗唤醒链路可用，需实板确认触发极性。 |
| TIM3 | 10ms 系统 tick | `System_Init.c` | 当前核心时基。 |
| TIM2 | ADC1 外部触发 | `ADC.c` | 不输出 TIM2_CH2 GPIO，内部触发合理。 |
| TIM4 | LedBar 扫描 | `LedBar.c` | 有 ISR，低功耗前关闭。 |
| ADC1 | PB1/PA2/PA1 三通道扫描 | `ADC.c` | 采样链路清楚。 |
| DMA1_Channel1 | ADC1 循环搬运 | `ADC.c` | 无 IRQ 模式合理。 |
| USART1 | 上位机 SCI1，PB6/PB7 remap | `Sci_Upper.c` | 当前主通信口。 |
| USART2 | 默认关闭 | `Sci_Upper.c` | 若打开会与 PA2/PA3 现有用途冲突。 |
| USART3 | 默认关闭，可 full remap PD8/PD9 | `Sci_Upper.c` | 当前未用。 |
| CAN1 | PA11/PA12，250kbps，CMNT_EN 控电 | `Can_HDX.c` | 低功耗策略较完整。 |
| RTC/BKP | RTC alarm、BKP 状态、LedBar/FactoryAging/fault snapshot | `RTC.c`、`rtc_sleep.c`、`LedBar.c`、`FactoryAging.c` | 依赖 backup domain，注意寄存器分配文档化。 |
| PWR/STOP | STOP + low power regulator | `conf.c`、`rtc_sleep.c` | 当前低功耗核心。 |
| IWDG | Release 打开 | `System_Init.c` | 与 RTC 周期联动。 |
| Flash | App/IAP flag、参数、SOC、log、aging | `Flash.c` | 最大风险在地址/容量源控一致性。 |
| CRC 外设 | 未见硬件 CRC 使用 | 无 | 当前用软件 `Sci_CRC16RTU()`。 |
| SPI/I2C 硬件 | 当前主链路未用硬件 SPI/I2C | `I2C_AFE1.c` 为软件 TWI | 若恢复 SPI，必须重做 LedBar pin 冲突检查。 |

## 6. 功能链路逐项分析

| 功能 | 输入 | 主处理 | 输出/影响 |
| --- | --- | --- | --- |
| 电芯采样 | SH367309 cell/temp/current | `App_AFEGet()`、`UpdateVoltageFromBqMaximo()` | `g_stCellInfoReport` |
| Type-C 输出电流 | PA2 ADC | `ADC_Current_Smooth()`、`SOC_GetTypeCBatEquivCurrentA10()` | SOC 净电流修正 |
| 总压分压 | PA1 ADC | `ADC_Vbc()` | `g_u32Vbat_mV` |
| MOS 温度 | PB1 ADC | `ADC_TTC()`、`DataLoad_Temperature()` | 温度/故障/显示 |
| SOC | AFE 电压、电流、Type-C、电芯体系、Flash snapshot | `SOC.c`、`SocEnhance.c` | SOC/SOH/容量、RS485/CAN/LedBar |
| CAN 广播 | SOC/电压/电流/状态 | `CanFeidaoFrames.c` | 扩展帧 ch0/ch1/ch2/ch3/ch4/ch5/ch8 |
| RS485 上位机 | USART1 | `Sci_Upper.c` | 读状态、写参数、SOC test、IAP flag |
| RTC 低功耗 | 电流、通信、按键、CAN active、低压 | `rtc_sleep.c` | STOP/RTC 唤醒/CAN service/SOC relaxation |
| LedBar 显示 | SOC、按键、充电、故障、BKP | `LedBar.c` | 数码管/灯条显示窗口 |
| Flash 参数 | 上位机写参数、SOC 保存、日志、老化 | `Flash.c`、`EEPROM.c` | 内部 Flash 双槽/journal |
| 出厂老化 | 首次运行和 Flash/BKP 进度 | `FactoryAging.c` | 3 天运行态老化和完成标记 |
| 事件记录 | 故障/状态边沿 | `LogRecord.c` | Flash log、RS485 读取 |

## 7. 重点问题清单

### P0：App 地址和 Flash 布局没有形成源控闭环

现象：

- 安全规则要求 App 地址 `0x08004800`，IAP `0x08000000`。
- `tools/soc_flash_app_safe.ps1` 正确检查 `0x08004800`。
- `Flash.h` 也定义 `FLASH_ADDR_APP_START 0x08004800`。
- 但 `.uvprojx` 中 IROM/TextAddressRange 仍显示 `0x08000000`，scatter 文件为空。
- 生成目录中的 `.sct` 显示 `0x08004800`，但 `Objects/` 是构建产物，不适合作为唯一真相。
- `AGENTS.md` 要求的 `FD_Release.sct` 当前不在仓库中，说明烧录安全规则还没有落到可源控复核的 scatter 文件。

风险：

- 如果某台电脑/某次 Keil 重新生成时使用 `.uvprojx` 中的 `0x08000000`，App 可能按 IAP 地址链接。
- 即使烧录脚本写到 `0x08004800`，向量表/链接地址错了也会导致启动异常。

建议：

1. 将 App scatter 文件纳入源控。
2. `.uvprojx` 明确使用 scatter 文件，或直接把 IROM/TextAddressRange 改成 `0x08004800`。
3. `project_check.py` 增加硬检查：Release/Debug target 不允许 App TextAddressRange 为 `0x08000000`。

### P0：后 64K 存储依赖真实 128KB Flash

现象：

- 存储地址从 `0x0801C000` 到 `0x0801FC00`。
- `.uvprojx` Device 是 `STM32F103C8`，必须确认真实芯片是否具备 128KB Flash。
- `StorageFlash_PrintBootCheck()` 在 flash size <128KB 时只跳过打印检查，没有统一禁止后续 save。

风险：

- 如果真实 Flash 只有 64KB，写 `0x0801C000+` 不可靠，SOC/参数/日志/老化状态都可能失效或触发异常。

建议：

1. 确认 BOM 和芯片丝印，若使用 128KB 器件，工程 Device/文档改成对应 xB/CB 口径。
2. 所有 `StorageFlash_Save*()` 前统一检查 `flash_size_kb >= 128`。
3. `project_check.py` 检查 `FLASH_ADDR_STORAGE_*` 与目标 Flash 容量匹配。

### P1：旧保护和旧驱动路径未彻底收口

现象：

- `Fault.c` 仍保留大量软件保护检查，但 `App_WarnCtrl()` 未运行。
- `IODrivers.c` 仍保留多硬件形态 MOS/Relay 控制，但主路径更多走 SH367309 AFE 控制。
- `SleepDeal.c`、`ChargerLoadFunc.c` 等历史模块仍在。

风险：

- 后续维护时容易误调用旧路径，造成同一 MOS/保护状态多 owner。

建议：

- 给旧模块加 `legacy` 标记或删除不用分支。
- 明确当前产品唯一保护 owner 和 MOS 控制 owner。

### P1：`DataDeal.c` 业务耦合过重

现象：

- `DataDeal.c` 既装载采样数据，又处理 charger、RF、MOS 温度、认证/老化、按键/DI 等。

风险：

- 数据采样 bug 和业务策略 bug 难以隔离。
- 后续新增功能会继续堆到 `new_todo_logi()`。

建议：

- 以 `g_stCellInfoReport` 为数据边界，采样层只产出数据。
- 业务策略按 charger、thermal、rf、cert、input 拆模块。

### P1：Heat/Cool 模块不适合作为当前量产功能直接启用

现象：

- 默认编译关闭。
- `Cool_Control()` 不在 `App_Heat_Cool_Ctrl()` 中运行。
- Heat/Cool 都映射 PC6。
- 文件有未完成注释和临时特例。

建议：

- 不用就保持关闭并从通信配置中弱化。
- 要用就先重写状态机和硬件输出定义。

### P2：测试门禁覆盖面还不够

现状：

- `project_check.py` 已覆盖 100 项，包括 Release 安全宏、SOC/Type-C/低功耗/CAN 文档一致性等。

缺口：

- 没有检查 Keil App 地址。
- 没有检查 Flash 容量/存储地址。
- 没有检查 pin ownership。
- 没有检查 startup 文件密度与 `STM32F10X_MD` 的一致性。

建议：

- 把这些检查纳入 `project_check.py`，避免靠人工记忆。

## 8. 优化路线

### 第一阶段：先补安全门禁

1. 源控固定 App scatter 和 `0x08004800`。
2. `project_check.py` 检查 App 地址、Flash 存储地址、Flash 容量假设。
3. 对 128KB 存储 API 增加统一 runtime guard。
4. 检查 Keil `CLOCK(12000000)` 与 `HSE_VALUE=8000000`，统一为硬件真实晶振。

### 第二阶段：收口模块 owner

1. `DataDeal.c` 只保留采样装载。
2. AFE driver 只负责 SH367309 通信和寄存器。
3. Protection adapter 只负责 AFE fault -> MCU fault/report。
4. MOS 控制只保留一个 public API。
5. 旧 `Fault.c/IODrivers.c/SleepDeal.c` 明确 legacy 或删除。

### 第三阶段：增强可复核测试

1. 保留 `python3 tools/project_check.py` 作为提交门禁。
2. SOC 改动必须跑真实 C host test 和 replay。
3. CAN/RTC/低功耗需要台架用例：有对端、无对端、BusOff、RTC 唤醒发送成功、IWDG 安全。
4. Flash 存储需要 64KB/128KB 两类器件或仿真地址测试。
5. RS485 需要固定读取 `0xD000`、`0xD100`、`0xD200`、`0xD300` 的冒烟脚本。

### 第四阶段：文档和资产化

1. README 持续作为文档入口。
2. 每个核心模块保留“owner、输入、输出、状态、测试方法”五项说明。
3. 将烧录地址、测试模式、Flash 容量假设、上位机启动方式继续写入脚本和文档，不依赖对话记忆。

## 9. 总体判断

当前工程不是简单 demo，已经具备较完整的 BMS 保护板应用结构：SOC 用户体验、CAN 低功耗、RTC/STOP、Flash 快照、量产/测试隔离、LedBar 显示都已经进入可维护状态。

但要作为长期量产工程，下一步最应该优先处理的不是继续叠功能，而是把几个“可能一次性导致板子不可启动或状态不可恢复”的边界做成硬门禁：

1. App 必须从源控工程配置层保证链接在 `0x08004800`。
2. 后 64K Flash 存储必须和真实芯片容量绑定。
3. 旧保护/旧驱动路径必须明确退出主链路。
4. `DataDeal.c` 必须拆出业务策略，避免采样层继续膨胀。

如果只做一件事，建议先做“App 地址 + Flash 容量 + 安全烧录 + project_check 门禁”的闭环。这比继续优化算法更优先，因为它直接影响是否能安全构建、烧录和恢复。
