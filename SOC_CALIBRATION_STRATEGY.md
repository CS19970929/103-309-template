# SOC 校准策略与参数调优说明

本文记录当前 SOC 模块的校准边界、参数口径和现场调优方法。当前实现已去掉复杂 FCC 学习和运行态连续 OCV 融合，主策略简化为：

```text
安时积分主导 + 满电确认 + 中低压弱约束 + 低压表收敛 + 静置/RTC OCV deferred target + 显示平滑
```

代码依据：

- `103 + 309/Project/Source/SOC.c`
- `103 + 309/Project/Source/SocEnhance.c`
- `103 + 309/Project/Source/Flash.c/.h`
- `tools/run_soc_host_c_test.py`
- `tools/soc_replay_test.py`

## 1. 设计原则

1. 正常骑行和充电时，SOC 主要来自安时积分，避免端电压瞬态造成跳变。
2. 电压只在可信场景影响 SOC：上电无快照、长时间静置、RTC 唤醒、满电确认、低压末端。
3. 低压区宁可保守下修，也不能让用户在临近欠压时看到明显偏高 SOC。
4. 对外显示 SOC 与内部 SOC 分离，所有普通修正都经过平滑。
5. SOH 先按循环次数映射，有效容量由 SOH 反推，不做容量学习。
6. 所有通信地址、函数签名和 Flash V2 快照结构保持兼容。
7. 运行态自动校准每次最多 `1%`，以后新增任何 SOC 校准策略都必须遵守该原则。
8. 静置校准不再依赖“到固定 `30min` 就校准”；运行态必须先确认电压回弹变化已经进入稳定窗口，并优先把 OCV 保存为 deferred target。
9. 重载骑行后的未回弹电压不能立刻用于校准；关机前的回弹风险需要跨重启保留。
10. 普通静置/RTC OCV 目标与当前 SOC 误差 `<=3%` 时不校准；静置/RTC OCV 永远不允许把 SOC 向上拉高。
11. 深度休眠 RTC OCV 不套普通 `3%` deadband，但只能按 OCV 向下单步 `1%`，不能跳变，也不能向上校准。

“每次最多 `1%`”是硬约束：满电确认、低压收敛、静置/RTC OCV、骑行中电压表修正都不能一次性跳变超过 `1%`。稳定静置/RTC OCV 默认只记录可信下修目标，不立即追平；后续由放电阶段或久置低 OCV 按小步消化。启动初始化、上位机 `0x1005` 设置一次 SOC、`SOC_Fixed/SOC_Zero` 显示覆盖不属于运行态自动校准。

大电流 voltage sag 也有硬约束：`Idsg > C/2` 时进入大电流 holdoff，持续 `30s`。holdoff 期间如果 `VCellMin > V0 + 50mV`，禁止使用端电压做低压表和静置 OCV 校准；如果已经低到 `V0 + 50mV` 以内，说明接近真实末端，仍允许按 `1%` 步进向 0 收敛。

如果大电流 holdoff 发生后用户直接关机，快照会保存 `u16Flags bit0`。下次开机会先给 `5min` 回弹保护窗口，避免“重载后电压还没回弹、马上开机”时按低电压把 SOC 校低。

## 2. SOC 校准路径

| 策略 | 触发条件 | 修正方式 | 用户体验 |
| --- | --- | --- | --- |
| 安时积分 | 充/放电电流 `>=0.2A` 且 AFE 电流样本更新 | 按 `200ms` 电流积分更新内部容量 | 显示平滑跟随 |
| 启动 OCV | 无有效 Flash 快照且单体电压有效 | 按 OCV 表初始化内部 SOC | 启动时同步显示 |
| 满电确认 | 充电或停充静置下满足高压、电压差和短时间累计 | 每次上修 `1%`，直到 `100%` | 显示平滑跟随 |
| 静置稳定 OCV | `RELAX` 下电压稳定至少 `5min`，且 `10min/step` 节拍到达 | 只记录低于当前 SOC 且差值 `>3%` 的 OCV deferred target；后续只在放电阶段按 `10min/1%` 消化 | 静置不立刻追平，不向上校准，减少停车跳变 |
| 久置低 OCV | 已有低于内部 SOC 且差值 `>3%` 的 OCV target，且继续稳定久置 | 静置阶段按 `30min/1%` 慢速下修；当前默认首次约 `10min target + 30min step` | 避免电池久置自放电后 SOC 长期虚高 |
| 低压表 | 非充电下 `VCellMin <=3400mV~2950mV` | 按电压/电流表每次下修 `1%` | 低压区快速跟随 |
| RTC OCV | RTC 唤醒后传入休眠时长和电压，连续唤醒电压稳定，且未被 rebound holdoff 阻断 | 稳定窗口按 `10min/step` 记录低于当前 SOC 且差值 `>3%` 的 deferred target；久置低 OCV 可按 `30min/1%` 下修 | 不随 `1s/10s` RTC 唤醒频率快速跳变，也不向上校准 |
| 深睡 RTC OCV | `DEEP_MODE` 复位式深睡中由 RTC Alarm 周期唤醒计时，累计达到 `PROJECT_CFG_SOC_RTC_CALIBRATION_MIN_SECONDS` 后，真正按键/充电唤醒并拿到首个有效 AFE 样本 | 按累计深睡秒数扣除板端自耗；若电压有效且压差 `<=200mV`，忽略普通 `3%` deadband，但最多按 OCV 下修 `1%`，随后清除该累计窗口 | 深睡期间不直接跳 SOC，用户真正唤醒后才做一次受限校准 |
| 中低压弱约束 | `VCellMin <= V0 + 700mV` 且高于低压表范围 | 只向下限制明显高估 SOC，每次 `1%` | 扩大收敛区间但避免瞬态误校 |
| 骑行中低压表修正 | `VCellMin <= V0 + 400mV` | 给内部 SOC 设置分电流档位上限 | 防止低压高估 |
| 设置一次 SOC | 上位机 `0x1005` | 内部和显示同步到设定值 | 立即生效 |
| `SOC_Fixed/SOC_Zero` | 调试或工厂模式 | 只覆盖对外显示 | 不破坏内部状态 |

## 3. 电压类修正门控

以下条件不满足时，不允许使用电压修正 SOC：

| 门控项 | 当前条件 |
| --- | --- |
| 单体电压有效 | `2000mV <= VCellMin <= VCellMax <= 5000mV` |
| 单体压差合理 | `VCellMax - VCellMin <=1000mV` |
| 保护状态 | `PROJECT_CFG_SOC_CALIBRATION_BLOCK_PROTECTION_FAULT=1` 时要求无三级保护故障 |
| 系统状态 | `PROJECT_CFG_SOC_CALIBRATION_BLOCK_SYSTEM_FAULT=1` 时要求 AFE1、AFE2、ADC、CBC_CHG、CBC_DSG、TEMP_BREAK 均正常 |

说明：

1. 门控失败只阻止 OCV、满电确认和低压电压类修正，不直接停止电流积分。
2. `VCellMax < VCellMin`、`0mV`、超上限等采样异常不能把 SOC 拉到 0。
3. 低压表仍要求基础电压合法，避免异常采样误触发。
4. 稳定静置和中低压弱约束额外要求单体压差 `<=200mV`，降低单串异常或均衡差异造成的误判。

## 4. 默认 OCV 表口径

当前三元锂默认表按 INR21700-45E 和 e-bike 3.0V 显示空电策略设置：

```text
4160/100, 4100/95, 4050/90, 3995/85, 3935/80,
3880/75, 3835/70, 3795/65, 3760/60, 3725/55,
3695/50, 3670/45, 3645/40, 3615/35, 3585/30,
3555/25, 3525/20, 3480/15, 3400/10, 3250/5,
3000/0
```

上位机 `0x2200` 的 SOC 表写入入口仍保留。自定义表在运行时生效，但当前不新增 Flash 地址保存 SOC 表。

该表不应被理解为“所有电池都准确”的绝对真值。不同批次、温度、老化和内阻都会改变端电压关系，所以当前策略只把 OCV 表用于可信场景：启动无快照可直接初始化；稳定静置/RTC 先记录 deferred target；骑行中只做上限约束，不做双向强融合。

## 5. 满电策略调优

满电确认不再硬依赖 taper 电流，避免充电器电流不满足 `C/20` 或停充后进入 `RELAX` 导致 SOC 永远不到 `100%`。

基础条件：

- 当前不是 `DSG`，允许 `CHG` 或停充后的 `RELAX`。
- 电压类校准门控通过。
- `VCellMax > 4180mV`，这是置 `100%` 的硬门槛；`4180mV` 本身不触发满电锚定。
- `VCellMax >= V100 - PROJECT_CFG_SOC_FULL_CONFIRM_MIN_CELL_MARGIN_MV`，当前默认 `4100mV`。

快速确认条件：

- `VCellMin >= V100 - PROJECT_CFG_SOC_FULL_CONFIRM_FAST_MARGIN_MV`，当前默认 `4150mV`。
- 单体压差 `<= PROJECT_CFG_SOC_FULL_CONFIRM_MAX_CELL_DELTA_MV`，当前默认 `120mV`。
- 持续 `5s`。

普通确认条件：

- 内部 SOC `>=95%`。
- `VCellMin >= V100 - PROJECT_CFG_SOC_FULL_CONFIRM_MIN_CELL_MARGIN_MV`，当前默认 `4100mV`。
- 单体压差 `<= PROJECT_CFG_SOC_FULL_CONFIRM_MAX_CELL_DELTA_MV`，当前默认 `120mV`。
- 累计满足 `15s`。

计数器满足时累加，不满足时递减，避免单次采样抖动把满电确认进度完全清掉。

调优建议：

1. 如果真实充满后长期不能到 `100%`，优先确认 `VCellMax` 是否已经大于 `4180mV`，再看 `V100`、`VCellMin` 和压差是否合理，而不是先看 taper 电流。
2. 如果过早显示 `100%`，先收紧 `VCellMin` 或压差条件，不建议恢复电流硬门槛。
3. 满电确认前内部 SOC 最高到 `99%`，这是为了避免用户看到未确认的 `100%`。

## 6. 中低压与低压策略调优

中低压弱约束用于解决“用户长期不用车或积分漂移后，SOC 明显高于电压可支持区间，但又还没进入 3400mV 以下低压表”的体验问题。默认 `V0=3000mV` 时表如下：

| VCellMin | RELAX | 轻载 `<=C/5` | 中载 `<=C/2` | 重载 `>C/2` |
| --- | --- | --- | --- | --- |
| `<=3500mV` | `25% / 90s` | `32% / 90s` | `42% / 120s` | 禁用 |
| `<=3600mV` | `35% / 120s` | `42% / 120s` | `50% / 150s` | 禁用 |
| `<=3650mV` | `45% / 150s` | `50% / 150s` | `58% / 180s` | 禁用 |
| `<=3700mV` | `55% / 180s` | `60% / 180s` | 禁用 | 禁用 |

该表的定位是弱约束，不是强校准：

1. 只下修，不上修。
2. 只在 `VCellMin > V0 + 400mV` 时运行，避免和低压表重叠。
3. 单体压差必须 `<=200mV`。
4. 大电流 sag/rebound holdoff 期间禁用。
5. 重载档位禁用，避免爬坡、急加速误校。
6. 条件中断后计数器清零，重新满足条件必须重新累计完整周期。

低压修正采用表驱动。默认 `V0=3000mV`，电流档位为 `RELAX`、轻载 `<=C/5`、中载 `<=C/2`、重载 `>C/2`。表项中的周期表示每下修 `1%` 所需时间。

| VCellMin | RELAX | 轻载 `<=C/5` | 中载 `<=C/2` | 重载 `>C/2` |
| --- | --- | --- | --- | --- |
| `<=3400mV` | `18% / 24s` | `22% / 20s` | `30% / 16s` | `40% / 12s` |
| `<=3300mV` | `14% / 18s` | `18% / 15s` | `25% / 12s` | `32% / 9s` |
| `<=3200mV` | `12% / 12s` | `14% / 10s` | `20% / 8s` | `25% / 6s` |
| `<=3100mV` | `8% / 7s` | `10% / 6s` | `14% / 5s` | `18% / 4s` |
| `<=3050mV` | `4% / 4s` | `5% / 3s` | `8% / 2s` | `12% / 1.6s` |
| `<=3000mV` | `0% / 2s` | `0% / 1s` | `0% / 1s` | `0% / 1s` |
| `<=2975mV` | `0% / 1s` | `0% / 1s` | `0% / 0.2s` | `0% / 0.2s` |
| `<=2950mV` | `0% / 0.2s` | `0% / 0.2s` | `0% / 0.2s` | `0% / 0.2s` |

该表只向下修正，不向上修正。SOC 低于表目标时继续相信安时积分；SOC 高于表目标时，按周期每次下修 `1%`。

大电流 holdoff 优先级高于该表：重载骑行或刚松油门未回弹时，不使用 `3400mV~3050mV` 的宽范围表项校准；只有进入 `3050mV` 以下末端区，才允许继续按表收敛到 0。

调优建议：

1. e-bike 控制器通常会在约 `2950mV` 单体附近保护，因此默认 `V0 - 50mV` 必须以最快 `1%/200ms` 收敛到 `0%`。
2. `3400mV~3050mV` 是骑行中宽范围预收敛区，用电流档位补偿 voltage sag，避免过快下修。
3. `3000mV` 以下是显示末端区，所有电流档位都要向 `0%` 加速收敛，避免实际保护时 SOC 还有很多。
4. 停止放电后如果电压仍在 `3000mV` 以下，`RELAX` 状态会继续向 `0%` 收敛。
5. 客户反馈低压区掉电快时，先查电芯压差、内阻和负载电流，不要直接取消低压表。
6. `3000mV` 是显示 0% 口径，不等于电芯物理极限；`2950mV` 是低压表最快收敛点，不再额外保留 `2750mV/2500mV` 特殊兜底分支。

## 7. SOH 与循环次数

当前 SOH 映射：

```text
SOH = clamp(100 - cycle / 100, 80, 100)
cap_full = cap_factory * SOH / 100
```

循环次数：

```text
每累计 factory_capacity / 100 的放电量，cycle_x100 + 1
对外循环次数 = cycle_x100 / 100
```

该策略的优点是稳定、可解释、不会因一次异常满空过程学习出错误容量。后续如需提高 SOH 精度，应基于台架容量数据增加分段映射，而不是先恢复复杂在线学习。

## 8. 显示体验参数

| 场景 | 当前速率 |
| --- | --- |
| 普通变化 | `5s/1%` |
| 充电上升 | `5s/1%` |
| 低压下降 | `VCellMin <= V0 + 50mV` 时 `1s/1%` |
| 设置一次 SOC、工厂显示覆盖 | 可立即同步 |
| 自动满电、自动空电、中低压弱约束、静置/RTC OCV | 每次实际修正 `1%`，显示继续平滑；deferred target 本身不显示 |

显示调优原则：

1. 普通区不要追求显示立即响应，优先稳定。
2. 充电上升与普通变化同为 `5s/1%`，避免插上充电器后立即跳高。
3. 低压下降必须更快，避免临近欠压时显示滞后。

## 9. 参数入口

| 参数 | 范围 | 影响 |
| --- | --- | --- |
| `OtherElement.u16Soc_Ah` / `0x2318` | `0..65535`，单位 `10*Ah`，0 使用默认 270 | 出厂容量、积分比例、C/5 轻载阈值、SOH 容量基准 |
| `OtherElement.u16Soc_Cycle_times` / `0x2319` | `0..65535 cycles` | 初始循环次数和初始 SOH |
| `OtherElement.u16Soc_V_100` / `0x231A` | `0..5000mV`，0 使用默认 4180mV | 满电确认的目标端点 |
| `OtherElement.u16Soc_V_0` / `0x231B` | `0..5000mV`，0 使用默认 3000mV | 低压表和显示空电端点 |
| `OtherElement.u16Soc_TableSelect` | `0..3` | 启动/静置 OCV 表选择 |
| `SOC_Table_Set[42]` / `0x2200` | 21 组 `(mV, SOC%)`，仅 RAM 生效 | `SOC_TABLE_TEST` 的 OCV 表 |
| `0x1005` 设置一次 SOC | 建议 `0..100%` | 直接设置内部和显示 SOC，并保存快照 |

内部固定常量：

| 常量 | 当前值 | 影响 |
| --- | ---: | --- |
| `SOC_SHORT_REST_MIN_SECONDS` | `300s` | 静置稳定可信度最短时间 |
| `SOC_SHORT_REST_STEP_SECONDS` | `600s` | 稳定静置刷新 deferred target、放电阶段消化 OCV 差值的节拍 |
| `PROJECT_CFG_SOC_RTC_CALIBRATION_MIN_SECONDS` | `600s` | RTC/深度休眠累计达到该时间后才允许 OCV 校准 |
| OCV 小误差死区 | `3%` | 普通静置/RTC/deferred OCV 目标与当前 SOC 差值不超过该值时不校准；深睡 RTC 例外 |
| `SOC_LONG_REST_DOWN_STEP_SECONDS` | `1800s` | 久置且 OCV target 低于内部 SOC 时的静置下修节拍 |
| `SOC_REST_STABLE_DELTA_MV` | `30mV` | 静置电压回弹稳定判定窗口 |
| RTC OCV 首次采样 | 建参考值，不累计稳定窗口 | 防止单次 RTC 唤醒电压直接触发校准 |
| `SOC_MID_MAX_DELTA_MV` | `200mV` | 中低压弱约束和静置稳定的单体压差门槛 |
| `SOC_REBOUND_BOOT_HOLDOFF_SECONDS` | `300s` | 重载后跨重启回弹保护时间 |

完整编译宏范围、内部常量和函数清单见 `SOC逻辑与参数影响梳理.md`。

## 10. 推荐验证

1. 真实 C 源码主机测试：`python3 tools/run_soc_host_c_test.py`。
2. 主机回放矩阵：`python3 tools/soc_replay_test.py`。
3. 语法检查：对 `SOC.c` 和 `SocEnhance.c` 运行 C99 `clang -fsyntax-only`。
4. Keil 编译：在 Windows + Keil MDK 环境编译 `FD_Debug` 和 `FD_Release`。
5. 台架验证：覆盖启动、满电、低压、RTC、SOC 设置、断电恢复、异常采样、稳定静置、重载关机后快速重启。
6. 上板验证：确认 CAN、RS485、LED 和内部 `g_stCellInfoReport.SocElement` 显示一致。

无板时按 [SOC 无板主机验证方案](SOC_HOST_VALIDATION_PLAN.md) 执行。`tools/run_soc_host_c_test.py` 直接编译真实 `SOC.c`、`SocEnhance.c` 和 `PubFunc.c`，当前每档覆盖 `22` 个关键 C 源码路径，并覆盖当前配置、debug watch 版以及 0mA/30mA/1000mA 板端自耗配置；`tools/soc_replay_test.py` 当前覆盖 `49` 个场景，包含表格矩阵、源码表格一致性、异常输入矩阵、静置不稳定超过 30min 仍不校准、普通 OCV 小误差死区、深睡 RTC 小误差单步校准、deferred OCV 放电消化、久置低 OCV 慢速下修和随机运行不变量。两者可以作为算法可靠性门禁，但不能替代 AFE 采样、Flash 擦写、通信电气和真实电芯回弹的台架验证。
