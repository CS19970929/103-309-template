# 项目逻辑完整梳理与架构简化建议

日期：2026-05-24
口径：以当前工作区源码为准，现有文档仅作为对照与索引。
重点：启动链路、主循环时基、AFE/ADC/SOC/CAN/LED/RTC、持久化、疑似 bug、后续简化方向。

## 1. 总体结论

当前工程已经不是简单的“主循环里各模块轮询”结构，而是围绕 `TIM3 10ms tick`、`200ms AFE/SOC 链路`、`g_stCellInfoReport` 运行数据总线、`Flash/BKP/RTC` 低功耗状态恢复、`CAN/SCI` 对外通信组成的一套 BMS 应用框架。

源码层面没有看到会立刻导致 SOC 大步跳变的明显错误。SOC 模块当前保留了用户体验优先的约束：单次校准最大 `1%`、压降未恢复前不做激进 OCV 校准、RTC 静置补偿走小步逻辑、满充/空端锚点有独立门控。真正复杂的地方不在单个公式，而在多个模块把状态耦合在一起：AFE 采样、ADC Type-C 等效电流、SOC 快照、CAN 低功耗补发、LED 唤醒显示、休眠进入/退出都互相影响。

最值得优先简化的不是 SOC 算法本身，而是模块边界：

- `DataDeal.c` 同时承担 AFE 数据装载、Type-C/总压关联、MOS/按键/充电器/UL fuse 业务策略，建议拆出 `PowerPolicy` 或 `ProtectionPolicy`。
- `ADC.c` 的 DMA 原始 rank 和处理后的派生量共用一个枚举体系，后续新增一路采样时容易改漏，建议先做“采样通道表”。
- `Can_HDX.c` 已具备队列和低功耗唤醒发送能力，但 RX app 命令只有一个 pending 槽位，滤波也较宽，建议补计数/拒绝反馈和硬件过滤。
- `LedBar.c` 运行态已经集中到 `LedBarRuntime`，但仍有空分支、全局按键状态、命名不准确和图标语义疑点，建议继续收敛为“输入状态 -> 显示模型 -> 扫描输出”三层。

## 2. 运行入口与调度链路

真实入口很短：

```c
int main(void)
{
    AppInit_Boot();
    while (1)
    {
        Runtime_RunOnce();
    }
}
```

启动链路：

1. `main.c` 调用 `AppInit_Boot()`。
2. `AppInit_InitDevice()` 初始化系统、GPIO、SCI、EEPROM、AFE、CAN、ADC、SOC、TIM3 和中断。
3. `AppInit_InitRuntimeState()` 初始化系统状态位、启动标志、休眠标志、日志标志。
4. `Init_RTC()` 处理 RTC 相关恢复。
5. 主循环进入 `Runtime_RunOnce()`。

运行时序：

```text
TIM3_IRQHandler()
  -> SysTime_Post10msTick()
     -> 置位 10/50/100/200/1000ms pending flag
     -> 200ms pending period 最多累计 5 个

Runtime_RunOnce()
  -> SysTime_LatchTaskFlags()
  -> FactoryAging_Task()
  -> APP_LedBar()
  -> App_AFEGet()
  -> AppInit_ServiceSci()
  -> App_AnlogCal()
  -> App_LowPowerProcess()
  -> App_Can()
  -> StorageFlash_AppUseTest_Task()
  -> App_FlashUpdate()
  -> App_LogRecord()
  -> App_ProID_Deal()
```

关键判断：

- `App_SOC()` 在 `Runtime.c` 的 IO/power 阶段被注释掉，实际 SOC 运行发生在 `App_AFEGet()` 内部。
- `App_AFEGet()` 消费 `SysTime_Take200msTaskPeriod()`，不是简单看 200ms flag，所以主循环短暂延迟后可以追赶最多 5 个 200ms 周期。
- `g_u32AfeCurrentSampleSeq` 在 `DataLoad_Current()` 之后递增，SOC 用它判断是否有新 AFE 电流样本，避免同一电流重复积分。

## 3. 核心数据总线

当前工程主要通过全局结构传递运行状态：

| 数据 | 主要写入方 | 主要读取方 | 说明 |
| --- | --- | --- | --- |
| `g_stCellInfoReport` | `DataDeal.c`、`SOC.c`、保护/状态模块 | SCI、CAN、LED、上位机 | 对外上报和内部显示的中心结构 |
| `SH367309_Read_AFE1` | `I2C_AFE1.c` | `DataDeal.c`、保护逻辑 | AFE 原始/换算电压、电流、温度 |
| `g_st_SysTimeFlag` | `System_Init.c` | 主循环各任务 | 10/50/100/200/1000ms 任务 flag |
| `SOC_Enhance_Element` | `SOC.c`、通信写参数 | `SocEnhance.c` | SOC 配置参数入口 |
| `SystemStatus` / `System_OnOFF_Func` / `System_ErrFlag` | `System_Monitor.c` 和各业务模块 | 保护、通信、CAN、LED、休眠 | 系统状态与故障位 |
| Flash/BKP/RTC | 存储、SOC、LED、低功耗模块 | 启动恢复、RTC 静置补偿、唤醒显示 | 断电/休眠状态保留 |

建议以后新增功能时，优先明确“写谁、读谁、什么时候刷新、是否能在休眠路径使用”。当前很多复杂度来自全局状态跨模块直接读写。

## 4. AFE 与 200ms SOC 链路

`App_AFEGet()` 是整机运行最关键的链路之一：

```text
App_AFEGet()
  -> 消费 200ms period
  -> MonitorAFE(0, UpdateVoltageFromBqMaximo())
  -> DataLoad_CellVolt()
  -> DataLoad_CellVoltMaxMinFind()
  -> DataLoad_Temperature()
  -> DataLoad_Current()
  -> g_u32AfeCurrentSampleSeq++
  -> App_SH367309()
  -> new_todo_logi()
  -> App_SOC()
```

AFE 数据来源：

- `UpdateVoltageFromBqMaximo()` 从 `MTP_TEMP1` 开始读取 `Registers_AFE1`。
- cell voltage 使用 `U16_SwapEndian(Registers_AFE1.Cell[i]) * 5 >> 5` 换算。
- AFE 温度通过 `TR_ResRef` 和 NTC 表换算。
- 电流目前直接保存 `U16_SwapEndian(Registers_AFE1.Cadc)`，后续在 `DataLoad_Current()` 里做符号、零点和比例换算。

电流处理要点：

- `DataLoad_Current()` 负责 AFE 电流正负方向、自动零点、mA 换算和 `u16Ichg/u16IDischg` 发布。
- `AFE_CURRENT_KB_CALIB_ENABLE` 当前为 `0`，K/B 标定路径未启用。
- `PROJECT_CFG_VIRTUAL_CURRENT_ENABLE=1` 时，debug 模式可用虚拟电流覆盖实际电流，量产逻辑要确认 `sys_time.isdebugenable` 不会误开。

主要风险：

- `new_todo_logi()` 仍在 `DataDeal.c` 内处理充电器、按键、MOS 过温、UL fuse、深度休眠等业务策略。它不是数据装载逻辑，建议从 AFE 数据链路剥离。
- `App_SH367309()` 每个 200ms 周期都会调用 `SH367309_UpdataAfeConfig()`，如果配置写入内部有较重 I2C 交互，后续应确认是否有必要每次执行。

## 5. ADC 模块逻辑与新增一路采样建议

当前 ADC 是 `ADC1 + DMA1_Channel1 + TIM2_CC2 外部触发 + circular DMA`。

现有 3 路硬件采样：

| DMA rank | GPIO | ADC channel | 当前语义 | 处理函数 |
| --- | --- | --- | --- | --- |
| 1 | PB1 | `ADC_Channel_9` | MOS NTC | `ADC_TTC()` |
| 2 | PA2 | `ADC_Channel_2` | Type-C 输出分流电流 | `ADC_Current_Smooth()` |
| 3 | PA1 | `ADC_Channel_1` | 总压 VBUS/VBAT 分压 | `ADC_Vbc()` |

`App_AnlogCal()` 每次主循环根据 10ms tick 补跑 ADC 后处理，最多一次追赶 10 个 tick：

```text
App_AnlogCal()
  -> ADC_TTC()
  -> ADC_Vbc()
  -> ADC_Current_Smooth()
```

Type-C 电流链路：

```text
PA2 ADC
  -> ADC_Current_Smooth()
  -> g_u16TypeCOutCurrent_mA / g_u16TypeCOutCurrent_A10
  -> SOC_GetTypeCBatEquivCurrentA10()
  -> 按 9V、90% 效率、pack voltage 折算电池侧等效放电电流
  -> SOC_GetNetCurrentForCalc()
```

后续新增一路 ADC 采样，建议按以下最小变更做：

1. 确认硬件引脚不与 USART、LED、CAN、I2C、按键唤醒冲突。源码已有注释说明 PC5 初始化会影响 USART2，不能直接复用。
2. 将 `AD_Used_amount` 从 `3` 改为 `4`。
3. 在 `InitADC()` 中初始化新增 GPIO 为 `GPIO_Mode_AIN`。
4. 在 `ADC_RegularChannelConfig()` 增加 rank 4，并同步 `ADC_InitStructure.ADC_NbrOfChannel`。
5. 在 `ADC.h` 明确新增原始通道索引，例如复用当前预留的 `ADC_TEMP_EV1=3`，但要避免和派生量混淆。
6. 增加一个独立处理函数，例如 `ADC_EvTemp()` 或 `ADC_ExtraSense()`，不要把业务换算塞进 `App_AnlogCal()` 主体。
7. 新增文档和回归测试：确认 DMA 数组长度、rank 顺序、索引枚举、通信上报地址是否一致。

中期建议：把 `ADC_TEMP_MOS1/ADC_CUR_AMP/ADC_VBC` 这类原始 DMA rank 和 `ADC_CURR` 这类处理后的派生值拆成两个 enum。这样新增一路采样时不容易把 DMA buffer、处理结果和业务含义混在一起。

## 6. SOC 模块逻辑

SOC 分两层：

- `SOC.c` 是适配层，负责配置初始化、从全局数据取样、Type-C 等效电流、通信命令和发布。
- `SocEnhance.c` 是算法层，负责容量状态、安时积分、满充/空端锚点、静置 OCV、小步校准、RTC 补偿、快照和显示平滑。

运行链路：

```text
InitData_SOC()
  -> 从 OtherElement 装载 SOC_Enhance_Element
  -> soc_param_lib_init()
  -> SOC_IntEnhance_Init()
  -> 发布 g_stCellInfoReport.SocElement

App_SOC()
  -> 判断 g_u32AfeCurrentSampleSeq 是否变化
  -> SOC_UpdateSampleData()
  -> SOC_IntEnhance_Ctrl()
  -> SOC_PublishReport()
```

SOC 输入：

- cell voltage min/max/sum 来自 AFE。
- 充电/放电电流来自 `g_stCellInfoReport.u16Ichg/u16IDischg`。
- Type-C 输出电流来自 ADC，折算为电池侧等效放电电流。
- 休眠静置时间来自 RTC 低功耗链路。
- 参数来自 `OtherElement` 和编译宏。

SOC 关键约束：

- `PROJECT_CFG_SOC_CALIBRATION_STEP_PERCENT=1`，单次校准最大 1%。
- `PROJECT_CFG_SOC_RUNTIME_TABLE_ENABLE=0`，运行时 0x2200 SOC 表写入关闭，OCV 表按 `PROJECT_CFG_BAT_CHEMISTRY` 编译期选择。
- `PROJECT_CFG_SOC_BOARD_SELF_CONSUMPTION_MA=30`，SOC 积分和 RTC 补偿会考虑板端自耗。
- sag/rebound holdoff 通过 `SOC_SAG_HOLDOFF_SECONDS` 等配置避免大电流压降期间误校准。
- 静置 OCV 支持 deferred target，当前只接受向下校准目标，避免静置回弹导致 SOC 被抬高。

SOC 快照与恢复：

- SOC 快照走后 64K Flash slot，启动时 `StorageFlash_LoadSocData()`。
- snapshot 有 CRC/sequence 机制，不依赖单份裸写。
- RTC 休眠唤醒时 `update_rtc_soc()` 会调用 SOC 的 RTC 补偿入口，再进入 CAN 低功耗发送服务。

建议：

- 短期不要把 `SocEnhance.c` 直接推倒重写。它虽然大，但当前策略之间有强耦合，直接拆算法容易引入体验回退。
- 可以先做无行为变化的文件级拆分：`soc_integrator.c`、`soc_ocv_calibrator.c`、`soc_anchor.c`、`soc_snapshot_adapter.c`、`soc_display.c`。对外仍保留 `SOC_IntEnhance_*` API。
- 每次拆分都必须跑真实 C host 测试、回放测试和随机不变量测试，确保 `1%` 步进和 sag holdoff 不变。

## 7. CAN 模块逻辑

CAN 初始化与运行：

```text
InitCan()
  -> GPIO/时钟/CAN 参数/过滤器
  -> CAN 电源默认控制

App_Can()
  -> feidao_can_service_tx()
  -> feidao_can_monitor_busoff()
  -> feidao_can_periodic_schedule()
  -> feidao_can_app_service()
  -> feidao_can_service_tx()
```

发送路径：

- `Can_HDX_Transmit()` 将 frame 入 32 深度软件队列。
- 标准帧会叠加地址偏移。
- `feidao_can_service_tx()` 管理一个当前 mailbox，处理 `Ok/Failed/timeout/cancel`。
- 1000ms 周期发送电压、电流、SOC。
- 5000ms 周期发送容量、SOH、版本、状态、出厂时间。

低功耗 RTC 唤醒 CAN：

```text
Can_RtcWakeService()
  -> 打开 CAN 电源
  -> 排 1000ms frame
  -> 循环服务 TX/RX/busoff，最多 1500ms
  -> 若仍忙则置 timeout 标志
  -> 关闭/准备睡眠
```

CAN app 命令：

- 标准 ID 命令帧：`((CAN_ADRESS_STD_ID << 7) | 0x60)`。
- payload 使用 `0xA5 0x5A + cmd + params + CRC16`。
- 支持 GET_STATUS、ENTER_IAP、READ_REG、WRITE_PREP/COMMIT、READ_BLOCK。
- 读写寄存器最终复用 `Sci_HostReadWords()` 和 `Sci_HostWriteWords()`。

疑点与风险：

- `CanFeidao_SendVoltageCurrent1000ms()` 中放电电流使用 unsigned 参与负号表达式，再赋给 `int32_t`。在当前 ARM two's complement 下通常可工作，但可读性和可移植性差，建议改为 `current = -((int32_t)g_stCellInfoReport.u16IDischg * 100);`。
- `CanFeidao_SendVersion5000ms()` 当前硬编码 `soft_version = 1U`，且只写低字节。若协议要求真实固件版本，应改为 `PROJECT_CFG_VERSION` 或年月日版本字段。
- `InitCan_Filter()` 当前硬件滤波接受全部帧，再软件判断目标 ID。共享 CAN 总线时会增加无效中断和解析压力。
- app 命令只有一个 pending 槽，新命令在 pending 未处理完时会被丢弃。建议至少增加 drop counter 或返回 busy/NACK。
- `CanFeidao_SendStatus5000ms()` 多个故障使用顺序覆盖，等价于隐式优先级。建议改为明确优先级表。

## 8. LED / LedBar 模块逻辑

LedBar 当前是 GPIO Charlieplexing 扫描方案，5 个 GPIO 通过 route 表点亮数字段、充电图标和百分号图标。

运行结构：

- `LedBarRuntime` 保存初始化、休眠、blank、number、indicator、frame、scan、key、MCU wake 等状态。
- `TIM4` 用于显示扫描，默认 `PROJECT_CFG_LEDBAR_SCAN_TIMER_100KHZ_TICKS=50`，即 0.5ms 扫描 tick。
- `APP_LedBar()` 在主循环前段运行，优先处理初始化、MCU_WK 滤波、按键、休眠、开机窗口、测试显示、SOC 显示窗口。

显示触发：

- 开机/唤醒显示窗口。
- `GPIO_MCU_WK` 有效。
- 按键请求。
- SOC 显示剩余窗口。

休眠相关：

- `LedBar_SaveSleepSoc()` 将 SOC 和反码写入 BKP DR4/DR5。
- `LedBar_LoadSleepSoc()` 校验 BKP 并回退到当前 SOC。
- 休眠/blank 时会停止 TIM4 和 GPIO 输出，有利于低功耗。

疑点与风险：

- 充电图标语义已确认：只由 `SH367309_Reg_Store.REG_BSTATUS3.bits.DSG_FET` 放电 MOS 状态决定；`GPIO_CHG_IN` 和充电滤波路径不再参与 LedBar 显示。
- `LedBar_UpdateIndicators()` 用 `discharge_mos_open` 点亮 `LEDBAR_ICON_CHARGE_MASK`。如果该 icon 真表示充电，这是显示逻辑疑点；如果实际表示输出/使能，应重命名。
- `LedBar_ServiceSwitch()` 依赖文件级全局 `key_release_wakeup`，导致上电前已经按住的长按不会立即进入长按计时。这可能是防止唤醒后误触发，也可能是隐藏行为，应写明或移入 `LedBarRuntime`。
- `LedBar_Scan1ms()` 命名与 0.5ms 默认扫描 tick 不一致，建议改名或在注释中说明。

建议简化为三层：

1. 输入采集：MCU_WK、key、charge/discharge/fault、SOC、sleep/startup。
2. 显示模型：本次应该显示什么数字、哪些图标、持续多久。
3. 扫描输出：Charlie route、TIM4、GPIO 高低阻切换。

这样 UI 规则不会散落在扫描和按键代码中。

## 9. 低功耗、RTC 与状态恢复

当前低功耗有两类路径：

- `SleepDeal.c` 管理深度休眠/复位式启动标志、长按进入休眠、BKP boot flag。
- `rtc_sleep.c` 管理 RTC STOP 周期唤醒、SOC RTC 补偿、CAN 低功耗补发和继续睡眠。

典型 RTC hiccup 路径：

```text
App_LowPowerProcess()
  -> rtc_sleep()
  -> BQ769x0_SleepMode_Ctrl()
  -> rtc_sleep_prepare_rtc()
  -> STOP
  -> restore after stop
  -> update_rtc_soc()
  -> Can_RtcWakeService()
  -> 若无异常则继续 STOP
```

睡眠前核心状态：

- `Can_PrepareSleep()`
- `SOC_SaveSnapshotBeforeSleep()`
- `FactoryAging_SaveProgressBeforeSleep()`

建议：

- 把“是否允许进低功耗”的条件集中成一个可读结构，例如 `LowPowerDecision`，包含阻断原因、当前模式、下一次 RTC 周期。
- 保留 `SleepDeal.c` 和 `rtc_sleep.c` 的物理差异，但对外提供统一 facade，例如 `LowPower_Service()`、`LowPower_RequestDeepSleep()`、`LowPower_OnWake()`。

## 10. 存储、通信与参数

Flash 布局要点：

- IAP/Bootloader：`0x08000000`。
- App：`0x08004800`。
- 后 64K 持久化区从 `0x0801C000` 开始，包含 AFE/RW_PARAM/log/SOC/sleep flag 等 slot。

持久化机制：

- 多数 slot 使用 magic、version、sequence、CRC。
- SOC snapshot 通过 storage adapter 读写，不直接裸写固定地址。
- BKP 用于休眠启动标志、LedBar sleep SOC 等短状态。

通信：

- SCI 是传统上位机协议入口。
- CAN app 命令复用 `Sci_HostReadWords()` 和 `Sci_HostWriteWords()`，避免重新实现一套寄存器表。
- `PROJECT_CFG_HOST_WRITE_ENABLE=1`，但写入仍按地址和范围校验。
- `PROJECT_CFG_SOC_RUNTIME_TABLE_ENABLE=0` 时，SOC OCV 表运行时写入被拒绝。

风险：

- `StorageFlash_CalcCrc()` 的 length 最终传入 `Sci_CRC16RTU()` 的 `UINT8` 长度参数。当前 payload 都小于 255 字节，所以不是现时 bug；但未来新增大 payload 时会静默截断长度。
- CAN 复用 SCI host read/write 时，如果串口忙，CAN 读写可能失败。这是合理的重入保护，但上位机体验上需要 busy 反馈。

## 11. Bug 与风险清单

| 优先级 | 位置 | 现象 | 建议 |
| --- | --- | --- | --- |
| P1 | `DataDeal.c/new_todo_logi()` | 数据装载函数中混入 charger/key/MOS/UL fuse/深度休眠策略，且函数名仍是 todo | 拆为 `PowerPolicy_Service()`，`DataDeal.c` 只保留 AFE 数据到 report 的转换 |
| P1 | `LedBar.c/LedBar_UpdateIndicators()` | `LEDBAR_ICON_CHARGE_MASK` 由 `discharge_mos_open` 驱动，图标语义疑似错误 | 确认产品定义；若表示输出使能则改名，若表示充电则修正条件 |
| P2 | `CanFeidaoFrames.c` | 放电电流用 unsigned 负号表达式后赋值 `int32_t` | 改为显式 signed 乘法和取负 |
| P2 | `CanFeidaoFrames.c` | 版本帧硬编码 `1`，且只写低字节 | 按协议改用 `PROJECT_CFG_VERSION`、年月日或完整版本字段 |
| P2 | `Can_HDX.c` | CAN app 命令只有一个 pending 槽，新命令可能静默丢弃 | 增加 busy/NACK 或 drop counter，并在状态帧/诊断中暴露 |
| P2 | `Can_HDX.c` | 硬件滤波接收全部帧 | 共享总线时改为硬件过滤目标标准 ID 和必要扩展 ID |
| P2 | `ADC.c/ADC.h` | 原始 DMA rank 与派生结果共用枚举，新增一路 ADC 容易改漏 | 拆分 raw channel enum 和 processed signal enum |
| P2 | `ADC.c` | `AD_Used_amount`、rank、GPIO、处理函数分散维护 | 建立 ADC channel descriptor 表，初始化和处理都从表驱动 |
| P2 | `Flash.c` | CRC length 传入 8 位长度，未来 payload >255 会截断 | 扩展 CRC API 或加静态断言/运行期保护 |
| P2 | `LedBar.c/LedBar_ServiceSwitch()` | 长按依赖 `key_release_wakeup`，上电前按住不会立即计时 | 明确为防误触策略，或移入 runtime 并增加状态名 |
| P3 | `LedBar.c/LedBar_Scan1ms()` | 函数名与 0.5ms 默认 tick 不一致 | 改名为 `LedBar_ScanTick()` 或补注释 |
| P3 | 多处 | `App_AnlogCal` 拼写错误、`new_todo_logi` 命名临时化 | 不影响功能，但增加维护成本，建议在低风险窗口重命名 |

## 12. 架构简化建议

### 12.1 可以马上做且不影响功能

1. 给 ADC 增加 channel descriptor 表，仅先覆盖现有 3 路：
   - GPIO port/pin
   - ADC channel
   - rank
   - raw index
   - process callback

2. 给 CAN app 命令增加统计：
   - `rx_drop_busy`
   - `crc_error`
   - `cmd_unsupported`
   - `tx_timeout`

3. LedBar 先做命名和空分支清理：
   - 保持充电图标只由放电 MOS 状态决定，避免重新引入 `GPIO_CHG_IN` 滤波路径。
   - `key_release_wakeup` 并入 `LedBarRuntime`。

4. `DataDeal.c` 中把 `new_todo_logi()` 改名并拆文件：
   - 第一步只移动，不改逻辑。
   - 函数名建议 `PowerPolicy_ServiceFromAfeState()`。
   - 以 `g_stCellInfoReport` 和 `SystemStatus` 为输入，输出 MOS/sleep/power policy 操作。

### 12.2 中期重构

1. SOC 文件拆分，但保持 API 不变：
   - `soc_integrator.c`
   - `soc_ocv.c`
   - `soc_anchor.c`
   - `soc_snapshot.c`
   - `soc_display.c`

2. LowPower facade：
   - `LowPower_CanEnter()`
   - `LowPower_PrepareStop()`
   - `LowPower_RestoreAfterStop()`
   - `LowPower_ServiceRtcWake()`

3. Report 总线收口：
   - 将 `g_stCellInfoReport` 的写入点列表化。
   - 每个字段明确 owner，避免多个模块无序写同一字段。

4. CAN 协议与发送调度分层：
   - frame packer 只负责协议字段。
   - tx scheduler 只负责周期、队列、mailbox。
   - app command 只负责请求/响应状态机。

### 12.3 现在不建议做

- 不建议立即大改 SOC 算法。当前 SOC 的复杂度主要来自用户体验保护，不是简单代码臃肿。
- 不建议把所有全局变量一次性封装成巨大的 context。嵌入式旧工程一次性替换风险太高。
- 不建议在新增一路 ADC 时顺手重构所有 ADC 业务。应先用最小改动接入，再做表驱动收口。

## 13. 新增一路 ADC 的推荐落地步骤

推荐分两步：

第一步，最小功能接入：

1. 明确新增采样对象：温度、外部电压、电流还是开关量模拟值。
2. 明确引脚和 ADC channel，确认不占用 USART2、LED Charlie、CAN、I2C、唤醒脚。
3. 修改 `AD_Used_amount`、`ADC_NbrOfChannel`、rank 4、DMA buffer。
4. 新增处理函数，只写一个新结果变量或 `g_i32ADCResult` 的明确索引。
5. 用上位机或调试输出确认 4 路原始 ADC 顺序正确。

第二步，结构收口：

1. 建立 `AdcRawChannel` 描述表。
2. 让 `InitADC()` 从描述表配置 GPIO/channel/rank。
3. 让 `App_AnlogCal()` 只调度处理函数，不硬编码采样语义。
4. 补文档：rank 顺序、换算公式、单位、上下限、通信地址。

验收重点：

- 新通道加入后，原 PB1/PA2/PA1 三路数值不串位。
- Type-C 电流 unplug 后能回零，不因平均窗口长期残留。
- SOC 净电流计算仍符合：放电电流 = AFE 放电 + Type-C 电池侧等效电流。
- 低功耗 STOP 退出后 ADC/DMA/TIM2 仍能恢复采样。

## 14. 建议验证命令

文档和小重构后建议至少运行：

```bash
python3 tools/project_check.py
python3 tools/soc_replay_test.py
python3 tools/run_soc_host_c_test.py
python3 tools/run_rewrite_ci.py
python3 tools/check_rewrite_keil_project.py
git diff --check
```

如果改动 ADC：

- 台架测 4 路 ADC 原始值和物理输入对应关系。
- 检查 DMA buffer rank 顺序。
- 检查 Type-C 电流拔除后的回零时间。
- 检查低功耗唤醒后 ADC 继续更新。

如果改动 SOC：

- 必须保留单次 `1%` 校准约束。
- 必须验证大电流 sag holdoff。
- 必须验证 RTC 静置只做小步补偿。
- 必须验证 snapshot CRC/sequence 恢复路径。

如果改动 CAN：

- 检查 1000ms/5000ms 周期帧。
- 检查 bus-off 自动恢复。
- 检查 RTC 唤醒后 1500ms 内能完成必要发送。
- 检查 app command busy 情况是否有可观察反馈。

## 15. 后续优先级

第一优先级：

- 保持 LedBar charge icon 条件与产品定义一致：仅由放电 MOS 状态决定。
- 修正 CAN 放电电流 signed 表达式。
- 确认 CAN 版本帧是否应使用真实固件版本。
- 为新增 ADC 一路预先整理 rank/channel 表。

第二优先级：

- `new_todo_logi()` 拆出 DataDeal。
- CAN app 命令增加 busy/drop 诊断。
- `StorageFlash_CalcCrc()` 增加长度保护。
- LedBar 输入/显示模型/扫描输出分层。

第三优先级：

- SOC 按功能文件拆分但保持 API 不变。
- LowPower facade 收口。
- `g_stCellInfoReport` 字段 owner 文档化。
